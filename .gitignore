# Rust
/target
**/target
Cargo.lock.bak

# Node
node_modules
dist
.vite

# Playwright
/test-results
/playwright-report
/.playwright

# Editor / OS
.DS_Store
*.swp

# Local scratch
/scratch
samples/*/publish
screenshots/
scripts/app-icon-1024.png

# Local verification build artifacts (scripts/build-appimage-container.sh)
*.AppImage

# Local agent working agreement (per-machine, not shared)
/CLAUDE.md

# Local agent scratch — subagent task specs + notes, and the internal
# deferred-work list. Working artifacts between Martin and Claude, not shared.
/subagent-tasks
/DEFERRED.md
