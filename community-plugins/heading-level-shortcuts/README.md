# Heading level shortcuts

Behavioral port of Richard Yu's Logseq plugin. The full usage guide and original
screencast live in the public source repository linked by `manifest.json`.

Tine uses remappable `mod+alt+0` through `mod+alt+6` defaults because plain
`mod+1…9` already focuses numbered panes.
