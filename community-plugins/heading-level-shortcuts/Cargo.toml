[package]
name = "tine-plugin-heading-level-shortcuts"
version = "0.1.0"
edition = "2021"
license = "MIT"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
tine-plugin-sdk = { path = "../../plugin-sdk/rust" }
