[package]
name = "tine-plugin-query-filter"
version = "0.2.0"
edition = "2021"
license = "MIT"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
tine-plugin-sdk = { path = "../../plugin-sdk/rust" }
