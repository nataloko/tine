PRAGMA quick_check(1)
SELECT DISTINCT p.path, b.result_id, b.order_key FROM reference_postings r JOIN names n ON n.name_id = r.target_name_id JOIN pages p ON p.page_id = r.source_page_id LEFT JOIN blocks b ON r.source_entity_type = 1 AND b.block_id = r.source_entity_id WHERE r.target_type = 0 AND r.reference_kind <= 4 AND n.key = ?
SELECT alias.raw, owner.raw, p.path FROM reference_alias_declarations d JOIN pages p ON p.page_id = d.source_page_id JOIN names owner ON owner.name_id = p.name_id JOIN names alias ON alias.name_id = d.alias_name_id WHERE d.source_entity_type = 0 AND d.source_entity_id = d.source_page_id ORDER BY d.source_page_id, d.ordinal, alias.raw
SELECT b.block_id, b.page_id, b.collapsed, b.heading_level, t.content, k.marker, pl.priority, pl.scheduled, pl.deadline FROM blocks b JOIN block_text t ON t.block_id = b.block_id LEFT JOIN tasks k ON k.block_id = b.block_id LEFT JOIN block_planning pl ON pl.block_id = b.block_id WHERE b.block_id IN (?)
SELECT b.block_id, b.page_id, b.parent_block_id, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.path, n.raw, p.text_kind, p.journal_day FROM blocks b JOIN pages p ON p.page_id = b.page_id JOIN names n ON n.name_id = p.name_id WHERE b.block_id IN (?) ORDER BY p.path, b.preorder
SELECT b.block_id, b.page_id, b.parent_block_id, bt.content, p.path FROM blocks b LEFT JOIN block_text bt ON bt.block_id = b.block_id LEFT JOIN pages p ON p.page_id = b.page_id WHERE b.block_id IN (?)
SELECT b.block_id, b.parent_block_id, b.page_id, b.order_key, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count FROM blocks b WHERE b.page_id = ? AND b.preorder > ? ORDER BY b.preorder LIMIT ?
SELECT b.block_id, b.parent_block_id, bt.content FROM blocks b JOIN pages p ON p.page_id = b.page_id JOIN block_text bt ON bt.block_id = b.block_id WHERE p.path = ? ORDER BY b.preorder
SELECT b.block_id, bt.content, p.path FROM (SELECT rowid AS block_id FROM search_fts WHERE search_fts MATCH ? ORDER BY rowid DESC) c JOIN blocks b ON b.block_id = c.block_id LEFT JOIN block_text bt ON bt.block_id = b.block_id LEFT JOIN pages p ON p.page_id = b.page_id LEFT JOIN names p_name ON p_name.name_id = p.name_id ORDER BY c.block_id DESC
SELECT b.block_id, bt.content, p.path FROM (SELECT rowid AS block_id FROM search_fts WHERE search_fts MATCH ? ORDER BY rowid DESC) c JOIN blocks b ON b.block_id = c.block_id LEFT JOIN block_text bt ON bt.block_id = b.block_id LEFT JOIN pages p ON p.page_id = b.page_id LEFT JOIN names p_name ON p_name.name_id = p.name_id WHERE p.path = ? ORDER BY c.block_id DESC
SELECT b.block_id, bt.content, p.path FROM blocks b LEFT JOIN block_text bt ON bt.block_id = b.block_id LEFT JOIN pages p ON p.page_id = b.page_id LEFT JOIN names p_name ON p_name.name_id = p.name_id ORDER BY b.block_id DESC
SELECT block_id FROM blocks WHERE result_id = ? OR logseq_uuid = ? OR block_id IN (SELECT o.owner_id FROM properties o JOIN names n ON n.name_id = o.name_id WHERE o.owner_type = 1 AND n.key = 'id' AND o.value = ?)
SELECT block_id, page_id, preorder FROM blocks WHERE block_id IN (?)
SELECT count(*), coalesce(sum(substr(revision, 1, ?) <> ?), 0) FROM direct_source_revisions
SELECT n.key, COALESCE(t.preamble, '') FROM pages p JOIN names n ON n.name_id = p.name_id LEFT JOIN page_text t ON t.page_id = p.page_id WHERE p.text_kind = 0 AND n.key = ? ORDER BY p.path
SELECT o.owner_id FROM properties o JOIN names n ON n.name_id = o.name_id WHERE o.owner_type = 1 AND n.key = 'template' AND o.value <> ''
SELECT o.owner_type, o.owner_id, o.page_id, n.raw, n.key, o.value, o.ordinal, b.page_id FROM properties o JOIN names n ON n.name_id = o.name_id LEFT JOIN blocks b ON o.owner_type = 1 AND b.block_id = o.owner_id ORDER BY o.owner_type, o.owner_id, o.name_id, o.ordinal
SELECT o.owner_type, o.owner_id, o.page_id, n.raw, n.key, o.value, o.ordinal, b.page_id, p.path, pn.raw, p.text_kind FROM properties o JOIN names n ON n.name_id = o.name_id LEFT JOIN blocks b ON o.owner_type = 1 AND b.block_id = o.owner_id LEFT JOIN pages p ON p.page_id = o.page_id LEFT JOIN names pn ON pn.name_id = p.name_id WHERE n.key = ? AND o.owner_type = 0 ORDER BY o.owner_type, o.owner_id, o.name_id, o.ordinal
SELECT o.owner_type, o.owner_id, o.page_id, n.raw, n.key, o.value, o.ordinal, b.page_id, p.path, pn.raw, p.text_kind FROM properties o JOIN names n ON n.name_id = o.name_id LEFT JOIN blocks b ON o.owner_type = 1 AND b.block_id = o.owner_id LEFT JOIN pages p ON p.page_id = o.page_id LEFT JOIN names pn ON pn.name_id = p.name_id WHERE n.key = ? ORDER BY o.owner_type, o.owner_id, o.name_id, o.ordinal
SELECT p.page_id, p.path, n.raw, p.text_kind FROM pages p JOIN names n ON n.name_id = p.name_id
SELECT p.path AS path, NULL AS result_id, p.page_id AS entity_id, 0 AS entity_type, NULL AS order_key FROM pages p JOIN names n ON n.name_id = p.name_id LEFT JOIN page_text pt ON pt.page_id = p.page_id WHERE n.key <> ? AND tine_query_rank(0, CAST(length(CAST(COALESCE(pt.preamble, '') AS BLOB)) AS TEXT) || ':' || COALESCE(pt.preamble, '') || p.path) IS NOT NULL UNION ALL SELECT p.path, b.result_id, b.block_id, 1, b.order_key FROM blocks b JOIN block_text bt ON bt.block_id = b.block_id JOIN pages p ON p.page_id = b.page_id JOIN names n ON n.name_id = p.name_id WHERE n.key <> ? AND tine_query_rank(1, CAST(length(CAST(bt.content AS BLOB)) AS TEXT) || ':' || bt.content || p.path) IS NOT NULL ORDER BY entity_id DESC LIMIT ?
SELECT p.path AS path, NULL AS result_id, p.page_id AS entity_id, 0 AS entity_type, NULL AS order_key FROM pages p JOIN names n ON n.name_id = p.name_id LEFT JOIN page_text pt ON pt.page_id = p.page_id WHERE n.key <> ? UNION ALL SELECT p.path, b.result_id, b.block_id, 1, b.order_key FROM blocks b JOIN block_text bt ON bt.block_id = b.block_id JOIN pages p ON p.page_id = b.page_id JOIN names n ON n.name_id = p.name_id WHERE n.key <> ? ORDER BY entity_id DESC
SELECT p.path, n.raw, p.text_kind, t.preamble, s.revision, p.journal_day FROM pages p JOIN names n ON n.name_id = p.name_id JOIN direct_source_revisions s ON s.path = p.path LEFT JOIN page_text t ON t.page_id = p.page_id ORDER BY p.path
SELECT p.path, p.journal_day, bt.content FROM pages p JOIN blocks b ON b.page_id = p.page_id JOIN block_text bt ON bt.block_id = b.block_id WHERE p.text_kind = 1 ORDER BY p.path, b.preorder
SELECT p.path, s.revision FROM pages p LEFT JOIN direct_source_revisions s ON s.path = p.path ORDER BY p.path
SELECT page_id, path FROM pages WHERE page_id IN (?)
SELECT page_name.raw FROM names AS page_name JOIN pages AS page INDEXED BY pages_name_idx ON page.name_id = page_name.name_id WHERE page_name.key = ? AND page.text_kind = 1 ORDER BY page.path LIMIT 1
SELECT parent_block_id, page_id, COUNT(*) FROM blocks WHERE parent_block_id IN (?) GROUP BY parent_block_id, page_id
SELECT path FROM pages WHERE path >= ? ORDER BY path
SELECT path, journal_day FROM pages WHERE text_kind = 1 AND journal_day IS NOT NULL
SELECT path, result_id, entity_id, entity_type, order_key FROM (SELECT c.rowid AS entity_id, CASE WHEN ep.page_id IS NOT NULL THEN 0 ELSE 1 END AS entity_type, owner.path, b.result_id, b.order_key, CASE WHEN ep.page_id IS NOT NULL THEN COALESCE(pt.preamble, '') ELSE bt.content END AS raw, owner_name.key AS owner_key FROM (SELECT rowid FROM search_fts WHERE search_fts MATCH ? ORDER BY rowid DESC) c LEFT JOIN pages ep ON ep.page_id = c.rowid LEFT JOIN blocks b ON b.block_id = c.rowid JOIN pages owner ON owner.page_id = COALESCE(ep.page_id, b.page_id) JOIN names owner_name ON owner_name.name_id = owner.name_id LEFT JOIN page_text pt ON pt.page_id = ep.page_id LEFT JOIN block_text bt ON bt.block_id = b.block_id WHERE ep.page_id IS NOT NULL OR b.block_id IS NOT NULL) candidates WHERE owner_key <> ? AND tine_query_rank(entity_type, CAST(length(CAST(raw AS BLOB)) AS TEXT) || ':' || raw || path) IS NOT NULL ORDER BY entity_id DESC LIMIT ?
SELECT path, result_id, entity_id, entity_type, order_key FROM (SELECT c.rowid AS entity_id, CASE WHEN ep.page_id IS NOT NULL THEN 0 ELSE 1 END AS entity_type, owner.path, b.result_id, b.order_key, CASE WHEN ep.page_id IS NOT NULL THEN COALESCE(pt.preamble, '') ELSE bt.content END AS raw, owner_name.key AS owner_key FROM (SELECT rowid FROM search_fts WHERE search_fts MATCH ? ORDER BY rowid DESC) c LEFT JOIN pages ep ON ep.page_id = c.rowid LEFT JOIN blocks b ON b.block_id = c.rowid JOIN pages owner ON owner.page_id = COALESCE(ep.page_id, b.page_id) JOIN names owner_name ON owner_name.name_id = owner.name_id LEFT JOIN page_text pt ON pt.page_id = ep.page_id LEFT JOIN block_text bt ON bt.block_id = b.block_id WHERE ep.page_id IS NOT NULL OR b.block_id IS NOT NULL) candidates WHERE owner_key <> ? ORDER BY entity_id DESC
SELECT path, revision FROM direct_source_revisions
SELECT preorder FROM blocks WHERE block_id = ? AND page_id = ?
SELECT property.owner_id, name.raw, property.value FROM properties property JOIN names name ON name.name_id = property.name_id WHERE property.owner_type = 1 AND property.owner_id IN (?) ORDER BY property.owner_id, property.ordinal
SELECT property.owner_id, property.page_id, name.raw, property.value, property.ordinal FROM properties property JOIN names name ON name.name_id = property.name_id WHERE property.owner_type = 0 AND property.owner_id IN (?) ORDER BY property.owner_id, property.ordinal, property.name_id
SELECT revision FROM direct_source_revisions WHERE path = ?
SELECT s.revision, b.result_id, b.order_key, b.estimated_bytes, b.page_id, (SELECT COUNT(*) FROM blocks child WHERE child.parent_block_id = b.block_id) FROM pages p JOIN direct_source_revisions s ON s.path = p.path JOIN blocks b ON b.page_id = p.page_id WHERE p.path = ? ORDER BY b.preorder
SELECT source_entity_id FROM reference_postings WHERE target_type = 1 AND source_entity_type = 1 AND raw_uuid_claim = ?
SELECT tag.owner_id, name.raw FROM tags tag JOIN names name ON name.name_id = tag.name_id WHERE tag.owner_type = 1 AND tag.owner_id IN (?) ORDER BY tag.owner_id, tag.ordinal
WITH RECURSIVE component(name_key) AS ( SELECT ? UNION SELECT alias_name.key FROM component JOIN names AS owner_name ON owner_name.key = component.name_key JOIN pages AS owner INDEXED BY pages_name_idx ON owner.name_id = owner_name.name_id JOIN reference_alias_declarations AS declaration INDEXED BY reference_alias_declarations_source_idx ON declaration.source_page_id = owner.page_id JOIN names AS alias_name ON alias_name.name_id = declaration.alias_name_id UNION SELECT owner_name.key FROM component JOIN names AS alias_name ON alias_name.key = component.name_key JOIN reference_alias_declarations AS declaration INDEXED BY reference_alias_declarations_name_idx ON declaration.alias_name_id = alias_name.name_id JOIN pages AS owner ON owner.page_id = declaration.source_page_id JOIN names AS owner_name ON owner_name.name_id = owner.name_id ) SELECT component.name_key, page.path, CASE WHEN page.page_id IS NULL THEN NULL ELSE page_name.raw END, alias_name.key FROM component LEFT JOIN names AS page_name ON page_name.key = component.name_key LEFT JOIN pages AS page INDEXED BY pages_name_idx ON page.name_id = page_name.name_id LEFT JOIN reference_alias_declarations AS declaration INDEXED BY reference_alias_declarations_source_idx ON declaration.source_page_id = page.page_id LEFT JOIN names AS alias_name ON alias_name.name_id = declaration.alias_name_id ORDER BY component.name_key, page.path, alias_name.key
WITH RECURSIVE relatives(block_id) AS (SELECT block_id FROM blocks WHERE block_id = ? UNION SELECT b.block_id FROM blocks b JOIN relatives r ON b.parent_block_id = r.block_id) SELECT block_id FROM relatives
WITH RECURSIVE relatives(block_id) AS (SELECT parent_block_id FROM blocks WHERE block_id = ? UNION SELECT b.parent_block_id FROM blocks b JOIN relatives r ON b.block_id = r.block_id) SELECT block_id FROM relatives WHERE block_id IS NOT NULL
WITH content_ranked AS MATERIALIZED (SELECT b.block_id, b.page_id, bt.content AS matched_text, p.path AS matched_path, tine_query_rank(?, CAST(length(CAST(bt.content AS BLOB)) AS TEXT) || ':' || bt.content || p.path) AS content_key FROM (SELECT block_id FROM blocks WHERE block_id IN (?)) c JOIN blocks b ON b.block_id = c.block_id JOIN block_text bt ON bt.block_id = b.block_id JOIN pages p ON p.page_id = b.page_id), content_verified AS MATERIALIZED (SELECT * FROM content_ranked WHERE content_key IS NOT NULL), content_choices AS (SELECT k.*, ROW_NUMBER() OVER (PARTITION BY k.page_id ORDER BY k.content_key) AS content_choice FROM content_verified k WHERE 1), content AS MATERIALIZED (SELECT 1 AS match_source, 0 AS candidate_kind, p.page_id, pn.raw AS name, p.text_kind, p.journal_day, p.path, k.matched_text, 0 AS source_kind, substr(k.content_key, 1, 29) AS global_key, k.content_key AS tie_key FROM content_choices k JOIN pages p ON p.page_id = k.page_id JOIN names pn ON pn.name_id = p.name_id WHERE k.content_choice = 1), candidates AS (SELECT * FROM content WHERE global_key IS NOT NULL) SELECT c.match_source, c.candidate_kind, c.page_id, c.name, c.text_kind, c.journal_day, c.path, c.matched_text, c.source_kind, c.global_key FROM candidates c ORDER BY c.match_source, c.global_key, c.tie_key COLLATE BINARY LIMIT ?
WITH content_ranked AS MATERIALIZED (SELECT b.block_id, b.page_id, bt.content AS matched_text, p.path AS matched_path, tine_query_rank(?, CAST(length(CAST(bt.content AS BLOB)) AS TEXT) || ':' || bt.content || p.path) AS content_key FROM (SELECT rowid AS block_id FROM search_fts WHERE search_fts MATCH ? ORDER BY rowid DESC) c JOIN blocks b ON b.block_id = c.block_id JOIN block_text bt ON bt.block_id = b.block_id JOIN pages p ON p.page_id = b.page_id), content_verified AS MATERIALIZED (SELECT * FROM content_ranked WHERE content_key IS NOT NULL), content_choices AS (SELECT k.*, ROW_NUMBER() OVER (PARTITION BY k.page_id ORDER BY k.content_key) AS content_choice FROM content_verified k WHERE 1), content AS MATERIALIZED (SELECT 1 AS match_source, 0 AS candidate_kind, p.page_id, pn.raw AS name, p.text_kind, p.journal_day, p.path, k.matched_text, 0 AS source_kind, substr(k.content_key, 1, 29) AS global_key, k.content_key AS tie_key FROM content_choices k JOIN pages p ON p.page_id = k.page_id JOIN names pn ON pn.name_id = p.name_id WHERE k.content_choice = 1), candidates AS (SELECT * FROM content WHERE global_key IS NOT NULL) SELECT c.match_source, c.candidate_kind, c.page_id, c.name, c.text_kind, c.journal_day, c.path, c.matched_text, c.source_kind, c.global_key FROM candidates c ORDER BY c.match_source, c.global_key, c.tie_key COLLATE BINARY LIMIT ?
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE (EXISTS (SELECT 1 FROM tasks t1 WHERE (t1.block_id = b.block_id AND t1.marker IN (?))) AND b.page_id IN (SELECT pg2.page_id FROM pages pg2 WHERE (pg2.journal_day IS NOT NULL AND (pg2.journal_day >= ? AND pg2.journal_day <= ?))))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))), qe_order_pages AS MATERIALIZED (SELECT p.page_id, n.raw AS name, p.text_kind, p.journal_day, p.path FROM pages p JOIN names n ON n.name_id = p.name_id WHERE p.page_id IN (SELECT page_id FROM r)) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, b.page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.page_id, COUNT(*) OVER () FROM r LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN qe_order_pages p ON p.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, p.path COLLATE BINARY ASC, b.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE (b.page_id IN (SELECT pg1.page_id FROM pages pg1 WHERE pg1.page_id IN (SELECT pr2.owner_id FROM properties pr2 JOIN names prn3 ON prn3.name_id = pr2.name_id WHERE (prn3.key = ? AND pr2.owner_type = ?))) AND (NOT EXISTS (SELECT 1 FROM pages pg4 WHERE (pg4.page_id = b.page_id AND (EXISTS (SELECT 1 FROM properties pr5 JOIN names prn6 ON prn6.name_id = pr5.name_id WHERE (pr5.owner_id = pg4.page_id AND (prn6.key = ? AND pr5.owner_type = ?))) AND (SELECT COUNT(*) FROM property_atoms ac7 JOIN names acn8 ON acn8.name_id = ac7.name_id WHERE ac7.owner_type = ? AND ac7.owner_id = pg4.page_id AND acn8.key = ?) = ?)))))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))), qe_order_pages AS MATERIALIZED (SELECT p.page_id, n.raw AS name, p.text_kind, p.journal_day, p.path FROM pages p JOIN names n ON n.name_id = p.name_id WHERE p.page_id IN (SELECT page_id FROM r)) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, b.page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.page_id, COUNT(*) OVER () FROM r LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN qe_order_pages p ON p.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, p.path COLLATE BINARY ASC, b.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.block_id IN (SELECT a3.owner_id FROM property_atoms a3 JOIN names an4 ON an4.name_id = a3.name_id WHERE (an4.key = ? AND (a3.owner_type = ? AND a3.atom_key = ?)))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))), qe_order_pages AS MATERIALIZED (SELECT p.page_id, n.raw AS name, p.text_kind, p.journal_day, p.path FROM pages p JOIN names n ON n.name_id = p.name_id WHERE p.page_id IN (SELECT page_id FROM r)) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, b.page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.page_id FROM r LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN qe_order_pages p ON p.page_id = r.page_id ORDER BY p.path COLLATE BINARY ASC, b.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.block_id IN (SELECT a3.owner_id FROM property_atoms a3 JOIN names an4 ON an4.name_id = a3.name_id WHERE (an4.key = ? AND (a3.owner_type = ? AND a3.atom_key = ?)))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))), qe_order_pages AS MATERIALIZED (SELECT p.page_id, n.raw AS name, p.text_kind, p.journal_day, p.path FROM pages p JOIN names n ON n.name_id = p.name_id WHERE p.page_id IN (SELECT page_id FROM r)) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, b.page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.page_id, COUNT(*) OVER () FROM r LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN qe_order_pages p ON p.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, p.path COLLATE BINARY ASC, b.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.block_id IN (SELECT r1.block_id FROM block_path_refs r1 JOIN names rn2 ON rn2.name_id = r1.name_id WHERE rn2.key = ?)), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))), qe_order_pages AS MATERIALIZED (SELECT p.page_id, n.raw AS name, p.text_kind, p.journal_day, p.path FROM pages p JOIN names n ON n.name_id = p.name_id WHERE p.page_id IN (SELECT page_id FROM r)) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, b.page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.page_id, COUNT(*) OVER () FROM r LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN qe_order_pages p ON p.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, p.path COLLATE BINARY ASC, b.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.block_id IN (SELECT t1.block_id FROM tasks t1 WHERE t1.marker IN (?))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))), qe_order_pages AS MATERIALIZED (SELECT p.page_id, n.raw AS name, p.text_kind, p.journal_day, p.path FROM pages p JOIN names n ON n.name_id = p.name_id WHERE p.page_id IN (SELECT page_id FROM r)) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, b.page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.page_id FROM r LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN qe_order_pages p ON p.page_id = r.page_id ORDER BY p.path COLLATE BINARY ASC, b.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.block_id IN (SELECT t1.block_id FROM tasks t1 WHERE t1.marker IN (?))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))), qe_order_pages AS MATERIALIZED (SELECT p.page_id, n.raw AS name, p.text_kind, p.journal_day, p.path FROM pages p JOIN names n ON n.name_id = p.name_id WHERE p.page_id IN (SELECT page_id FROM r)) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, b.page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.page_id, COUNT(*) OVER () FROM r LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN qe_order_pages p ON p.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, p.path COLLATE BINARY ASC, b.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.page_id IN (SELECT pg1.page_id FROM pages pg1 WHERE (pg1.journal_day IS NOT NULL AND (pg1.journal_day >= ? AND pg1.journal_day <= ?)))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))), qe_order_pages AS MATERIALIZED (SELECT p.page_id, n.raw AS name, p.text_kind, p.journal_day, p.path FROM pages p JOIN names n ON n.name_id = p.name_id WHERE p.page_id IN (SELECT page_id FROM r)) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, b.page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.page_id, COUNT(*) OVER () FROM r LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN qe_order_pages p ON p.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, p.path COLLATE BINARY ASC, b.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.page_id IN (SELECT pg1.page_id FROM pages pg1 WHERE pg1.name_id IN (SELECT name_id FROM names WHERE (key >= ? AND key < ?)))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))), qe_order_pages AS MATERIALIZED (SELECT p.page_id, n.raw AS name, p.text_kind, p.journal_day, p.path FROM pages p JOIN names n ON n.name_id = p.name_id WHERE p.page_id IN (SELECT page_id FROM r)) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, b.page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.page_id, COUNT(*) OVER () FROM r LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN qe_order_pages p ON p.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, p.path COLLATE BINARY ASC, b.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.page_id IN (SELECT pg1.page_id FROM pages pg1 WHERE pg1.page_id IN (SELECT a4.owner_id FROM property_atoms a4 JOIN names an5 ON an5.name_id = a4.name_id WHERE (an5.key = ? AND (a4.owner_type = ? AND a4.atom_key = ?))))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))), qe_order_pages AS MATERIALIZED (SELECT p.page_id, n.raw AS name, p.text_kind, p.journal_day, p.path FROM pages p JOIN names n ON n.name_id = p.name_id WHERE p.page_id IN (SELECT page_id FROM r)) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, b.page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.page_id, COUNT(*) OVER () FROM r LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN qe_order_pages p ON p.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, p.path COLLATE BINARY ASC, b.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE b.page_id IN (SELECT pg1.page_id FROM pages pg1 WHERE pg1.page_id IN (SELECT a4.owner_id FROM property_atoms a4 JOIN names an5 ON an5.name_id = a4.name_id WHERE (an5.key = ? AND (a4.owner_type = ? AND a4.atom_key IN (?)))))), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))), qe_order_pages AS MATERIALIZED (SELECT p.page_id, n.raw AS name, p.text_kind, p.journal_day, p.path FROM pages p JOIN names n ON n.name_id = p.name_id WHERE p.page_id IN (SELECT page_id FROM r)) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, b.page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.page_id, COUNT(*) OVER () FROM r LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN qe_order_pages p ON p.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, p.path COLLATE BINARY ASC, b.preorder ASC
WITH m(block_id, page_id, parent_block_id) AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id FROM blocks b WHERE tine_query_rank(?, (SELECT CAST(length(CAST(bt1.content AS BLOB)) AS TEXT) || ':' || bt1.content || vp2.path FROM block_text bt1 JOIN pages vp2 ON vp2.page_id = b.page_id WHERE bt1.block_id = b.block_id)) IS NOT NULL), r(block_id, page_id) AS (SELECT m.block_id, m.page_id FROM m WHERE (m.parent_block_id IS NULL OR m.parent_block_id NOT IN (SELECT block_id FROM m))), qe_order_pages AS MATERIALIZED (SELECT p.page_id, n.raw AS name, p.text_kind, p.journal_day, p.path FROM pages p JOIN names n ON n.name_id = p.name_id WHERE p.page_id IN (SELECT page_id FROM r)) SELECT r.block_id, r.page_id, p.name, p.text_kind, p.journal_day, p.path, b.page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, b.order_key, p.page_id, COUNT(*) OVER () FROM r LEFT JOIN blocks b ON b.block_id = r.block_id LEFT JOIN qe_order_pages p ON p.page_id = r.page_id ORDER BY p.name COLLATE BINARY ASC, CASE p.text_kind WHEN 1 THEN 0 ELSE 1 END ASC, p.path COLLATE BINARY ASC, b.preorder ASC
WITH page_text_candidates(page_id, name, text_kind, journal_day, path, matched_text, source_kind, source_ordinal) AS (SELECT p.page_id, pn.raw, p.text_kind, p.journal_day, p.path, pn.raw, 0, -1 FROM pages p JOIN names pn ON pn.name_id = p.name_id UNION ALL SELECT p.page_id, pn.raw, p.text_kind, p.journal_day, p.path, an.raw, 1, MIN(a.ordinal) FROM reference_alias_declarations a JOIN pages p ON p.page_id = a.source_page_id JOIN names pn ON pn.name_id = p.name_id JOIN names an ON an.name_id = a.alias_name_id WHERE a.source_entity_type = 0 AND a.source_entity_id = a.source_page_id GROUP BY p.page_id, a.alias_name_id, an.raw), ranked AS MATERIALIZED (SELECT c.*, tine_query_rank(?, CAST(length(CAST(c.name AS BLOB)) AS TEXT) || ':' || c.matched_text) AS rank_key FROM page_text_candidates c), choices AS (SELECT r.*, ROW_NUMBER() OVER (PARTITION BY r.page_id ORDER BY substr(r.rank_key, 1, 9), r.source_kind, r.source_ordinal) AS owner_choice FROM ranked r WHERE r.rank_key IS NOT NULL), physical AS MATERIALIZED (SELECT 0 AS match_source, 0 AS candidate_kind, w.page_id, w.name, w.text_kind, w.journal_day, w.path, w.matched_text, w.source_kind, substr(w.rank_key, 10, 8) AS global_key, w.path AS tie_key FROM choices w WHERE w.owner_choice = 1), real_identities(name_key) AS (SELECT n.key FROM pages p JOIN names n ON n.name_id = p.name_id UNION SELECT n.key FROM reference_alias_declarations a JOIN names n ON n.name_id = a.alias_name_id), reference_choices AS (SELECT n.raw AS raw_name, n.key AS normalized_name, ROW_NUMBER() OVER (PARTITION BY n.key ORDER BY n.raw, n.key, n.name_id) AS name_choice FROM names n WHERE EXISTS (SELECT 1 FROM reference_postings r INDEXED BY reference_postings_navigation_names_idx WHERE r.target_name_id = n.name_id AND r.target_type = 0 AND r.reference_kind <= 4) AND NOT EXISTS (SELECT 1 FROM real_identities i WHERE i.name_key = n.key)), virtual AS MATERIALIZED (SELECT 0 AS match_source, 1 AS candidate_kind, NULL AS page_id, v.raw_name AS name, 0 AS text_kind, NULL AS journal_day, '' AS path, v.raw_name AS matched_text, 0 AS source_kind, substr(tine_query_rank(?, CAST(length(CAST(v.raw_name AS BLOB)) AS TEXT) || ':' || v.raw_name), 10, 8) AS global_key, v.normalized_name AS tie_key FROM reference_choices v WHERE v.name_choice = 1), candidates AS (SELECT * FROM physical WHERE global_key IS NOT NULL UNION ALL SELECT * FROM virtual WHERE global_key IS NOT NULL) SELECT c.match_source, c.candidate_kind, c.page_id, c.name, c.text_kind, c.journal_day, c.path, c.matched_text, c.source_kind, c.global_key FROM candidates c ORDER BY c.match_source, c.global_key, c.tie_key COLLATE BINARY LIMIT ?
WITH page_text_candidates(page_id, name, text_kind, journal_day, path, matched_text, source_kind, source_ordinal) AS (SELECT p.page_id, pn.raw, p.text_kind, p.journal_day, p.path, pn.raw, 0, -1 FROM pages p JOIN names pn ON pn.name_id = p.name_id UNION ALL SELECT p.page_id, pn.raw, p.text_kind, p.journal_day, p.path, an.raw, 1, MIN(a.ordinal) FROM reference_alias_declarations a JOIN pages p ON p.page_id = a.source_page_id JOIN names pn ON pn.name_id = p.name_id JOIN names an ON an.name_id = a.alias_name_id WHERE a.source_entity_type = 0 AND a.source_entity_id = a.source_page_id GROUP BY p.page_id, a.alias_name_id, an.raw), ranked AS MATERIALIZED (SELECT c.*, tine_query_rank(?, CAST(length(CAST(c.name AS BLOB)) AS TEXT) || ':' || c.matched_text) AS rank_key FROM page_text_candidates c), choices AS (SELECT r.*, ROW_NUMBER() OVER (PARTITION BY r.page_id ORDER BY substr(r.rank_key, 1, 9), r.source_kind, r.source_ordinal) AS owner_choice FROM ranked r WHERE r.rank_key IS NOT NULL), physical AS MATERIALIZED (SELECT 0 AS match_source, 0 AS candidate_kind, w.page_id, w.name, w.text_kind, w.journal_day, w.path, w.matched_text, w.source_kind, substr(w.rank_key, 10, 8) AS global_key, w.path AS tie_key FROM choices w WHERE w.owner_choice = 1), real_identities(name_key) AS (SELECT n.key FROM pages p JOIN names n ON n.name_id = p.name_id UNION SELECT n.key FROM reference_alias_declarations a JOIN names n ON n.name_id = a.alias_name_id), reference_choices AS (SELECT n.raw AS raw_name, n.key AS normalized_name, ROW_NUMBER() OVER (PARTITION BY n.key ORDER BY n.raw, n.key, n.name_id) AS name_choice FROM names n WHERE EXISTS (SELECT 1 FROM reference_postings r INDEXED BY reference_postings_navigation_names_idx WHERE r.target_name_id = n.name_id AND r.target_type = 0 AND r.reference_kind <= 4) AND NOT EXISTS (SELECT 1 FROM real_identities i WHERE i.name_key = n.key)), virtual AS MATERIALIZED (SELECT 0 AS match_source, 1 AS candidate_kind, NULL AS page_id, v.raw_name AS name, 0 AS text_kind, NULL AS journal_day, '' AS path, v.raw_name AS matched_text, 0 AS source_kind, substr(tine_query_rank(?, CAST(length(CAST(v.raw_name AS BLOB)) AS TEXT) || ':' || v.raw_name), 10, 8) AS global_key, v.normalized_name AS tie_key FROM reference_choices v WHERE v.name_choice = 1), content_ranked AS MATERIALIZED (SELECT b.block_id, b.page_id, bt.content AS matched_text, p.path AS matched_path, tine_query_rank(?, CAST(length(CAST(bt.content AS BLOB)) AS TEXT) || ':' || bt.content || p.path) AS content_key FROM (SELECT rowid AS block_id FROM search_fts WHERE search_fts MATCH ? ORDER BY rowid DESC) c JOIN blocks b ON b.block_id = c.block_id JOIN block_text bt ON bt.block_id = b.block_id JOIN pages p ON p.page_id = b.page_id), content_verified AS MATERIALIZED (SELECT * FROM content_ranked WHERE content_key IS NOT NULL), content_choices AS (SELECT k.*, ROW_NUMBER() OVER (PARTITION BY k.page_id ORDER BY k.content_key) AS content_choice FROM content_verified k WHERE 1 AND NOT EXISTS (SELECT 1 FROM physical x WHERE x.page_id = k.page_id)), content AS MATERIALIZED (SELECT 1 AS match_source, 0 AS candidate_kind, p.page_id, pn.raw AS name, p.text_kind, p.journal_day, p.path, k.matched_text, 0 AS source_kind, substr(k.content_key, 1, 29) AS global_key, k.content_key AS tie_key FROM content_choices k JOIN pages p ON p.page_id = k.page_id JOIN names pn ON pn.name_id = p.name_id WHERE k.content_choice = 1), candidates AS (SELECT * FROM physical WHERE global_key IS NOT NULL UNION ALL SELECT * FROM virtual WHERE global_key IS NOT NULL UNION ALL SELECT * FROM content WHERE global_key IS NOT NULL) SELECT c.match_source, c.candidate_kind, c.page_id, c.name, c.text_kind, c.journal_day, c.path, c.matched_text, c.source_kind, c.global_key FROM candidates c ORDER BY c.match_source, c.global_key, c.tie_key COLLATE BINARY LIMIT ?
WITH page_text_candidates(page_id, name, text_kind, journal_day, path, matched_text, source_kind, source_ordinal) AS (SELECT p.page_id, pn.raw, p.text_kind, p.journal_day, p.path, pn.raw, 0, -1 FROM pages p JOIN names pn ON pn.name_id = p.name_id UNION ALL SELECT p.page_id, pn.raw, p.text_kind, p.journal_day, p.path, an.raw, 1, MIN(a.ordinal) FROM reference_alias_declarations a JOIN pages p ON p.page_id = a.source_page_id JOIN names pn ON pn.name_id = p.name_id JOIN names an ON an.name_id = a.alias_name_id WHERE a.source_entity_type = 0 AND a.source_entity_id = a.source_page_id GROUP BY p.page_id, a.alias_name_id, an.raw), ranked AS MATERIALIZED (SELECT c.*, tine_query_rank(?, CAST(length(CAST(c.name AS BLOB)) AS TEXT) || ':' || c.matched_text) AS rank_key FROM page_text_candidates c), choices AS (SELECT r.*, ROW_NUMBER() OVER (PARTITION BY r.page_id, r.matched_text ORDER BY substr(r.rank_key, 1, 9), r.source_kind, r.source_ordinal) AS owner_choice FROM ranked r WHERE r.rank_key IS NOT NULL), physical AS MATERIALIZED (SELECT 0 AS match_source, 0 AS candidate_kind, w.page_id, w.name, w.text_kind, w.journal_day, w.path, w.matched_text, w.source_kind, substr(w.rank_key, 10, 8) AS global_key, w.path || char(0) || w.matched_text AS tie_key FROM choices w WHERE w.owner_choice = 1), real_identities(name_key) AS (SELECT n.key FROM pages p JOIN names n ON n.name_id = p.name_id UNION SELECT n.key FROM reference_alias_declarations a JOIN names n ON n.name_id = a.alias_name_id), reference_choices AS (SELECT n.raw AS raw_name, n.key AS normalized_name, ROW_NUMBER() OVER (PARTITION BY n.key ORDER BY n.raw, n.key, n.name_id) AS name_choice FROM names n WHERE EXISTS (SELECT 1 FROM reference_postings r INDEXED BY reference_postings_navigation_names_idx WHERE r.target_name_id = n.name_id AND r.target_type = 0 AND r.reference_kind <= 4) AND NOT EXISTS (SELECT 1 FROM real_identities i WHERE i.name_key = n.key)), virtual AS MATERIALIZED (SELECT 0 AS match_source, 1 AS candidate_kind, NULL AS page_id, v.raw_name AS name, 0 AS text_kind, NULL AS journal_day, '' AS path, v.raw_name AS matched_text, 0 AS source_kind, substr(tine_query_rank(?, CAST(length(CAST(v.raw_name AS BLOB)) AS TEXT) || ':' || v.raw_name), 10, 8) AS global_key, v.normalized_name AS tie_key FROM reference_choices v WHERE v.name_choice = 1), candidates AS (SELECT * FROM physical WHERE global_key IS NOT NULL UNION ALL SELECT * FROM virtual WHERE global_key IS NOT NULL) SELECT c.match_source, c.candidate_kind, c.page_id, c.name, c.text_kind, c.journal_day, c.path, c.matched_text, c.source_kind, c.global_key FROM candidates c ORDER BY c.match_source, c.global_key, c.tie_key COLLATE BINARY LIMIT ?
WITH r(page_id, name, text_kind, journal_day, path) AS (SELECT p.page_id, pn.raw, p.text_kind, p.journal_day, p.path FROM pages p JOIN names pn ON pn.name_id = p.name_id WHERE (p.page_id IN (SELECT pr1.owner_id FROM properties pr1 JOIN names prn2 ON prn2.name_id = pr1.name_id WHERE (prn2.key = ? AND pr1.owner_type = ?)) AND (NOT (EXISTS (SELECT 1 FROM properties pr3 JOIN names prn4 ON prn4.name_id = pr3.name_id WHERE (pr3.owner_id = p.page_id AND (prn4.key = ? AND pr3.owner_type = ?))) AND (SELECT COUNT(*) FROM property_atoms ac5 JOIN names acn6 ON acn6.name_id = ac5.name_id WHERE ac5.owner_type = ? AND ac5.owner_id = p.page_id AND acn6.key = ?) = ?)))) SELECT r.page_id, r.name, r.text_kind, r.journal_day, r.path, stored.page_id, stored.estimated_bytes, stored.property_count, COUNT(*) OVER () FROM r LEFT JOIN pages stored ON stored.page_id = r.page_id ORDER BY stored.path COLLATE BINARY LIMIT ?
WITH ranked AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id, b.order_key, bt.content AS raw_content, p_name.raw AS name, p.text_kind, p.path, b.page_id AS result_page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, CASE WHEN bt.block_id IS NULL OR p.page_id IS NULL THEN zeroblob(1) ELSE tine_query_rank(?, CAST(length(CAST(bt.content AS BLOB)) AS TEXT) || ':' || bt.content || p.path) END AS rank_key, CASE WHEN bt.block_id IS NULL OR p.page_id IS NULL THEN 1 ELSE 0 END AS missing_text FROM (SELECT block_id FROM blocks WHERE block_id IN (?)) c JOIN blocks b ON b.block_id = c.block_id LEFT JOIN block_text bt ON bt.block_id = b.block_id LEFT JOIN pages p ON p.page_id = b.page_id LEFT JOIN names p_name ON p_name.name_id = p.name_id WHERE p.path = ?), verified AS MATERIALIZED (SELECT * FROM ranked r WHERE r.missing_text = 1 OR r.rank_key IS NOT NULL) SELECT r.block_id, r.page_id, r.parent_block_id, r.order_key, r.raw_content, r.name, r.text_kind, r.path, r.result_page_id, r.preorder, r.result_id, r.estimated_bytes, r.tag_count, r.property_count, substr(r.rank_key, 1, 29) FROM verified r ORDER BY r.missing_text DESC, substr(r.rank_key, 1, 29), r.path COLLATE BINARY, r.preorder LIMIT ?
WITH ranked AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id, b.order_key, bt.content AS raw_content, p_name.raw AS name, p.text_kind, p.path, b.page_id AS result_page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, CASE WHEN bt.block_id IS NULL OR p.page_id IS NULL THEN zeroblob(1) ELSE tine_query_rank(?, CAST(length(CAST(bt.content AS BLOB)) AS TEXT) || ':' || bt.content || p.path) END AS rank_key, CASE WHEN bt.block_id IS NULL OR p.page_id IS NULL THEN 1 ELSE 0 END AS missing_text FROM (SELECT block_id FROM blocks WHERE block_id IN (?)) c JOIN blocks b ON b.block_id = c.block_id LEFT JOIN block_text bt ON bt.block_id = b.block_id LEFT JOIN pages p ON p.page_id = b.page_id LEFT JOIN names p_name ON p_name.name_id = p.name_id), verified AS MATERIALIZED (SELECT * FROM ranked r WHERE r.missing_text = 1 OR r.rank_key IS NOT NULL) SELECT r.block_id, r.page_id, r.parent_block_id, r.order_key, r.raw_content, r.name, r.text_kind, r.path, r.result_page_id, r.preorder, r.result_id, r.estimated_bytes, r.tag_count, r.property_count, substr(r.rank_key, 1, 29) FROM verified r ORDER BY r.missing_text DESC, substr(r.rank_key, 1, 29), r.path COLLATE BINARY, r.preorder LIMIT ?
WITH ranked AS MATERIALIZED (SELECT b.block_id, b.page_id, b.parent_block_id, b.order_key, bt.content AS raw_content, p_name.raw AS name, p.text_kind, p.path, b.page_id AS result_page_id, b.preorder, b.result_id, b.estimated_bytes, b.tag_count, b.property_count, CASE WHEN bt.block_id IS NULL OR p.page_id IS NULL THEN zeroblob(1) ELSE tine_query_rank(?, CAST(length(CAST(bt.content AS BLOB)) AS TEXT) || ':' || bt.content || p.path) END AS rank_key, CASE WHEN bt.block_id IS NULL OR p.page_id IS NULL THEN 1 ELSE 0 END AS missing_text FROM (SELECT rowid AS block_id FROM search_fts WHERE search_fts MATCH ? ORDER BY rowid DESC) c JOIN blocks b ON b.block_id = c.block_id LEFT JOIN block_text bt ON bt.block_id = b.block_id LEFT JOIN pages p ON p.page_id = b.page_id LEFT JOIN names p_name ON p_name.name_id = p.name_id), verified AS MATERIALIZED (SELECT * FROM ranked r WHERE r.missing_text = 1 OR r.rank_key IS NOT NULL) SELECT r.block_id, r.page_id, r.parent_block_id, r.order_key, r.raw_content, r.name, r.text_kind, r.path, r.result_page_id, r.preorder, r.result_id, r.estimated_bytes, r.tag_count, r.property_count, substr(r.rank_key, 1, 29) FROM verified r ORDER BY r.missing_text DESC, substr(r.rank_key, 1, 29), r.path COLLATE BINARY, r.preorder LIMIT ?
WITH requested(path) AS (VALUES (?)) SELECT o.owner_type, o.owner_id, o.page_id, n.raw, n.key, o.value, o.ordinal, b.page_id, p.path, pn.raw, p.text_kind, requested.path, p.page_id FROM requested LEFT JOIN pages p ON p.path = requested.path LEFT JOIN names pn ON pn.name_id = p.name_id LEFT JOIN properties o ON o.page_id = p.page_id LEFT JOIN names n ON n.name_id = o.name_id LEFT JOIN blocks b ON o.owner_type = 1 AND b.block_id = o.owner_id
WITH requested(text_kind, name_key) AS (VALUES (?)) SELECT page.path, revision.revision FROM requested JOIN names AS page_name ON page_name.key = requested.name_key JOIN pages AS page INDEXED BY pages_name_idx ON page.name_id = page_name.name_id AND page.text_kind = requested.text_kind LEFT JOIN direct_source_revisions AS revision ON revision.path = page.path ORDER BY page.path
