Android Direct Files saves work again after 0.6.981 refused them. Bullets gain an "Insert block above" menu item, a query no longer returns its own block, and Tab inside an embed keeps the caret where you are typing. This release also honors the linked-references collapse threshold from config.edn, restyles links when a page gains an alias, fixes Query Builder and submenu popovers, makes right-sidebar pages typable, keeps note text out of logs, and speeds up queries and references.
