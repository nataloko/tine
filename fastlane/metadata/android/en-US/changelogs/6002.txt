Tine 0.6.2 adds named workspaces (save and switch a whole window context per graph) and a Show-brackets toggle. Fixes: Android 9/10 launch crash; property names fold like Logseq; org #+BEGIN blocks with lists no longer split; page-header property save error; page-bottom "+ Add block"; editing inside existing [[page]]/((block)) refs; Linked References parity (Unicode identity, alias merging, honest counts, truncation); global block search ranking; mobile settings scroll and sidebar swipe.
