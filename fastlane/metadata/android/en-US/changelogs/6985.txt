Search on large graphs is far more dependable: the index is reused across launches, only changed pages are re-indexed, search no longer gets stuck on "Indexing", and settings apply without a rebuild. Opening a graph no longer rebuilds the index at every launch. Also new: a Conflicts overview, advanced queries written with DataScript patterns, shareable links in live exports, and a desktop command line. Many save, rename and sync fixes.
