{
 "schemaVersion": 1,
 "entries": [

{
 "id": "REG-PAGE-RENAME-REBIND-TITLE-451",
 "title": "Renaming a page rebinds an own title property that carries its old identity",
 "family": "direct-files",
 "sources": {
  "issues": [451],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/tests/gh451_research.rs"],
  "scenario": "direct-files.rename-rebinds-own-title",
  "failBefore": "proven"
 },
 "notes": "GH #451. A namespace page copied from the guide carried title:: old/name; rename moved its file and references but left that identity property stale, so the new route was empty and first edit conflicted. The rename transaction now rebinds only an own title property equal to the moved page's old identity."
},
{
 "id": "REG-COLD-OPEN-SHARED-PAGE-BUILD-550",
 "title": "Cold page inventory shares the whole-graph parse flight with startup consumers",
 "family": "direct-files",
 "sources": {
  "issues": [550],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/tests/issue550_cold_open_contention.rs"],
  "scenario": "direct-files.cold-open-single-page-build-flight",
  "failBefore": "proven"
 },
 "notes": "GH #550. list_pages and find_entry used an independent cold whole-graph parse while templates and warm-up owned the page-build flight, starving first content on slower Android hardware. Inventory now joins the shared flight, removing the duplicate parse. Android wall-clock verification remains a platform boundary."
},

{
 "id": "REG-QE-BLOCK-INDEXED-BACKLINK-NARROWING",
 "title": "Indexed explicit-reference candidate blocks preserve walked results while reducing classifications",
 "family": "query-engine",
 "sources": {
  "issues": [],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "needs-revalidation",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/sql_gates_tests.rs::block_narrowing_answers_exactly_what_the_walk_answers",
   "crates/tine-core/src/query/sql_gates_tests.rs::block_narrowing_answers_exactly_what_the_walk_answers_on_a_real_corpus"
  ],
  "scenario": "reg-qe-block-indexed-backlink-narrowing",
  "failBefore": "Not established by this metadata registration."
 },
 "notes": "Fast-corpus assertions require identical walked/narrowed reference rows, nonempty narrowing and fewer classifications. Plain-reference candidates remain outside explicit block narrowing. The real-corpus test is ignored by default and requires TINE_QUERY_IDENTITY_GRAPH; no fresh corpus or wall-clock speedup receipt is claimed. Registered 2026-09-13 by inspection of existing test bodies. No new test execution or fail-before/pass-after evidence; full candidate CI pending."
},
{
 "id": "REG-QE-PAGE-ANCHOR-PAGE-ROWS",
 "title": "Page-anchored execution returns page rows rather than block descriptors",
 "family": "query-engine",
 "sources": {
  "issues": [],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "needs-revalidation",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/conformance.rs::the_page_anchor_returns_page_rows_and_reads_no_document",
   "crates/tine-core/src/query/results_tests.rs::a_page_anchored_statement_has_no_block_descriptor"
  ],
  "scenario": "reg-qe-page-anchor-page-rows",
  "failBefore": "Not established by this metadata registration."
 },
 "notes": "Conformance checks page-row variant and namespace membership; SQL result test rejects block-descriptor construction for page anchoring. These backend assertions do not certify every page predicate or native page-row rendering. Registered 2026-09-13 by inspection of existing test bodies. No new test execution or fail-before/pass-after evidence; full candidate CI pending."
},
{
 "id": "REG-QE-TYPED-CASE-LIST-ATOM-COMPARISONS",
 "title": "Property atoms compare by effective type and explicit case and list comparison modes",
 "family": "query-engine",
 "sources": {
  "issues": [],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "needs-revalidation",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/conformance.rs::property_atoms_compare_by_their_keys_effective_type",
   "crates/tine-core/src/query/conformance.rs::the_case_matrix_mode_og_reproduces_ogs_measured_answers",
   "crates/tine-core/src/query/conformance.rs::each_mode_changes_only_the_answers_its_decision_owns",
   "crates/tine-core/src/query/conformance.rs::the_typed_case_separates_both_untyped_from_both"
  ],
  "scenario": "reg-qe-typed-case-list-atom-comparisons",
  "failBefore": "Not established by this metadata registration."
 },
 "notes": "Assertions distinguish numeric/date comparison from text ordering, reject uncoercible values even for inequality while retaining presence, and isolate case folding, comma splitting and declared-number coercion against frozen OG expectations. Oracle comments are historical provenance, not a new live OG run; these fixtures do not imply exhaustive operator or graph parity. Registered 2026-09-13 by inspection of existing test bodies. No new test execution or fail-before/pass-after evidence; full candidate CI pending."
},
{
 "id": "REG-QE-SELECTIVE-CONJUNCTION-SQL",
 "title": "Positive conjunction bounds and indexed subqueries preserve query results",
 "family": "query-engine",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "Release impact registration for Unreleased item 54 at 6bc9c675"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "needs-revalidation",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/sql_gates_tests.rs::a_nested_conjunction_is_part_of_the_root_conjunction",
   "crates/tine-core/src/query/sql_gates_tests.rs::a_positively_bounded_query_searches_its_anchor_and_indexes_its_subqueries",
   "crates/tine-core/src/query/sql_gates_tests.rs::the_baseline_and_current_statements_are_measured_against_each_other"
  ],
  "scenario": "reg-qe-selective-conjunction-sql",
  "failBefore": "Not established by this metadata registration; existing test source inspected only."
 },
 "notes": "Nested conjunctions participate in root conjunction bounds and positively bounded queries retain anchor/indexed-subquery behavior. The baseline comparison is ignored by default and requires TINE_QUERY_IDENTITY_GRAPH plus TINE_QUERY_STATEMENT_BASELINE; it is a benchmark harness, not a fresh speedup receipt. Registered 2026-09-13 from existing executable test bodies. No new test execution, fail-before/pass-after, performance or native delivery result is claimed. Full candidate CI is pending; current release acceptance remains with the integrating owner."
},
{
 "id": "REG-QE-ORDER-SAMPLE-COMPLETE-STATISTICS",
 "title": "Ordering and sampling use the complete match set before display caps",
 "family": "query-engine",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "Release impact registration for Unreleased item 58 at 6bc9c675"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "needs-revalidation",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/results_tests.rs::q4_blocks_sort_all_before_row_cap_and_sample",
   "crates/tine-core/src/query/results_tests.rs::q4_unsorted_sample_uses_complete_base_order",
   "crates/tine-core/src/query/results_tests.rs::q4_statistics_cap1_of3_includes_tail_numeric_group"
  ],
  "scenario": "reg-qe-order-sample-complete-statistics",
  "failBefore": "Not established by this metadata registration; existing test source inspected only."
 },
 "notes": "Block ordering precedes caps and sampling; unsorted samples use complete base order and statistics include tail numeric groups beyond the displayed cap. Registered 2026-09-13 from existing executable test bodies. No new test execution, fail-before/pass-after, performance or native delivery result is claimed. Full candidate CI is pending; current release acceptance remains with the integrating owner."
},
{
 "id": "REG-QE-PRINT-INDEXED-QUERY-SUBTREES",
 "title": "Print uses indexed query subtrees and preserves export readiness and cancellation",
 "family": "query-engine",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "Release impact registration for Unreleased item 59 at 6bc9c675"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "needs-revalidation",
 "coverage": {
  "tests": [
   "src/print.query.test.ts"
  ],
  "scenario": "reg-qe-print-indexed-query-subtrees",
  "failBefore": "Not established by this metadata registration; existing test source inspected only."
 },
 "notes": "Print uses SQLite query subtrees; query routes do not hydrate parsed graphs. Frontend Print tests cover its query preparation contract; native PDF rendering needs separate receipts. Registered 2026-09-13 from existing executable test bodies. No new test execution, fail-before/pass-after, performance or native delivery result is claimed. Full candidate CI is pending; current release acceptance remains with the integrating owner."
},
{
 "id": "REG-DIRECT-REGISTRY-INDEXED-ROW-SOURCE",
 "title": "Ready Direct registry rebuilds read the projection and preserve row-source equivalence",
 "family": "query-engine",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "Release impact registration for Unreleased item 60 at 6bc9c675"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "needs-revalidation",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_tests.rs::every_implemented_row_source_builds_the_same_registry",
   "crates/tine-core/src/model_tests.rs::registry_row_sources_agree_on_a_real_graph"
  ],
  "scenario": "reg-direct-registry-indexed-row-source",
  "failBefore": "Not established by this metadata registration; existing test source inspected only."
 },
 "notes": "Constructed graph test checks equivalent registries across row sources and proves projection wiring with indexed-read counters. Real-graph equivalence test is ignored by default and needs TINE_REGISTRY_TIMING_GRAPH; no real-corpus or timing run claimed. Registered 2026-09-13 from existing executable test bodies. No new test execution, fail-before/pass-after, performance or native delivery result is claimed. Full candidate CI is pending; current release acceptance remains with the integrating owner."
},
{
 "id": "REG-DIRECT-WARM-REOPEN-NO-PARSE",
 "title": "Unchanged Direct warm reopen parses no pages and answers public queries through SQL",
 "family": "query-engine",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "Release impact registration for Unreleased item 63 at 6bc9c675"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "needs-revalidation",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::warm_reopen_parses_nothing_and_answers_from_sql",
   "crates/tine-core/src/direct_projection_tests.rs::public_ir_query_on_warm_reopen_uses_sql_without_parsed_cache"
  ],
  "scenario": "reg-direct-warm-reopen-no-parse",
  "failBefore": "Not established by this metadata registration; existing test source inspected only."
 },
 "notes": "Counters assert zero page/stream parses and lowerings on unchanged warm reopen; public bounded and IR queries use SQLite without parsed-cache hydration. This structural proof is not a wall-clock latency measurement. Registered 2026-09-13 from existing executable test bodies. No new test execution, fail-before/pass-after, performance or native delivery result is claimed. Full candidate CI is pending; current release acceptance remains with the integrating owner."
},
  {
   "id": "REG-DIRECT-RENAME-LITERAL-SCANNER-406",
   "title": "Direct rename copies literal runs and omits scans whose required syntax is absent",
   "family": "page-rename",
   "sources": {
    "issues": [
     406
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/refs.rs",
     "crates/tine-core/tests/graph.rs"
    ],
    "scenario": "direct-files.rename-literal-run-scanner-preserves-references",
    "failBefore": "proven"
   },
   "notes": "Partial GH #406 performance improvement. Controlled optimized Linux Graph::rename_page_reporting measurement with 10005 generated files, 164430159 bytes, and 5 edited files: baseline median 2087.725 ms, identical-binary control 2078.299 ms, candidate median 1366.6345 ms (34.54% reduction). Scanner differential probe compared 40000 generated Markdown/Org inputs against frozen prior scanner with zero mismatches. Every current file remains read; namespace/ref rewrite, no-follow paths, collisions, base revisions, publication and rollback are unchanged. Hosted title workflow and Windows reporter latency remain unverified; do not interpret this row as closing the full issue."
  },
{
 "id": "REG-QE-EXPORT-EXPLICIT-TQL-DIALECT",
 "title": "Copy and export preserve the declared TQL dialect and subtree membership",
 "family": "query-engine",
 "sources": { "issues": [], "commits": [], "internal": "RET3 export dialect preservation" },
 "platforms": ["all"], "layer": "rust-core", "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/export_results_tests.rs::tql_export_preserves_the_declared_dialect_and_exact_subtree_membership",
   "crates/tine-core/src/query_tests.rs::query_export_spec_defaults_a_missing_simple_dialect_to_og",
   "src/components/ExportModal.queryBudget.test.tsx"
  ],
  "scenario": "query-engine.export-explicit-dialect",
  "failBefore": "Manager frontend58147 restores only old ExportModal production code: new TQL spec assertion fails missing simple_dialect, three other tests pass. Exact candidate restored."
 },
 "notes": "Missing simple dialect defaults to OG; a parsed tine-query sends TQL explicitly and retains authored text. Advanced dispatch/current-page binding takes precedence. Gate19760 passes32 export tests with1ignored, including exact root and descendant identities; frontend24179 passes4 plus typecheck. This does not claim production SQL export adapter completion or native delivery."
},
{
 "id": "REG-QE-PAGE-BLOCKS-SEMANTICS",
 "title": "Page queries quantify the ordinary blocks physically owned by each page",
 "family": "query-engine",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "C2B page Blocks semantic completion"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "needs-revalidation",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/sql_gates_tests.rs::page_blocks_quantifies_the_physical_page_forest_with_full_block_semantics",
   "crates/tine-core/src/query/sql_gates_tests.rs::page_blocks_keeps_invalid_and_wrong_scope_inputs_empty",
   "crates/tine-core/src/query/sql_tests.rs::page_blocks_bounds_only_from_a_selective_positive_block_predicate",
   "crates/tine-core/src/query/sql_gates_tests.rs::page_blocks_supports_finite_page_blocks_reentry"
  ],
  "scenario": "query-engine.page-blocks-physical-page-semantics",
  "failBefore": "Manager gate 32038 failed expected TODO page membership on base 464f9808. With the semantic changes and TQL dependency 4c0fd750, gate 43010 passed all four page-block tests; gate 11427 passed 46 SQL tests with eight ignored; gate 99375 passed 402 query tests with twelve ignored."
 },
 "notes": "The relation ranges over all ordinary roots and descendants on one page_id, excludes the page preamble, preserves vacuous Every/None, block predicates and nested Page/Blocks re-entry, and admits each outer page once. Equal display names do not merge physical pages. Manager verified fail-before and pass-after at the core layer. This does not claim completed page Display or native delivery. 2026-09-13 coverage reconciliation at 3602f3e4: removed unresolved executable references (historical evidence only): crates/tine-core/src/query/sql_gates_tests.rs::page_blocks_supports_finite_page_blocks_reentry_and_outer_physical_masks. The original contract and fail-before history above are retained, but current full coverage is not established. Surviving tests remain partial evidence; this metadata repair does not certify the contract or authorize release. The current reentry test verifies nested Page/Blocks against both SQL and walk; it no longer includes the removed outer-physical-mask assertions, so this is partial replacement only."
},
{
 "id": "REG-QE-PUBLICATION-WINDOWS-PRIVATE-DIRECTORY",
 "title": "Windows publication query storage does not inherit a permissive temporary-directory ACL",
 "family": "query-engine",
 "sources": { "issues": [], "commits": [], "internal": "RET2 publication private-root correction" },
 "platforms": ["windows"], "layer": "rust-core", "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/publish/private_directory.rs::tests::private_child_rejects_parent_permissions_and_preserves_existing_paths"],
  "scenario": "query-engine.publication-windows-private-directory"
 },
 "notes": "Creates a private child below a permissive parent, validates actual protected owner-only ACL, and preserves an existing path on collision. Cross-compilation is separate from required hosted Windows runtime evidence."
},
{
 "id": "REG-QE-PUBLICATION-WRITER-LIFETIME",
 "title": "Publication retains temporary query resources through writer close timeout",
 "family": "query-engine",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "RET2 Direct correction manager review"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "needs-revalidation",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::a_timed_out_close_retains_resources_until_the_writer_really_exits"
  ],
  "scenario": "query-engine.publication-writer-lifetime"
 },
 "notes": "Real paused writer cannot release resources merely because a zero-duration wait expires; resources release after exit and late registration cannot leak. Windows permissions have their own platform fixture. 2026-09-13 coverage reconciliation at 3602f3e4: removed unresolved executable references (historical evidence only): crates/tine-core/src/publish.rs::tests::publication_snapshot_query_index_is_owner_private_and_dies_with_its_root. The original contract and fail-before history above are retained, but current full coverage is not established. Surviving tests remain partial evidence; this metadata repair does not certify the contract or authorize release."
},
{
 "id": "REG-QE-P1D-PERSISTENT-CACHE-RECOVERY",
 "title": "Failed Direct query repairs persistent projection damage without a source edit",
 "family": "query-engine",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "Query campaign P1-d manager review"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::a_damaged_query_table_is_rebuilt_without_a_source_edit"
  ],
  "scenario": "query-engine.persistent-cache-recovery",
  "failBefore": "P1d-recovery-fail-before.log at c682a7cf: second query remains on fallback after ready"
 },
 "notes": "A full snapshot must clear stale source-revision stamps after failed reads."
},
  {
   "id": "REG-W3-E-TYPED-ERROR-BOUNDARY-001",
   "title": "Backend refusal handling branches on typed fixed-shape envelopes instead of prose",
   "family": "typed-error-boundary",
   "sources": {"issues": [], "commits": [], "internal": "Harvest Wave 3 E"},
   "platforms": ["all"],
   "layer": "rust-tauri-frontend-boundary",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/typedErrorRatchet.test.ts",
     "src-tauri/src/debug.rs::tests::caught_named_thread_panic_records_fixed_shape_location_thread_and_kind"
    ],
    "scenario": "backend-errors.typed-fixed-shape-no-prose-classification",
    "failBefore": "e-typed-branches-fail-before.txt, e-scenario-io-fail-before.txt, and e-panic-fields-fail-before.txt at 74d00d18e292e6bde629b833e832c4bc7a7a7945"
   },
   "notes": "The native boundary emits bounded kind and reason-code envelopes, frontend consumers branch on BackendError subclasses, ScenarioError retains ErrorKind instead of prose, and the panic recorder keeps only location, thread, and message kind. The clean-open source taxonomy that completes this family for managed open is REG-W4-E3-CLEAN-OPEN-TAXONOMY-001."
  },
  {
   "id": "REG-W3-I5-CONTENT-OUT-OF-LOGS-001",
   "title": "Production diagnostics cannot emit graph-authored content",
   "family": "diagnostics-privacy",
   "sources": {"issues": [], "commits": [], "internal": "Harvest Wave 3 I5; invariants I-5 and I-11"},
   "platforms": ["all"],
   "layer": "rust-and-typescript-diagnostics",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/debug.rs::tests::diag_is_silent_when_debug_is_disabled",
     "crates/tine-core/tests/content_out_of_logs.rs",
     "src/contentOutOfLogs.ratchet.test.ts",
     "crates/tine-core/src/direct_projection_tests.rs::retired_class_c_projection_database_open_emits_no_planted_marker",
     "crates/tine-core/src/direct_projection_tests.rs::retired_class_c_projection_apply_failure_emits_no_planted_marker",
     "src/failureShape.test.ts",
     "src/pdfRenderCoordinator.test.ts"
    ],
    "scenario": "diagnostics.content-out-of-logs",
    "failBefore": "i5-diag-fail-before.txt and i5-lsdoc-shape-fail-before.txt at 64ed1c21838d4d7e9b75dd9ece90d90787b300fa; class-c retirement: class-c-projection-fail-before.log and frontend-class-c-fail-before.log at 20bb15de"
   },
   "notes": "Detailed native diagnostics are opt-in, core print sites are content-free and flag-gated, parser failures cross worker boundaries only as fixed-shape measurements, and equality ratchets freeze the reviewed Rust and TypeScript censuses."
  },
  {
   "id": "REG-W3-R1-WINDOWS-BACKUP-NOREPLACE-001",
   "title": "Windows backup recovery cannot clobber a concurrently delivered destination",
   "family": "backup-restore-publication",
   "sources": {"issues": [], "commits": [], "internal": "Harvest Wave 3 R1-6"},
   "platforms": ["windows"],
   "layer": "tauri-backup",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/backup.rs::tests::restore_stack_keeps_capability_bound_publication_and_orders_crash_barriers",
     "src-tauri/src/backup.rs::tests::windows_backup_recovery_rejects_a_concurrent_sync_delivery"
    ],
    "scenario": "backup-restore.windows-concurrent-sync-delivery",
    "failBefore": "r1-r1-6-fail-before.txt at a38c84f8fb97b8a0aa2d0f32aed6058ca40e34d5: Windows fell through a negated four-platform cfg to plain rename after an existence check"
   },
   "notes": "Windows uses capability-bound hard-link-create followed by source removal; an occupied destination returns AlreadyExists and neither the delivered destination nor the live source is overwritten. All five Tine targets are explicit cfg arms."
  },
  {
   "id": "REG-W3-CC-PUBLISH-PUBLIC-PROPERTY-001",
   "title": "Static publication recognizes public pages through the same property-line grammar as Logseq",
   "family": "static-publication-privacy",
   "sources": {"issues": [], "commits": [], "internal": "Harvest Wave 3 Cc item C5a; DUP-7"},
   "platforms": ["all"],
   "layer": "rust-core-publish",
   "status": "covered",
   "coverage": {
    "tests": ["crates/tine-core/src/publish_tests.rs::publish_macros_never_expand_private_graph_content"],
    "scenario": "publish.public-property-lsdoc-boundary",
    "failBefore": "cc-c5a-fail-before-full.txt at ae3d97da170c2aaef41a3cb64b3e574f26a8ca58: a page containing public::true was published even though lsdoc rejects that line as a property; a SUB-indented valid public property was omitted"
   },
   "notes": "page_is_public delegates to the shared parse_property_line transcription of lsdoc markdown_property_line and normalizes the key through doc::property_key_norm. The publisher never falls back to its former starts_with/trim recognizer. This is a privacy and interoperability boundary."
  },
  {
   "id": "REG-W3-P0-PAGE-TAG-QUERY-DEDUPE-001",
   "title": "A routed named page shares one tag query per graph revision across its toggle, table, and split panes",
   "family": "waited-path-query-cost",
   "sources": {"issues": [], "commits": [], "internal": "Harvest Wave 3 P0 item 3"},
   "platforms": ["all"],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": ["src/components/Page.test.tsx::tag-page table"],
    "scenario": "page-tag-table.shared-query-per-data-revision",
    "failBefore": "tine-agents/evidence/wave3/P0/item3-fail-before.log at b51e54e3ee3891f1385a50fed2a336ade443e88e: the toggle and enabled table made two identical runQuery calls"
   },
   "notes": "The existing sharedQueryResult graph scope is reused; journals retain their null resource key and issue no query. No presentation catalog row is required."
  },
  {
   "id": "REG-W3-P0-QUERY-BUILDER-FACETS-DEDUPE-001",
   "title": "One Query Builder instance reads the graph vocabulary once per revision for all mounted controls",
   "family": "waited-path-query-cost",
   "sources": {"issues": [], "commits": [], "internal": "Harvest Wave 3 P0 item 4"},
   "platforms": ["all"],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": ["src/components/QueryBuilder.transient.test.tsx::asks the graph nothing at rest, reads the registry when the sheet opens, and again per data revision"],
    "scenario": "query-builder.vocabulary-once-per-instance-revision",
    "failBefore": "tine-agents/evidence/wave3/P0/item4-fail-before.log at b51e54e3ee3891f1385a50fed2a336ade443e88e: SummarizeControl and PropertyPick made two queryFacets calls in one builder"
   },
   "notes": "The shared resource is owned by the builder instance and keyed by graphEpoch plus dataRev. Harvest W4-P1 then made the REQUEST shared across instances too, through sharedQueryResult under a query-facets key namespace: the scope stays per-builder by decision, so this row's outcome is unchanged and now holds a fortiori. Its proof test lost the words \"per builder\" from its title in that packet, because per-builder fetching is exactly what stopped being true; see REG-W4-B4B-DIRECT-INDEXED-QUERY-ROUTING-001's sibling UI row UI-HARVEST-W4-P1-QUERY-BUILDER-FACET-SHARING for the cross-instance bound. P3 moved the first fetch later still: a query block at REST is one sentence and asks for no facets at all, so the title now names the moment the sheet opens. The bound per revision is unchanged. P4 replaced the QUESTION: `queryFacets(false)` was deleted with the two-stage property chooser it fed, and the one read is now `query_registry`, keyed by graph scope, `dataRev` and a shared declaration revision. Both consumers in a builder \u2014 the vocabulary picker and Summarize \u2014 are served from that one read, so this row's outcome is unchanged."
  },
  {
   "id": "REG-HARVEST-F-CONTENT-DEPTH-BOUNDS-001",
   "title": "Crafted deeply nested formulas, queries, previews, and backlink trees stop at their consumption-point bounds",
   "family": "hostile-content-consumption",
   "sources": {"issues": [], "commits": [], "internal": "Harvest F depth sweep"},
   "platforms": ["all"],
   "layer": "frontend-and-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/sheet/formula/eval.test.ts",
     "src/editor/queryBuilder.test.ts",
     "src/render/PeekPopup.test.ts"
    ],
    "scenario": "content.deep-recursion-consumption-bounds",
    "failBefore": "proven on e19514f9"
   },
   "notes": "Every cap-plus-one fixture is constructed iteratively. Before Harvest F each consumer fully traversed it; afterwards formulas return a named error, the visual query path falls back or truncates, previews count omitted descendants, and both Direct and application backlink filter entries mark truncation while reusing the managed block-depth contract."
  },
  {
   "id": "REG-SESSION-ATOMIC-DURABILITY-001",
   "title": "App-private session state is published through a named crash-durable atomic replacement protocol",
   "family": "app-private-data-safety",
   "sources": { "issues": [], "commits": [], "internal": "2026-09 invariant sweep B5s" },
   "platforms": ["all"],
   "layer": "tauri-native",
   "status": "covered",
   "coverage": {
    "tests": ["src-tauri/src/settings.rs"],
    "scenario": "settings.session-atomic-durable-publication",
    "failBefore": "proven on exact base 2dcd01341e6797f1223d35bc2d1fdd7a4f98f910 by app_private_durable_publications_stay_on_named_audited_paths"
   },
   "notes": "The pre-fix session saver used a bare whole-file temp write followed by rename, without syncing the file or destination directory, while sibling durable settings writers already used audited protocols. The guard requires named audited publication; behavioral tests cover stale-temp isolation, burst-valid atomic replacement, and temp cleanup. Legacy migration receives the same strict directory-barrier error policy."
  },
  {
   "id": "REG-HARVEST-B3-LIVE-CONFLICT-CAPSULE-001",
   "title": "Live-save conflict drafts survive restart through one durable app-private capsule protocol on Direct Files and Managed Storage",
   "family": "live-save-conflict-data-safety",
   "sources": {"issues": [], "commits": [], "internal": "Harvest B3"},
   "platforms": ["all"],
   "layer": "tauri-native-and-frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/conflict_capsule.rs::tests::replacement_and_retirement_reopen_as_complete_transitions",
     "src-tauri/src/conflict_capsule.rs::tests::unreadable_envelope_is_set_aside_and_the_queue_keeps_working",
     "src/liveSaveConflictRetirement.test.tsx",
     "src/backendCallFunnel.test.ts",
     "src/components/ConflictResolution.test.tsx",
     "scripts/e2e-concord-live-save.mjs"
    ],
    "scenario": "harvest-b3.live-conflict-capsule-restart-parity",
    "failBefore": "capsule protocol: b3-conflict-capsule-fail-before.log on 7623fb76's parent (no capsule module; archived under tine-agents/evidence/wave2). Fix-up 2026-09-02 on 7623fb76: liveSaveConflictRetirement (clear path never retired the on-disk capsule, so a resolved draft resurrected after restart) and backendCallFunnel (only savePage classified the conflict wire code; seven resolve commands threw raw strings) fail before, pass after."
   },
   "notes": "Before Harvest B3, Direct Files retained the only copy of a refused live draft in localStorage, which WebKitGTK does not persist, while the Managed Storage conflict branch retained no restart capsule at all. The replacement uses a graph-keyed app-private atomic publication and retirement protocol, restores exact draft bytes and binding on both backends, and requires Managed authority to be re-observed after restart. The native restart journey itself is quarantined (stop-loss exhausted); restart restoration, post-restart resolution, and Managed re-observation are proved only at the unit/crate layer until that journey is repaired."
  },
  {
   "id": "REG-REPORT-EXPORT-ATOMIC-001",
   "title": "User-selected diagnostic and graph-verification reports publish atomically instead of truncating destinations in place",
   "family": "data-safety",
   "sources": { "issues": [], "commits": [] },
   "platforms": ["desktop"],
   "layer": "native",
   "status": "covered",
   "coverage": {
    "tests": ["src-tauri/src/debug.rs", "src-tauri/src/graph_verification.rs", "crates/tine-core/src/model.rs"],
    "scenario": "reports.user-selected-atomic-export",
    "failBefore": "proven"
   },
   "notes": "Fail-before on 3e275614: both savers called std::fs::write directly. They now stage and flush complete bytes, atomically replace the selected name, and flush its parent through the shared small-output publication family."
  },
  {
   "id": "REG-BACKUP-RESTORE-DIRECTORY-DURABILITY-001",
   "title": "Capability-bound backup restore never acknowledges recovery reservation retirement or publication before directory entries are durable",
   "family": "backup-recovery",
   "sources": { "issues": [], "commits": [] },
   "platforms": ["all"],
   "layer": "native",
   "status": "covered",
   "coverage": {
    "tests": ["src-tauri/src/backup.rs"],
    "scenario": "backup.restore-directory-barrier-crash-safety",
    "failBefore": "proven"
   },
   "notes": "Fail-before on 3e275614: recovery directory creation and cross-directory live retirement had no directory barriers, and live publication silently skipped a failed parent-handle clone. Injected EIO tests now prove each operation refuses success while retaining only complete old or new bytes; cross-directory barriers are recovery-first."
  },
  {
   "id": "REG-CORE-TEST-COMPLETION-350",
   "title": "The complete tine-core release corpus terminates and reports every current failure without stale passing exclusions",
   "family": "release-test-honesty",
   "sources": {
    "issues": [
     350
    ],
    "commits": []
   },
   "platforms": [
    "linux"
   ],
   "layer": "rust-test-infrastructure",
   "status": "covered",
   "coverage": {
    "tests": [
     "scripts/test-tine-core-nextest-contract.mjs",
     "scripts/tine-core-nextest-contract.mjs"
    ],
    "scenario": "release.tine-core-complete-honest-corpus",
    "failBefore": "proven"
   },
   "notes": "Fail-before on exact base 22478c64559ab1b7235df9b486481f62a2244d72: sync_runtime::tests::shared_join_releases_ordinary_operation_ownership_between_turns remained blocked beyond a 75-second diagnostic cap because the clean join now completes in one actor request and never reaches the retired BetweenTurns pause. Four sibling tests waited at equally unreachable BetweenTurns, AfterProviderScan, or AfterLocalScan hooks. Pass-after evidence replaced those hooks with finite current clean-join cuts and all five rewritten tests passed. Honest unfiltered nextest run d8601013-53d9-4e02-ad2b-880423efdafb completed all 2,116 tests in 143.706 seconds: 2,071 passed, 45 failed normally, 41 were ignored, and none hung or timed out. All 45 failures were already named in the old 92-test exclusion, no unexpected test failed, and 47 stale or passing exclusions were removed. The exact residual set is classified into activation/enrollment, application/semantic convergence, provider recovery/publication, and bounded discovery/traversal families. Those legacy-oracle failures do not authorize production changes without independent current-runtime fail-before proof; this change has no production runtime delta."
  },
  {
   "id": "REG-DIRECT-COLD-CREATION-318",
   "title": "Direct Files safely creates pages and journals after graph open or identity-cache invalidation without mistaking cold proof state for a collision",
   "family": "direct-files-creation-authority",
   "sources": {
    "issues": [
     318
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "released",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::cold_graph_creation_repairs_identity_evidence_once",
     "crates/tine-core/src/model.rs::generation_bound_identity_validation_avoids_save_time_graph_reparse",
     "crates/tine-core/src/model.rs::default_journal_format_creates_journal",
     "crates/tine-core/src/model.rs::direct_cold_creation_joins_a_paused_paced_warm",
     "crates/tine-core/src/model.rs::concurrent_direct_creation_proofs_join_one_build_without_graph_censuses",
     "crates/tine-core/src/model.rs::racing_save_page_creators_serialize_and_reuse_one_published_build",
     "crates/tine-core/src/model.rs::late_page_build_claim_after_completed_install_is_non_owner",
     "crates/tine-core/src/model.rs::generation_drift_before_direct_install_refuses_without_retry_or_census",
     "crates/tine-core/src/model.rs::broad_invalidation_rebuilds_current_titles_once_before_creation",
     "crates/tine-core/src/model_tests_unreadable_pages.rs::cold_malformed_owner_refuses_without_census_or_mutation"
    ],
    "scenario": "direct-files.cold-generation-safe-page-and-journal-creation",
    "failBefore": "proven"
   },
   "notes": "Fail-before on 2f935398: ordinary missing-target page and journal creation returned AlreadyExists when Graph::open had not yet completed its asynchronous parsed-cache warm. The covered core matrix proves one generation-coherent shared identity build, join behavior with paced warm and racing creators, exact-generation refusal without retry, current identity publication after invalidation, malformed-owner refusal before census or mutation, and graph-parse-free subsequent warm creation. Existing semantic, portable-path, hard-link, symlink, external-race, unreadable-owner, and no-overwrite refusals remain covered. Managed storage is out of scope and unchanged."
  },
  {
   "id": "REG-PAGE-FILENAME-IDENTITY-249",
   "title": "Page titles map injectively to Windows-safe reversible filenames and survive save and reopen without changing existing graph files",
   "family": "filesystem-safety",
   "sources": {
    "issues": [
     249
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::page_name_encoding_is_injective_reversible_and_windows_safe",
     "crates/tine-core/src/model.rs::unsafe_page_titles_save_and_reopen_through_one_safe_identity",
     "crates/tine-core/src/model.rs::unsafe_page_titles_rename_and_rescue_use_the_same_safe_identity",
     "crates/tine-core/src/model.rs::safe_filename_collisions_refuse_without_overwriting_existing_bytes"
    ],
    "scenario": "page-file.safe-reversible-identity",
    "failBefore": "proven"
   },
   "notes": "Fail-before on 1eec95c1: the shared codec emitted 2026-07-23_18:01:20 unchanged, the create/rename journeys selected that Windows-illegal stem, and an occupied A%3AB.md did not prevent an unsafe A:B.md twin. The covered core matrix proves both filename formats, Windows-reserved characters/control bytes, trailing dot/space, DOS devices, literal percent escapes, namespace ambiguity, Unicode, create/edit/save/reopen, rename/rescue, direct/managed/sparse derivation, exact existing-path preservation, and collision refusal. Literal Windows desktop proof remains a manager-level packet gate."
  },
  {
   "id": "REG-JOURNAL-TEMPLATE-ROLLOVER-260",
   "title": "A configured default journal template materializes exactly once when the loaded graph crosses local midnight",
   "family": "journal-durable-materialization",
   "sources": {
    "issues": [
     260
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "render-core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/components/Page.test.tsx",
     "src/graph.test.tsx",
     "crates/tine-core/tests/graph.rs"
    ],
    "scenario": "journals.configured-template-local-day-rollover",
    "failBefore": "proven"
   },
   "notes": "The accepted contract requires the timer/focus/visibility lifecycle to single-flight one graph-generation/day-bound template ensure before the feed refresh; dirty content and stale graph/day work must remain inert, no-template rollover must stay lazy, and guarded native serialization must survive restart byte-for-byte."
  },
  {
   "id": "REG-QUERY-OG-MEMBERSHIP-BOUNDS-001",
   "title": "Simple query page terms unquoted search and date ranges match OG membership and bounds across presentations",
   "family": "query-correctness",
   "sources": {
    "issues": [],
    "commits": [
     "b54a331",
     "8fa8c51",
     "cb9e749",
     "83f902b"
    ]
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native-ui",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query.rs",
     "src/editor/queryBuilder.test.ts",
     "src/components/QueryMacro.test.tsx"
    ],
    "scenario": "queries.og-membership-and-date-bounds",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-EXTERNAL-ASSETS-TRUST-001",
   "title": "A graph can use an explicitly approved external assets link without widening filesystem access",
   "family": "filesystem-safety",
   "sources": {
    "issues": [
     127
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "closed",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs",
     "src-tauri/src/settings.rs",
     "src/graph.test.tsx",
     "scripts/e2e-external-assets.mjs"
    ],
    "scenario": "filesystem.external-assets-trust",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-ASSET-OBSERVATION-001",
   "title": "Externally changed asset images refresh without admitting asset bytes to graph text or managed storage",
   "family": "external-files",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "native-ui",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/watcher.rs",
     "src/mediaEditors.test.ts",
     "scripts/e2e-external-assets.mjs"
    ],
    "scenario": "filesystem.asset-observation-and-cache-refresh",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-ANDROID-BACKUP-RESTORE-EXDEV-001",
   "title": "Backup restore pages journals sidecars and config keep recovery and publication on the live graph filesystem when app data is on another device",
   "family": "backup-recovery",
   "sources": {
    "issues": [
     130
    ],
    "commits": []
   },
   "platforms": [
    "android",
    "linux"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/backup.rs"
    ],
    "scenario": "backup.restore-cross-filesystem-recovery",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-PDF-HIGHLIGHT-PAIR-ATOMICITY-001",
   "title": "A failed PDF annotation-page save restores the highlight sidecar to its exact pre-write state",
   "family": "data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs"
    ],
    "scenario": "pdf.highlight-pair-failure-atomicity",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-ABSENT-FILE-PUBLISH-NOREPLACE-001",
   "title": "Creating pages PDF artifacts and demo files never replaces an external race winner",
   "family": "data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs",
     "crates/tine-core/src/onboarding.rs"
    ],
    "scenario": "filesystem.absent-publish-no-replace",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-GUIDE-COPY-CONTAINMENT-001",
   "title": "Copy Guide revalidates page and asset targets after managed-directory swaps",
   "family": "filesystem-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs",
     "crates/tine-core/src/onboarding.rs"
    ],
    "scenario": "filesystem.guide-copy-containment",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-IOS-GUIDE-COPY-ATOMIC-PUBLICATION-001",
   "title": "Copying the bundled Guide can atomically create pages on iOS",
   "family": "platform-compatibility",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "ios"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/iosAtomicPublicationContract.test.ts",
     "crates/tine-core/src/onboarding.rs",
     ".github/workflows/ios-probe.yml"
    ],
    "scenario": "ios.guide-copy-atomic-publication",
    "failBefore": "proven"
   },
   "notes": "A physical iPhone running the first public TestFlight beta reached the bundled Guide but Copy the guide into your graph failed because model.rs selected renameatx_np(RENAME_EXCL) only for macOS and compiled iOS into the unsupported fallback. The correction applies the Darwin primitive to iOS across graph projection, managed no-clobber moves, enrollment publication, and cleanup-marker rotation. The contract test fails if a macOS-only atomic-publication branch returns, while the manual iOS probe invokes the real bound-graph Guide-copy backend inside the Simulator and requires the copied index page to exist."
  },
  {
   "id": "REG-IOS-CONTAINER-PATH-REBASE-001",
   "title": "An iOS app update reopens a remembered local graph after the sandbox container UUID changes",
   "family": "platform-compatibility",
   "sources": {
    "issues": [],
    "commits": [],
    "internal": "physical TestFlight update report 2026-08-27"
   },
   "platforms": [
    "ios"
   ],
   "layer": "ios-native-bridge",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/graph.test.tsx",
     ".github/workflows/ios-probe.yml"
    ],
    "scenario": "ios.remembered-local-graph-rebases-across-container-relocation",
    "failBefore": "proven"
   },
   "notes": "The first TestFlight update retained an absolute Documents path containing the old app-container UUID. The current installation correctly refused that outside-container path, but startup rendered the generic superseded/cancelled recovery screen until all app data was cleared. The iOS bridge now recognizes only the exact local app-container path shape, rebases its Documents-relative suffix onto the current container when that directory exists, and returns the effective path that both Rust and frontend persistence adopt. The simulator launches with a deliberately stale UUID and must open the current graph and copy the bundled Guide into it."
  },
  {
   "id": "REG-IOS-APP-ICON-OPAQUE-001",
   "title": "Every TestFlight app icon is an opaque Tine icon accepted by App Store Connect",
   "family": "release-packaging",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "ios"
   ],
   "layer": "release",
   "status": "covered",
   "coverage": {
    "tests": [
     "scripts/prepare-ios-project.mjs",
     "scripts/verify-ios-app-icon.mjs",
     ".github/workflows/ios-testflight.yml"
    ],
    "scenario": "ios.testflight-opaque-app-icon",
    "failBefore": "proven"
   },
   "notes": "The first post-beta signed IPA carried the tracked Tine artwork but App Store Connect rejected it with error 90717 because all tracked PNGs retained an alpha-bearing color type. The tracked catalog is now opaque, project preparation refuses any alpha color type or tRNS transparency before Xcode runs, and signed-IPA verification applies the same encoding check to the compiled primary icon in addition to its visual identity comparison. Fail-before: hosted upload run 32897900691 compiled, signed, and verified the IPA locally before Apple rejected its large icon."
  },
  {
   "id": "REG-WATCHER-ATOMIC-SAVE-SCOPE-001",
   "title": "Atomic page-save events reconcile incrementally and never rescan unrelated graphs",
   "family": "performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "desktop"
   ],
   "layer": "native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/watcher.rs"
    ],
    "scenario": "watcher.atomic-save-incremental-scope",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-BLOCK-REF-COUNTS-SCOPED-001",
   "title": "Ordinary non-reference edits reuse the block-reference count index",
   "family": "performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs",
     "src-tauri/src/commands.rs"
    ],
    "scenario": "references.count-index-scoped-invalidation",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-CONFIG-ABSENT-PUBLISH-NOREPLACE-001",
   "title": "A missing config or device-settings file preserves a concurrent external creator",
   "family": "data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs",
     "src-tauri/src/settings.rs"
    ],
    "scenario": "settings.absent-publish-no-replace",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-GUIDE-TWIN-WITHDRAW-RECOVERY-001",
   "title": "Copy Guide never unlinks a Markdown page replaced during twin withdrawal",
   "family": "data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs"
    ],
    "scenario": "guide.twin-withdraw-recovery",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-RENAME-ROLLBACK-DEST-RECOVERY-001",
   "title": "Rename rollback preserves a destination replaced after the rollback check",
   "family": "data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs"
    ],
    "scenario": "rename.rollback-destination-recovery",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-PUBLISH-PUBLIC-MACRO-CAPABILITY-001",
   "title": "Static-publish queries embeds and namespaces can expose only public pages",
   "family": "privacy",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/publish.rs"
    ],
    "scenario": "publish.public-macro-capability",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-PUBLISH-BEGIN-QUERY-EXEC-001",
   "title": "Static publish executes #+BEGIN_QUERY blocks (title + bounded results) through the same public-only capability as {{query}}; rows filtered as non-public are reported as a count only, and malformed payloads fail closed (no raw payload in HTML or the search index)",
   "family": "privacy",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/publish.rs"
    ],
    "scenario": "publish.begin-query-execution-public-capability",
    "failBefore": "proven"
   },
   "notes": "Martin-approved Q2 delta (2026-07-21 sweep). Publish already ran {{query}} via render_query with publish_page_allowed filtering; BEGIN_QUERY containers were inert source. Now: whole-container detection mirrors the frontend BeginQuery.tsx contract (delimiters, balanced EDN, :title string, :query vector); the query source is sliced from raw and delegated to the existing bounded/cached render_query; the publication snapshot includes ALL freshly parsed pages so omitted private matches can be counted honestly, while HTML hydration capability remains public-only; a muted query-omitted note reports the count (all-pages-public never shows it); malformed containers render an inert alert and their payload is excluded from the search index; print keeps its whole-graph behavior. Fail-before (worker-run, Sol/high): 2 publish_begin_query tests fail pre-fix. Full publish suite 34 pass + whole tine-core 514 pass (manager-run)."
  },
  {
   "id": "REG-PUBLISH-ATOMIC-TREE-REPLACEMENT-001",
   "title": "Republishing retires stale pages and binds staging and recovery writes to directory capabilities",
   "family": "data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/publish.rs",
     "crates/tine-core/src/model.rs"
    ],
    "scenario": "publish.atomic-tree-replacement-and-bound-directories",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-BENCH-PREVIOUS-RELEASE-CURRENCY-001",
   "title": "The rolling performance gate always compares with the most recently published release",
   "family": "performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "release-process",
   "status": "covered",
   "coverage": {
    "tests": [
     "scripts/check-bench-policy.mjs",
     "scripts/test-release-pipeline.mjs",
     ".github/workflows/ci.yml",
     ".github/workflows/release.yml"
    ],
    "scenario": "performance.previous-release-baseline-currency",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-BACKUP-RESTORE-RECOVERY-CAPABILITY-001",
   "title": "Backup restore recovery moves stay bound to the graph and approved assets roots",
   "family": "data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/backup.rs"
    ],
    "scenario": "backup.restore-recovery-capability-boundary",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-ANDROID-PHOTO-BOUNDED-NATIVE-IMPORT-001",
   "title": "Android photo capture and picking stream a bounded native cache token without base64 amplification",
   "family": "performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "android"
   ],
   "layer": "native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/mediaCaptureNative.test.ts",
     "src-tauri/src/commands.rs",
     "src-tauri/gen/android/app/src/main/java/page/tine/app/MediaCapturePlugin.kt"
    ],
    "scenario": "media.android-photo-bounded-native-import",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-RESULT-SUBTREE-NONOVERLAP-001",
   "title": "Nested query/reference memberships stay complete and shallow while previews and multi-query exports share native construction budgets",
   "family": "performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native-ui",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query.rs",
     "src-tauri/src/commands.rs",
     "src/components/ExportModal.queryBudget.test.tsx",
     "src/components/Block.embedHierarchy.test.tsx",
     "src/render/pagePeek.test.tsx"
    ],
    "scenario": "performance.complete-shallow-membership-and-native-shared-preview-export-budgets",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-BENCH-MULTIROUND-RELIABILITY-001",
   "title": "Release performance decisions use interleaved distributions and reject metric-level variance",
   "family": "performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux"
   ],
   "layer": "release-process",
   "status": "covered",
   "coverage": {
    "tests": [
     "scripts/test-bench-ab.mjs",
     "scripts/test-release-pipeline.mjs",
     ".github/workflows/ci.yml"
    ],
    "scenario": "performance.interleaved-multiround-median-and-spread-gate",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-PRINT-SCRIPT-FREE-PRIVILEGE-BOUNDARY-001",
   "title": "PDF export locally renders math and code into a script-free sandbox that cannot inherit app IPC privileges",
   "family": "security",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "desktop"
   ],
   "layer": "core-native-ui",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/publish.rs",
     "src/print.security.test.tsx",
     "scripts/e2e-print-security.mjs"
    ],
    "scenario": "security.print-script-free-sandbox-and-local-rendering",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-HELP-IMPROVE-IRREVERSIBLE-SCRUB-001",
   "title": "Help improve Tine exposes only irreversibly scrubbed parser reproductions and fails closed when privacy-safe tiers lose the mismatch",
   "family": "privacy",
   "sources": {
    "issues": [
     82
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/devtools/lsdoc-diff/engine.test.ts",
     "src/components/ImproveTab.test.tsx"
    ],
    "scenario": "privacy.help-improve-irreversible-fail-closed-anonymization",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-PDF-CANVAS-AGGREGATE-BUDGET-001",
   "title": "PDF rendering admits canvases by aggregate backing-store pixels and zeroes evicted allocations",
   "family": "performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/components/PdfViewer.test.tsx"
    ],
    "scenario": "performance.pdf-canvas-aggregate-backing-store-budget",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-PRINT-ASSET-CUMULATIVE-BUDGET-001",
   "title": "PDF export rejects individual and cumulative image bytes before base64 and IPC amplification",
   "family": "performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "desktop"
   ],
   "layer": "core-native-ui",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/publish.rs",
     "scripts/e2e-print-security.mjs"
    ],
    "scenario": "performance.print-image-per-file-and-cumulative-budget",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-PUBLISH-CLOSED-CAPABILITY-URL-001",
   "title": "Static publication authorizes exact public source identities and contextualizes every anchor and navigable URL",
   "family": "security-privacy",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-browser",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/publish.rs",
     "scripts/e2e-publish-security.mjs"
    ],
    "scenario": "publish.exact-source-capability-anchor-and-url-policy",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-PDF-FIND-BOUNDED-LIFECYCLE-001",
   "title": "PDF Find bounds retained text and occurrences and prevents cancelled searches from admitting stale work",
   "family": "performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/components/PdfViewer.test.tsx"
    ],
    "scenario": "performance.pdf-find-text-match-and-cancellation-budgets",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-GRAPH-BACKGROUND-WORK-BOUND-001",
   "title": "Graph switches cancel stale warm and backup work under process-wide concurrency bounds",
   "family": "performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "desktop"
   ],
   "layer": "native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/graph.rs",
     "src-tauri/src/backup.rs",
     "src-tauri/src/state.rs"
    ],
    "scenario": "performance.graph-switch-cancellable-bounded-background-workers",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-BACKUP-PARTIAL-CLEANUP-001",
   "title": "Failed and crash-abandoned backup staging trees cannot accumulate outside retention",
   "family": "data-safety-performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "desktop"
   ],
   "layer": "native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/backup.rs"
    ],
    "scenario": "backup.partial-raii-cleanup-and-scavenge",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-CLIPBOARD-IMAGE-INGRESS-BOUND-001",
   "title": "Clipboard images are admitted by pixels and bytes before RGBA canvas and base64 amplification",
   "family": "performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/backend.clipboard.test.tsx",
     "src/components/Block.assetPaste.test.tsx",
     "src-tauri/src/commands.rs"
    ],
    "scenario": "performance.clipboard-image-predecode-and-native-ingress-budgets",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-RESULT-CONSTRUCTION-BUDGET-001",
   "title": "Queries references facets and export enforce row and byte budgets while constructing results",
   "family": "performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native-ui",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query.rs",
     "src-tauri/src/commands.rs"
    ],
    "scenario": "performance.result-family-construction-time-budgets",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-QUERY-SOURCE-RESOURCE-BOUNDS-001",
   "title": "Persisted simple and advanced queries fail closed at dialect-aware source-size and nesting-depth limits before parser recursion or cache-key construction, including bounded static-publication memoization",
   "family": "performance-data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native-ui",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query.rs",
     "crates/tine-core/src/model.rs",
     "src-tauri/src/commands.rs",
     "crates/tine-core/src/publish.rs"
    ],
    "scenario": "performance.query-source-size-and-recursion-bounds",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-BOUNDED-RESULT-EVIDENCE-CACHE-001",
   "title": "Bounded query and reference results cap occurrence construction and retain every warm result family across unrelated edits, including pages with unchanged aliases, without preserving stale overflow metadata",
   "family": "performance-correctness",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/reference_evidence.rs",
     "crates/tine-core/src/query.rs",
     "crates/tine-core/src/model.rs"
    ],
    "scenario": "performance.bounded-result-evidence-and-scoped-family-cache",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-ANDROID-RENAMEAT2-DLOPEN-001",
   "title": "Android 9 / API 28 launches Tine without an unresolved renameat2 symbol in libtine_lib.so",
   "family": "android-platform-lifecycle",
   "sources": {
    "issues": [
     192
    ],
    "commits": []
   },
   "platforms": [
    "android"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/backup.rs",
     "crates/tine-core/src/model.rs",
     ".github/workflows/release.yml",
     "scripts/test-release-pipeline.mjs"
    ],
    "scenario": "android.renameat2-syscall-no-api-gated-symbol",
    "failBefore": "artifact-proven"
   },
   "notes": "Reopened 2026-07-22 after the reporter reproduced the same loader crash on v0.6.2. Direct inspection of the published v0.6.4 arm64 APK proved libtine_lib.so still imported undefined renameat2: the first fix covered backup.rs but missed the Android-built model.rs trash path. The second call now uses the syscall entry point, and an actual packaged debug APK (SHA-256 d2c8280524ca9adaf450d62f44efb735ada1d381eff55625e5c47cf137e2561f) imports syscall but not renameat2. The release workflow now enforces the same symbol-table gate on the canonical APK. Android 9 device confirmation remains desirable but is no longer the only protection against recurrence."
  },
  {
   "id": "REG-REF-ALIAS-COMPONENT-001",
   "title": "Linked and Unlinked References count every alias-connected page (transitive, order-independent), matching OG page-alias-set, so duplicate-alias and chained-alias owners are never dropped or resolved by filesystem layout",
   "family": "references-correctness",
   "sources": {
    "issues": [
     137
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/tests/issue137_investigation.rs::issue137_alias_component_is_transitive_and_unions_duplicate_owners",
     "crates/tine-core/tests/issue137_investigation.rs::issue137_fail_before_duplicate_alias_is_deterministic"
    ],
    "scenario": "references.alias-transitive-connected-component",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-REF-OCCURRENCE-COUNT-HONEST-001",
   "title": "A block mentioning a page more than the per-block occurrence cap reports its true mention total instead of silently undercounting at the bound",
   "family": "references-correctness",
   "sources": {
    "issues": [
     137
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/tests/issue137_investigation.rs::issue137_fail_before_occurrence_count_is_not_silently_truncated",
     "crates/tine-core/src/reference_evidence.rs::tests::occurrence_cap_reports_total_and_truncation",
     "src/components/UnlinkedReferences.test.tsx"
    ],
    "scenario": "references.honest-occurrence-total-under-cap",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-EXTERNAL-LINK-BROWSER-ENV-001",
   "title": "Clicking a Markdown external link on Linux opens the browser with desktop/session environment intact while only bundle/loader variables are scrubbed",
   "family": "external-links",
   "sources": {
    "issues": [
     195
    ],
    "commits": []
   },
   "platforms": [
    "linux"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/platform.rs"
    ],
    "scenario": "external-links.browser-opener-env",
    "failBefore": "source-proven"
   }
  },
  {
   "id": "REG-ORG-CONTAINER-OUTLINE-ROUNDTRIP-001",
   "title": "Terminated org container bodies with list markers remain one outline block and round-trip byte-exactly",
   "family": "data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/doc.rs::org_container_outline_tests"
    ],
    "scenario": "reader.org-container-outline-round-trip",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-PAGE-HEADER-ENTER-SAVE-001",
   "title": "Typing a page property and pressing Enter no longer shows an error or jams subsequent page saves",
   "family": "persistence-correctness",
   "sources": {
    "issues": [
     210
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "store",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/store.test.ts"
    ],
    "scenario": "persistence.page-header-enter-trailing-newline",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-SEARCH-PAGE-PANIC-ISOLATION-001",
   "title": "One un-indexable page no longer removes other pages' blocks from search and the indexing failure is reported",
   "family": "search-reliability",
   "sources": {
    "issues": [
     209
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::tests::search_cache_isolates_one_page_projection_panic"
    ],
    "scenario": "search.page-panic-isolation-and-diagnostic",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-SEARCH-ALIAS-OWNER-SCOPE-001",
   "title": "An alias only matches its owning page; a duplicate-named sibling is not falsely returned",
   "family": "search-reliability",
   "sources": {
    "issues": [
     209
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query_plan.rs::tests::page_alias_search_is_scoped_to_its_physical_owner"
    ],
    "scenario": "search.alias-owner-exact-safety-interop",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-SEARCH-RESULT-TRUNCATION-001",
   "title": "Graph search reports per-category truncation instead of presenting a bounded result window as complete",
   "family": "search-reliability",
   "sources": {
    "issues": [
     209
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query_plan.rs::tests::graph_search_reports_per_category_truncation"
    ],
    "scenario": "search.per-category-result-truncation",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-BLOCK-PROPERTY-UPDATE-ORDER-001",
   "title": "Editing one field-table cell reorders columns because updating a block property moved its line to the end",
   "family": "persistence-correctness",
   "sources": {
    "issues": [
     216
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "store",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/store.test.ts",
     "src/sheet/fields.test.ts"
    ],
    "scenario": "persistence.block-property-update-preserves-order",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-AUTOCOMPLETE-LITERAL-186",
   "title": "#/[[/(( autocomplete omit valid pages/blocks because literal input (bare OR, -x, quotes, /re/) was parsed as the Ctrl+K search DSL",
   "family": "search-retrieval-correctness",
   "sources": {
    "issues": [
     186
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-query",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query.rs",
     "crates/tine-core/src/query_plan.rs"
    ],
    "scenario": "search.autocomplete-treats-input-literally-not-as-dsl",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-PAGE-TOPK-DETERMINISM-186",
   "title": "Equal-score page autocomplete top-K is nondeterministic across builds because ties break on list_pages / referenced_page_names (HashMap) enumeration order",
   "family": "search-retrieval-correctness",
   "sources": {
    "issues": [
     186
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-query",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query.rs"
    ],
    "scenario": "search.page-topk-ties-stable-and-input-order-independent",
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-LSDOC-MALFORMED-HTML-INDEX-221",
   "title": "A page containing a malformed HTML fragment still loads and stays searchable instead of being silently dropped from the index",
   "family": "parser-robustness",
   "sources": {
    "issues": [
     221
    ],
    "commits": [
     "70976ac"
    ]
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native-ui",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/tests/gh221_malformed_html.rs"
    ],
    "scenario": null,
    "failBefore": "proven"
   },
   "notes": "Fixed by the lsdoc v0.5.4 pin bump; the fail-before was reproduced on master (lsdoc v0.5.3), where the same test panicked with 'lsdoc v2 parser does not yet own \"md\" input' and page_index_failures listed pages/test-page.md."
  },
  {
   "id": "REG-GRAPH-WIDE-TEXT-SCOPE-246",
   "title": "Eligible graph documents outside configured page roots and lowercase .markdown files remain discoverable and exact-path load/save never expose a blank writable substitute",
   "family": "data-safety",
   "sources": {
    "issues": [
     246
    ],
    "commits": [
     "993b8445",
     "585750a1"
    ]
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/tests/graph.rs",
     "crates/tine-core/src/model.rs",
     "crates/tine-core/src/config.rs"
    ],
    "scenario": "filesystem.graph-wide-text-scope",
    "failBefore": "proven"
   },
   "notes": "GH #246 accepted harm: an eligible non-empty document outside pages/ could appear blank, external add/delete stayed invisible, and editing the blank state could overwrite real bytes. The base reproduction at be54af62 failed because list_pages/find_entry/load_by_path only admitted configured pages/journals roots. This row also covers OG-compatible lowercase .markdown through bootstrap/source capture, exact projection, external reconciliation/feed, edit/writeback/delete observation, configured-root rename spelling preservation, and .md/.markdown/.org same-stem twin refusal."
  },
  {
   "id": "REG-MISSING-REMEMBERED-GRAPH-250",
   "title": "A missing remembered graph path cannot prevent startup and leaves the graph chooser usable",
   "family": "data-safety",
   "sources": {
    "issues": [
     250
    ],
    "commits": [
     "abf7af83"
    ]
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/App.startupRecovery.test.tsx",
     "src/startupReveal.test.ts"
    ],
    "scenario": "startup.missing-remembered-graph-recovery",
    "failBefore": "proven"
   },
   "notes": "GH #250 failed on v0.6.5 because the missing remembered graph was opened synchronously from native setup and panicked before a usable app appeared. The later abf7af83 startup refactor co-fixed it by deferring graph loading to the visible frontend. The semantic regression proves the failed remembered-root load completes startup and exposes an enabled Open an existing graph action; the native structural neighbor proves setup no longer invokes graph loading. No filesystem path is created or mutated by this frontend recovery path."
  },
  {
   "id": "REG-EXTERNAL-GRAPH-WIDE-EVENTS-268",
   "title": "External edits to pages outside journals/ and pages/ reach the open app instead of staying invisible until reopen",
   "family": "external-change-reconciliation",
   "sources": {
    "issues": [
     268
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "macos",
    "windows"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/watcher/tests.rs::graph_wide_external_paths_are_routed_like_discovery_routes_them",
     "src-tauri/src/watcher/tests.rs::snapshot_covers_exactly_what_discovery_covers",
     "src-tauri/src/watcher/tests.rs::unclassified_paths_force_a_full_scan_only_where_pages_could_live",
     "crates/tine-core/src/model_watch_reach_tests.rs::watch_predicates_track_the_same_scope_discovery_walks"
    ],
    "scenario": "direct-markdown.external-change-graph-wide",
    "failBefore": "proven"
   },
   "notes": "Discovery went graph-wide in the #246 fix; watcher event routing did not follow. The OS watch was already installed recursively on the graph ROOT, so events for a page at the root or in a custom folder arrived correctly -- and were then discarded by the reconcile lane, which filtered pending paths against [journals/, pages/] and built its diff snapshot from those two directories only. Such a page was therefore listed at open and never updated again while the app stayed open. Fixed by giving the watcher the same authority discovery uses (GraphTextScope), via Graph::graph_text_watch_relevant / _descend / _could_contain. Fail-before proven by restoring the two pre-fix predicates in place: the snapshot returned only [journals/2026_08_06.md, pages/Page.md] against an expected five, and event routing returned only [pages/Page.md] against an expected three. Conflict copies stay routed (they are not eligible text but must refresh the conflicts panel); excluded trees (assets/, dot-directories, logseq/bak/) stay excluded on both the routing and the force-a-full-scan paths, so dropping an image into assets/ still cannot trigger a graph rescan. Freshness defect only, not write-safety: the pre-debounce write-guard lane always saw every event and stayed fail-closed."
  },
  {
   "id": "REG-POLL-MODE-ADMISSION-INDEX-REBUILD-001",
   "title": "A network-share (poll-mode) graph keeps its save-time admission index warm instead of rebuilding the whole graph on every save",
   "family": "direct-save-performance",
   "sources": {
    "issues": [
     267
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "macos",
    "windows"
   ],
   "layer": "core-native",
   "status": "released",
   "coverage": {
    "tests": [
     "src-tauri/src/watcher/tests.rs::quiet_poll_cycles_publish_an_exact_empty_observation",
     "src-tauri/src/watcher/tests.rs::a_poll_cycle_reports_what_it_actually_observed",
     "src-tauri/src/watcher/tests.rs::an_incomplete_poll_scan_publishes_uncertainty",
     "src-tauri/src/watcher/tests.rs::an_unreadable_directory_makes_the_scan_report_itself_incomplete"
    ],
    "scenario": "direct-markdown.poll-mode-save-latency",
    "failBefore": "proven"
   },
   "notes": "When the watcher runs in poll mode (a network share, or TINE_WATCH=poll), every 3 s cycle invalidated the guarded graph-text admission index unconditionally before reconciling. Since a Direct save consults that index and rebuilds it when invalid -- a two-pass byte-retaining walk of the whole graph -- such a user could never hold a warm index and paid a full-graph rebuild on essentially every save. The blanket invalidation was compensating for the poll rescan being narrower than the index (journals/+pages/ vs graph-wide); once REG-EXTERNAL-GRAPH-WIDE-EVENTS-268 widened the rescan to the same scope, the rescan's own result became a complete account of what changed and is published as exact observations instead. Ordering is load-bearing and asserted: the observation happens after the scan and BEFORE any reconcile, so the index is never staler than the scan that produced it -- the same window the inotify callback lane already had, not a wider one. A scan that could not read part of the graph reports itself incomplete and still invalidates. Fail-before proven by restoring the blanket invalidation in place: the quiet-cycle test then reported invalidated: true with complete_builds unchanged at 1."
  },
  {
   "id": "REG-DIRECT-SAVE-WHOLE-GRAPH-REPARSE-267",
   "title": "Saving an existing page never captures or reparses the whole graph",
   "family": "direct-save-performance",
   "sources": {
    "issues": [
     267
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "macos",
    "windows"
   ],
   "layer": "core-native",
   "status": "released",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::invalidated_graph_index_does_not_expand_an_existing_save",
     "crates/tine-core/src/model.rs::changed_existing_save_has_one_portable_traversal_and_no_graph_capture",
     "crates/tine-core/src/model.rs::steady_state_direct_saves_never_build_the_graph_index",
     "crates/tine-core/src/model.rs::direct_save_latency_manual_benchmark"
    ],
    "scenario": "direct-markdown.save-latency",
    "failBefore": "proven"
   },
   "notes": "Reopened after the reporter confirmed the defect remained in v0.6.91 Direct Files. The diagnostic refusal was `precheck.limit: guarded graph-text identity construction failed: initial shadow peak build memory bound exceeded`; GH #295 independently measured one complete 1,046-file / 16.7 MB capture on every Windows save (roughly 0.9-1.1 s). Root cause was architectural: both existing-target validation and the atomic replacement transaction invoked the complete managed shadow-import admission index. The cut keeps an existing save local: one exact target parse/read, one capability-safe traversal following only portable-equivalent target path components (including case/NFC aliases in ancestor directories), exact file identity and link-count-one checks, and the existing no-replace/retire/restore/fsync protocol. Missing-target creation now uses its separate single-census cached-evidence proof recorded by REG-DIRECT-CREATE-RETAINED-SHADOW-LIMIT-249-266. On the attached #295 graph in release mode, warm save median was 1.13 ms and forcibly invalidated save median 0.98 ms, with complete builds unchanged at one; the prior current-master measurement was 2.75 ms warm and 70 ms invalidated. Structural tests prove exactly one portable traversal, zero graph capture enumerations, one target parse, and no write on portable/hard-link/external-byte conflicts."
  },
  {
   "id": "REG-DIRECT-CREATE-RETAINED-SHADOW-LIMIT-249-266",
   "title": "Creating a Direct Files page does not build the managed shadow-import retained-byte inventory or strand the edit behind precheck.limit",
   "family": "direct-save-data-safety",
   "sources": {
    "issues": [
     249,
     266,
     267
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "macos",
    "windows"
   ],
   "layer": "core-native",
   "status": "released",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::missing_target_creation_ignores_the_retained_capture_peak_limit",
     "crates/tine-core/src/model.rs::missing_target_creation_has_zero_graph_census_capture_or_parse_work",
     "crates/tine-core/src/model.rs::identity_preserving_existing_save_keeps_creation_evidence_warm",
     "crates/tine-core/src/model.rs::identity_changing_existing_save_refreshes_creation_evidence",
     "crates/tine-core/src/model.rs::same_path_same_length_retitle_after_reconciliation_refuses_creation",
     "crates/tine-core/src/model.rs::missing_target_creation_refuses_an_externally_retitled_owner",
     "crates/tine-core/src/model.rs::creation_refuses_historical_arbitrary_and_explicit_semantic_owners",
     "crates/tine-core/src/model_alias_admission_tests.rs::unrelated_creation_does_not_mutate_existing_hardlinks",
     "crates/tine-core/src/model_alias_admission_tests.rs::creation_refuses_portable_leaf_and_ancestor_aliases_without_mutation",
     "crates/tine-core/src/model.rs::creation_refuses_symlink_parent_and_leaf_without_touching_outside_bytes",
     "crates/tine-core/src/model.rs::external_exact_target_creator_wins_without_byte_change",
     "crates/tine-core/src/model.rs::external_portable_alias_creator_wins_before_creation_publication",
     "crates/tine-core/src/model.rs::external_semantic_owner_creator_wins_before_creation_publication",
     "crates/tine-core/src/model.rs::external_portable_symlink_alias_refuses_creation_publication",
     "crates/tine-core/src/model.rs::unsafe_page_titles_save_and_reopen_through_one_safe_identity",
     "crates/tine-core/src/model.rs::generation_bound_identity_validation_avoids_save_time_graph_reparse",
     "crates/tine-core/src/model_tests_unreadable_pages.rs::failed_and_stale_effective_identity_evidence_blocks_name_only_creation",
     "crates/tine-core/src/model.rs::changed_existing_save_has_one_portable_traversal_and_no_graph_capture",
     "crates/tine-core/src/model.rs::invalidated_graph_index_does_not_expand_an_existing_save",
     "crates/tine-core/src/model.rs::existing_save_allows_portable_ancestor_neighbor_but_refuses_colliding_leaf",
     "crates/tine-core/src/model.rs::existing_save_allows_portable_equivalent_symlink_neighbor",
     "crates/tine-core/src/model.rs::steady_state_direct_saves_never_build_the_graph_index"
    ],
    "scenario": "direct-markdown.missing-target-save-admission",
    "failBefore": "proven"
   },
   "notes": "Post-v0.6.92 reporter evidence revoked the complete save/reopen claim for GH #249 without disproving its Windows-safe filename codec: the timestamp page reaches the editor but save fails with the exact diagnostic `precheck.limit: guarded graph-text identity construction failed: initial shadow peak build memory bound exceeded`. GH #266's v0.6.92 Windows trace records the same 12.8-14.2 s refusal three times while instrumented graph open itself takes 18 ms, leaving open/switch blocked for about 41 s and close eventually offering to discard unsaved work. Commit 0394d6e0 correctly cut the complete admission index from existing-target saves, but deliberately left missing-target creation and its atomic writer on the two-pass byte-retaining managed shadow-import capture. The source fix binds the generation-current parsed identity index, exact cached disk revisions, and page-index failures to one capability/no-follow streaming census; creation retains paths, digests, resource identities, and link counts only, applies the retained raw-byte ceiling per file rather than cumulatively, performs no save-time graph parse or retained-byte capture, fails closed without retry, and gives the resulting proof to the exact no-replace publisher. A final streaming census immediately before no-replace publication closes GH #321's external cross-path race without parsing or retaining graph bytes; deterministic hook tests cover both a portable alias and a distinct path claiming the same semantic title. Identity-preserving existing saves advance warm semantic evidence in O(1), while explicit-title identity changes replace it coherently. Creation refuses portable case/NFC target aliases including ancestors, historical graph extensions and arbitrary eligible paths, explicit semantic owners, hard links/resource aliases, symlink parents/leaves, stale same-length retitles, generation changes, and exact-target or cross-path races without changing incumbent or outside bytes. Existing exact-target saves preserve the prior traversal rule: noncolliding siblings under portable-equivalent or symlink ancestor neighbors do not block the save, while a colliding leaf still refuses. A test-only initial-capture charge equal to the parent peak limit proved the parent failed locally with the exact suffix `initial shadow peak build memory bound exceeded`, classifier `precheck.limit`, and no created file; the same armed limit is untouched by the fixed creation path. This construction used generated temporary graph data only. The reversible timestamp codec is exercised in both filename formats. Packaged Windows pass-after remains required before a released Windows claim."
  },
  {
   "id": "REG-SAME-BYTES-EXTERNAL-REPLACE-STRANDS-EDITOR-001",
   "title": "A page whose file was replaced with identical content can still be saved instead of stranding the edit",
   "family": "direct-save-data-safety",
   "sources": {
    "issues": [
     267
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "macos",
    "windows"
   ],
   "layer": "core-native",
   "status": "released",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::a_same_bytes_external_replace_does_not_strand_the_editor",
     "crates/tine-core/src/model.rs::a_changed_bytes_external_replace_still_conflicts"
    ],
    "scenario": "direct-markdown.external-atomic-replace",
    "failBefore": "proven"
   },
   "notes": "A save proves it is not clobbering someone else's work two ways: base_rev must still match the bytes on disk, AND the file must still be the same physical file read at load. External tools break the second without touching the first -- OneDrive rehydrating a Files-On-Demand placeholder, a Syncthing pull landing identical bytes, cp over a hardlink. Every later save then failed with 'existing page identity changed since load', and BOTH exits from the resulting conflict prompt were dead ends: Keep-mine re-ran the same check and failed too, and Use-disk-version discarded the user's edit. The watcher now re-pins the retained identity when, and only when, the retained revision still matches the bytes now on disk, so the proof it stands on is unchanged. Fail-before proven by disabling the re-pin in place. The control (a replace that also changes the bytes) still conflicts and still writes nothing, and is asserted alongside."
  },
  {
   "id": "REG-SYMLINK-BLOCKS-ALL-SAVES-001",
   "title": "A symlink inside the graph does not make every other page unsaveable",
   "family": "direct-save-data-safety",
   "sources": {
    "issues": [
     267
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "macos"
   ],
   "layer": "core-native",
   "status": "released",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::unrelated_symlinks_do_not_expand_an_existing_save"
    ],
    "scenario": "direct-markdown.symlink-in-scope",
    "failBefore": "proven"
   },
   "notes": "One symlink in a descended, non-excluded scope (graph root, pages/, journals/, a custom folder) aborted the save-time admission capture, and because every Direct save takes that capture to answer a filename question, the whole graph became read-only -- the user's OTHER pages could not be saved either. Skipping is what the rest of Tine already does: every traversal is no-follow, the watcher snapshot never descends a symlinked directory, and graph_inventory_entry never admits a symlinked file, so the capture was the only place escalating a non-entity to a fatal error. The shadow-import bootstrap capture still refuses, asserted separately: an import that silently leaves out a file the user considers part of their graph is a worse failure than a refused import. NOTE the audit's own repro for this finding is WRONG -- it used assets/, which is fixed-excluded, so that experiment goes green and would retire a real finding; both halves are asserted in one test so nobody re-runs it. Known bounded gap: a symlink is no longer a member of the portable-key collision group, matching discovery, which never knew about it either."
  },
  {
   "id": "REG-SAVE-ERROR-MISCLASSIFIED-AS-CONFLICT-001",
   "title": "Save failures that a conflict prompt cannot resolve are reported as themselves, not as a content conflict",
   "family": "direct-save-data-safety",
   "sources": {
    "issues": [
     267
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "released",
   "coverage": {
    "tests": [
     "src-tauri/src/commands.rs::only_a_real_base_revision_conflict_raises_the_conflict_prompt"
    ],
    "scenario": "direct-markdown.save-error-classification",
    "failBefore": "proven"
   },
   "notes": "The backend collapsed EVERY io::ErrorKind::AlreadyExists to the literal string 'conflict', and the frontend recognised a conflict by testing whether the message CONTAINED that substring. A portable-filename collision, a physical-resource alias, a symlink refusal and 'another document owns this page identity' therefore all raised the content-conflict prompt, whose only options are Keep-mine and Use-disk-version -- neither of which can resolve any of them. Choosing either left the page marked conflicted, and a conflicted page silently refuses to save from then on, so the user's page was stranded by an error message. Only a real base-revision conflict carries the 'conflict' contract now; everything else carries its bounded D0 failure code as a stable prefix and falls through to the retryable path. The frontend match is exact rather than substring. Fail-before is structural and asserted directly on the mapping function."
  },
  {
   "id": "REG-CAPTURE-TWO-PASS-CONCURRENCY-001",
   "title": "A generation change during the Direct creation census fails closed without repeated whole-graph work",
   "family": "direct-save-data-safety",
   "sources": {
    "issues": [
     267
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "macos",
    "windows"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::missing_target_creation_has_zero_graph_census_capture_or_parse_work",
     "crates/tine-core/src/model.rs::missing_target_creation_refuses_an_externally_retitled_owner",
     "crates/tine-core/src/model.rs::external_semantic_owner_creator_wins_before_creation_publication"
    ],
    "scenario": "direct-markdown.creation-census-concurrency",
    "failBefore": "proven"
   },
   "notes": "The former Direct missing-target admission capture ran two byte-retaining full-graph passes and retried their disagreement, amplifying unrelated concurrent filesystem activity into repeated whole-graph work. Ordinary Direct creation no longer enters that managed shadow-import capture. It performs one capability/no-follow streaming evidence census, binds it to the generation-current cached semantic snapshot, then performs one final streaming census immediately before publication to detect external cross-path races. Neither census parses or retains graph bytes, and disagreement fails closed without retry or mutation. The deterministic generation-change and final-publication hooks prove bounded work and unchanged neighboring bytes."
  },
  {
   "id": "REG-SAVE-RETRY-AMPLIFICATION-001",
   "title": "A save failure a retry cannot change is reported once instead of re-running the whole check three times",
   "family": "direct-save-performance",
   "sources": {
    "issues": [
     267
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend",
   "status": "released",
   "coverage": {
    "tests": [
     "src/persistence.test.ts::does not retry failures a retry cannot change"
    ],
    "scenario": "direct-markdown.save-retry-policy",
    "failBefore": "proven"
   },
   "notes": "scheduleTransientRetry re-ran a failed save at 100 ms and 300 ms before toasting -- three full attempts per dirty page. Each attempt re-runs the whole pre-save admission check, which on a large graph is the expensive part, only to reach the same answer for a failure class that cannot change on retry: a portable-filename collision, a physical-resource alias, a symlink in scope, a non-portable path, a nofollow refusal, a limit breach, or another page already owning the title. #267's 'about a minute, then a red toast' is that multiplier on top of an already slow check. Those classes now report once; genuinely transient ones (precheck.interrupted, identity.changed_since_load, unclassified I/O) still retry. Depends on REG-SAVE-ERROR-MISCLASSIFIED-AS-CONFLICT-001 for the bounded codes that make the distinction possible."
  },
  {
   "id": "REG-DIRECT-FORCE-SAVE-WIRE-DTO-001",
   "title": "Resolving a save conflict with \"keep mine\" overwrites the file, including after a real external change",
   "family": "direct-save-data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "macos",
    "windows"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::force_save_succeeds_on_the_revless_dto_the_frontend_sends",
     "crates/tine-core/src/model.rs::force_save_overrides_a_real_external_change_with_the_wire_dto"
    ],
    "scenario": "direct-markdown.conflict-keep-mine",
    "failBefore": "proven"
   },
   "notes": "force_save_page proved its right to write the pinned file from PageDto.rev, which pageToDto never sets -- ordinary saves carry the load revision in the separate base_rev argument, as the parameter's own comment said. With rev=None the retained-owner check could not hold, so every override failed AlreadyExists 'path-pinned page does not match its captured exact owner', leaving discard-my-edits as the only working exit from the conflict prompt. The suite missed it because every test force-saved a load_named/load_by_path DTO, which does carry rev=Some -- a shape the wire never produces. Replaced the untyped revision argument with an explicit PinnedSaveAuthority. Audit 2026-08-06 N1 (Critical)."
  },
  {
   "id": "REG-DIRECT-SAVE-ERROR-CLASSIFICATION-002",
   "title": "An unclassified save failure reports what happened instead of raising an unresolvable conflict prompt",
   "family": "direct-save-data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "macos",
    "windows"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::direct_save_failure_codes_are_stable",
     "src-tauri/src/commands.rs::only_a_real_base_revision_conflict_raises_the_conflict_prompt",
     "src-tauri/src/commands.rs::an_unobserved_external_change_still_raises_the_conflict_prompt"
    ],
    "scenario": "direct-markdown.save-error-codes",
    "failBefore": "proven"
   },
   "notes": "direct_save_failure_code ended with a catch-all mapping any AlreadyExists to conflict.other, which commands.rs turned into the literal 'conflict' -- the string that raises the keep-mine/use-disk banner. About forty-five other conditions raise AlreadyExists (managed and projection internals, reservation and page-name collisions) for which neither button helps and 'use disk' discards the user's edits; the mapping also replaced the message text, so a failure that had RETAINED those edits under a recovery name arrived as a bare unexplained conflict. conflict.* membership is now decided by naming a condition. Audit 2026-08-06 N3 (High) and the still-open half of N2."
  },
  {
   "id": "REG-DIRECT-MOVE-BARRIER-FORCE-003",
   "title": "A conflict resolution during a cross-page move waits for the destination write instead of writing the moved block out of existence",
   "family": "direct-save-data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/persistenceMoveBarrier.test.ts::defers a keep-mine on a held source, then applies it on release"
    ],
    "scenario": "direct-markdown.move-barrier",
    "failBefore": "proven"
   },
   "notes": "heldSources holds a moved subtree's SOURCE pages until the DESTINATION write is durable, so the block is never in neither place. The guard read heldSources.has(name) && !force, so a forced save walked through it. Latent while force-save could not reach the backend (N1); live once it could. A deferred keep-mine is now remembered and re-issued when the destination lands, because the ordinary save path skips a conflicted page and dropping it would strand the resolution. Audit 2026-08-06 N6."
  },
  {
   "id": "REG-ADVANCED-CURRENT-PAGE-INPUT-301",
   "title": "Advanced Logseq queries bind :inputs [:current-page] positionally without weakening typed date inputs",
   "family": "query-compatibility",
   "sources": {
    "issues": [
     301
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query_tests.rs::advanced_current_page_input_lowers_the_standard_page_relationship",
     "crates/tine-core/src/query_tests.rs::advanced_typed_inputs_keep_date_bounds_numeric",
     "crates/tine-core/src/model.rs::tests::advanced_current_page_input_filters_real_graph_blocks",
     "scripts/e2e-compat-home-current-page.mjs"
    ],
    "failBefore": "proven"
   },
   "notes": "OG source at 6e7afa8eb040686ff057156ee877193b581dd369 resolves the typed :current-page keyword positionally through current-page-fn and lowercases the page name before query execution. Tine now retains typed AdvancedInput::Date values while binding that one keyword as AdvancedInput::Page, then conservatively lowers only the exact standard [?p :block/name ?current-page] identity joined to one [?b :block/refs ?p] or [?b :block/page ?p] relationship. Ambiguous and unrelated patterns remain visibly unsupported; this does not claim full DataScript joins. Contract-doc delta: none; this is a bounded compatibility extension within the existing partial advanced-query contract."
  },
  {
   "id": "REG-ADVANCED-ATTRIBUTE-PATTERNS-542",
   "title": "Advanced queries written with block attribute patterns (:block/marker, :block/scheduled, the block's journal page) return the blocks Logseq returns, and never a narrowed answer from a partly understood not/or",
   "family": "query-compatibility",
   "sources": {
    "issues": [
     542
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model_tests_advanced_queries.rs::gh542_reporter_advanced_queries_answer_like_logseq",
     "crates/tine-core/src/model_tests_advanced_queries.rs::gh542_a_partly_understood_not_or_or_is_not_narrowed",
     "crates/tine-core/src/query_tests.rs::gh542_attribute_patterns_lower_only_block_local_meaning",
     "crates/tine-core/src/onboarding.rs::gh542_guide_advanced_query_example_runs_whole"
    ],
    "failBefore": "proven"
   },
   "notes": "GH #542 (bgzo, macOS 0.6.982). The advanced lowerer understood only rule heads such as (task ?b ...) and refused every DataScript attribute pattern, so [?b :block/marker \"TODO\"] -- also Tine's own screenshot fixture -- rendered as unsupported. Worse, a not/or containing a clause it did not understand ran as the part it did: (not [?b :block/scheduled ?d]) was dropped and the OVERDUE query returned scheduled tasks, and a partly understood not can hide blocks the query keeps. Fix (query/advanced_patterns.rs): patterns on the :find variable's marker/priority/scheduled/deadline and on its page's journal?/journal-day lower to typed filters, alone, as literals, or as a value variable narrowed by contains?/=/not=/</<=/>/>= against a literal or an :inputs date; (not [pattern]) whose variable is local to the not; single-branch or/or-join and nested and are opened. A value variable used anywhere else (a join) is not lowered. A not/or that loses any child is dropped whole, so a partial answer is only ever a disclosed superset; (not A B) now means not(A and B) rather than not(A). :result-transform is reported as not run. Fail-before: both model tests fail on the old code (first query unsupported; partial not narrowed). Out of scope: rendering config.edn :default-queries on the Journals page (not implemented in Tine at all; product decision), running :result-transform functions (ADR 0042), and the reporter's NEXT query as written, which pulls ?b while every clause binds ?h and is refused by Logseq too. Contract-doc delta: none; a bounded extension of the documented partial advanced-query subset (BACKLOG 'Advanced Datalog queries')."
  },
  {
   "id": "REG-GRAPH-HOME-CONFIG-OWNER-269",
   "title": "The graph home page round-trips through Logseq-compatible config.edn without replacing other default-home keys",
   "family": "config-compatibility",
   "sources": {
    "issues": [
     269
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/config.rs::tests::default_home_reads_only_the_page_inside_the_logseq_map",
     "crates/tine-core/src/config.rs::tests::default_home_writer_preserves_siblings_clears_only_page_and_refuses_malformed_owner",
     "src/homePage.test.ts",
     "src/components/Settings.homePage.test.tsx"
    ],
    "failBefore": "proven"
   },
   "notes": "Config.default_home reads only the direct :page entry inside Logseq's :default-home map. Graph::set_default_home_page uses the existing guarded atomic config boundary, preserves sibling keys such as :sidebar, and refuses a malformed/non-map owner without changing its bytes. The frontend gives graph metadata precedence; when no valid graph-owned page exists it attempts to copy the old home.page.<root> app setting, deleting that legacy value only after the graph write succeeds. A refused migration remains usable for the current session and is retried later. Contract-doc delta: none; ADR 0004 and ADR 0049 already establish config.edn as the compatible graph owner outside the managed oplog domain."
  },
  {
   "id": "REG-UNDO-ACROSS-EVICTION-305",
   "title": "Undo cannot replay a snapshot recorded against a page instance that was evicted and re-opened",
   "family": "direct-files-data-safety",
   "sources": {
    "issues": [
     305
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend-store",
   "status": "released",
   "coverage": {
    "tests": [
     "src/store.test.ts::working-set eviction::refuses an undo entry recorded before the page was evicted (GH #305)"
    ]
   },
   "notes": "Working-set eviction keeps undo history by design, but the kept entry describes the evicted instance. Re-opening installs a fresh instance carrying the file's current content, and invalidateUndoForPage only runs when a load REPLACES a page that is still present, so the stale entry stayed eligible. Replaying it restored pre-eviction content and marked the page dirty; the next save submitted that content under the fresh instance's baseline, which the base-revision guard accepts because the baseline genuinely matches disk, so no conflict was ever raised. Undo/redo entries now carry the page-instance generations they were recorded against and are refused (with the page's history dropped and a toast) when any of those instances is gone. Verified fail-before: the structural-edit repro restored ['keep me','delete me'] over the external content on the unfixed tree."
  },
  {
   "id": "REG-NESTED-ASSET-READ-300",
   "title": "Images and streamed media can be read from nested assets directories",
   "family": "assets",
   "sources": {
    "issues": [
     300
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "released",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::tests::nested_asset_reads_accept_relative_paths_but_not_traversal",
     "crates/tine-core/src/model.rs::tests::nested_asset_reads_cannot_follow_a_symlink_outside_assets"
    ]
   },
   "notes": "The frontend already reduced ./assets/dir/file.png to the assets-relative name dir/file.png, but all native read entry points applied the top-level-only validator used by asset creation. Read paths now accept ordinary nested relative components and retain canonical containment beneath the approved assets root. Absolute paths, empty/dot/parent components, portable backslash traversal and symlink escapes remain refused. Asset creation/import still accepts only a top-level filename."
  },
  {
   "id": "REG-DIRECT-LARGE-GRAPH-OPEN-266",
   "title": "Opening a large Direct Files graph does not synchronously scan its pages or assets",
   "family": "direct-files-performance",
   "sources": {
    "issues": [
     266
    ],
    "commits": []
   },
   "platforms": [
    "windows",
    "linux"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/graph.rs::tests::direct_large_graph_open_manual_benchmark",
     "scripts/e2e-windows-direct-large-open.mjs",
     "src/graphStartup.test.ts",
     "src/homePage.test.ts",
     "src/components/Page.test.tsx::Journals feed generation lifecycle"
    ]
   },
   "notes": "The reporter measured 44.9 seconds inside the frontend load_graph call on Windows for about 13,000 pages and 25,884 files / 24.6 GB. The representative release benchmark creates 13,000 Markdown pages plus 12,884 sparse assets in 281 buckets (25.2 GiB apparent assets): current core open took 0.4 ms and the full current Linux app returned from load_graph in 65 ms; the released v0.6.90 Linux AppImage returned in 26 ms on the same fixture. Graph size alone therefore did not reproduce the Windows report. The frontend nevertheless performed two optional page-discovery operations synchronously after native binding: default-journal template discovery and configured-home-page lookup. Those may cold-scan a Direct graph and are unnecessary for first interaction. Visible Journals now owns and awaits template materialization before its first feed fetch, preserving #73, while graph open no longer duplicates that work. Configured-home lookup runs in the background and may navigate only if graph, epoch, and landing route are unchanged, so a slow result cannot override user navigation. Frontend phase logs now distinguish native binding, session restoration, and interactivity; native debug logs retain the lower-level phase timings. The Windows scenario and reporter retest remain valuable platform evidence, but the demonstrated synchronous graph-size-dependent frontend work is now removed and its ordering contracts are covered."
  },
  {
   "id": "REG-ANDROID-STRUCTURAL-BLOCK-MOVE-312",
   "title": "Move up and down crosses child-list edges like Logseq on Android and desktop",
   "family": "outline-structural-movement",
   "sources": {
    "issues": [
     312
    ],
    "commits": [],
    "og": [
     "src/main/frontend/handler/editor.cljs:1731-1753",
     "src/main/frontend/modules/outliner/core.cljs:760-797",
     "src/main/frontend/mobile/mobile_bar.cljs:99-100"
    ]
   },
   "platforms": [
    "android",
    "desktop"
   ],
   "layer": "state-model",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/store.test.ts::OG structural block move parity",
     "src/store.test.ts::cross-day move (journal feed as one list)"
    ]
   },
   "notes": "Tine's mobile toolbar and desktop editor command already dispatched the same moveBlockFeed operation, but that operation only swapped siblings and stopped at every nested child-list edge. OG uses one shared move-up/down handler too. Its outliner rule swaps ordinary siblings; at a boundary, moving the last child down enters the next parent sibling as its first child, and moving that first child up enters the previous parent sibling as its last child. Tine now implements that same reversible structural rule while retaining root-level journal-feed crossing, subtree identity, inherited list-style semantics, caret-preserving keyed DOM movement, and one undo unit. The regression covers both directions, a child subtree, ordinary sibling movement with Undo/Redo, and the true top boundary."
  },
  {
   "id": "REG-PAGE-RENAME-COLLISION-MERGE-327",
   "title": "Renaming a page onto an existing page offers the Logseq-compatible complete merge",
   "family": "page-identity-mutation",
   "sources": {
    "issues": [
     327
    ],
    "commits": [],
    "og": [
     "src/main/frontend/components/page.cljs:244-266",
     "src/main/frontend/handler/page.cljs:568-645"
    ]
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/graph.test.tsx::page rename collisions",
     "crates/tine-core/src/model.rs::tests::rename_collision_merges_content_and_rewrites_graph_refs"
    ]
   },
   "notes": "The previous Tine title editor sent every edit through ordinary rename, whose correct collision guard surfaced only a backend error. OG instead asks whether to merge. The parity behavior is broader than appending two files: Cancel mutates nothing; confirmation appends the source outline after the destination, retains non-conflicting Markdown page properties, rewrites references in the source, destination and every external referrer, renames source namespace descendants to the destination namespace, removes the source, refreshes all open editor state and navigates to the destination. Managed Storage composes the SQLite-indexed exact referrer plan, descendant renames, destination edit and source deletion into one semantic transaction. Direct Files commits the recoverable source-to-trash merge before invoking its existing transactional graph-wide rename/ref rewrite, so a later failure cannot lose either page's bytes. Duplicate-journal merge remains unchanged and never rewrites page identities. Catalog maintenance 2026-08-21: references to deleted pre-0.7 runtime-oracle files were removed. Remaining or replacement references name the current clean-runtime semantic contract; the retired implementation is not accepted as release evidence."
  },
  {
   "id": "REG-CASE-INSENSITIVE-TRACKED-PATHS-280",
   "title": "A fresh checkout has no tracked paths that collide on a case-insensitive filesystem",
   "family": "build-portability",
   "sources": {
    "issues": [
     280
    ],
    "commits": []
   },
   "platforms": [
    "windows",
    "macos"
   ],
   "layer": "release-tooling",
   "status": "released",
   "coverage": {
    "tests": [
     "scripts/test-release-pipeline.mjs"
    ]
   },
   "notes": "docs/UI-REGRESSION-TESTING.md is the current referenced execution contract; the case-only lowercase twin was an older planning document with no references and caused Git to omit one file on Windows checkout. The obsolete twin is removed, and the release-pipeline contract now folds every tracked path through NFC plus lowercase and refuses future collisions."
  },
  {
   "id": "REG-DIRECT-SAVE-UNTOUCHED-BYTES-AUDIT20260809",
   "title": "Editing one block does not rewrite bytes belonging to blocks the user never touched",
   "family": "direct-save-fidelity",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos"
   ],
   "layer": "tine-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/tests/graph.rs"
    ]
   },
   "notes": "Direct Files data-safety audit 2026-08-09, finding 3, reproduced and re-measured independently: editing one block and reverting it failed to restore the bytes of 96 of 983 files (9.8%) on a copy of ~/research/logseq-anonymized. 88 of the 96 were an unbulleted page-level '## Heading' gaining a '- ' prefix; the worst shape injected the source layout whitespace ('\\t  ') into the block TEXT and added a nesting level. Root cause was not the parser: doc.rs's source-layout retention machinery existed but every production call site passed an empty StructuralLayoutIdentity slice, so identity_bound_unbulleted_headings was always empty and the retention was inert on the Direct save path (test-only). doc::layout_identities_of now reads the locator->identity map off the document tree and serialize_page_dto_for_path passes it. Corpus after the fix: 96 damaged -> 0; unchanged-save no-op still byte-exact at 1045/0. Residual, backlogged: a whitespace-only separator line normalises to empty, which is why the continuation-heading test asserts text and depth rather than exact bytes."
  },
  {
   "id": "REG-DIRECT-SAVE-KEEPS-PAGE-INVENTORY-AUDIT20260809",
   "title": "A content-only save keeps the physical page inventory warm instead of forcing a whole-graph re-read",
   "family": "direct-save-performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos"
   ],
   "layer": "tine-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/tests/graph.rs"
    ]
   },
   "notes": "Direct Files perf audit 2026-08-09, F1, found independently by both the Opus and Codex lanes with agreeing numbers (243 ms vs 263-281 ms). list_pages is memoized on raw cache_gen and its only rebuild path walks the graph text scope, re-reads every file and re-parses each one: ~35 us/file, linear from 506 to 8,006 pages, and find_entry rebuilds straight from it. Every save bumped the generation, so the first navigation or [[ autocomplete after any typing pause stalled ~1/4 s on a real 5,225-file graph. page_list_cache is now re-tagged to the new generation under the same condition the effective-identity index already uses. DELIBERATELY NARROWER than the auditor's recommendation: list_pages parses every file so a title:: edit really does move an entry (hence the !identity_changed gate), and referenced_page_names is genuinely content-derived and is left to rebuild. The test asserts a ratio, not an absolute time, so it survives a loaded shared box."
  },
  {
   "id": "REG-DIRECT-REFERENCE-DELTA-HANDOFF-400",
   "title": "A reference lookup overlapping an ordinary edit waits for its queued one-page SQLite delta instead of scanning the whole graph",
   "family": "direct-reference-performance",
   "sources": {
    "issues": [
     400
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "tine-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/direct_projection_tests.rs::reference_lookup_waits_for_an_inflight_one_page_projection_delta",
     "crates/tine-core/src/direct_projection_tests.rs::reference_wait_is_zero_cost_when_no_projection_work_exists"
    ],
    "failBefore": "proven"
   },
   "notes": "GH #400 reported Unlinked References taking one to ten seconds. Direct Files already had a disposable SQLite candidate projection, but every one-page cache publication advanced the parser generation and marked SQLite unavailable until its worker applied the queued delta. A reference request in that short interval immediately used the exact but graph-wide parser fallback. The regression pauses that worker boundary and requires the read to remain pending until the bounded delta converges, while unavailable or failed projections retain the parser fallback. The separate conflict.save_baseline_present event in the report remains an intentional no-write guard for bytes that differ from the editor's loaded revision."
  },
  {
   "id": "REG-BACKGROUND-FLUSH-255",
   "title": "Pending edits are written when the app is backgrounded or hidden, not only on a clean window close",
   "family": "durability",
   "sources": {
    "issues": [
     255
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android",
    "ios"
   ],
   "layer": "frontend",
   "status": "released",
   "coverage": {
    "tests": [
     "src/backgroundFlush.test.ts"
    ]
   },
   "notes": "Direct Files data-safety audit 2026-08-09, finding 7. The ONLY durability barrier in the app was onCloseRequested. Android/iOS never send one - the OS backgrounds the app and reclaims it later - so everything inside the 400ms save debounce was RAM-only. The three pre-existing visibilitychange listeners (Page.tsx, journal.ts, assetRefresh.ts) all act on becoming VISIBLE; this is the other edge. FAIL-BEFORE is an absence, not a wrong value: no listener flushed on hide, verified by grep across the tree, so there is no pre-fix test that can fail. Confidence for #255 is therefore MODERATE, not high: this is the plainest explanation for the Android half and is a real missing handler, but no same-platform reproduction of the reporter's loss exists. Findings 2 and 6 are other live candidates for the same report."
  },
  {
   "id": "REG-FLUSH-WEDGE-PHANTOM-DIRTY-AUDIT20260809",
   "title": "A dirty page name with no page behind it no longer wedges every later flush",
   "family": "durability",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android",
    "ios"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/persistence.test.ts"
    ]
   },
   "notes": "Direct Files data-safety audit 2026-08-09, finding 6. doSave bailed on a null DTO BEFORE dirty.delete(name), so the name stayed dirty forever and flushAll() could never return true again: every graph switch aborted with 'Some pages couldn't be saved' and every window close offered to discard edits that were already on disk. Reachable because pinnedPages() did not pin in-flight saves - doSave removes a page from dirty before awaiting, so the working-set cap could evict it mid-write, and a transient failure then re-added a name with no page. Two parts: savingPages() is now pinned (prevents the eviction outright), and a dirty name with no page in the store is dropped rather than wedging. Deliberately NOT dropped when the page exists but the page-header firewall refused the DTO - that is a real unsaved edit and must keep blocking."
  },
  {
   "id": "REG-WATCHER-RELOAD-RECHECK-AUDIT20260809",
   "title": "A watcher-driven disk reload re-checks safety at the moment it applies, not before its IPC",
   "family": "durability",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android",
    "ios"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/store.test.ts"
    ]
   },
   "notes": "Direct Files data-safety audit 2026-08-09, finding 5. reloadDisposition is correct, but all four watcher sites read its verdict and then awaited backend().getPage(...) before acting - tens to hundreds of ms on a large graph, and a Syncthing burst fires many concurrently. Text typed inside that window was destroyed: upsertPage replaced the page, dropping the edit AND its undo history, with no conflict raised and nothing written to disk. store.upsertUnlessDirty already re-checked at the moment of the upsert; only these sites skipped it. reloadPageIfStillSafe re-checks centrally so a fifth watcher site cannot reintroduce the hole. reloadPage itself is deliberately left as a clobber - 'use disk version' is an explicit user decision."
  },
  {
   "id": "REG-DELETE-PAGE-OFF-COMMAND-THREAD-AUDIT20260809",
   "title": "Deleting a page does its graph-wide work on the blocking pool, not the command thread",
   "family": "direct-save-performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos"
   ],
   "layer": "tauri-commands",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/commands.rs"
    ]
   },
   "notes": "Direct Files perf audit 2026-08-09, F3. delete_page re-derives the page inventory and rebuilds three O(pages) indexes: measured ~95 us/file, linear, 757 ms at 8,006 files and ~1.9 s projected at 20,000. It was the only such command still running synchronously on the command thread; get_page, save_page and rename_page already cross the blocking pool. The guard test expensive_reference_and_rename_commands_cross_the_blocking_pool existed and simply did not list delete_page - a coverage omission, not a missing mechanism. This does not make delete faster; it stops it freezing command dispatch. The O(graph) cost itself is a separate, still-open finding."
  },
  {
   "id": "REG-DIRECT-SAVE-ATOMIC-REPLACEMENT-GH254",
   "title": "An ordinary save after an external atomic replacement compares bytes instead of refusing on physical identity",
   "family": "direct-save-fidelity",
   "sources": {
    "issues": [
     254
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos"
   ],
   "layer": "tine-core",
   "status": "released",
   "coverage": {
    "tests": [
     "crates/tine-core/tests/graph.rs"
    ]
   },
   "notes": "GH #254 increment 1, ordinary save only. An external editor that writes by temp+rename (the common safe pattern, and what Syncthing/Dropbox deliver) leaves the same path on a NEW inode. save_page authorized with PinnedSaveAuthority::LoadedRevision, whose retained-identity check refused before the byte comparison ever ran, returning 'path-pinned page does not match its captured exact owner' - a message isRetryableSaveFailure retries forever, so the page became permanently unsaveable with no user-resolvable path out. Measured pre-fix: 4 of 5 cases fail, and NOTABLY the different-byte case failed with the same unresolvable code, so a genuine external change was equally unrecoverable, not just the same-byte one. A new authority OrdinaryEditorSave proves exact path ownership and nothing about physical identity; the byte comparison then decides. Equal bytes on a new inode re-pin to the current snapshot and proceed; different bytes return the literal 'conflict' the frontend can resolve. LoadedRevision is UNCHANGED and still used by verify_existing_journal_page_projection, which authenticates before an opaque durable append and must not inherit this reorder - the contract verifier rejected a helper-wide change twice on exactly this boundary. At increment 1, delete conflicts and the force-save half were deliberately deferred to increment 2; the next record covers both."
  },
  {
   "id": "REG-DIRECT-SAVE-EXACT-CONFLICT-AUTHORITY-GH254",
   "title": "Keep mine writes only over the exact disk snapshot shown by its conflict",
   "family": "direct-save-fidelity",
   "sources": {
    "issues": [
     254
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos"
   ],
   "layer": "tine-core",
   "status": "released",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs",
     "src-tauri/src/commands.rs",
     "src/persistence.test.ts"
    ]
   },
   "notes": "GH #254 increment 2. A conflict now mints one per-Graph, per-editor-episode, exact-path, monotonic-epoch token holding Present { revision, resource_identity } or Absent plus the coherently observed baseline bytes. Force consumes it before every attempt and runs the ordinary save protocol with those bytes and identity substituted as its rechecked baseline; it never delegates by revision alone. Under Martin's state-not-events ruling (2026-08-09) a byte-identical republication of the shown snapshot is a no-op, so force DOES rebind to the live inode in exactly that case and refuses a different-byte winner on a new inode; two tests pin both halves. S1-S9 each have a deterministic site test, including both final-reread arms and post-restore/post-publication observation timing. Stable observations get per-site conflict.* codes; exceptional observation failure gets conflict_retry.*, which cannot reach the keep-mine/use-disk banner and now also RETRACTS an existing one: a force has already spent its token by then, so leaving the banner up left Keep-mine permanently dead beside a destructive Use-disk (adversarial implementation verification, finding 3). Loads, watcher observations, forget_file, successful saves, and the shared Tine-owned graph-text mutation boundary revoke authority, covering rename/delete/trash/merge, managed projection/import/migration, sync-conflict resolution, and PDF-highlight page writes. Hard admission refusals remain tokenless."
  },
  {
   "id": "REG-UNLINKED-REFS-SEE-CODE-GH270",
   "title": "Unlinked References finds a page name written inside code, math and other literal text",
   "family": "references",
   "sources": {
    "issues": [
     270
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos"
   ],
   "layer": "tine-core",
   "status": "released",
   "coverage": {
    "tests": [
     "crates/tine-core/src/reference_evidence.rs",
     "crates/tine-core/tests/graph.rs"
    ]
   },
   "notes": "Logseq matches unlinked references with a raw regex over the whole block content (db/model.cljs get-page-unlinked-references, only drawer/remove-logbook stripped), so code fences, inline code, math, raw HTML, examples, latex environments and hiccup are all searched. Tine derived its search region from an ALLOWLIST of block kinds in reference_evidence walk_blocks, whose `_ => {}` arm silently dropped every other variant - so the region lost coverage each time lsdoc grew a block type. Now derived by SUBTRACTION: plain_search_ranges takes the complement of what the parser already claimed (explicit refs, block refs, structural properties, :LOGBOOK: drawers), which is Logseq's own semantics reached through the parser rather than a second scanner - alias/NFC/boundary logic unchanged and new lsdoc variants covered for free. Logseq's `#`/doubled-`[` prefix rule had to be ported explicitly (og_prefix_allows): it used to be implied by overlap with a parsed reference, but inside a fence there is no parsed reference, so `[[Page]]` in code would otherwise have matched. That also corrected an inversion - for `\\[[Target]]` Tine reported the escaped bracket and hid both code mentions, exactly backwards from Logseq. DELIBERATE DIVERGENCE, named: structural id::/collapsed::/logseq.* property text stays withheld (Logseq scans it) because a page named after a uuid fragment is not a real outcome. PERF: widening the region cost +13% (285->325 ms cold on the 1,045-file anonymized graph), so an ASCII prefilter was added to visit_plain_matches - it skips a start position only when BOTH the name's first char and the source char are ASCII, where lowercase is exact and NFC is identity, leaving folding cases like the Kelvin sign to the authoritative matcher. Net 285 ms -> 36 ms, ~8x faster than before the change. Reusing string buffers instead was measured WORSE (400 ms) because flat_map(char::to_lowercase) loses str::to_lowercase's ASCII fast path. Necessity: 6 unit tests and 2 of 3 integration tests fail on the pre-fix tree; the third asserts the complement did NOT over-widen (an explicit link and a CLOCK: line stay out) and passes both sides."
  },
  {
   "id": "REG-UNRESOLVED-PAGE-LINK-VISIBLE-20260809",
   "title": "A link naming no existing page is visibly distinguished, and a fileless #tag is not",
   "family": "references",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/pageExistsBatch.test.ts",
     "scripts/e2e-page-identity-links.mjs"
    ]
   },
   "notes": "Martin, 2026-08-09: a generated graph wrote each report to `pages/c01 Swap Bribery.md` with `title:: Swap Bribery` and linked the index by FILENAME. Since Logseq resolves a page's name as title:: > filename > first block (graph-parser extract.cljc get-page-name), the filename named nothing and all 29 links opened blank pages with no signal. Tine <=0.6.5 used the filename and ignored title::; aa4a4b29 (2026-07-29, shipped in 0.6.90) corrected that, which is why the graph looked fine when built. Backend was never at fault - load_named returned full content for all 30 pages. DELIBERATE DIVERGENCE, approved by Martin: Logseq does NOT distinguish unresolved links (components/block.cljs:548 dims only untitled-page?, i.e. a page that EXISTS under a uuid name); Tine dims them, as Obsidian does. Applied to [[refs]] only, NOT #tags - a tag with no page file is ordinary Logseq usage and dimming those would mark most tags in a normal graph. existing_page_names resolves like load_named (normalized keys + alias table, no PageKind filter, so a link to an existing journal is alive) and costs one memoized page-list read per call; pageExistsBatch coalesces every reference in a tick into one IPC, asks each name once per graph, and treats UNKNOWN AS ALIVE so a page of good links never flashes as dead. Verified on the real 30-page graph: 8/8 refs dimmed before the repair, 0/8 after, all six sampled links clicking through to 24-27 block pages."
  },
  {
   "id": "REG-SAVE-DEBOUNCE-CEILING-20260809",
   "title": "A fluent typing burst is never written to disk until the typist pauses",
   "family": "direct-save-fidelity",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/persistenceDebounceCeiling.test.ts"
    ]
   },
   "notes": "Direct Files data-safety audit 2026-08-09, finding 9. scheduleSave cleared and re-armed a 400 ms timer on every markDirty, with no ceiling, so the debounce fired only during a pause in typing. Measured fail-before: 40 keystrokes at 300 ms intervals - 12 seconds of prose at a pace slower than most people type - produced ZERO writes. A crash, power loss or OOM kill during a long passage therefore lost the whole passage, and the loss window grew with how well the writing was going. Fixed by measuring a MAX_SAVE_DELAY_MS ceiling of 3 s from the FIRST dirty mark of a burst rather than the last, so the bound is on file staleness rather than typing speed; the burst clock restarts whenever no save is armed, which every drain path (flushAll, resetSaveState, the timer itself) already guarantees by nulling saveTimer. Coalescing is unchanged: a short burst still costs exactly one write."
  },
  {
   "id": "REG-CONFLICT-RESOLVE-PINNED-FILE-20260809",
   "title": "\"Use disk version\" on a page pinned to a specific file loads a different file",
   "family": "direct-save-fidelity",
   "sources": {
    "issues": [
     21
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android"
   ],
   "layer": "frontend",
   "status": "needs-revalidation",
   "coverage": {
    "tests": []
   },
   "notes": "Direct Files data-safety audit 2026-08-09, finding 10. Two files can carry one page NAME - the duplicate-day stray of GH #21, or same-titled pages in different folders - and the editor pins such a page to its exact path so saves write back to the file it was loaded from. ConflictBar's \"Use disk version\" resolved by NAME (backend getPage), which reaches the backend's canonical owner of that name, so the button silently re-pointed the tab at a different file: the user discarded their edits to THIS file and got someone else's file loaded in its place. Fixed by resolving through getPageByPath whenever the loaded page carries a path - the same idiom store.ts, RightSidebar and LiveRefGroup already use. An absent pinned file drops the page rather than falling back to the name, since that fallback is the same defect; an unpinned (never-saved) page still resolves by name because it has nothing else. needs-revalidation 2026-09-15: its cited tests were deleted with the Managed Storage removal; the Direct Files outcome still needs its own proof."
  },
  {
   "id": "REG-DELETE-PAGE-UNSAVED-WARNING-20260809",
   "title": "Deleting a page with unsaved edits promises a recoverable trash copy that does not contain them",
   "family": "direct-save-fidelity",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/components/ContextMenu.test.tsx"
    ]
   },
   "notes": "Direct Files data-safety audit 2026-08-09, finding 18. The delete confirmation said only 'The file moves to the graph's .tine-trash folder', which reads as fully recoverable. The trash receives the FILE, so a dirty page's latest keystrokes - and, provably, EVERY edit on a conflicted page, whose saves are refused by design until the conflict is resolved - are destroyed with nothing to recover from. The prompt now names that outcome when the page is dirty or conflicted, while the user can still answer no; wording is unchanged for a page with nothing unsaved. Deliberately NOT fixed by flushing first: making delete trigger a save is a behavior change with its own risk, and it cannot help the conflicted case at all."
  },
  {
   "id": "REG-CLOSE-FLUSH-GRACE-20260809",
   "title": "Closing offers to discard work that is still being written",
   "family": "direct-save-fidelity",
   "sources": {
    "issues": [
     161
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/safeClose.test.ts"
    ]
   },
   "notes": "Direct Files data-safety audit 2026-08-09, finding 11. The shared close transaction (desktop window-close and Android root Back) bounded flushAll at a flat 4 s and treated expiry as failure, so a flush that had merely not FINISHED was reported in the same words as one that could never succeed - 'unsaved changes that couldn't be saved... close anyway and lose them?' - inviting the user to throw away a write in progress. A slow or network filesystem, a fsync behind a busy disk, or many dirty pages can exceed 4 s with nothing wrong; on the 1,045-file anonymized corpus a save averages ~7 ms, so the bound is already several hundred pages' worth. The close now races the SAME flush promise twice: at the soft bound it says 'still saving' and grants a 26 s grace period, and only a flush that genuinely finished false, threw, or outlasted the grace period reaches a discard prompt - which now distinguishes 'failed' from 'still-saving' in its wording. The pre-existing test asserted that the timeout case behaved identically to failure, i.e. it encoded the defect; it was split rather than grandfathered."
  },
  {
   "id": "REG-CONFLICT-SELF-CLEARS-20260809",
   "title": "A conflict raised by a transient file removal is never cleared when the file returns",
   "family": "direct-save-fidelity",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/App.test.tsx"
    ]
   },
   "notes": "Direct Files data-safety audit 2026-08-09, finding 17. Raising a conflict was gated on a per-page divergence proof; clearing one was gated on nothing, because only a user's click (or forgetPage) ever cleared a conflict. handleGraphChange's REMOVED branch calls markConflict with no proof at all, so an external editor writing by temp+rename, or a mid-delivery Syncthing pass, could park a page behind the banner permanently. doSave then refuses that page forever, and both banner buttons destroy something: 'Use disk version' the unsaved edit, 'Keep mine' a file it never needed to overwrite. Fixed by making the proof two-directional in ONE owner, applyDivergenceVerdict, used by both observation sites (legacy watcher and sparse-v2 epoch): a negative proof on a currently-conflicted page clears the conflict and reschedules the edit it was freezing. A third site cannot reintroduce the asymmetry. A file that returns with DIFFERENT content still conflicts."
  },
  {
   "id": "REG-SHADOW-JOURNAL-TEXT-EXTENSIONS-20260809",
   "title": "The direct shadow-journal rule asked two extensions while its managed twin asked three",
   "family": "filesystem-safety",
   "sources": {
    "issues": [
     21
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android"
   ],
   "layer": "tine-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/tests/graph.rs"
    ]
   },
   "notes": "Direct Files data-safety audit 2026-08-09, finding 15, landed as a CONSISTENCY fix with an explicit NEGATIVE result. Graph::is_shadow_journal (direct mode) hard-coded canonical candidates '{stem}.md' and '{stem}.org'; its managed twin is_shadow_journal_under_permit already asked configured_text_variant_paths, i.e. all of LOGSEQ_TEXT_EXTENSIONS = md/markdown/org. The audit predicted that in a '.markdown' graph a title-named leftover would therefore escape the #21 shadow rule and poison the (kind,name) cache. IT DOES NOT: probes at the observation boundary show load_named and journals_desc both still serve the canonical file for both extensions, because later layers mask the misclassification. The necessity gate is therefore NOT satisfied and no user-visible defect is claimed - the divergence between the two classifiers is real and was removed, and the three new tests pin the #21 rule per extension so that a change in whichever layer is currently masking it cannot silently take the guarantee with it."
  },
  {
   "id": "REG-ALL-PAGES-LABEL-LINEAR-20260809",
   "title": "The All Pages sidebar list scanned the whole page list once per visible row",
   "family": "performance",
   "sources": {
    "issues": [
     21
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/pages.test.ts"
    ]
   },
   "notes": "Direct Files performance audit 2026-08-09, finding F9. pageListLabel disambiguates a row whose display name collides with another page (same title in different folders, or the same stem as .md and .org) by scanning the whole list twice - once for the name, once for the parent sub-path. The sidebar called it per rendered row, so rendering All Pages cost O(rows x pages): measured 21 ms at 5,000 pages and 39 ms at 20,000, for 300 visible rows. pageListLabels now counts both keys in ONE pass and returns a lookup, memoized in Sidebar alongside the existing filter+sort memo. The label RULE is unchanged and the three original correctness tests still pass verbatim. Necessity: the shape tests COUNT element reads through a Proxy rather than timing anything - a linear implementation reads the array once while building the index and never again, a per-row scan reads it once more per row - and both fail against the pre-fix implementation. A first attempt asserted wall-clock and was replaced after it flaked on a loaded box: 47 ms against a 26 ms ceiling with the fix in place. The index is keyed by value, not object identity, so a caller holding a copy of an entry gets the same label."
  },
  {
   "id": "REG-STARTUP-UNWRITABLE-DATA-HOME-20260809",
   "title": "Tine crashes at launch with a backtrace when the app-data home is not writable",
   "family": "availability",
   "sources": {
    "issues": [
     303
    ],
    "commits": []
   },
   "platforms": [
    "linux"
   ],
   "layer": "tauri-app",
   "status": "released",
   "coverage": {
    "tests": [
     "src-tauri/src/data_home.rs"
    ]
   },
   "notes": "Found while working the 2026-08-09 Direct Files audit backlog; reproduced on the stock deployed release binary, so not a regression from that work. Tauri creates the WebView user-data directory ($XDG_DATA_HOME/page.tine.Tine) inside its OWN setup(), while building the configured windows (tauri::manager::webview calls create_dir_all there), and an IO error at that point is unreachable by the application: it panics with 'Failed to setup app: Permission denied (os error 13)' at tauri-2.11.2/src/app.rs:1417, before Tine's setup hook runs. A user whose ~/.local/share is root-owned - the shape of the box this was found on, drwxr-xr-t root root - therefore got a hard crash with a backtrace and no actionable message, made more confusing by Tine's own desktop-identity step logging the same Permission denied and explicitly treating it as NON-fatal one line earlier. data_home::ensure_usable now probes the directory before anything resolves it (create_dir_all is not the question: it succeeds on an existing directory nobody may write) and, when it is unusable, points XDG_DATA_HOME at the first writable fallback for that launch - ~/.tine-data, then $XDG_RUNTIME_DIR/tine-data, then a uid-namespaced temp dir - so settings, session and the WebView store live somewhere real. XDG_DATA_HOME is the one lever that redirects Tauri's path resolver, WebKitGTK's website-data manager and Tine's own settings together. The frontend toasts once, stickily, naming the directory, because a silent relocation is its own defect. If nothing at all is writable the app prints one actionable sentence and exits 1 rather than panicking. Verified on the built binary: unwritable base -> app stays up, ~/.tine-data/page.tine.Tine populated with tine-settings.json and the WebKit store, nothing written to the read-only base; ordinary writable base -> no relocation diagnostic at all and no probe residue."
  },
  {
   "id": "REG-WATCH-RETRY-UNWATCHED-ROOT-20260809",
   "title": "A graph whose watch() failed stays blind to external changes while another graph is watched",
   "family": "external-change-reconciliation",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "macos",
    "windows"
   ],
   "layer": "tauri-app",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/watcher.rs"
    ]
   },
   "notes": "Direct Files data-safety audit 2026-08-09, finding 16, landed in its REACHABLE form only. The audit claimed an inotify watch() failure leaves the watcher permanently blind with no poll fallback; that is not what the code does. A failed root is never inserted into `watched` and is retried on the next cycle, and when EVERY root fails `watched` is empty, which takes the else branch and its bounded <=3 s recv_timeout - a working recovery loop. The reachable case is mixed: with two graphs open, the inotify branch blocks on rx.recv() until an event arrives from a root that IS watched, so a root the kernel refused is retried only when some other graph happens to change. inotify limits are per-user (fs.inotify.max_user_watches), so opening a second large graph is exactly how one root fails while the other is healthy, and on a quiet healthy graph the failing one stays invisible indefinitely. inotify_cycle_wait now bounds the wait to UNWATCHED_ROOT_RETRY (3 s, matching the poll branch ceiling) whenever a desired root is unwatched, and never delays a sooner scheduled retry. Coverage gaps stated in the tests themselves: they pin the wait policy, not a real kernel watch() failure (which needs privileges unavailable here) and not the call-site wiring, and they are a specification rather than fail-before evidence because the function is new."
  },
  {
   "id": "REG-REFERENCED-NAMES-DIGEST-GATE-20260809",
   "title": "Every typing lull shipped the whole reference-name inventory across IPC",
   "family": "direct-files-performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android"
   ],
   "layer": "tine-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs",
     "src/pages.inventory.test.ts"
    ]
   },
   "notes": "Direct Files performance audit 2026-08-09, finding F7. The frontend's reference-name resource is keyed on dataRev, which a save bumps ~700 ms after every typing lull, so referenced_page_names ran and its result crossed IPC on every pause in typing: measured 15-23 ms and about 5,000 names at 5,225 files, with the JSON parse landing on the UI thread. The answer almost never changes, because typing inside a block rarely adds or removes a [[link]]. referenced_page_names now takes the digest of the set the caller already holds and omits the payload when it still matches, so the ordinary case is an integer. The digest is a commutative fold with the length mixed in: the memo's order comes from a HashMap, so a sequence-dependent hash would report a change on every rebuild and the gate would save nothing. NOT fixed here: the whole-graph recompute itself still runs once per cache generation, because computing the digest needs the names; making it incremental needs per-name reference counts in cache_upsert/cache_remove and is a separate packet. The frontend test pins the dangerous reading of an omitted payload - treating it as an empty set would erase every reference-only page from autocomplete and the namespace tree - and that the digest is actually presented back."
  },
  {
   "id": "REG-SAVE-PORTABLE-ALIAS-SCAN-ASCII-20260809",
   "title": "Every save case-folded and NFC-normalised every filename in the target's directory",
   "family": "direct-files-performance",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android"
   ],
   "layer": "tine-core",
   "status": "needs-revalidation",
   "coverage": {
    "tests": []
   },
   "notes": "Direct Files performance audit 2026-08-09, finding F6. validate_graph_text_portable_aliases_path_local enumerates each path component's directory looking for case/NFC aliases of the target, and built a PortablePathKey for EVERY entry - a String allocation plus NFD -> default case fold -> NFC per filename. The audit's controlled experiment held graph size constant and varied only layout: 4.87 ms/save with 4,000 pages in one directory versus 0.70 ms with 40 per subdirectory, i.e. 6.9x on layout alone, growing without bound on the one directory that only ever grows (journals/) and running inside the identity-mutation lock. PortablePathKey::graph_text_components_match answers the same question without building either key when both sides are ASCII, where NFD/NFC are the identity and default case folding is exactly ASCII lowercasing. Controlled A/B on this box, same graph and build, 4,000 pages in one directory, 60 saves x 3 runs: 2.54-2.67 ms median before, 1.39-1.41 ms after (~1.8x). The both-sides-ASCII guard is load-bearing rather than redundant: folding can PRODUCE ASCII from non-ASCII (U+212A KELVIN SIGN folds to k, U+FB01 to fi), so a non-ASCII name can legitimately collide with an ASCII one and must still take the full fold. This is an identity boundary whose failure mode is admitting a collision, so the test asserts agreement with the full fold over every ordered pair of a name set that includes NFC/NFD twins, the sharp s, Cyrillic, and both fold-to-ASCII traps. Residual NOT fixed: the directory enumeration is still O(entries); removing that needs the maintained admission index to become the authority for this check, which is a data-safety boundary change and its own packet. needs-revalidation 2026-09-15: its cited tests were deleted with the Managed Storage removal; the Direct Files outcome still needs its own proof."
  },
  {
   "id": "REG-CONFLICT-OVERRIDE-NAMES-ITS-OBSERVATION-GH254",
   "title": "A duplicated \"Keep mine\" spent authority minted for a newer winner nobody was shown",
   "family": "direct-save-fidelity",
   "sources": {
    "issues": [
     254
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android"
   ],
   "layer": "tine-core",
   "status": "released",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs",
     "src/persistence.ts"
    ]
   },
   "notes": "GH #254 increment 2, adversarial implementation verification finding 1 (CRITICAL) and the reachable half of finding 2. A force request named its authority only by path plus loaded revision, so consume_conflict_authority could only check SELF-consistency - that the live token is the newest - never that the user had seen it. Reachable data loss with no malicious writer: an external editor or sync client publishes B after the banner showed A; two force requests are in flight under that one banner (the button is not disabled while its request is pending) and the per-page save queue serializes them; the first correctly refuses B and, being a coherent observation, mints fresh authority FOR B; the second, issued before B existed, consumed it and overwrote B. A conflict error now carries the observation epoch it minted at, direct_save_error_message emits it as 'conflict:<n>', the frontend stores it with the banner and echoes it back on force, and consumption requires the presented epoch to equal the live token's. Found while fixing: checking ownership AFTER taking would let one stray duplicate click disarm the banner permanently, so a mis-addressed override is now refused WITHOUT spending the live token - covered by its own test. A force that can name no observation is refused at the command boundary rather than allowed to consume whatever is current. Necessity: all three new core tests fail against the pre-fix check."
  },
  {
   "id": "REG-DIRTY-EDITOR-SURVIVES-SAME-NAME-REPLACEMENT-GH304",
   "title": "Unsaved edits were destroyed when a same-named file at a different path was loaded",
   "family": "direct-save-fidelity",
   "sources": {
    "issues": [
     304,
     254
    ],
    "commits": []
   },
   "platforms": [
    "linux",
    "windows",
    "macos",
    "android"
   ],
   "layer": "frontend",
   "status": "released",
   "coverage": {
    "tests": [
     "src/instanceReplacement.test.ts"
    ]
   },
   "notes": "GH #254 increment 3, contract rule 2. ensurePageLoaded replaced the loaded instance whenever the requested path differed from the incumbent's, with no check that the incumbent held unsaved work. Reproduced: the incumbent's nodes are purged, the dirty mark SURVIVES and now belongs to the replacement's content, and a live conflict banner survives describing a file the editor no longer holds. The gate is the existing reloadDisposition predicate AND an empty editor-lease set - enumerating dirty/conflicted/saving kept losing, because IME composition holds text in the textarea until compositionend and the page-title rename holds its draft in component-local signals, so neither is visible to any store predicate. A component holding uncommitted input now declares itself with a lease keyed by a unique per-component handle, released idempotently from the component lifecycle rather than only from commit/cancel - a lease that outlives its component would refuse every later replacement forever. The gate is re-evaluated synchronously at the final replacement boundary because every caller awaits a backend read first and the incumbent can become dirty during that await. Necessity: both behavioural tests fail against the pre-fix code; the lease-hygiene and clean-replacement cases pass both ways by design and are not regression guards for the refusal itself."
  },
  {
   "id": "REG-WATCH-MIDEDIT-DEFERRED-RELOAD-337",
   "title": "An external change to a page whose block is being edited (or mid block-move, or under an editor lease) is deferred and replayed when the blocking state clears, not silently dropped",
   "family": "external-change-freshness",
   "sources": {
    "issues": [
     337
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend-store",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/App.test.tsx::deferred replay of externally changed pages skipped mid-edit"
    ],
    "scenario": "watch.mid-edit-deferred-replay",
    "failBefore": "proven"
   },
   "notes": "reloadDisposition's \"skip\" (block edit / block move) and mayReplaceInstance's lease refusal correctly declined the reload but the watcher event was then dropped: backend cache fresh, visible page stale indefinitely. The declined GraphChange is now recorded and re-dispatched through handleGraphChange when onPageBecameReplaceable fires, so the disposition is re-evaluated at replay time (a page that became dirty meanwhile takes the applyDivergenceVerdict path). Fail-before: all three App.test.tsx cases fail on pre-fix code with the page stuck at its stale content. Concord P1 freshness lane."
  },
  {
   "id": "REG-WATCH-RELOAD-ON-FOCUS-337",
   "title": "Returning to the window replays deferred reloads and asks the backend watcher for one full stat diff, for filesystems that deliver no event",
   "family": "external-change-freshness",
   "sources": {
    "issues": [
     337
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend-store",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/reloadOnFocus.test.tsx::reload on focus"
    ],
    "scenario": "watch.reload-on-focus-fallback",
    "failBefore": "proven"
   },
   "notes": "Network mounts, some sync clients and a suspended app deliver no inotify edge at all, so a page could sit stale indefinitely with the watcher healthy. Returning to the window now (a) calls sweepReplaceable() -- the P1 net, for a reload deferred mid-edit whose page never announced it became replaceable because the user alt-tabbed away -- and (b) invokes rescan_graph_now, which arms one full stat diff on the watcher thread. Findings arrive as ordinary graph-changed/graph-changed-bulk events, so the disposition, divergence proof and deferred replay all apply exactly as for a live event and no caret is stolen. The rescan is throttled (1.5 s); the sweep never is. Fail-before: with refreshOnReturnToWindow's body removed, 4 of the 6 cases fail with rescanGraphNow called 0 times. Concord P5."
  },
  {
   "id": "REG-CONCORD-ALWAYS-ASK-POLICY-337",
   "title": "The always-ask policy converts only the SILENT external-change case into an asked one, and leaves every path that already asks or defers alone",
   "family": "external-change-freshness",
   "sources": {
    "issues": [
     337
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend-store",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/App.test.tsx::with always-ask on"
    ],
    "scenario": "watch.always-ask-policy",
    "failBefore": "proven"
   },
   "notes": "Obsidian 1.9.7's one user-visible conflict policy switch (landscape survey U3). Tine's only silent adoption of external bytes is L0 freshness: a loaded, clean page is replaced with the disk version. With concord_always_ask on that change is HELD and the page offers Reload from disk / Keep mine, never a modal. The gate sits AFTER the skip and conflict branches and requires disp === reload plus a loaded page, so a dirty page still gets applyDivergenceVerdict and a mid-edit page still defers through the P1 replay machinery -- proved by the third case, where a mid-edit change is deferred, not held, and is only held once the replay re-enters with the page clean. Default is OFF and the fourth case pins the unchanged silent reload. Holding is frontend-only by design: the backend cache has already adopted the bytes, so Keep mine is honest -- the next save meets the base_rev guard and raises the ordinary conflict banner, which is Obsidian's create-a-conflict mode. Fail-before: with the policy gate removed from applyExternalChange, 3 of the 4 cases fail (getPage called once, the page shows 'fresh from disk', nothing is held). Concord P5."
  },
  {
   "id": "REG-DIRECT-WINDOWS-EXACT-WATCHER-CREATION-366",
   "title": "Windows watcher handling stranded new-page saves after external events and Tine's own publication echoes",
   "family": "direct-files-data-safety-performance",
   "sources": {
    "issues": [
     366,
     374
    ],
    "commits": []
   },
   "platforms": [
    "windows"
   ],
   "layer": "native-filesystem-watcher",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/watcher/tests.rs::windows_exact_page_events_stay_incremental_through_the_pending_queue",
     "src-tauri/src/watcher/tests.rs::windows_unicode_event_reconciles_and_releases_new_page_creation",
     "src-tauri/src/watcher/tests.rs::windows_tine_owned_create_echoes_do_not_block_following_pages_or_journals",
     "src-tauri/src/watcher/tests.rs::windows_external_replacement_of_tine_publication_keeps_creation_blocked",
     "crates/tine-core/src/model.rs::tests::windows_direct_publication_event_waits_for_inflight_writer_receipt",
     "crates/tine-core/src/model.rs::tests::windows_ambiguous_callback_cannot_interrupt_inflight_direct_creation",
     "crates/tine-core/src/model.rs::tests::windows_direct_publication_receipt_requires_revision_and_file_identity",
     "src-tauri/src/watcher/tests.rs::atomic_page_save_temp_events_stay_incremental",
     "crates/tine-core/src/model.rs::tests::direct_creation_round_trips_a_chinese_page_name",
     "crates/tine-core/src/model_alias_admission_tests.rs::creation_refuses_portable_leaf_and_ancestor_aliases_without_mutation",
     "crates/tine-core/src/model.rs::tests::external_semantic_owner_creator_wins_before_creation_publication"
    ],
    "scenario": "direct.windows-exact-event-and-self-publication-create-reconcile-create",
    "failBefore": "proven"
   },
   "notes": "GH #366 on v0.6.94 / Windows 11 / Direct Files: creating TINE\u7248\u672c\u66f4\u65b0\u63d0\u793a\u8bcd failed three times with `external graph-text changes arrived before creation publication`, then the editor became stuck dirty. Commit 416be3cb correctly made the raw Windows watcher treat ReadDirectoryChangesW `Create(Any)` and `Modify(Any)` as exact file events, but Pending::add_event independently classified the same event as unknown and scheduled a whole-graph diff. On the reporter's 13k-page graph that kept the safe external-change admission frontier raised beyond the frontend's complete 100 ms + 300 ms retry window. GH #374 on v0.6.96 was a literal negative follow-up and revoked that coverage: Tine's own newly-published journal raised the raw external-observation frontier before debounced reconciliation could recognize the self write, so an immediate second page failed with both `awaiting watcher reconciliation` and `arrived before creation publication`. Deterministic fail-before saves journal then injects its Windows `Create(Any)` echo before the second page and observes epoch 1 instead of 0. The first repair proved exact-path self echoes, but the reporter's v0.6.97 negative follow-up exposed the missing sibling: an ambiguous callback could still publish the graph-wide epoch before waiting for an in-flight writer, making that writer refuse its own creation at the final pre-publication check. The second deterministic interleaving pauses a create at that check and proves the callback frontier waits behind the resource identity authority. The repaired raw boundary omits an exact frontier only after two coherent no-follow reads match Tine's completed revision and physical file identity; ambiguous callbacks still enter ordinary debounced reconciliation, but only after an overlapping Tine publication is complete. Different external bytes and an identical-byte replacement inode both retain the barrier. External semantic owners, exact-target races, and case/NFC aliases remain fail-closed."
  },
  {
   "id": "REG-GUIDE-COPY-DIRECT-WATCHER-391",
   "title": "Copying the multi-page Guide was interrupted by Tine's own native watcher echo",
   "family": "direct-files-data-safety-onboarding",
   "sources": {
    "issues": [
     391
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "tine-core-onboarding-native-filesystem-watcher",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/onboarding.rs::tests::copied_guide_pages_are_owned_for_native_watcher_echoes",
     "scripts/probe-guide.mjs"
    ],
    "scenario": "guide.open-read-only-copy-whole-namespace-navigate-copied-pages",
    "failBefore": "proven"
   },
   "notes": "Literal current-master real-app fail-before reproduced the reporter's Windows v0.6.97 outcome on Linux: the first Guide page was published, its native watcher echo raised the external-change frontier, and the next Guide page failed with `external graph-text changes arrived before creation publication`, leaving no copied namespace. Guide creation used the shared atomic Direct Files primitive but bypassed the ordinary editor commit path that records exact final bytes and physical identity for self-echo recognition. Every copied page now records that receipt after a final exact reread. The whole-guide real-app probe is the semantic proof: all pages and assets are copied, links resolve, and navigation reaches the writable copied Guide."
  },
  {
   "id": "REG-AUTOCOMPLETE-SURFACE-POOLS-186",
   "title": "Ctrl+K, #, [[ and (( share candidates within each surface's designed entity pool and window; autocomplete input stays literal and stable across character swaps",
   "family": "search-autocomplete-reliability",
   "sources": {
    "issues": [
     186
    ],
    "commits": [
     "294edbd0"
    ]
   },
   "platforms": [
    "all"
   ],
   "layer": "rust-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/tests/issue186_surfaces.rs::literal_autocomplete_is_stable_across_character_swaps_and_dsl_tokens",
     "crates/tine-core/tests/issue186_surfaces.rs::page_and_block_surfaces_share_candidates_within_designed_limits"
    ],
    "scenario": "search.autocomplete-surface-pool-contract",
    "failBefore": "inferred"
   },
   "notes": "B1 follow-up reconciliation on current master: the remaining confirmed defect from the issue timeline (autocomplete interpreting literal text as the Ctrl+K DSL, e.g. bare OR collapsing to an empty matcher) was already fixed on master by 294edbd0 (shipped v0.6.4) and stays green; this row instead pins the remaining cross-surface contract for the \u533b\u4fdd-style report on a synthetic CJK graph (163 eligible page candidates): the page surfaces (quick_switch = the # / [[ pool, Ctrl+K's page branch) return the SAME top-12 order, the block surfaces (( picker and Ctrl+K blocks) both find all content blocks, truncation at the shared 100 window is identical and signalled only in Ctrl+K (has_more), and pools differ ONLY by design (( searches block TEXT, # / [[ search pages, Ctrl+K searches both). The character-swap ladder (1..3 chars, incl. DSL token literals like bare uppercase OR) is re-pinned at the public quick_switch/block_search_literal APIs, complementing the shipped fix's own token tests. failBefore=inferred: written after the shipped fix; the DSL rows' pre-fix failure mode is guaranteed by the shipped fix's own token tests, and the rest documents designed behavior."
  },
  {
   "id": "REG-DIRECT-RENAME-RETAINED-SHADOW-LIMIT-364",
   "title": "Direct Files page rename stays bounded on a 13,000-page graph",
   "family": "direct-files-page-mutation",
   "sources": {
    "issues": [
     364
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "rust-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::tests::rename_transaction_does_not_build_the_guarded_graph_index",
     "crates/tine-core/src/model.rs::tests::rename_transaction_inventory_still_refuses_physical_file_aliases",
     "crates/tine-core/src/model.rs::tests::rename_transaction_succeeds_on_thirteen_thousand_page_graph"
    ],
    "scenario": "direct-files.rename-large-graph-transaction",
    "failBefore": "proven"
   },
   "notes": "GH #364 reported that both title and page-menu rename failed intermittently on a roughly 13,000-page Windows Direct Files graph with `guarded graph-text identity construction failed: initial shadow peak build memory bound exceeded`. The rename transaction already held graph-text identity authority, completed one bounded no-follow inventory, rejected directory/file aliases and logical/twin destinations, locked every touched path, re-read exact bytes before each publication, and retained rollback copies. Its nested generic atomic-write and no-replace-move helpers nevertheless tried to construct a second whole-graph retained-shadow identity index. The causal fail-before injects the reporter's exact retained-shadow peak refusal and proves rename no longer consumes that builder. Transaction-local publication now repeats only exact-target, single-link, portable-path and no-clobber checks; it updates an already-warm optional index but never builds one. The hard-link witness preserves the physical-alias refusal, and the explicit release-gate probe creates 13,000 real page files, renames one page, rewrites its distant reference, preserves the file count and records elapsed time."
  },
  {
   "id": "REG-RELEASE-WINDOWS-X86-275",
   "title": "Release assembly carries an experimental 32-bit Windows lane without promising updater support",
   "family": "release-platform-matrix",
   "sources": {
    "issues": [
     275
    ],
    "commits": []
   },
   "platforms": [
    "windows"
   ],
   "layer": "release",
   "status": "covered",
   "coverage": {
    "tests": [
     "scripts/test-release-pipeline.mjs"
    ],
    "scenario": "release.windows-i686-pilot",
    "failBefore": "proven"
   },
   "notes": "The fail-before inventory had no i686 lane or assets. The release matrix now cross-builds MSVC i686, stages an x86 installer and portable ZIP, and deliberately leaves latest.json unchanged until a real reporter validates usefulness on old hardware."
  },
  {
   "id": "REG-DIAGNOSTIC-FLIGHT-RECORDER-343",
   "title": "Production diagnostics retain bounded content-free events and exact-build decoding evidence",
   "family": "diagnostic-observability",
   "sources": {
    "issues": [
     343
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "tauri-release",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/debug.rs",
     "scripts/test-release-pipeline.mjs"
    ],
    "scenario": "diagnostics.bounded-safe-recorder-and-symbols",
    "failBefore": "proven"
   },
   "notes": "The recorder writer accepts only fixed event names, catalogued IPC command names, enums, booleans, counts and durations; detailed debug strings never enter the report. Current/previous one-MiB segments, unclean-exit marker, private files and process lock are native-tested. Release tests pin line-level symbols, hidden maps copied outside dist, exact-SHA artifact naming and removal of maps from shipped assets."
  },
  {
   "id": "REG-OLD-WEBKIT-ENGINE-FLOOR-572",
   "title": "On a web engine older than Safari 15.4 (macOS before 12.3) Tine says so at startup and how to fix it, instead of opening half-working with a failed parser",
   "family": "startup-platform",
   "sources": {
    "issues": [
     572
    ],
    "commits": []
   },
   "platforms": [
    "macos",
    "ios"
   ],
   "layer": "frontend-browser",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/engineCheck.test.ts",
     "scripts/test-release-pipeline.mjs"
    ],
    "scenario": null,
    "failBefore": "proven"
   },
   "notes": "GH #572, jaemskyle: macOS Big Sur 11.7.8, 0.6.984 DMG. The system WKWebView was Safari-14-era: the lsdoc wasm failed to compile ('can't get 0th argument Type' -- the vendored module declares an externref parameter, the reference-types feature, Safari 15) and the frontend then threw on Array.prototype.at (Safari 15.4), so every page showed 'This page could not be displayed' and the block-renderer banner. Tine's declared floors were false: macOS had Tauri's default 10.13 and iOS was set to 14.0. Martin chose (2026-09-21) to require the Safari 15.4 engine with a clear message rather than support Safari 14: index.html runs an ES5 classic check (at, Object.hasOwn, structuredClone, and WebAssembly.validate of a one-type externref module) before the app module, renders the explanation and remedy (update Safari; iOS 15.4), and main.tsx stops and reveals the window on its flag. iOS minimumSystemVersion is 15.4. A source scan keeps post-15.4 built-ins out of src/. Verified in Chromium against the production build with Array.prototype.at deleted and with the reference-types probe failing: flag set, message shown, app not mounted; unmodified, the app mounts. Not verified on a real Safari 14 engine. Known limit, documented in the Guide: pdf.js needs Safari 17.4 (Promise.withResolvers, AbortSignal.any, class static blocks in the PdfViewer chunk only). Negative follow-up after v0.6.985 (jaemskyle, 2026-09-21): the Safari APP was 16.5.2 while Tine's web view still lacked Array.prototype.at -- on older macOS a Safari update installs a staged WebKit only Safari uses, so the remedy 'update Safari' was wrong. Martin approved (2026-09-24) a macOS floor instead: bundle.macOS.minimumSystemVersion 12.3 (the release that shipped the Safari 15.4 engine) in tauri.macos.conf.json, and the startup message and Guide now name macOS 12.3 and say updating the Safari app does not help. engineCheck.test.ts ties the message to the bundle floor; failed 5/9 on the old text/config, passes 9/9."
  },
  {
   "id": "REG-DIRECT-SAVE-FLAG-REFUSING-STORAGE-538",
   "title": "Creating, saving and renaming pages works on storage that refuses atomic no-replace renames",
   "family": "storage-durability",
   "sources": {
    "issues": [
     538
    ],
    "commits": []
   },
   "platforms": [
    "android",
    "linux",
    "macos",
    "ios"
   ],
   "layer": "rust-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model_tests.rs::gh538_flag_refusing_storage_creates_saves_and_renames_pages",
     "crates/tine-core/src/model_tests.rs::gh538_flag_refusing_storage_never_replaces_an_occupied_name"
    ],
    "scenario": null,
    "failBefore": "proven"
   },
   "notes": "GH #538 (sulisu, Android 12): every Direct Files save failed with renameat2(RENAME_NOREPLACE) 'Invalid argument (os error 22)'. Android 11-14 MediaProvider's FUSE daemon rejects any rename flag (AOSP FuseDaemon do_rename; NOREPLACE supported only from bff215d832, 2024), while a plain rename works; NFS behaves the same. Martin's decision B1 (2026-09-24): when the platform refuses the flag itself (EINVAL/ENOSYS/EOPNOTSUPP/ENOTSUP, unix only), check that the destination is absent and rename plainly; an occupied destination stays AlreadyExists. Applies to both graph-text primitives (rename_projection_noreplace, rename_graph_text_noreplace), so create, the save's retire and publish steps, and page rename/move all complete. Contract §2.10b records the trade: an external create in the microseconds between check and rename is replaced, on such storage only. Fail-before: both tests fail with the fallback disabled (the create path with the reporter's exact error). Real filesystem: on this box's NFS mount, 26 save/rename/create tests fail with os error 22 without the fallback and pass with it (evidence tine-agents/evidence/gh538-noreplace-fallback/). Not verified on an Android 11-14 device."
  },
  {
   "id": "REG-CONFIG-STRANDED-ON-FLAG-REFUSING-STORAGE-538",
   "title": "A settings write on storage that refuses atomic no-replace renames leaves config.edn in place, and a graph already hit by it opens again",
   "family": "storage-durability",
   "sources": {
    "issues": [
     538
    ],
    "commits": []
   },
   "platforms": [
    "android"
   ],
   "layer": "rust-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model_tests_availability.rs"
    ],
    "scenario": null,
    "failBefore": "proven"
   },
   "notes": "GH #538 (Android, 0.6.982): after a failed write, the graph never opened again with a bare 'unsafe graph layout: Invalid argument (os error 22)'. atomic_replace_expected (the config.edn writer) retired the live file with a plain rename, then published with renameat2(RENAME_NOREPLACE); on storage that answers the flag with EINVAL both the publish and the no-replace undo failed, the undo error was ignored, and config.edn stayed stranded under .config.edn.<pid>.<seq>.retired. The open-time restore used the same refused primitive, so every open failed. The Android flag refusal is recorded from Android CI run 32091898520; it is device-dependent and was not reproduced on the reporter's device. Fix: prove the no-replace move on the staged temp before retiring the live name (refusal leaves config.edn untouched, with a named error); restore a vacated name by plain rename only when the storage refuses the flag AND the target is absent; name the step and file on recovery errors. Tests gh538_* inject an EINVAL mover; 3 of 4 fail on the old logic (the fourth covers error wording). Not covered: Direct page saves on such storage still fail (named error), a separate decision."
  },
  {
   "id": "REG-PROJECTION-REBUILT-EVERY-LAUNCH-550",
   "title": "Reopening a graph whose search index is current does not rebuild the index because the Journals feed read pages before the warm",
   "family": "direct-files-performance",
   "sources": {
    "issues": [
     550
    ],
    "commits": []
   },
   "platforms": [
    "android",
    "linux",
    "windows",
    "macos",
    "ios"
   ],
   "layer": "rust-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/direct_projection_tests.rs"
    ],
    "scenario": null,
    "failBefore": "proven"
   },
   "notes": "GH #550 (Android, 764 pages, ~40 s open on every launch). The Journals feed loads its first days before the warm has validated the reopened projection; each load_page published a Replace delta, and the cold queue numbered those pages 0,1,2,... while the image already stored other pages at those positions. The delta turn failed with 'UNIQUE constraint failed: pages.position', the projection went stale, and the warm rebuilt all of it -- every launch. Where the positions happened to line up, the feed pages were still re-lowered for nothing. Fix: before an inventory seeds the session's page order a delta carries no position; the worker drops pre-seed deltas whose page the image holds at the same source revision, applies changed ones in place (storage keeps their stored position), and hands a page the image does not hold to the warm's fresh build. Tests: gh550_journal_feed_before_the_warm_keeps_a_clean_reopen_clean fails deterministically on the old code (projection failure on drain; 3 needless re-lowerings when positions line up); gh550_launch_reads_of_changed_and_new_pages_settle_without_failing covers the changed/new siblings. Real corpus (logseq-anonymized with journals renamed to dates ending yesterday, feed's 3 days loaded before the warm): old code 'projection is stale' + 1,078 lowerings on each relaunch, fixed 0. Not measured on the reporter's phone."
  },
  {
   "id": "REG-LAUNCH-JOURNAL-OPEN-WALKS-GRAPH-550",
   "title": "Opening today's journal at launch does not first enumerate every file in the graph",
   "family": "direct-files-performance",
   "sources": {
    "issues": [
     550
    ],
    "commits": []
   },
   "platforms": [
    "android",
    "linux",
    "windows",
    "macos",
    "ios"
   ],
   "layer": "rust-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model_tests_journal_lookup.rs",
     "src-tauri/src/backup.rs"
    ],
    "scenario": null,
    "failBefore": "proven"
   },
   "notes": "GH #550 launch-cost follow-up. get_page for a journal title went through find_entry, whose miss on a cold cache enumerated the whole graph inventory before loading one file (most of an 8 s get_page on the reporter's phone). load_named now resolves a journal title to its date-stem file (md/org variants) and loads it directly, answering only when exactly one date-stem file exists and the loaded page keys to the requested title; every other shape (title:: rename, md+org twins, title-named stray, absent day) falls back to the inventory path. gh550_todays_journal_opens_without_walking_the_graph fails on the old code (43 inventory visits for a 41-file graph; 0 with the fast path); gh550_journal_by_date_answers_what_the_inventory_lookup_answers checks the sibling shapes answer as the inventory lookup does. Same batch: the per-launch whole-graph backup no longer starts 1 s after open; it waits for the window's warm-done plus a 5 s quiet period (180 s deadline). That schedule is covered only at the policy level (gh550_launch_backup_waits_for_startup_to_go_idle); not measured on the reporter's phone."
  },
  {
   "id": "REG-JOURNALS-FEED-BLANK-ON-NON-PAGE-ENTRY-385",
   "title": "A non-page entry in journals/ or pages/ (symlink, unreadable sidecar, hidden hard link) no longer blanks the Journals view or refuses today's journal and renames",
   "family": "journals-feed",
   "sources": {
    "issues": [
     385
    ],
    "commits": []
   },
   "platforms": [
    "macos",
    "linux",
    "windows",
    "android",
    "ios"
   ],
   "layer": "rust-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model_tests_availability.rs",
     "src/activationNeighbors.test.ts"
    ],
    "scenario": null,
    "failBefore": "proven"
   },
   "notes": "GH #385 (macOS, iCloud Drive graph): Journals showed 'No journal entries found' while All pages worked. Chain: the feed prepends an empty today placeholder; its activate_absent_editor(Journal) resolves through the configured walk of journals/+pages/, which (unlike the graph-wide inventory) refused any symlink, opened every non-page file (an unreadable .DS_Store, attachment, or refused iCloud download fails the open), and refused any two files on one inode. Since v0.6.94 loadFeed aborted the whole feed on one refused page and restartJournalFeed returned null, so the generic empty message showed and the retry loop repeated the failing call (5 activate_absent_editor failures in the reporter's diagnostic). Fix: the configured walk skips symlinks, hidden entries and non-page files before opening them (the save capture already skips symlinks since GH #267; write targets are opened no-follow); two page files on one inode are still refused. loadFeed leaves out a page whose activation failed and installs the rest; unsaved work and stale requests still defer the feed atomically. The same walk backs page rename, merge, PDF and journal migration, which are fixed too. The exact entry on the reporter's graph is not known; the test covers Emacs lock symlink, page symlink, unreadable .DS_Store and attachment, hidden hard link, hidden unparseable page name, and an iCloud placeholder. Not verified on macOS/iCloud."
  },
  {
   "id": "REG-BLANK-GRAPH-ON-ONE-UNREADABLE-ENTRY-332",
   "title": "One entry the page scan cannot read (FIFO, unreadable folder or page, oddly named file) no longer leaves the graph with zero pages, every page opening blank",
   "family": "graph-open",
   "sources": {
    "issues": [
     332
    ],
    "commits": []
   },
   "platforms": [
    "windows",
    "linux",
    "android",
    "macos",
    "ios"
   ],
   "layer": "rust-core",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model_tests_availability.rs",
     "src-tauri/src/backend_command_parity.rs"
    ],
    "scenario": null,
    "failBefore": "proven"
   },
   "notes": "GH #332 (theaffman, Steve-21-bassman, dealingwith; Windows and Android, since after v0.6.5): graphs opened with every page blank. v0.6.90 replaced the tolerant list_md walk with the strict graph-wide inventory walk, which aborted on any entry it could not admit; the page cache was then installed with zero pages, list_pages returned [] and get_page returned 'absent', which the frontend renders as an empty new page (hence activate_absent_editor after every open in the reporters' diagnostics). The config (custom journals dir, dd-MM-yyyy) plays no part. Fix: the graph-wide READ walk skips an entry it cannot admit; an unreadable page file is recorded as a page index failure (source not complete), an unreadable folder or non-page entry is skipped without one. The configured walk (writes) and inventory limits stay fail-closed. Separately, list_sync_conflicts and list_vcs_marker_conflicts read every page file as sync commands on the main thread, freezing every command for 10-16 s at startup in the reporters' diagnostics; both are now async + spawn_blocking with a source guard. Test gh332_* reproduces FIFO, non-UTF-8 image name, unreadable folder, unreadable nested page and unreadable page: pre-fix list_pages is empty. Which entry each reporter has is not known. Not addressed: Windows FILE_ATTRIBUTE_REPARSE_POINT refusal of cloud-placeholder files (OneDrive etc.), unverified here and not indicated in the thread; the empty-looking UI does not yet name page-index failures."
  },
  {
   "id": "REG-JOURNAL-TEMPLATE-OVERWRITES-CHILD-TEXT-550",
   "title": "The default journal template is never re-applied over text the user typed into a child block of today's journal",
   "family": "journal-template",
   "sources": {
    "issues": [
     550
    ],
    "commits": []
   },
   "platforms": [
    "windows",
    "linux",
    "android",
    "macos",
    "ios"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/graph.test.tsx"
    ],
    "scenario": null,
    "failBefore": "proven"
   },
   "notes": "Found while tracing GH #550 (not the reporter's symptom). materializeJournalTemplate treated today's journal as empty when every TOP-LEVEL block was blank. The usual template shape leaves an empty parent with children, so text typed into a child was overwritten by the template on the next launch, focus, visibility or watcher refresh. The check now looks at the whole block tree. Reproduced by the #550 investigation with a backend probe; frontend test fails before, passes after."
  },
  {
   "id": "REG-JOURNAL-TEMPLATE-STARTUP-CONFLICT-CUSTOM-TITLE-550",
   "title": "With a custom journal title format, opening a graph no longer saves the journal template over an already-delivered today's journal (a spurious save conflict on every launch)",
   "family": "journal-template",
   "sources": {
    "issues": [
     550
    ],
    "commits": []
   },
   "platforms": [
    "windows",
    "linux",
    "android",
    "macos",
    "ios"
   ],
   "layer": "frontend",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/graph.test.tsx"
    ],
    "scenario": null,
    "failBefore": "proven"
   },
   "notes": "GH #550 diagnostics showed save_page failing with direct.save conflict.save_baseline_present about 15 s after every launch with no edit. Graph bind bumped the epoch (waking the Journals surface) before applyConfigDerivedState set the graph's journal title format, so the template lookup used the default 'MMM do, yyyy' title, found no page for a custom-format graph, and saved the template with no base revision; the backend's date fallback mapped it onto today's real file delivered by Syncthing, hence the conflict (or a silent create when the file was absent). The error was swallowed, so no data was lost. The title format is now set before setGraphMeta/bumpGraphEpoch at bind and on live config change. The reporter's config lines were not confirmed. The 40 s open itself is not addressed here."
  },
  {
   "id": "REG-CLOSE-DISCARD-DIAGNOSTIC-540",
   "title": "A close that discarded unsaved drafts is visible in the diagnostic report instead of reading as an ordinary clean exit",
   "family": "diagnostic-observability",
   "sources": {
    "issues": [
     540
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "unit",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/safeClose.test.ts::records a discard only when the user accepts losing work, and a failing record never blocks the close (GH #540)",
     "src-tauri/src/debug.rs::a_close_that_discards_drafts_records_only_a_fixed_reason_and_a_count"
    ],
    "scenario": null,
    "failBefore": "proven"
   },
   "notes": "GH #540, DanTremonti: a session in which the user closed through the unsaved-changes warning and accepted discarding was still recorded as runtime.clean_shutdown, so the report could not tell it from a normal exit. The safe-close coordinator now records runtime.close_discarded_unsaved (fixed reason token failed|still-saving and a page count; never titles or paths) only after the user confirms discarding, bounded to 1 s and never blocking the close. The coordinator test fails on the pre-fix safeClose.ts. Layer: coordinator unit + Rust field filter; the Tauri invoke wiring in App.tsx is typechecked, not driven natively."
  },
  {
   "id": "REG-CONFIG-LIVE-RELOAD",
   "title": "An outside change to logseq/config.edn is picked up while Tine is running, instead of being ignored until the next graph open",
   "family": "graph-configuration",
   "sources": {
    "issues": [
     102
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "rust-unit",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/watcher.rs",
     "crates/tine-core/src/config.rs",
     "src/graphConfigChange.test.ts",
     "src/favoritesArrangementContract.test.ts",
     "docs/contracts/config-live-reload.md"
    ],
    "scenario": "config.live-reload",
    "failBefore": "proven"
   },
   "notes": "Reported by Martin 2026-08-25 while reviewing the favorites work: config.edn was read once per graph open, so a settings change made in Logseq, a text editor, or delivered by Syncthing was invisible for the rest of the session AND the next settings write from Tine was computed against the stale copy. Root cause: the OS watcher already subscribed recursively to the graph root and already classified logseq/config.edn as GraphTextExactFeedPathClass::Configuration, but incremental_page_paths discarded the path because .edn is not an eligible page extension, so nothing ever re-read the file. Fixed by queueing configuration paths separately (Pending::config_paths) and refreshing the graph when the on-disk bytes differ from those the running Graph was opened with (Graph::open_config_description vs model::config_file_description). That byte gate is mandatory, not an optimization: a refresh discards the whole page cache, and Logseq rewrites config.edn on many ordinary UI actions. The watcher never blocks on the storage transition lane (RefreshLaneWait::TryOnce, deferred and retried) because blocking would stall reconciliation for every graph behind one graph's load or promotion. Configuration is a plain filesystem file under BOTH storage engines - never in GraphTextScope, never in the oplog - so there is one mechanism, not a Direct/managed fork. Fail-before: the pre-fix Pending queue drops an exact config.edn event entirely (incremental_page_paths returns Some(vec![])), which is what the two new watcher tests assert against; before the change config_paths did not exist and the queue was empty for every writer shape. NOT CLOSED: :favorites is still written wholesale from the frontend's snapshot, so a favorite added in Logseq inside the reload window can be overwritten by the next star toggled in Tine; that needs a three-way merge in set_favorites' own atomic_update closure. ALSO FIXED HERE: src-tauri/src/sync_runtime.rs constructed a GraphMeta literal in a test and was never updated when favorites_page was added in b4847dd7, so `cargo test -p tine --lib` did not compile on master. The npm/tsc gates do not build src-tauri tests and the Rust suite compared by name was tine-core, so nothing caught it. SELF-WRITE: a settings write leaves the running graph's parsed view stale (it always has; set_favorites never refreshed it), so the byte gate alone would read every star toggled in the sidebar as an outside change. Graph::write_config is now the single funnel all 13 setters publish through and records what it wrote; the watcher accepts either the open-time digest or that marker. A managed slot retains no Graph to ask, so it re-reads unconditionally and the FRONTEND declines to re-seed favorites it is already showing - fail-before proven by forcing that guard false (the test fails). Also classified set_favorites_page in MANAGED_COMMAND_SURFACE: a third defect from b4847dd7 whose own guard test could not run because the crate did not compile."
  },
  {
   "id": "REG-DUP4-RECENCY-CONFIGURED-FORMAT",
   "title": "(sort-by modified) ranks journals correctly on graphs with a custom :journal/page-title-format on every producer (Direct sparse index, managed sparse index, managed application queries), instead of ranking all journals last",
   "family": "query-recency",
   "sources": {
    "issues": [],
    "commits": [],
    "internal": "duplication-audit-2026-08-25 DUP-4"
   },
   "platforms": [
    "all"
   ],
   "layer": "rust-unit",
   "status": "needs-revalidation",
   "coverage": {
    "tests": [],
    "scenario": null,
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-DUP6-BACKLINK-TRUNCATION-FLAG",
   "title": "The Direct backlink-filter context reports truncated=true when a page-property root entry hits its text/facet budget, matching the managed path for identical content",
   "family": "backlink-filter",
   "sources": {
    "issues": [],
    "commits": [],
    "internal": "duplication-audit-2026-08-25 DUP-6"
   },
   "platforms": [
    "all"
   ],
   "layer": "rust-unit",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query.rs"
    ],
    "scenario": null,
    "failBefore": "source-traced (flag was provably never set on that branch; both audits confirmed independently)"
   }
  },
  {
   "id": "REG-DUP3-CONFIG-NESTED-SHADOW",
   "title": "config.edn setters edit only the real top-level key: a same-named keyword nested inside another map is never spliced over (which corrupted the nested map) and survives byte-for-byte",
   "family": "graph-configuration",
   "sources": {
    "issues": [],
    "commits": [],
    "internal": "duplication-audit-2026-08-25 DUP-3"
   },
   "platforms": [
    "all"
   ],
   "layer": "rust-unit",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/config.rs::tests::setters_edit_the_top_level_key_never_a_nested_shadow"
    ],
    "scenario": null,
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-DUP5-ATOMIC-WRITE-POLICY",
   "title": "Every Tine writer that renames into a watched or durable location uses the audited publish shape: unique create_new temps, a recognized temp name (including the backup-restore shape), and dir-fsync errors reported rather than swallowed",
   "family": "atomic-write",
   "sources": {
    "issues": [],
    "commits": [],
    "internal": "duplication-audit-2026-08-25 DUP-5"
   },
   "platforms": [
    "all"
   ],
   "layer": "rust-unit",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/watcher/tests.rs::recognizes_every_tine_temp_shape_that_lands_in_the_live_graph"
    ],
    "scenario": null,
    "failBefore": "proven"
   }
  },
  {
   "id": "REG-JOURNAL-FEED-READ-ERROR-385",
   "title": "A journal feed read failure is not presented as an empty graph",
   "family": "journal-feed",
   "sources": {
    "issues": [
     385
    ],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend-native-boundary",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/components/Page.test.tsx::Journals feed generation lifecycle::surfaces an initial feed read failure instead of claiming the graph has no journals",
     "src/components/Page.test.tsx::Journals feed generation lifecycle::retains the old feed after a current-generation error and retries it"
    ],
    "failBefore": "The backend rejection was caught and converted into deferred retry state; with no prior feed, PageView remained loading or rendered the ordinary no-journals guidance instead of the causal error."
   },
   "notes": "The reporter's privacy-safe graph verification listed 748 local dated Markdown/Org journals, while the diagnostic recorded journal_feed_page as failed. This repair distinguishes a failed read from a successful empty inventory without weakening background refresh recovery. The native reason for the reporter's iCloud-specific failure remains reporter-verification evidence, not a claimed root cause."
  },
  {
   "id": "REG-DIRECT-FILES-WINDOWS-DURABLE-NAME-PUBLICATION-001",
   "title": "A successful Direct Files save durably publishes the sole-authority Markdown or Org name on Windows",
   "family": "direct-files-data-safety",
   "sources": {"issues": [], "commits": [], "internal": "MS-19 final data-safety audit"},
   "platforms": ["windows", "linux", "macos", "android"],
   "layer": "rust-core-storage",
   "status": "needs-revalidation",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::tests::direct_files_graph_text_publication_uses_the_graph_tree_noreplace_rename",
     "crates/tine-core/src/model.rs::tests::a_same_bytes_external_replace_does_not_strand_the_editor",
     "crates/tine-core/src/model.rs::tests::a_changed_bytes_external_replace_still_conflicts",
     "crates/tine-core/src/model.rs::tests::checked_open_retries_editor_retired_cleanup_without_restoring_it",
     "crates/tine-core/src/model.rs::tests::editor_retired_cleanup_is_never_document_authority"
    ],
    "scenario": "direct-files.windows-write-through-create-replace-retire-restore",
    "failBefore": "proven from the literal Windows path: Direct Files used raw NtSetInformationFile name changes and the following directory sync was a successful no-op"
   },
   "notes": "Direct Files remains independent of the managed journal/SQLite/oplog. Its create, expected-owner retirement, staged publication, recovery restore and recovery set-aside carry an exact-byte protocol over the graph tree's own no-clobber rename (on Windows FileRenameInformation with ReplaceIfExists=false plus the required directory barrier); GH #466 moved them off tine-storage's DurableDirectoryPublication, whose Android arm is hard-link based and refused by shared storage, so the Windows write-through evidence below predates that move and this row is needs-revalidation until the Windows physical suite runs on the current primitive. The v0.9.2 Windows physical suite exercises MoveFileExW write-through behavior; same-inode interrupted move recovery on macOS/Android is idempotent while foreign same-bytes targets fail closed. A post-publication .editor-retired entry is cleanup-only, never document authority; the bounded checked-open walk retries an interrupted or failed unlink without restoring its displaced bytes."
  },
  {
   "id": "REG-THEME-CHECKER-PROVENANCE-410",
   "title": "The registry theme checker certifies a ported theme that Tine then refuses to install",
   "family": "theme-package-intake",
   "sources": {"issues": [410], "commits": []},
   "platforms": ["all"],
   "layer": "node-cli-and-frontend-parser",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/themes/themeCheckerCli.test.ts::never certifies a port record the app would refuse to install",
     "src/themes/themeCheckerCli.test.ts::keeps the published schema, the checker and the app on one vocabulary"
    ],
    "scenario": "themes.ported-theme-intake",
    "failBefore": "proven"
   },
   "notes": "A theme author told by `tine-theme.mjs check` that a package passes must not then have it rejected on install. The checker asked only for portedFrom.source, portedFrom.revision and a non-empty authors array, so an unsupported ecosystem, a missing relationship, name or license, an unknown provenance field, an over-long author list and non-text values all cleared intake and were refused by parseThemeManifest in the app. The checker now mirrors that parser field for field. The contract is one-directional and encoded as such: the checker may refuse more than the app (the SPDX allow-list, required screenshots and the dotted id are registry intake policy), never less. plugin-sdk/schema/theme.schema.json and docs/themes/README.md were already correct; the CLI was the single drifted authority, and the new test pins all three together."
  },
  {
   "id": "REG-GRAPHTEXT-GATE-ORDER-001",
   "title": "Annotating a PDF while the same hls__ page is being saved no longer wedges every graph-text write in the process",
   "family": "data-safety",
   "sources": {
    "issues": [],
    "commits": []
   },
   "platforms": [
    "all"
   ],
   "layer": "core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/tests/graph_text_gate_order.rs::saving_and_annotating_the_same_hls_page_never_deadlocks",
     "crates/tine-core/src/model.rs::tests::graph_text_writers_take_the_identity_gate_before_any_page_lock"
    ],
    "scenario": "graph-text.identity-gate-before-page-lock",
    "failBefore": "proven"
   },
   "notes": "Fail-before on 3a123bc1 in the RELEASE profile (the shipped configuration): saving and annotating the same hls__ page hung both writers permanently, 0 of 2 finished in 60s; the same tree passes in 0.51s after the fix. The saver holds the graph-global graph-text identity gate while it waits for the page lock the annotator holds, so no later graph-text write in the process can proceed either. Debug builds hit debug_assert at model.rs:1542 instead and never reached the hang, which is why it survived. Six writers took the two locks in the wrong order: resolve_vcs_marker_conflict, resolve_durable_live_save_conflict, resolve_sync_conflict, open_pdf, write_highlights and confirm_removed_page_projection_with_attempts. All six now take the identity gate before any page lock; the order is proved statically over model.rs's call graph; and the epoch read refuses instead of silently reading without authority."
  },
  {
   "id": "REG-HARVEST-H-THEME-RMW-001",
   "title": "Two concurrent theme changes do not lose one another or leave storage different from the UI",
   "family": "theme-package-persistence",
   "sources": {"issues": [], "commits": [], "internal": "Harvest H"},
   "platforms": ["all"],
   "layer": "frontend-persistence",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/themes/manager.test.ts::serializes overlapping mutations of the shared package array"
    ],
    "scenario": "themes.concurrent-shared-array-read-modify-write",
    "failBefore": "proven: two deliberately overlapped installs completed with only the first theme in durable storage and the installedThemes signal"
   },
   "notes": "All installs and removals rewrite theme.packages.v1, so one module-wide promise chain serializes them. Each queued mutation recomputes from installedThemes inside the chain, persists that exact array, then publishes the same array to the signal."
  },
  {
   "id": "REG-HARVEST-B5P-PLUGIN-PACKAGE-DURABILITY-001",
   "title": "A crash during plugin install or uninstall cannot wedge that version until manual filesystem surgery",
   "family": "plugin-package-durability-and-recovery",
   "sources": {
    "issues": [],
    "commits": [],
    "internal": "Harvest B5p"
   },
   "platforms": ["all", "windows", "android", "ios"],
   "layer": "tauri-native-and-tine-storage",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/plugins.rs::tests::reopen_reclaims_transient_and_wedged_packages_before_retry",
     "src-tauri/src/plugins.rs::tests::concurrent_different_installs_leave_one_complete_immutable_winner",
     "src-tauri/src/plugins.rs::tests::settings_clear_cut_leaves_a_recoverable_package_then_retry_finishes",
     "src-tauri/src/plugins.rs::tests::production_plugin_writes_stay_on_named_audited_paths"
    ],
    "scenario": "APP-REF-PLUGIN-IMMUTABLE-COLLISION plus crash-recovered staging and retirement",
    "failBefore": "A half-package with plugin.wasm but no manifest was simultaneously not reinstallable and not uninstallable; the production source guard also found bare write, rename, recursive-delete, and directory-delete operations."
   },
   "notes": "tine-storage v0.12.0 owns the staged-directory protocol: exact file barriers, a five-target native no-clobber directory move, parent barriers, retire-then-reclaim, and reopen cleanup for .install-/.retired-/incomplete shapes. Tauri keeps manifest and symlink policy, uses the audited settings replacement before retirement, and preserves same-bytes idempotence while refusing different bytes for one immutable id/version. No plugin capability or guest/native authority changes."
  },
  {
   "id": "REG-HARVEST-H2-PDF-AREA-ROLLBACK-001",
   "title": "A failed PDF highlight sidecar write moves its already-written nested area crop to recoverable asset trash",
   "family": "pdf-area-highlight-data-safety",
   "sources": {"issues": [], "commits": [], "internal": "Harvest H2 item 2a"},
   "platforms": ["all"],
   "layer": "frontend-and-core-native",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/components/PdfViewer.test.tsx::rolls a nested area crop back to trash when the sidecar write fails",
     "src-tauri/src/commands.rs::application_page_authority_tests::pdf_area_rollback_moves_the_real_nested_crop_to_typed_asset_trash"
    ],
    "scenario": "pdf-area.crop-written-sidecar-refused-recoverable-rollback",
    "failBefore": "Before Harvest H2 the PNG was written before the sidecar transaction, but the failure path only reverted the optimistic UI highlight and left the nested crop orphaned."
   },
   "notes": "The rollback command accepts only the PDF/page/highlight-id/stamp tuple, derives the same nested OG-compatible source path as the writer, and moves that exact crop into typed asset trash. It is invoked only when highlight persistence returns false before sidecar commit; later reload failures do not erase a committed crop."
  },
  {
   "id": "REG-HARVEST-H2-TYPED-SAVE-CONFLICT-001",
   "title": "Conflict resolution branches on an exact typed save-conflict tag instead of arbitrary error prose",
   "family": "save-conflict-classification",
   "sources": {"issues": [], "commits": [], "internal": "Harvest H2 E-1"},
   "platforms": ["all"],
   "layer": "frontend-tauri-boundary",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/components/ConflictResolution.test.tsx::routes ordinary prose containing conflict through the generic failure path",
     "src/components/ConflictResolution.test.tsx::has no message-sniffing conflict branch in the production component",
     "src/persistence.test.ts::tags only the complete Direct Files conflict payload at the backend boundary",
     "src/persistence.test.ts::recognizes only bounded content-conflict contracts"
    ],
    "scenario": "save-conflict.exact-wire-tag-no-message-sniff",
    "failBefore": "The conflict resolver used String(error).includes(\"conflict\"), so unrelated prose containing that word was routed into the stale-file re-observation path."
   },
   "notes": "The Tauri save_page boundary recognizes only the complete conflict or conflict:<epoch> wire shape and raises SaveConflictError. Persistence and the resolver consume its save-conflict tag; managed actor refusal remains on its separate exact reason-code envelope."
  },
  {
   "id": "REG-W3-B4-DIRECT-SHARED-QUERY-READS-001",
   "title": "Direct Files property facets and PageRef simple queries use the generation-bound shared SQLite read",
   "family": "waited-path-query-cost",
   "sources": {"issues": [], "commits": [], "internal": "Harvest Wave 3 B4"},
   "platforms": ["all"],
   "layer": "rust-core-storage",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/direct_projection_tests.rs::b4_page_ref_and_property_facets_record_indexed_reads",
     "crates/tine-core/src/direct_projection_tests.rs::b4_corpus_page_ref_and_facets_match_oracle_with_route_evidence",
     "crates/tine-core/tests/query_lowering_guards.rs::simple_query_read_family_census_is_exact"
    ],
    "scenario": "direct-query.shared-facet-and-page-ref-candidate-read",
    "failBefore": "b4-indexed-routes-fail-before.log: PageRef and property-facet calls left the Direct projection indexed-read counter unchanged."
   },
   "notes": "One source-to-read-family lowering now serves the existing Managed caller and Direct PageRef narrowing. Exact parser evaluation remains authoritative; stale or unavailable projections count and use the full parser fallback. Both property-facet consumers stream the existing property_facet_rows_after family. No schema, index, or memo was added."
  },
  {
   "id": "REG-GH-466-ANDROID-DIRECT-SAVE-NOREPLACE-001",
   "title": "Editing a page on Android with Direct Files saves normally instead of failing with a permission-denied error",
   "family": "direct-files-graph-text-publication",
   "sources": {
    "issues": [
     466
    ],
    "commits": [],
    "internal": "v0.6.981 regression introduced by 65258e7d (Direct Files name transitions moved onto tine-storage's hard-link-based Android move)"
   },
   "platforms": ["android"],
   "layer": "rust-core-direct-files-publication",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model.rs::direct_files_graph_text_publication_uses_the_graph_tree_noreplace_rename",
     "crates/tine-core/src/model.rs::graph_text_exact_move_publishes_expected_bytes_and_refuses_a_replaced_source"
    ],
    "scenario": "android.direct-files.edit-existing-page-and-create-page-saves",
    "failBefore": "Structural on the host: the source-scan guard fails on the v0.6.981 code, which routes every Direct Files create, retirement, publication and restore through tine-storage's DurableDirectoryPublication::move_exact_no_replace; its Android arm is hard-link-then-unlink, Android shared storage (FUSE) refuses hard links, and the raw errno surfaced as 'unknown: Permission denied (os error 13)'. Reporter receipt on GH #466: 0.6.98 saves, 0.6.981 fails on every save, Android 16, Direct Files. The host has no Android runtime harness; same-platform confirmation is pending the reporter on the next release, so platform confidence is tentative."
   },
   "notes": "Blocking core operation. Fix carries the exact-byte move protocol over the graph tree's own no-clobber rename (renameat2(RENAME_NOREPLACE) through the raw syscall on Linux/Android, renameatx_np on Apple, FileRenameInformation on Windows: the primitive 0.6.98 shipped on every target); tine-storage's typed boundary stays for app-private authorities where hard links exist. A refused call now names itself in the receipt instead of a bare errno."
  },
  {
   "id": "REG-W4-E2-COMMAND-ERROR-PHASE-A-001",
   "title": "Phase-A backend refusals retain their typed producer while preserving the legacy rejection string",
   "family": "typed-error-boundary",
   "sources": {"issues": [], "commits": [], "internal": "Harvest W4-E2; invariants I-9, I-11, I-12"},
   "platforms": ["linux", "windows", "macos", "android", "ios"],
   "layer": "rust-tauri-frontend-boundary",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/command_error.rs::tests::phase_a_production_wire_matches_legacy",
     "src-tauri/src/backend_command_parity.rs::tests::phase_a_command_error_manifest_is_exact_for_every_target",
     "src/backendCallFunnel.test.ts",
     "src/typedErrorRatchet.test.ts"
    ],
    "scenario": "backend-errors.phase-a-command-error-keeps-kind-and-wire",
    "failBefore": "Before W4-E2 every fallible phase-A command returned String, so typed I/O, worker, core, and DirectSaveError sources were flattened before the Tauri boundary and a blanket Prose wrapper could pass every wire-only check."
   },
   "notes": "CommandError has one custom string serializer, no From<String>, producer-coupled family goldens, and a per-target registration guard; E2b owns the receipt-listed command remainder."
  },
  {
   "id": "REG-W4-E2B-COMMAND-ERROR-PHASE-B-001",
   "title": "All native backend refusals retain their production family while preserving the legacy rejection string",
   "family": "typed-error-boundary",
   "sources": {"issues": [], "commits": [], "internal": "Harvest W4-E2b; invariants I-9, I-11, I-12"},
   "platforms": ["linux", "windows", "macos", "android", "ios"],
   "layer": "rust-tauri-frontend-boundary",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/command_error.rs::tests::phase_b_production_wire_matches_legacy",
     "src-tauri/src/backend_command_parity.rs::tests::phase_b_command_error_manifest_is_exact_for_every_target",
     "src/backendCallFunnel.test.ts",
     "src/typedErrorRatchet.test.ts"
    ],
    "scenario": "backend-errors.phase-b-command-error-keeps-kind-and-wire",
    "failBefore": "proven — on post-E2 commit 0b5ebd899 the production guard failed on live stringify bridges and the structural guard named every remaining phase-B String-result file"
   },
   "notes": "All registered fallible commands and src-tauri helpers share CommandError; JSON, durability, worker, plugin, clipboard, platform, graph-verification and managed-runtime producers remain distinct while Serialize emits the unchanged string rejection."
  },
  {
   "id": "REG-W4-E4-DIRECT-SAVE-TYPED-CODE-001",
   "title": "Direct save failures retain the producer code when page text resembles a conflict",
   "family": "direct-save-typed-errors",
   "sources": {"issues": [], "commits": [], "internal": "Harvest W4-E4"},
   "platforms": ["all"],
   "layer": "rust-core-tauri-boundary",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/model_tests.rs::direct_save_failure_code_does_not_inherit_conflict_from_page_text",
     "crates/tine-core/src/model_tests.rs::direct_save_conflict_sites_produce_their_own_codes",
     "crates/tine-core/src/model_tests.rs::direct_save_precheck_helpers_produce_their_own_codes",
     "crates/tine-core/tests/direct_save_code_producers.rs::every_direct_save_failure_code_has_a_production_producer",
     "src-tauri/src/commands.rs::direct_save_error_tests"
    ],
    "scenario": "direct-save.page-text-cannot-mint-conflict",
    "failBefore": "An unrelated error whose page path contained the pinned-owner sentence classified as conflict.pinned_owner."
   },
   "notes": "Exact safety: DirectSaveFailureCode and the conflict epoch cross the command boundary as tagged JSON; error display text is never parsed for control flow."
  },
  {
   "id": "REG-FLAKE-FLIGHT-RECORDER-LOCK-001",
   "title": "The flight recorder releases its single-writer lock when it is dropped, not when the last forked copy of the descriptor closes",
   "family": "diagnostics-process-lifecycle",
   "sources": {"issues": [], "commits": [], "internal": "Flaky first-run census 2026-09-03, finding 2"},
   "platforms": ["all"],
   "layer": "rust-tauri-process-lifecycle",
   "status": "covered",
   "coverage": {
    "tests": [
     "src-tauri/src/debug.rs::tests::a_recorder_releases_its_lock_even_while_a_duplicated_descriptor_lives"
    ],
    "scenario": "diagnostics.flight-recorder-lock-release",
    "failBefore": "proven \u2014 with the Drop impl removed the new test fails with the exact signature the census recorded from the real flakes: Os { code: 11, kind: WouldBlock, message: \"Resource temporarily unavailable\" }"
   },
   "notes": "FlightRecorder held its process.lock in a `_process_lock: File` field with no Drop and no unlock, so release was a side effect of closing that descriptor. An flock belongs to the open file description, not to the descriptor: every process spawn duplicates the description into a child that holds it until exec, so a recorder dropped inside that window left the lock held and the next open on the same directory got EAGAIN. Observed twice in campaign evidence (wave3 I5, wave4 G1) as a first-run failure of the two session-cleanliness tests that then passed on rerun \u2014 four extra `cargo test -p tine` invocations in the G1 lane alone, on the gate every lane runs. Drop now calls unlock, which acts on the description and so releases every copy at once."
  },
  {
   "id": "REG-W4-B4B-DIRECT-INDEXED-QUERY-ROUTING-001",
   "title": "Direct Files queries use SQLite result reads even for unselective predicates",
   "family": "waited-path-query-cost",
   "sources": {
    "issues": [],
    "commits": [],
    "internal": "Harvest W4-B4b"
   },
   "platforms": [
    "all"
   ],
   "layer": "rust-core-storage",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/direct_projection_tests.rs::b4_page_ref_and_property_facets_record_indexed_reads",
     "crates/tine-core/src/direct_projection_tests.rs::an_unselective_shape_answers_through_the_statement_without_a_candidate_superset",
     "crates/tine-core/src/direct_projection_tests.rs::storage_contract_names_the_generation_bound_cutover",
     "crates/tine-core/tests/retirement_candidates.rs::retirement_candidate_markers_are_pinned"
    ],
    "scenario": "direct-query.sqlite-selection-regardless-of-selectivity",
    "failBefore": "proven — tine-agents/evidence/wave4/b4b-pass2/. Routing, checked at 9bad907f: every newly routed shape (non-sparse Task, BlockProperty, PageProperty, Page, Namespace, Journal, mixed And, complete Or) left the candidate-read counter unchanged and entered the full-graph evaluator. Hatch, checked at 9bad907f with the cutoff neutralised (hatch-fail-before.log): the unselective plan completed a candidate query instead of abandoning — `an abandoned plan must complete no candidate query, left: 2, right: 1`."
   },
   "notes": "Historical Harvest evidence described candidate-page narrowing and a selectivity escape hatch to traversal. The database-owned result campaign supersedes that policy: the current unselective-shape regression requires statement execution without a candidate superset, and selectivity never selects a different evaluator. The historical fail-before evidence remains provenance for the old packet, not a requirement to retain the escape hatch. Remaining Direct readiness/recovery traversal is separately scheduled for RET2 retirement; this row does not claim it is already removed."
  },
  {
   "id": "REG-W4-I5B-DEBUG-FLAG-FRONT-DOOR-001",
   "title": "One producer answers \"are debug diagnostics on\", and the fatal app-data refusal names its error kind",
   "family": "diagnostics-privacy",
   "sources": {
    "issues": [],
    "commits": [],
    "internal": "Harvest Wave 4 I5b; invariants I-12, I-9, I-5, I-11"
   },
   "platforms": [
    "all"
   ],
   "layer": "rust-core-and-tauri-diagnostics",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/tests/content_out_of_logs.rs::exactly_one_function_reads_the_debug_diagnostics_flag",
     "src-tauri/src/data_home.rs::tests::fatal_data_home_message_keeps_error_kind_without_prose_or_path"
    ],
    "scenario": "diagnostics.one-debug-flag-and-identifiable-fatal-refusal",
    "failBefore": "i12-front-door-fail-before.log, flag-fold-fail-before.log and data-home-kind-fail-before.log at 20bb15de"
   },
   "notes": "Four production readers parsed TINE_DEBUG/--debug independently (src-tauri/src/debug.rs, crates/tine-core/src/sync_runtime.rs and twice in crates/tine-core/src/oplog/import.rs), so the host process could not steer core diagnostics and the answer could drift per crate. Separately, the always-on fatal 'nowhere to keep its application data' line carried no error kind: the only diagnosable detail sat on the debug-only channel behind a process::exit the user cannot get past."
  },
  {
   "id": "REG-P0-QUERY-UNKNOWN-HEAD-001",
   "title": "An unrecognised query head silently truncated the query instead of refusing it",
   "family": "query-engine",
   "sources": {"issues": [], "commits": [], "internal": "2026-09 query-engine campaign, packet P0-rust Wave A; SPEC 4.1; invariants I-12, I-22"},
   "platforms": ["all"],
   "layer": "rust-core-query-parser",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query/og.rs::tests::an_unknown_head_keeps_the_rest_of_the_query_and_reports_itself",
     "crates/tine-core/src/query/og.rs::tests::an_unknown_head_alone_is_raw_and_spanned",
     "crates/tine-core/src/query/conformance.rs::an_unknown_head_returns_nothing_rather_than_a_shorter_query"
    ],
    "scenario": "query-engine.unknown-head-refuses-instead-of-narrowing",
    "failBefore": "tine-agents/evidence/qe/p0-rust/wave-b/unknown-head-fail-before.log at 2e41f52e89d93dacd7310130b65e96daed869e32"
   },
   "notes": "OG's build-query returns nil for an unrecognised head, and Tine's parser propagated that as \"the parse failed from here\": (and (task TODO) (frobnicate x)) ran as (task TODO) and returned rows for a query the author never wrote. Blocking for core operation -- the answer was wrong rather than missing, and nothing on screen said so. The unknown head is now a Filter::Raw leaf carrying its source span plus an UnknownHead diagnostic; the rest of the tree still parses, the query is invalid, and it returns no rows."
  },
  {
   "id": "REG-P0-QUERY-ALL-PAGE-TAGS-001",
   "title": "(all-page-tags) was not a head Tine knew, so a graph using it returned nothing",
   "family": "query-engine",
   "sources": {"issues": [], "commits": [], "internal": "2026-09 query-engine campaign, packet P0-rust Wave A; SPEC 4.1 and 3.2; decision D-9"},
   "platforms": ["all"],
   "layer": "rust-core-query-parser",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query/og.rs::tests::all_page_tags_is_the_pages_tags_property_with_at_least_one_atom",
     "crates/tine-core/src/query/og.rs::tests::all_page_tags_takes_no_arguments",
     "crates/tine-core/src/query/conformance.rs::all_page_tags_selects_every_page_carrying_a_tag"
    ],
    "scenario": "query-engine.all-page-tags-selects-tagged-pages",
    "failBefore": "tine-agents/evidence/qe/p0-rust/wave-b/all-page-tags-fail-before.log at 2e41f52e89d93dacd7310130b65e96daed869e32"
   },
   "notes": "Interoperability: (all-page-tags) is in OG's build-query head set (build-all-page-tags), so a graph authored in Logseq can carry it. Tine treated it as an unknown head, which -- before REG-P0-QUERY-UNKNOWN-HEAD-001 -- also truncated whatever it was conjoined with. Transcribed from OG as the page's tags property with at least one atom."
  },
  {
   "id": "REG-P0-EMPTY-PROPERTY-VALUE-BORROW-001",
   "title": "A bare key:: line panicked the reference-evidence reader with a subtraction overflow",
   "family": "parser-integration",
   "sources": {"issues": [], "commits": [], "internal": "2026-09 query-engine campaign, packet P0-rust Wave A; found while building the SPEC 3.4 conformance fixture"},
   "platforms": ["all"],
   "layer": "rust-core-parser-recognizer",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/property_line.rs::tests::both_halves_are_borrowed_from_the_line_even_when_the_value_is_empty",
     "crates/tine-core/src/query/conformance.rs::property_leaves_are_two_valued_over_atoms"
    ],
    "scenario": "parser.empty-property-value-borrows-from-its-line",
    "failBefore": "tine-agents/evidence/qe/p0-rust/wave-b/empty-property-value-fail-before.log at 2e41f52e89d93dacd7310130b65e96daed869e32"
   },
   "notes": "parse_property_line documents that it returns borrowed slices, and reference_evidence recovers each half's source offset by subtracting the line's base pointer. For a bare key:: with no trailing space the empty value was the static \"\" literal instead, so the subtraction underflowed and panicked in debug builds. A size:: line with no value is legal Logseq and is exactly the SPEC 3.3 \"present key, zero atoms\" case, so the crash was reachable from an ordinary graph. The empty value now borrows the tail of its own line."
  },
  {
   "id": "REG-CONCORD-LEDGER-DANGLING-ENTRY-001",
   "title": "A concord-ledger index or pin entry survived every prune once its blob was removed from outside the ledger",
   "family": "concord-base-ledger",
   "sources": {"issues": [], "commits": [], "internal": "GH #411 residue, response sweep 2026-09-05"},
   "platforms": ["all"],
   "layer": "tine-core-concord-ledger",
   "status": "covered",
   "coverage": {
    "tests": ["crates/tine-core/src/concord_ledger.rs::prune_reclaims_index_and_pin_entries_whose_blob_vanished"],
    "scenario": "concord-ledger.prune-reclaims-dangling-entries",
    "failBefore": "cargo test -p tine-core --lib concord_ledger::tests::prune_reclaims with the two new match arms reverted: dangling_entries_removed 0, expected 3"
   },
   "notes": "prune() collected the hash named by each index and pin file and deleted only blobs nothing referenced, plus files that failed to parse. An entry whose blob had been removed from outside the ledger -- antivirus quarantine (the GH #411 reporter's case), a disk cleaner, a partial restore -- still parsed, so it was kept and its hash was even added to the referenced set. Every lookup through it already answered None, because base() verifies the blob's content hash, so no user-visible behaviour was wrong; the entry was simply dead metadata that accumulated and was re-read by every later prune. prune() now also removes index and pin files naming an absent blob. This is hygiene only: the ledger is disposable state that may never warn, refuse, or report a missing blob, so no diagnostic was added."
  },
  {
   "id": "REG-P0-QUERY-MACRO-UNREADABLE-001",
   "title": "A saved query could be written as text the document parser will not read back",
   "family": "query-engine",
   "sources": {"issues": [], "commits": [], "internal": "2026-09 query-engine campaign, packet P0-rust Wave C; SPEC 4.3.1 and 4.3 (R1); invariants I-4, I-12"},
   "platforms": ["all"],
   "layer": "rust-core-query-printer",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query/macro_text.rs::tests::recognition_rejects_a_comma_that_exposes_a_reference_and_keeps_the_plain_one",
     "crates/tine-core/src/query/macro_text.rs::tests::recognition_rejects_a_leading_page_reference_and_accepts_the_anchored_form",
     "crates/tine-core/src/query/conformance.rs::an_unsafe_argument_is_refused_and_the_original_bytes_are_untouched",
     "crates/tine-core/src/query/conformance.rs::a_quantifier_over_a_page_ref_round_trips_in_the_pane_and_is_refused_as_a_macro"
    ],
    "scenario": "query-engine.macro-print-refuses-what-the-document-parser-cannot-read",
    "failBefore": "c6-recognition-fail-before.log at 3014c3b80d20fd9e76b18460f356de36112b66e7"
   },
   "notes": "The user edits a query and saves. The printer emitted bytes that satisfied every lexical check but that mldoc/lsdoc does not parse as a Macro at all -- a leading page reference followed by anything else, or a comma that exposes a reference at a later argument boundary. The block then renders as literal text and the query is gone; reopening cannot recover a query the reader no longer sees. Blocking for core operation. Every macro print now runs the already pinned inline parser in BOTH Markdown and Org modes and requires the intended Macro node plus exact raw recovery of form and options; a failure is a located refusal that writes nothing, so the authored bytes stay byte-identical."
  },
  {
   "id": "REG-P0-QUERY-DISABLED-ROW-MEANING-001",
   "title": "Disabling a row could change the query's answer instead of removing that row",
   "family": "query-engine",
   "sources": {"issues": [], "commits": [], "internal": "2026-09 query-engine campaign, packet P0-rust Wave C; SPEC 3.5 (R2); invariant I-12"},
   "platforms": ["all"],
   "layer": "rust-core-query-ir",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query/ir.rs::tests::normalized_flattens_same_kind_groups_and_keeps_active_constants",
     "crates/tine-core/src/query/ir.rs::tests::an_originally_empty_group_inside_a_relation_is_an_active_constant",
     "crates/tine-core/src/query/conformance.rs::normalization_preserves_truth_over_constants_off_not_and_relations",
     "crates/tine-core/src/query/conformance.rs::normalization_never_conflates_two_trees_that_differ_in_truth",
     "crates/tine-core/src/query/conformance.rs::both_print_forms_round_trip_semantically"
    ],
    "scenario": "query-engine.normalization-preserves-truth-across-off",
    "failBefore": "c8-normalization-fail-before.log at 3014c3b80d20fd9e76b18460f356de36112b66e7"
   },
   "notes": "The user turns one row off in a group that also holds a constant. Normalization dropped boolean identity elements, so a group whose only remaining sibling was disabled became a fully disabled node -- and a fully disabled root evaluates as 'match everything'. A query that answered nothing suddenly answered every row, and because the same normalization is the cache key, two trees that differ in truth shared one entry. Normalization now flattens and collapses but removes nothing: identity removal is legal only on the executable tree, after Off omission. An originally empty group stays the active constant it is, both in the tree and in without_off."
  },
  {
   "id": "REG-P0-QUERY-LOSSLESS-LEAF-001",
   "title": "An unreadable or legacy-regex condition was lost when an unrelated row was edited",
   "family": "query-engine",
   "sources": {"issues": [], "commits": [], "internal": "2026-09 query-engine campaign, packet P0-rust Wave C; SPEC 4.3.2 and 4.2.1 (R4); invariants I-4, I-22"},
   "platforms": ["all"],
   "layer": "rust-core-query-parser",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query/conformance.rs::a_broken_disabled_row_survives_a_save_and_reopen_beside_an_active_row",
     "crates/tine-core/src/query/conformance.rs::a_capsule_preserves_line_breaks_quotes_braces_and_unicode",
     "crates/tine-core/src/query/conformance.rs::every_diagnostic_kind_round_trips_through_its_capsule",
     "crates/tine-core/src/query/conformance.rs::a_bad_capsule_is_syntax_and_never_an_executable_predicate",
     "crates/tine-core/src/query/conformance.rs::the_legacy_regex_head_and_the_tql_regexp_spelling_are_the_same_leaf",
     "crates/tine-core/src/query/conformance.rs::an_invalid_regex_pattern_is_retained_and_matches_false"
    ],
    "scenario": "query-engine.invalid-and-legacy-leaves-survive-a-neighbouring-edit",
    "failBefore": "c9-lossless-leaf-fail-before.log at 3014c3b80d20fd9e76b18460f356de36112b66e7"
   },
   "notes": "Two ways to lose a condition. A malformed disabled row (`-- task = '` with an unmatched quote) was replaced by off(false) -- the author's text was gone after one save, and worse, the unmatched quote was scanned as part of the surrounding text and swallowed the next ACTIVE row. And a legacy (content-regex ...) condition had no TQL spelling at all, so editing any other row in the query serialized it away. Malformed disabled operands are now captured before the surrounding parse as a lossless raw_hex capsule carrying the exact payload and its diagnostic kind; content regexp '...' is the TQL spelling of the legacy head and parses back to the same leaf, with today's semantics preserved exactly (an invalid pattern is retained and matches false, so negating it matches true)."
  },
  {
   "id": "REG-P0-QUERY-ADVANCED-RUNTIME-INPUT-001",
   "title": "An advanced query answered for the wrong page, or for the day it was parsed on",
   "family": "query-engine",
   "sources": {"issues": [], "commits": [], "internal": "2026-09 query-engine campaign, packet P0-rust Wave D; SPEC 4.4 (R5), CLOSURE 2; invariants I-12, I-22"},
   "platforms": ["all"],
   "layer": "rust-core-query-parser",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query/conformance.rs::one_parse_answers_differently_on_two_current_pages",
     "crates/tine-core/src/query/conformance.rs::a_relative_date_query_answers_for_the_execution_day_across_a_rollover",
     "crates/tine-core/src/query/conformance.rs::a_missing_runtime_input_is_strict_no_results_with_a_report",
     "crates/tine-core/src/query/conformance.rs::run_explain_and_export_agree_on_results_and_report",
     "crates/tine-core/src/query/conformance.rs::explain_empty_reports_a_failed_resolution_without_misleading_counts",
     "crates/tine-core/src/query/conformance.rs::resolution_replaces_the_provisional_inspection_diagnostic",
     "crates/tine-core/src/query/conformance.rs::og_and_tql_resolve_to_themselves_with_an_empty_report"
    ],
    "scenario": "query-engine.advanced-runtime-inputs-bind-at-execution",
    "failBefore": "tine-agents/evidence/qe/p0-rust/wave-d/d1-fail-before.log (base f8149fbc)"
   },
   "notes": "An advanced (datalog) query whose inputs are :current-page or a relative date has no answer until it is executed: the same authored text means different things on two pages and on two days. Binding those inputs anywhere but at execution -- in the parse, or once per cache fill -- shows the reader another page's linked references, or yesterday's journal window, with no sign anything is stale. One resolver now runs before invalidity checks, normalization, cache lookup and the explain-empty decomposition: query_parse stays context-free and retains the complete authored form, and resolve_for_execution(query, context, today) lowers it through the ONE existing advanced_pred, returning the bound query plus the ran/ignored/supported report. A resolution that cannot bind an input answers with strict no results and says so in the report rather than answering with a plausible wrong set, and run, explain and export all go through the same resolver, so they cannot disagree."
  },
  {
   "id": "REG-P0-QUERY-CONTENT-MATCH-ENGINE-001",
   "title": "A content match answer depended on which engine ran it",
   "family": "query-engine",
   "sources": {"issues": [], "commits": [], "internal": "2026-09 query-engine campaign, packet P0-rust Wave D; SPEC 5.10 (R3), CLOSURE 4; invariants I-12, I-13"},
   "platforms": ["all"],
   "layer": "rust-core-query-parser",
   "status": "covered",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query/conformance.rs::match_is_a_substring_search_not_a_word_token_search",
     "crates/tine-core/src/query/conformance.rs::match_carries_the_whole_search_grammar_onto_the_leaf",
     "crates/tine-core/src/query/conformance.rs::an_empty_or_invalid_match_is_a_false_leaf_including_under_not",
     "crates/tine-core/src/query/conformance.rs::match_is_legal_only_on_content",
     "crates/tine-core/src/query/conformance.rs::the_legacy_search_head_and_content_match_are_the_same_leaf",
     "crates/tine-core/src/query/conformance.rs::one_parse_of_the_match_payload_serves_every_engine",
     "crates/tine-core/src/query/conformance.rs::every_displayed_search_example_holds_for_content_match"
    ],
    "scenario": "query-engine.content-match-is-one-semantic-contract",
    "failBefore": "tine-agents/evidence/qe/p0-rust/wave-d/d2-fail-before.log (base f8149fbc)"
   },
   "notes": "`content match` promises the graph search box's semantics: substring terms (so `foobar` answers `foo`, `oob` and `oo`), quoted phrases with their internal whitespace, OR arms, negated terms and an exclusion-only query. A standard word-token FTS index gets the first three wrong, so the walk and a token-index route would answer the same query differently and the user would watch results appear and disappear by route rather than by content. The leaf now carries the payload parsed ONCE by the search box's own search_query::Matcher, reached through Filter::match_sources and CompiledLeaves::match_program, so any future engine inherits that parse instead of re-deriving it; an empty or invalid-regex Match is a false leaf, including under `not`; and Match is legal only on `content`. The named fixture list -- foobar/foo/oob/oo, phrases with leading, repeated and tab or newline whitespace, whitespace-only terms, OR arms mixing short and long, negatives, exclusion-only, invalid regex, Unicode, escaped quotes -- plus every example the search help text displays is the corpus the future SQL route must reproduce row for row."
  },
  {
   "id": "REG-P0-QUERY-MACRO-RAW-SOURCE-001",
   "title": "A query macro was read from the document parser's comma-split arguments instead of the raw source",
   "family": "query-engine",
   "sources": {
    "issues": [],
    "commits": [],
    "internal": "2026-09 query-engine campaign, packet P0-ts; SPEC 4.3.1 and 7.1; invariants I-4, I-12, D-14"
   },
   "platforms": [
    "all"
   ],
   "layer": "frontend-render-and-publish-boundary",
   "status": "covered",
   "coverage": {
    "tests": [
     "src/components/QueryMacroSource.test.tsx",
     "src/editor/queryMacro.test.ts",
     "crates/tine-core/tests/query_macro_extents.rs",
     "crates/tine-core/src/publish_tests.rs::publish_reads_a_query_macro_from_the_raw_source_not_the_split_args",
     "crates/tine-core/src/publish_tests.rs::publish_renders_both_query_macro_names_and_never_leaks_the_source"
    ],
    "scenario": "query-engine.macro-argument-comes-from-the-raw-source",
    "failBefore": "t5-span-fail-before.log (renderer span anchoring), t5-publish-fail-before.log (publishing), t3-splitter-fail-before.log (the deleted options-map splitter disagreeing with Rust on 8 of 20 shared corpus cases)"
   },
   "notes": "mldoc/lsdoc splits a macro's arguments on commas and stops before the first `}`, so name+args cannot rebuild a query carrying a trailing options map or a literal comma inside a string. Rendering and publishing both consumed that reconstruction: an inline {{query (task TODO) {:title \"T\"}}} lost the map's closing brace, so the title was dropped and publishing emitted a stray } as visible text beside results computed from a truncated query. Both sides now recover the exact source slice with the shared raw-extent reader, pinned on both sides by one corpus (crates/tine-core/tests/fixtures/query-macro/extents.json). The renderer's join is the subtle half: lsdoc reports UTF-8 byte offsets into the re-bulleted parse input (\"- \" + raw.trimStart()), so anchoring by the span as if it indexed raw never matched and always fell back to the lossy reconstruction; queryMacroExtentAtSpan does the same span[0] - 2 + leadBytes mapping render/facets.ts already used, and anchors exactly so a span measured against some other string finds nothing rather than the wrong macro."
  }
,
{
 "id": "REG-QE-P2-DIRECTIVE-MIGRATION-BYTE-STABILITY",
 "title": "A view directive removed in the builder came back on reparse, because the OG text and the block properties both claimed to own it",
 "family": "query-engine",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "2026-09 query-engine campaign, packet P2; SPEC 4.3 directive migration (Q15) and 7.6; invariants I-4, I-12"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core-query-printer",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/print.rs::the_og_printer_leaves_aggregates_and_group_by_out_of_the_text",
   "crates/tine-core/src/query/print.rs::every_typed_property_operator_the_builder_offers_round_trips",
   "src/components/QueryMacro.ir.test.tsx::B6: directive migration for blocks that stay {{query}}",
   "src/components/QueryMacro.ir.test.tsx::leaves an untouched block byte-identical (no save, no property lines)"
  ],
  "scenario": "query-engine.a-view-directive-has-exactly-one-home",
  "failBefore": "T4 in RECEIPT-p2.md: the OG printer re-emitted `(aggregate count) (aggregate sum hours) (group-by status)` after the same view had been written to `tine.col-aggregates::`/`tine.group-by::`; four `B6` cases read `null` where a `tine.*` line was required."
 },
 "notes": "User outcome: aggregates and grouping lived in TWO places on a block that stayed a `{{query}}` -- in the macro text, printed by the OG printer, and in the `tine.col-aggregates::`/`tine.group-by::` block properties, which `view.rs::merge_block_property_view` reads with absolute precedence. Removing a column total or a grouping in the builder cleared the properties but reprinted the directive into the text, so the next parse restored what the user had just deleted; and because precedence is unconditional, a stale text copy could also outlive a change made anywhere else. Classification: exact safety/interoperability -- the disagreement is about what the file MEANS. One question, one answer (I-12): `og_view` no longer emits `aggregate` or `group-by` at all, while `sort-by` and `sample` keep their text form because OG itself reads those, and every save now writes all SIX SPEC 7.6 view properties (`tine.view`, `tine.sort`, `tine.group-by`, `tine.fields`, `tine.col-aggregates`, `tine.sample`) under both macro names -- writing `null` for the absent ones, so a removal is persisted as an absence rather than left behind. `og_expressible` is deliberately unchanged: a query with aggregates is still OG-expressible and still stays a `{{query}}`. Byte stability is the other half (I-4): `markdownRawWithProperty(raw, key, null)` is the identity when the key is absent, so an ordinary block that is opened and closed without an edit is written not at all, and a saved block gains only the property lines its view actually needs."
},
{
 "id": "REG-QE-P2-NOTICE-DISMISSAL-DEVICE-LOCAL",
 "title": "The `don't show this again` choice for the query-crossing notice is device-local and graph-keyed, and never enters the graph",
 "family": "app-private-data-safety",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "2026-09 query-engine campaign, packet P2; SPEC 4.3/7.5; invariants I-18, D-3, D-7, D-11, G2"
 },
 "platforms": [
  "all"
 ],
 "layer": "tauri-storage",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/settings.rs::notice_dismissals_live_beside_the_session_file_and_are_graph_keyed",
   "src-tauri/src/settings.rs::a_dismissed_notice_round_trips_and_survives_a_reopen",
   "src-tauri/src/settings.rs::a_malformed_notices_file_reads_as_empty_and_the_next_save_repairs_it",
   "src-tauri/src/settings.rs::saving_notices_leaves_the_graph_bytes_untouched",
   "src/components/QueryCrossingNotice.test.tsx::C3: [Keep it] and the device-local dismissal"
  ],
  "scenario": "query-engine.dismissing-a-notice-is-device-local-state",
  "failBefore": "T3 in RECEIPT-p2.md: all eleven cases red at base, the store's entry point reported as `Error: loadNotices does not exist`."
 },
 "notes": "Capability with a data-safety edge: a user who ticks `Don't show this again` on the query-crossing notice must not thereby write anything into the graph, and must not have the choice follow them to another device (I-18) -- while a second graph on the same device must still get its own answer. The dismissal set is stored in app-data as `sessions/<graph_storage_key>-notices.json`, keyed exactly the way `session_path`/`workspaces_path` key theirs (D-11), and written with `tine_core::model::atomic_write` rather than `fs::write` (D-7) -- the audited-write allowance in this file moves 3 -> 4 with the new save, which is the point of that allowance being a number. Three refusals are deliberate and each names an in-scope failure: a missing file, an unreadable file and a MALFORMED file all read as `{\"dismissed\":[]}` and write nothing back, so a truncated or externally corrupted device file costs the user a re-shown notice and never a refused save; the next ordinary dismissal repairs it. `load_workspaces_at`'s body is NOT reused (D-3/G2): it carries a migration this store has no history to migrate, and copying it would have imported a fallback that writes on read. The frontend reads the store at most once per graph epoch, so nothing here runs on a query or keystroke path (I-13), and a store that cannot be read leaves the notice SHOWING rather than silently suppressed."
},
{
 "id": "REG-QE-P5A-COLUMNS-ARE-NOT-SCHEMA",
 "title": "A query block's visible columns and its typed sheet schema no longer share one property, so neither writer destroys the other's value",
 "family": "persistence-correctness",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "2026-09 query-engine campaign, checkpoint P5A; SPEC 4.1/7.6; P5r-VERIFY N3; invariants I-4, I-12, D-4, D-12, D-14; docs/contracts/query-display.md"
 },
 "platforms": [
  "all"
 ],
 "layer": "frontend-render-and-publish-boundary",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/view.rs::tine_columns_owns_the_visible_columns_and_tine_fields_keeps_the_typed_schema",
   "crates/tine-core/src/query/view.rs::a_typed_or_mixed_tine_fields_is_schema_and_never_columns",
   "crates/tine-core/src/query/view.rs::a_present_but_empty_or_invalid_columns_list_clears_and_never_falls_back",
   "crates/tine-core/src/query/view.rs::a_legacy_bare_tine_fields_list_still_names_columns_when_the_new_key_is_absent",
   "crates/tine-core/tests/query_columns_resolution.rs::the_rust_column_resolver_matches_the_shared_fixtures",
   "crates/tine-core/src/publish_tests.rs::publish_query_table_shows_the_selected_columns_in_the_selected_order",
   "crates/tine-core/src/publish_tests.rs::publish_query_table_reads_a_legacy_bare_field_list_as_columns",
   "crates/tine-core/src/publish_tests.rs::publish_query_table_treats_a_present_empty_columns_list_as_no_selection",
   "crates/tine-core/src/publish_tests.rs::publish_children_sheet_ignores_a_query_column_selection",
   "src/editor/queryViewProperties.test.ts::the shared visible-column resolver::matches the Rust resolver on every shared fixture",
   "src/components/QueryColumns.test.tsx::keeps a declared tine.fields schema across an unrelated filter save",
   "src/components/QueryColumns.test.tsx::rescues a legacy list into tine.columns when a schema is declared over it",
   "src/components/QueryColumns.test.tsx::never overwrites a PRESENT columns property with a legacy rescue",
   "src/components/QueryColumns.test.tsx::renders exactly the selected columns, in the selected order"
  ],
  "scenario": "query-engine.a-query-block-keeps-both-its-schema-and-its-columns",
  "failBefore": "RECEIPT-p5a.md, evidence/qe/p5a/10-failbefore-publish.txt and 11-failbefore-ts-clobbers.txt: 13 of 19 TypeScript cases and 2 of 3 publisher cases red at e5ad911d."
 },
 "notes": "Two live producers wrote `tine.fields::` from opposite directions and each destroyed the other's value: `Macro.tsx::writeViewProperties` wrote `(view.columns ?? []).join(\";\")` into it on EVERY save, so any filter edit replaced or deleted a declared `cost=number` schema; `SheetTable.tsx::writeSchemaFields` wrote the declared schema into the same key from `declareField`/`reorderFieldHeader`, so declaring a schema deleted the column list. Visible columns now live in `tine.columns::` and the typed schema stays in `tine.fields::`. The precedence is frozen in docs/contracts/query-display.md and resolved by ONE Rust function (`query::view::resolve_query_columns`), which `publish.rs` CALLS rather than mirroring -- the published page and the app cannot disagree about which columns a note selects. `=`, NUL, CR or LF in one token invalidates the whole list; a PRESENT but empty/invalid value is an explicit statement with no legacy and no DSL fallback behind it, so clearing cannot resurrect an old list; `tine.fields` is read as a legacy column list only when absent AND every nonempty token is bare. That legacy branch is authored-note compatibility, not a D-1 private-format migration, and reading it never rewrites the note (I-4). The TypeScript adapter exists because rendering a table is a synchronous in-memory walk that cannot take an IPC round-trip per block; it is pinned to Rust by one shared corpus, crates/tine-core/tests/fixtures/query-columns/resolution.json."
},
{
 "id": "REG-QE-P5A-VIEW-SAVE-PRESERVES-PERSISTED-FACTS",
 "title": "A query save persists the display facts the reprint would drop, and rewrites nothing else on the block",
 "family": "persistence-correctness",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "2026-09 query-engine campaign, checkpoint P5A; SPEC 4.3 Y2/7.6 Q15; P5r-VERIFY B1/N6; invariants I-4, I-9, D-7, D-14; docs/contracts/query-display.md"
 },
 "platforms": [
  "all"
 ],
 "layer": "frontend-store",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/editor/queryViewProperties.test.ts::the lossless query view-property patch::materializes an OG-only grouping and aggregate on a FILTER-only save",
   "src/editor/queryViewProperties.test.ts::the lossless query view-property patch::never writes or deletes the typed schema, the widths, the filter, or an unknown key",
   "src/editor/queryViewProperties.test.ts::the aggregate segment merge::edits recognized segments in place and keeps table-only ones verbatim",
   "src/components/QueryColumns.test.tsx::persists an OG-only grouping and aggregate that the reprint is about to drop",
   "src/components/QueryColumns.test.tsx::preserves widths, filter, formulas and unknown keys through a save",
   "src/components/QueryColumns.test.tsx::writes nothing at all when every persisted fact already agrees",
   "src/components/QueryColumns.test.tsx::keeps a query block's bytes identical in ORG too",
   "src/components/QueryMacro.ir.test.tsx::persists all six §7.6 fields, so the block re-parses to the saved view",
   "src/sheet/renameField.test.ts::carries a query aggregate configuration across a sheet field rename",
   "src/sheet/renameField.test.ts::does not widen the sheet aggregate vocabulary to make avg renameable"
  ],
  "scenario": "query-engine.a-filter-edit-does-not-cost-the-block-its-view",
  "failBefore": "RECEIPT-p5a.md, evidence/qe/p5a/11-failbefore-ts-clobbers.txt and 12-failbefore-rename.txt: the aggregate-preservation and byte-identity cases red at e5ad911d, and both new rename fixtures refused outright."
 },
 "notes": "The baseline for what a save writes is the block's currently PERSISTED property value, never a 'user touched this control' flag. `og_view` re-emits only `(sort-by ...)` and `(sample ...)`, so a grouping or an aggregate authored in the query text is dropped by the reprint of an unrelated FILTER edit -- only a property write keeps it, and a crossing to `{{tine-query}}` (whose text carries no directives at all) therefore materializes the whole effective view in the same undo unit. A fact the property already spells is not rewritten to reformat it. Everything outside the six display facts is left byte-identical: schema, widths, header, filter, formulas and unknown keys. `tine.col-aggregates` is shared ground between the query reader (bare `count`, `key=count|sum|avg`, an ordered LIST with meaningful repeats) and the sheet footer (a 17-name vocabulary in a `Map` that collapses repeats and has no keyless spelling), so a save MERGES: recognized segments replaced in place, surplus removed, remainder appended, unrecognized slots kept verbatim and in order. `avg` is recognized by the rename helper without being added to `AggregateFn`/`isAggregateFn`/`applyAggregate` -- a 'valid' sheet aggregate with no implementation is worse than an unrecognized one -- and the rename's preservation proof is now an ordered segment comparison admitting exactly the intended key substitution, replacing a `Map`-size check that proved nothing about the segments being renamed."
}
 ,
{
 "id": "REG-QE-RET3-EXPORT-SUBTREE-BUDGET-RECURSION",
 "title": "A database-read export subtree drops a later ancestor sibling once any earlier descendant does not fit",
 "family": "query-engine",
 "sources": { "issues": [], "commits": [], "internal": "RET3 SQL export core packet, base 37b15bc1" },
 "platforms": ["all"], "layer": "rust-core", "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/query/export_results_tests.rs::a_child_that_cannot_fit_stops_its_parent_but_not_its_grandparents_next_child", "crates/tine-core/src/query/export_results_tests.rs::the_node_budget_is_recursive_too", "crates/tine-core/src/query/export_results_tests.rs::export_core_answers_exactly_what_the_walk_export_answers"],
  "scenario": "query-engine.ret3-export-subtree-budget-recursion"
 },
 "notes": "block_to_bounded_dto spends its node and byte budgets RECURSIVELY, not as one global preorder prefix: when a child does not fit, only that parent stops taking further children, and an ancestor\u2019s later sibling can still be admitted. Reading the same subtree out of the SQLite projection replaces the oracle\u2019s recursion with an explicit stack (crates/tine-core/src/query/export_results.rs admit_subtree), where the natural mistranscription is to break the whole traversal on the first rejection. Mutation-checked on 2026-09-08: replacing the frame pop with a global break turned exactly the named budget test red while the rest of the module stayed green, so the gate is necessary and not redundant with parity. The parity sweep was then widened to run every macro alone as well as all macros under one shared budget \u2014 under the combined budget alone the earlier macros exhausted the cap before the shape could appear, and the sweep did not see the mutation."
},
{
 "id": "REG-QE-EXPORT-ROOT-PUBLIC-ID-CONFLATION",
 "title": "An export root resolves to the first block advertising a public id, not the block that was selected",
 "family": "query-correctness",
 "sources": { "issues": [], "commits": [], "internal": "RET3 SQL export core packet, base 37b15bc1" },
 "platforms": ["all"], "layer": "rust-core", "status": "reproduced",
 "coverage": {
  "tests": ["crates/tine-core/src/query/export_results_tests.rs::a_root_is_the_physical_block_that_was_selected_not_the_first_block_that_shares_its_id", "crates/tine-core/src/query/export_results_tests.rs::equal_display_names_do_not_merge_two_physical_subtrees"],
  "scenario": "query-correctness.export-root-public-id-conflation"
 },
 "notes": "Measured on 37b15bc1, 2026-09-08. The walk-backed hydrate_selected_export_queries resolves each export root by scanning the named page for the FIRST block whose uuid OR id:: property equals the root\u2019s public id, so a decoy block carrying another block\u2019s structural id in its id:: property wins and the export emits the decoy\u2019s subtree instead of the selected one; equal page display names are ambiguous the same way. The named tests pin the correct physical answer for the new database-read core (which carries a ResultLocator of source index plus physical page and block id from selection through hydration) AND assert that the oracle really does answer wrongly, so the divergence is recorded rather than copied. Not fixed here: the remaining wrong answer is in the public walk adapters, outside this packet\u2019s write set."
}
,
{
   "id": "REG-QE-TQL-STRUCTURE-AND-CURRENT-SCOPE-001",
   "title": "Persisted TQL retains editable groups and resolves page hops from the current nested row",
   "family": "query-engine",
   "sources": {"issues": [], "commits": [], "internal": "P6 native root diagnostic and PageBlocks89744"},
   "platforms": ["all"],
   "layer": "rust-core",
   "status": "fixing",
   "coverage": {
    "tests": [
     "crates/tine-core/src/query/tql.rs::tests::unparenthesized_boolean_chains_are_nary_but_parenthesized_groups_survive",
     "crates/tine-core/src/query/tql.rs::tests::page_hops_follow_the_current_nested_scope",
     "crates/tine-core/src/query/tql.rs::tests::quantifier_calls_after_boolean_operators_keep_their_exact_ir",
     "crates/tine-core/src/query/tql.rs::tests::quantifier_disambiguation_ignores_quoted_text_and_comments",
     "crates/tine-core/src/query/tql.rs::tests::quantified_page_properties_and_page_tags_use_the_same_current_scope_rule",
     "crates/tine-core/src/query/print.rs::tests::tql_preserves_flat_chains_and_explicit_group_boundaries_exactly",
     "crates/tine-core/src/query/print.rs::tests::layered_tql_keeps_a_same_kind_group_beside_an_off_sibling",
     "crates/tine-core/src/query/print.rs::tests::reopened_structural_tql_accepts_an_edit_and_round_trips_again"
    ],
    "scenario": "query-engine.tql-structure-and-current-scope",
    "failBefore": "manager-native-root-diagnostic exits 1 with a flat four-row query reopened as nested binary groups; PageBlocks89744 returns no page for a matching nested page_prop"
   },
   "notes": "The P6 native journey persisted the right one-line tine-query and kept its sheet open, but sqlparser's binary associativity leaked into the editable IR. TQL lowering also decided Page hops from the outer anchor, so page_prop inside an @page blocks relation stayed on the block. The correction preserves explicit same-operator parentheses, flattens only unparenthesized parser chains, and resolves page targets from the current Scope. Exact manager-owned Rust and native pass-after evidence remains pending."
  },
{
 "id": "REG-REF-INDEXED-PANEL-POLICY-001",
 "title": "A references panel waits for the index instead of parsing every page while the projection is mid-turn",
 "family": "references",
 "sources": { "issues": [], "commits": [], "internal": "Martin 2026-09-10: repeated stale-projection lines while opening a query-bearing page" },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::a_working_projection_refuses_an_indexed_reference_read_instead_of_parsing_every_page",
   "crates/tine-core/tests/public_query_executor_census.rs::the_reference_panel_commands_use_the_indexed_policy",
   "src/lib/referenceFetch.test.ts"
  ],
  "scenario": "references.indexed-panel-policy",
  "failBefore": "With the projection worker held mid-delta, reference_candidate_pages_indexed answered Ok with indexed=false over all 7 pages; excising the ProjectionProgress arm fails the test with 'the panel policy must report the working projection, not parse every page: None'"
 },
 "notes": "Linked and Unlinked References answered a projection that was indexing, recovering, applying pending edits or busy by parsing every page in the graph. During the cold-open window that is the whole graph, once per panel, for rows the index serves a moment later. The panels now use the indexed policy and wait through runQueryWhenCurrent, the same readiness retry a query block uses. Print, publish, diagnostics and the unbounded entry points keep the walk because they have no retry loop to wait on. A stopped worker, an absent projection, and a Plain target with no alphanumeric character also keep the walk: nothing is coming for them, and refusing there would remove the only route to an answer rather than delay it."
  },
{
 "id": "REG-QUERY-REFRESH-DROPS-PROJECTION-001",
 "title": "A settings change (Guide toast, default home, brackets, format, restore) keeps queries answering",
 "family": "query-engine",
 "sources": { "issues": [], "commits": [], "internal": "Martin 2026-09-11: dismissing the Guide toast made every query 'unavailable' until the graph was reopened" },
 "platforms": ["all"], "layer": "rust-core", "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/state.rs::tests::a_config_refresh_keeps_queries_answering",
   "src-tauri/src/graph.rs::tests::every_direct_files_graph_is_attached_through_one_function"
  ],
  "scenario": "query.refresh-keeps-projection",
  "failBefore": "With attach_direct_files_services removed from reopen_legacy_for_refresh the test fails: after refresh the query answers None (ProjectionUnavailable) where it answered Some(3) before"
 },
 "notes": "User outcome: on a Direct Files graph, dismissing the Guide toast -- and every other command that refreshes the graph after a config write (default home, time tracking, show brackets, doc-mode Enter, logical outdenting, preferred format, journal title format, restore from backup, an external config.edn rewrite) -- turned every query block into 'Query results unavailable' until the graph was reopened. Classification: blocking core operation. Mechanism: refresh_graph_for_label built a new Graph and bound it without the Direct Files projection (and Concord ledger) that prepare_direct_files_open attaches, and without a warm. Fix: one attach_direct_files_services used by both the open and the refresh; the refresh retires the old projection worker first (a second DirectProjection::start on the same path races the writer lease), reopens, attaches, and warms through warm_cache_async like the open path. Managed graphs were never affected (refresh returns early for a sparse binding)."
},
{
 "id": "REG-QUERY-NEW-BLOCK-WEDGES-PROJECTION-001",
 "title": "Creating a block in the editor no longer leaves every query 'Rebuilding the query index...' until restart",
 "family": "query-engine",
 "sources": { "issues": [], "commits": [], "internal": "2026-09-11 why-empty repro lane: after one save every query stayed 'recovering' for 95 s and never cleared" },
 "platforms": ["all"], "layer": "rust-core", "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/tests/search_edit.rs::a_block_created_in_the_editor_keeps_queries_answering"
  ],
  "scenario": "query.new-block-keeps-projection",
  "failBefore": "On d61cfb3d the test panics after 10 s: 'queries never answered again after a save that created a block; last: not ready (recovering)', with 495 'projection is stale; indexed reads are unavailable' lines (tine-agents/evidence/2026-09-11-query-fixes/wedge-fail-before.log)"
 },
 "notes": "User outcome: pressing Enter to create a block, then saving, made every query in the app say 'Rebuilding the query index...' permanently (a plain text edit did not). Classification: blocking core operation. Mechanism: a block created in the editor is saved with the frontend's live id (b<base36 time>-<n>), which the in-memory save path deliberately keeps; lower_blocks refused the WHOLE page delta because that id is not a UUID, the failed turn latched a full rebuild, and every rebuild re-lowered the same live document and failed the same way. Fix: a non-UUID live id gets a deterministic 16-byte projection key (the row's public identity is the id string, so answers still name the block by the editor's id). The walk fallback the query campaign retired used to hide this refusal."
},
{
 "id": "REG-WINDOWS-SMB-CAPABILITY-UNC-533",
 "title": "Capability directory enumeration preserves absolute UNC paths on Windows SMB graphs",
 "family": "direct-files-graph-open",
 "sources": {
  "issues": [
   533
  ],
  "commits": [
   "d5600500"
  ],
  "internal": "Windows loopback SMB fail-before and pass-after using exact cap-primitives4.0.2 dependency"
 },
 "platforms": [
  "windows"
 ],
 "layer": "filesystem-capability-dependency",
 "status": "covered",
 "coverage": {
  "tests": [
   "vendor/cap-primitives-4.0.2/src/windows/fs/get_path.rs::tests::removing_extended_prefix_keeps_unc_paths_absolute",
   "scripts/probes/windows-smb-open/run.ps1",
   ".github/workflows/ci.yml#windows-smb-probe"
  ],
  "scenario": "windows.smb-unc-and-mapped-retained-handle-enumeration",
  "failBefore": "https://github.com/martinkoutecky/tine/actions/runs/34755503592 : local path passes; both UNC and mappedZ pass ambient writes/reads, cap no-follow directory open and FileIdInfo but entries() fails NotFound OS3."
 },
 "notes": "The owning cap-primitives helper reconstructed a retained Windows handle's extended UNC path by stripping only the first4 prefix code units, yielding relative UNC/server/share. The local patch converts that prefix to ordinary absolute UNC while preserving the existing local-drive conversion and caller no-follow/conflict/durability guards. Pass-after https://github.com/martinkoutecky/tine/actions/runs/34755743020 : same local/UNC/mapped paths enumerate and read, nested Unicode files read, all3 outside junction controls refuse with OS681, and the prefix unit cases pass. This is a proven Windows loopback capability-layer correction, not a claim that every NAS implementation or full Tauri workflow was exercised."
},
{
 "id": "REG-QUERY-EXPORT-CLOSURE-001",
 "title": "A query export contains exactly the whole owner pages of the results, keeps outside links and nested-query rows inert, copies assets under one budget or refuses, and its output is never read back as pages",
 "family": "static-publication-privacy",
 "sources": {
  "issues": [],
  "commits": [
   "9f37234d",
   "b5a0a419",
   "cbc348f3"
  ],
  "internal": "tine-agents/specs/notes/2026-09-14-publish-query-stage1.md (v3, Astra-verified r1+r2)"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core-publish",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/publish_tests.rs::query_export_publishes_whole_owner_pages_and_nothing_else",
   "crates/tine-core/src/publish_tests.rs::query_export_refuses_a_stale_review_and_respects_create_vs_replace",
   "crates/tine-core/src/publish_tests.rs::query_export_refuses_twins_and_empty_results",
   "crates/tine-core/src/publish_tests.rs::query_export_copies_referenced_assets_into_the_leaf",
   "crates/tine-core/src/publish_tests.rs::query_export_over_the_asset_budget_is_refused_whole",
   "crates/tine-core/src/publish_tests.rs::query_export_nested_query_is_silent_about_the_outside_but_flags_sampling",
   "crates/tine-core/src/graph_text_scope.rs::tests",
   "crates/tine-core/src/model_tests.rs::published_queries_output_never_becomes_pages"
  ],
  "scenario": "publish.query-export-closure",
  "failBefore": "proven"
 },
 "notes": "Exact safety/interoperability. Selection is the set of physical owner pages; the fingerprint covers anchor, row count, folder and every selected page's path/name/kind/revision, so a changed graph between review and commit is refused. Create fails if the folder appeared meanwhile (own stage retired as unused-stage); Replace retires the occupant into logseq/.tine-trash/conflicts and reports the path. Assets copy into <leaf>/assets/<rel> under the request's byte budget; exceeding it refuses the whole export with a typed error. published-queries/ is a fixed graph-text exclusion and GRAPH_TEXT_SCOPE_VERSION moved 1→2 (one Managed rebuild). The 57 pre-existing publish/ tests are unchanged: golden equivalence of the graph site."
},
{
 "id": "REG-QUERY-EXPORT-APP-CLOSURE-001",
 "title": "The app snapshot of a query export holds exactly the exported pages and the queries recorded over them: no outside page, block, backlink, alias, icon or result row, and every query row was computed by the native engine at export time",
 "family": "static-publication-privacy",
 "sources": {
  "issues": [],
  "commits": [
   "8a274ac7"
  ],
  "internal": "tine-agents/specs/notes/2026-09-14-publish-query-stage2.md (v2, Astra-verified)"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core-publish",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/publish_tests.rs::query_export_ships_the_app_over_a_closed_snapshot",
   "crates/tine-core/src/publish_tests.rs::query_export_without_an_embedded_frontend_warns_and_ships_no_app",
   "crates/tine-core/src/publish/app_export.rs::tests"
  ],
  "scenario": "publish.query-export-app-closure",
  "failBefore": "proven"
 },
 "notes": "Exact safety/interoperability. The snapshot is built from a closed sub-graph (PublicationGraphSnapshot over the selected pages only), so backlinks, block_ref_counts, aliases and icons cannot name an outside page. Query rows are recorded by the same executor that renders the static site, with the export's scope rules (<% current page %> substitution, on-page context, BEGIN_QUERY table view, nested rows not recorded, host block removed from the home run). The home page is synthesized (name = export name, or '<name> (export)' on collision) and never written to the graph. A build without the embedded frontend warns and ships no app/. Byte search for a private sentinel is the privacy oracle."
},
{
 "id": "REG-AUTOCOMPLETE-PAGE-PROPERTIES-FALLBACK-001",
 "title": "While the query index is still building, property autocomplete offers an Org page's :PROPERTIES: drawer and does not offer a key:: line inside a code block, the same keys it offers once the index is ready",
 "family": "query-autocomplete",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "Consolidation K2 (2026-09-15): the not-ready fallback read page properties through a second extractor (page_facets) that ignored Org drawers and fenced code; K2 routes it through the projection's DocBlock::preamble grammar"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::autocomplete_fallback_reads_page_properties_like_the_projection"
  ],
  "scenario": "query.autocomplete-not-ready-page-properties",
  "failBefore": "proven"
 },
 "notes": "Blocking stateful UX: one answer to 'which property keys does this graph have' in both index states. Fail-before on base 37226c0b: the fallback offered [fenced, status] where the projection offers [owner, status]. Evidence: tine-agents/evidence/consolidation/k2/autocomplete-fail-before.log."
},
{
 "id": "REG-QUERY-MATRIX-OPERATORS-001",
 "title": "Every comparison the TQL operator matrix accepts answers in both engines: like on task, priority and page.namespace, page.journal !=, page.name not in, and content in / not in return their matches, and like on a date or checkbox attribute is refused with a syntax diagnostic",
 "family": "query-correctness",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "tine-agents/specs/campaigns/2026-09-consolidation/PLAN.md (K4); query-engine SPEC.md §4.2.3"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/sql_gates_tests.rs::every_operator_the_matrix_accepts_answers_in_both_engines",
   "crates/tine-core/src/query/tql.rs::tests::like_on_a_date_or_checkbox_attribute_is_a_syntax_diagnostic",
   "crates/tine-core/tests/query_lowering_guards.rs::every_operator_decision_in_the_query_engines_names_every_operator"
  ],
  "scenario": "query.matrix-operators",
  "failBefore": "proven"
 },
 "notes": "Blocking core operation: a query the parser accepted silently matched nothing, and like on a date was accepted instead of refused. Found by the K4 census of CmpOp wildcard arms. Both engines are asserted against an explicit expected set, so a shared wrong answer cannot pass as parity. Fail-before on a2aaae07 + K4.1: the walk for task like 'DO%' returned {} and scheduled like '2026%' carried no diagnostic. Evidence: tine-agents/evidence/consolidation/k4/matrix-fail-before.log and matrix-pass-after.log."
},
{
 "id": "REG-CONCORD-LEDGER-EXIT-DRAIN-001",
 "title": "Quitting right after a save keeps that save's Concord merge base: every exit path waits, at most 200 ms in total, for the queued ledger updates of every open graph",
 "family": "concord-base-ledger",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "tine-agents/specs/campaigns/2026-09-consolidation/PLAN.md §7 (Concord exit wait); Martin 2026-09-15 (T2)"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core-tauri-boundary",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/concord_ledger.rs::tests::exit_drain_lands_every_queued_update",
   "crates/tine-core/src/concord_ledger.rs::tests::exit_drain_never_starts_the_worker",
   "crates/tine-core/src/concord_ledger.rs::tests::storage_contract_names_the_concord_ledger_and_its_schema",
   "src-tauri/src/lib.rs::concord_exit_drain_tests::every_exit_path_drains_the_concord_ledgers"
  ],
  "scenario": "concord.ledger-exit-drain",
  "failBefore": "proven"
 },
 "notes": "Flexible degraded behaviour, not data loss: the ledger is a merge hint and no note text is ever lost. ConcordLedger::flush() had only test callers, so app.exit(0) dropped queued Record/Pin/DropPin/Prune jobs and the next conflict on that page merged against an older base (more lines to review by hand). Quitting right after a save is the ordinary multi-device case Concord serves, so Martin chose a bounded exit wait over a doc-only fix. Fail-before: the lib.rs exit-path guard failed on base a36724c7 because the RunEvent::Exit arm did not drain. Evidence: tine-agents/evidence/concord-exit-flush/."
},
{
 "id": "REG-DIRECT-CARRY-SOURCE-BARRIER-001",
 "title": "Carried or moved blocks are never written out of their source page before they are on disk in the destination: carry holds its source days behind today, and a second move into one page no longer strands the first move's sources",
 "family": "direct-save-data-safety",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "tine-agents/subagent-tasks/notes/2026-09-15-k6-store-move-census.md (H1); tine-agents/specs/campaigns/2026-09-consolidation/PLAN.md §7"
 },
 "platforms": [
  "all"
 ],
 "layer": "frontend",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/directMoveOrder.test.ts::does not write the task out of its source day when a later edit saves that day",
   "src/persistenceMoveBarrier.test.ts::releases every source held behind one destination, not only the latest move's"
  ],
  "scenario": "direct-markdown.move-barrier",
  "failBefore": "proven"
 },
 "notes": "Exact safety (data loss). Carry was the one cross-page move with no source barrier (audit C#1): carryUnfinished left the source days clean and carry.ts marked them only after today saved, but nothing held them, so when today's save conflicted (e.g. Syncthing delivered a newer today) an unrelated edit to a source day wrote the post-removal file and the carried tasks survived only in memory and the move record. Fail-before on base a36724c7: the source day was written as ['a note that stays, edited'] without the task. Neighbour: holdSourcesForDest replaced an earlier hold on the same destination, so the first move's sources stayed held after it landed and their edits could never save. Evidence: tine-agents/evidence/move-carry-barrier/."
},
{
 "id": "REG-QUERY-PROJECTION-REUSE-543",
 "title": "Reopening a graph reuses the persisted query index instead of erasing and rebuilding it, so search becomes ready promptly on a large graph",
 "family": "query-readiness",
 "sources": {
  "issues": [
   543
  ],
  "commits": [],
  "internal": ""
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::a_query_before_the_warm_keeps_a_clean_reopen_clean",
   "crates/tine-core/src/direct_projection_tests.rs::a_repair_in_flight_reports_work_in_progress_not_a_stale_projection"
  ],
  "scenario": "query.projection-reuse",
  "failBefore": "proven"
 },
 "notes": "Blocking core operation: Ctrl-K and the search tab stayed on “Indexing — waiting for search to be ready…” indefinitely on a graph over ~10,000 pages, and the app wrote continuously while idle. Root cause: the app issues queries (journal feed, backlinks, Ctrl-K) before `warm_cache_async` reaches the projection; such a query found the worker idle, unvalidated and not yet failed, which `progress_at` reports as `Stale`, and the `Stale` repair latched `pending.rebuild`. The worker consumes that flag with the warm it scheduled, so it `reset()` the database — dropping every source stamp — and re-lowered every page on EVERY launch. Fail-before on the exact test: lowerings()==2 instead of 0 on an unchanged two-page reopen. Measured at the observation boundary on a 32,250-file corpus (real app, Linux): before, three consecutive unchanged restarts took 97 s / 102 s / 142 s to search-ready and rewrote a 524 MB WAL each time, although all 32,250 stored source revisions were byte-identical across the restart. After the fix, the same unchanged reopen of that corpus reached search-ready with real results in 14 s and left the database file untouched. A second defect surfaced while fixing: without the erase\u2019s side effect of publishing `pending.rebuild`, two concurrent startup queries raced on the recovery lock and the loser reported the terminal \u201cThe query index could not be read\u201d; a repair now publishes an in-flight marker that `progress_at` reports as Working(Recovering), so the losing query retries."
},
{
 "id": "REG-QUERY-PROJECTION-BUILD-CACHE-543",
 "title": "A cold or rebuilt query index on a large graph finishes in bounded time instead of stalling for minutes inside row insertion",
 "family": "query-readiness",
 "sources": {
  "issues": [
   543
  ],
  "commits": [],
  "internal": ""
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/projection_budget.rs::small_graph_gets_the_floor_and_large_graph_the_ceiling",
   "crates/tine-core/src/projection_budget.rs::physical_memory_caps_the_budget_but_never_below_the_floor",
   "crates/tine-core/src/direct_projection_tests.rs::a_fresh_build_restores_the_serving_writer_cache_budget"
  ],
  "scenario": "query.projection-build-cost",
  "failBefore": "tine-agents/evidence/gh543-build-path/core-fail-before.log (a_full_build_sizes_the_writer_cache_and_hands_it_back against the pre-fix apply_pending: cache budget stayed at the 2,048,000-byte default; probe curves in probe-1k.log and probe-knee.log)"
 },
 "notes": "Blocking core operation: on a 10,000-page graph the first index build (fresh install, upgrade, or repair) never reached search-ready on Windows within 10 minutes and search stayed on the indexing notice. Root cause: the whole-graph build is one SQLite transaction inserting every row of every UUID-keyed index onto a random B-tree leaf while the connection ran at SQLite's ~2 MiB default page cache, so each dirty page was spilled to the WAL and read back (8.8M cache misses, 27 GB read / 19 GB written for 49 MB of Markdown); Windows makes each of those round trips ~10x dearer. Fix (tine-storage v0.20.1 + tine-core): the writer's page cache is sized to the build from the projected text bytes (`projection_budget`, RAM-capped, returned after commit), and a build into an empty projection creates its 35 secondary indexes once after the rows instead of maintaining them per row. Measured in tine-storage's `gh543_build_probe` (recorded in the packet record). Coverage update (2026-09-21): compact-projection P4 (1f80e35a) moved the full build into an unpublished stage and retired a_full_build_sizes_the_writer_cache_and_hands_it_back; the build budget is still computed by projection_budget (the two tests above), and a_fresh_build_restores_the_serving_writer_cache_budget proves the serving writer gets its budget back after publication. The fail-before evidence above is for the retired test and is historical."
},
{
 "id": "REG-QUERY-PROJECTION-REFERENCE-NAMES-543",
 "title": "Search and autocomplete on a large graph answer within seconds of launch instead of after a per-launch read storm over the reference index",
 "family": "query-readiness",
 "sources": {
  "issues": [
   543
  ],
  "commits": [],
  "internal": ""
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::direct_projection_matches_literal_search_and_virtual_reference_names",
   "crates/tine-core/src/direct_projection_tests.rs::real_corpus_projection_converges_and_matches_task_query"
  ],
  "scenario": "query.projection-build-cost",
  "failBefore": "tine-agents/evidence/gh543-reader/fail-before.md (EXPLAIN QUERY PLAN of the v0.20.1 reader on the 10k projection: SCAN + TEMP B-TREE FOR ORDER BY, 1.37 s per 512-row batch, 65 s full drain; the plan guard rejects that exact query as its built-in counterexample; local-543-reopen-io.log shows the storm on a reopen with a finished index)"
 },
 "notes": "Blocking core operation, all platforms: every launch drains `navigation_reference_names_after` (tine-core `referenced_page_names`, called by page inventory / autocomplete) in 512-row keyset batches ordered by the joined page path, which no index serves, so each batch scanned and sorted all 600,000 postings (110,000 distinct rows at 10,000 pages): O(N^2), 215 batches x 1.3 s, ~11 GB of reads, 30-40 s on one tokio worker before Ctrl+K answered, on a reopen as well as after a cold build. Fix (tine-storage v0.20.2): both navigation readers page on the columns of an existing index with a row-value keyset; signatures and cursor tuples unchanged, only row order, and the one consumer builds an order-independent set. Full drain on that projection: 65 s -> 1.3 s. The plan-shape guard and the batch-edge paging test live in tine-storage (`paged_navigation_readers_use_an_index_range`, `navigation_reference_names_page_without_gaps_or_repeats`, certified with the v0.20.2 pin receipt); the tine-core tests listed here drive the real consumer against the reordered reader and the anonymized-corpus oracle, proving the answer is order-independent. Coverage update (2026-09-21): the first test was renamed direct_projection_matches_literal_search_and_virtual_reference_names when search stopped being fuzzy (08ab3dfd); its reference-name assertions are unchanged in purpose."
},
{
 "id": "REG-QUERY-PROJECTION-PARTIAL-ADMISSION-543",
 "title": "Ctrl+K and the (( picker answer while a cold or changed graph's query index is still being built, and the build is never restarted by a parsed snapshot or an edit",
 "family": "query-readiness",
 "sources": {
  "issues": [
   543
  ],
  "commits": [],
  "internal": ""
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query_plan_tests.rs::pre_ready_fallback_is_limited_to_ctrl_k_and_block_picker_routes",
   "crates/tine-core/src/query_plan_tests.rs::pre_ready_ctrl_k_ranks_all_page_names_and_aliases_before_limiting",
   "crates/tine-core/src/direct_projection_tests.rs::a_fresh_build_has_one_atomic_publication_and_no_active_reset",
   "crates/tine-core/src/direct_projection_tests.rs::edit_delete_and_rename_during_staged_build_reconcile_after_publication",
   "src/components/QuickSwitcher.test.tsx::says a rebuild is a rebuild while search is not ready"
  ],
  "scenario": "query.projection-build-cost",
  "failBefore": "tine-agents/evidence/gh543-partial-admission/fail-before.md (the three tine-core tests against the pre-packet admission predicate and warm driver: NotReady(Indexing) during the stream, an empty snapshot erasing the streamed pages, and drift falling to the whole-graph parse; the master warm-scale probe log showing the stream superseded by its own derived-map parse at 10,000 pages)"
 },
 "notes": "Blocking core operation, all platforms: Ctrl+K and the `((` picker on a cold or changed graph refused with NotReady(Indexing) until the whole graph was lowered (minutes at 10,000 pages on Windows), and the build itself was restarted by every path that parsed the whole graph beside the stream (page inventory, reference names, the warm task's own derived maps) and by any save during the stream (abandon -> needs_full -> parse fallback -> one huge transaction on a half-filled file). Fix: query capture is admitted while a warm validation or stream is in flight (answer over the committed rows, labelled by `query_index_progress` = (indexed, total)); `enqueue_full` is a no-op while a stream is open unless a rebuild is requested; the warm task retries the validation on drift instead of parsing; a turn deferred for want of an inventory no longer marks the worker failed (so the retry warm applies without a reset); the streaming writer gets the build page-cache budget and relaxed durability (tine-storage v0.20.3 `set_build_durability`) restored when the stream closes. Readiness (`ready_at`) is unchanged and still waits for the complete inventory. Superseded mechanism (2026-09-21): compact-projection P4 (1f80e35a, 13239e6b) retired answering over a partly built SQLite image. The user outcome is kept by a different mechanism: before the index is ready, Ctrl+K and the (( picker search one loaded parsed snapshot and rerun against the complete index when it is published; a cold or replacement build runs in an unpublished stage with one atomic publication, and edits during it reconcile afterwards instead of restarting it. Coverage now points at those tests; the fail-before evidence above is for the retired mechanism and is historical."
},
{
 "id": "REG-QUERY-PROJECTION-WARM-ANNOUNCE-543",
 "title": "A search typed while the query index is still being read for validation at launch retries as indexing instead of starting a second index build on the search thread",
 "family": "query-readiness",
 "sources": {
  "issues": [
   543
  ],
  "commits": [],
  "internal": "Windows diagnose543verify run 35225823831 on 6df34960"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::a_query_during_the_warm_inventory_read_retries_instead_of_repairing"
  ],
  "scenario": "query.projection-build-cost",
  "failBefore": "tine-agents/evidence/gh543-warm-announce/fail-before.log (the test with the announce replaced by a no-op: the search thread traced `dispatch: repairing`, enqueued its own warm, streamed Replacements(5) and answered Ok; assertion panicked with \"must report Indexing, got Ok\")"
 },
 "notes": "Blocking core operation, all platforms, worst on Windows: the open-path warm published nothing until it had read every page's bytes for its inventory (seconds on Linux, tens of seconds on Windows at 10,000 files), so a search landing in that window read the projection as idle and stale, took the repair route and ran a second warm on the query thread racing the open path's; the loser retried and fell to the whole-graph parse, whose snapshot then superseded the stream with one huge transaction. Windows run 35225823831 measured the 10k cold open at 587 s (150 s before packet 3), 35.7 GB written, switcher frozen at \"Indexing 240 of 10,000\". Fix: `warm_projection_cancellable` holds a `WarmInFlight` marker from before its first page read until it has enqueued; `progress_at` reports Working(Indexing) while any is held, so the query retries."
},
{
 "id": "REG-QUERY-PROJECTION-MUTATION-PUBLISHES-543",
 "title": "A page renamed, merged, rescued, trashed, migrated or written to a second file of the same journal day is findable as what it now is, instead of leaving search answering from what it was",
 "family": "query-readiness",
 "sources": {
  "issues": [
   543
  ],
  "commits": [],
  "internal": "convergence audits 3-6 on branch batch/gh543-convergence"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::a_rename_converges_search_instead_of_answering_with_the_old_name",
   "crates/tine-core/src/direct_projection_tests.rs::a_merge_and_a_rescue_tell_the_index_what_they_changed",
   "crates/tine-core/src/direct_projection_tests.rs::a_page_set_mutation_publishes_through_one_front_door",
   "crates/tine-core/src/direct_projection_tests.rs::a_resolved_duplicate_day_retires_the_stray_it_trashed",
   "crates/tine-core/src/direct_projection_tests.rs::a_trashed_journal_file_leaves_search",
   "crates/tine-core/src/direct_projection_tests.rs::a_journal_filename_migration_republishes_the_file_it_moved",
   "crates/tine-core/src/direct_projection_tests.rs::a_path_pinned_save_reaches_search",
   "crates/tine-core/src/direct_projection_tests.rs::a_merged_away_source_cannot_be_resurrected_by_a_repair",
   "crates/tine-core/src/direct_projection_tests.rs::a_pinned_save_cannot_be_undone_by_another_producer",
   "crates/tine-core/src/direct_projection_tests.rs::an_external_edit_to_a_duplicate_days_stray_reaches_search",
   "crates/tine-core/src/direct_projection_tests.rs::a_migrated_legacy_highlight_page_leaves_search",
   "crates/tine-core/src/direct_projection_tests.rs::a_moved_file_is_never_retired_by_a_lookup_that_can_come_back_empty",
   "crates/tine-core/src/direct_projection_tests.rs::a_page_cache_by_name_map_is_not_a_lookup",
   "crates/tine-core/src/direct_projection_tests.rs::a_migration_publishes_a_move_whose_durability_step_failed",
   "crates/tine-core/src/model_tests.rs::a_failed_merge_puts_the_source_back_in_the_index_too",
   "crates/tine-core/src/model_tests.rs::a_failed_merge_that_cannot_restore_its_source_publishes_nothing",
   "crates/tine-core/src/model_tests.rs::a_journal_filename_migration_serializes_against_other_writers"
  ],
  "scenario": "query.projection-build-cost",
  "failBefore": "tine-agents/evidence/gh543-convergence/round6/necessity.log (each sixth-audit production fix reverted alone at abd0536f: A6-N1, A6-N2, A6-N3 with its class guard, A6-N4 and A6-N5 each red, the rest of the suite green) and .../round7/necessity.log (A7-N1, A7-N2 and A7-N3 the same way, A7-N3 showing page_order=2 pre-fix). Rounds 3-5 were proved the same way per fix during implementation."
 },
 "notes": "Blocking core operation, all platforms: from v0.6.983 search answers from the SQLite projection rather than an in-memory walk, and a mutation that changed what the graph contains without publishing a producer left the index with a committed image nothing was converging. Partial admission then made the answer SUCCEED, and a successful answer never reaches the repair a refusal would start, so it never corrected itself: a rename kept offering the old name, a merge returned both pages with one of them in the trash, a rescued file was never findable, a trashed or filename-migrated journal answered twice, a path-pinned save and an externally delivered edit to a duplicate-day file reached the index never, and a migrated PDF highlight page answered from the trash. Fix: every mutation publishes through one front door - cache_upsert for a file this process rewrote, cache_remove_path for a file that left, direct_projection_publish_page_set for a multi-page set under one queue lock - the parsed whole-graph cache is treated as the authoritative producer it is (so a repair cannot republish a page a mutation removed), the journal filename migration holds the identity gate across its parse and publication, and a merge rollback compensates only when the restore actually put the file back. Class guard: a_moved_file_is_never_retired_by_a_lookup_that_can_come_back_empty fails on a retirement by name that hands over no entry. Seventh audit: `graph_text_move_noreplace` renames BEFORE its fallible durability and identity work, so an Err from it does not mean the file stayed put - the merge rollback now publishes whatever the source path actually holds afterwards, and the journal migration asks the filesystem whether each move landed, publishes the moves that did happen on every exit, and no longer reports Ok(0) for a file it moved. An unchanged redelivery of a duplicate-day file publishes nothing, matching the suppression the ordinary file path has always had."
},
{
 "id": "REG-QUERY-PROJECTION-FAILED-MUTATION-PUBLISHES-543",
 "title": "An operation that fails AFTER its file move already committed still leaves search describing the files that are actually on disk",
 "family": "query-readiness",
 "sources": {
  "issues": [543],
  "commits": [],
  "internal": ""
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::a_merge_whose_durability_fails_after_the_move_stops_serving_the_source",
   "crates/tine-core/src/direct_projection_tests.rs::a_rescue_whose_durability_fails_after_the_move_answers_at_the_new_name",
   "crates/tine-core/src/direct_projection_tests.rs::a_delete_whose_durability_fails_after_the_move_stops_serving_the_page",
   "crates/tine-core/src/direct_projection_tests.rs::a_journal_trash_whose_durability_fails_after_the_move_stops_serving_the_file",
   "crates/tine-core/src/model_tests.rs::a_failed_rename_publishes_the_destination_that_survived_it",
   "crates/tine-core/src/model_tests.rs::a_failed_duplicate_resolution_publishes_the_occupant_it_could_not_remove"
  ],
  "scenario": "query.failed-mutation-convergence",
  "failBefore": "tine-agents/evidence/gh543-convergence/round8/necessity.log (each fix disabled in turn on the round-7 head 93dd3c14: the four move regressions fail on \"retired source still searchable\" / \"search must describe the live paths\", the rename regression expects [\"Beta\"] and observes [], the conflict regression finds no producer for the surviving occupant; all six pass with the fixes restored)"
 },
 "notes": "Blocking core operation, and the error-path half of REG-QUERY-PROJECTION-MUTATION-PUBLISHES-543. graph_text_move_noreplace performs the no-clobber rename FIRST and only then makes the chain durable and finishes identity, so an Err from it means the file HAS moved and a later step failed. Every caller that exited on that error skipped its ordinary publication: merge staging, file rescue, page deletion and journal trash each left the vanished source answering searches (rescue additionally kept returning the retired page name instead of the live one); a rename whose compensation preserved an external replacement at the destination left those real, readable bytes unindexed; and a failed duplicate-day resolution kept describing the stray text it could no longer remove. In each case the projection reported itself idle, ready and validated at generation 0 with nothing queued, so nothing was ever going to correct it - a successful search served from an image with no convergence work, which is the other half of GH #543's indexing-forever. Fix: the shared move primitive reconciles the actual eligible source and destination paths when its own post-rename work fails - readable bytes through parse_exact_page/cache_upsert, confirmed absence through cache_remove_path - and still returns the original error; rename_page and resolve_duplicate_journal_day reconcile their final edit paths after failed compensation. graph_text_move_to_trash needed no separate fix because it delegates to the same primitive. The second rename site, graph_text_move_editor_recovery_noreplace, has the same partial-commit shape but runs only inside recover_interrupted_editor_publications at graph open, before any index exists, so it cannot strand a stale image; it is deliberately not covered here."
},
{
 "id": "REG-QUERY-PROJECTION-FAILED-SAVE-PUBLISHES-543",
 "title": "A save that fails after it has already changed the file still leaves search finding the text that is really on disk",
 "family": "query-readiness",
 "sources": {
  "issues": [543],
  "commits": [],
  "internal": ""
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_tests.rs::a_save_that_fails_after_editor_publication_still_indexes_the_live_text",
   "crates/tine-core/src/model_tests.rs::a_save_that_fails_at_the_editor_final_read_still_indexes_the_live_text",
   "crates/tine-core/src/model_tests.rs::a_save_that_loses_editor_publication_to_an_external_owner_still_indexes_the_live_text"
  ],
  "scenario": "query.failed-save-convergence",
  "failBefore": "tine-agents/evidence/gh543-convergence/round10/necessity.log (the reconciliation disabled in commit_editor_write on the integrated head 2a77c84d: all three regressions fail at the live-text search assertion after their disk and error assertions pass; all three pass restored)"
 },
 "notes": "Blocking core operation, and the ordinary-save sibling of REG-QUERY-PROJECTION-FAILED-MUTATION-PUBLISHES-543 - reached far more often than a failed move, because it is the editor path every keystroke eventually takes. An editor write that changed the file and then failed propagated its error through write_page's `?` before cache_upsert, so nothing published: search went on serving the pre-save text and could not find the text now on disk, with the projection idle, ready and validated at generation 0 and nothing queued. Worse for the variant that fails after the inner commit returns: the leftover self-write marker made the FIRST checked watcher delivery a no-op (sync_file_checked returns Ok(None) because the marker matches the new bytes), and the native reconciler treats Ok(None) as success and advances its file stamp, so the one delivery that would have repaired it was consumed. Third variant: an external writer wins the path after publication, production raises conflict.replace_post_publication, and that real readable winner reached no producer either. Fix: commit_editor_write reconciles the final path through the same front door the failed-move fix uses (reconcile_failed_graph_text_paths) on every error from its commit/final-read body, clearing the marker and publishing the actual readable owner or confirmed absence, and returns the original error unchanged; the success path and the conflict/no-clobber protocol are untouched. The DTO is never published merely because its write was attempted - the external-winner regression pins that by rejecting the attempted editor text. Assertion change recorded: the two foreign_destination_* tests previously required a failed save to advance NO cache generation and to leave the stale cached document standing; that assertion was the defect written down, and they now require the surviving foreign winner's document, which still fails if the editor's own bytes are published."
},
{
 "id": "REG-FRIENDLY-BLOCK-CANDIDATE-BOUND-001",
 "title": "Searching a large graph reads only the blocks that can match the typed needle, instead of every block in the graph for every keystroke",
 "family": "waited-path-query-cost",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "2026-09-17 SQL full-scan census, first item; before/after measured with read_latency_probe_reports_per_surface_timings on the 10,000-page probe graph"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/friendly_tests.rs::a_selective_needle_ranks_only_its_candidates",
   "crates/tine-core/src/query/friendly_tests.rs::the_candidate_bound_drops_no_block_the_exact_predicate_admits"
  ],
  "scenario": "friendly-search.block-cost-scales-with-selectivity",
  "failBefore": "tine-agents/evidence/bounded-reads/fail-before.log (bound neutralised: one matching block, but the statement ranked all 205 rows of the page; assertion \"one candidate, but the statement ranked 205 rows of a 205-block page\"); before/after pair from one build in tine-agents/evidence/bounded-reads/ctrl-k-before-after.md (unbounded 4372 ms zero-hit and 4524 ms at 101 hits, bounded 1677 ms and 1721 ms, both runs exit 0)"
 },
 "notes": "Blocking stateful UX on large graphs. The Friendly read scanned every block in scope TWICE per keystroke: the block-results CTE carried no content condition, and the page-by-content membership CTE carried none either, so both materialised every block, called the rank program on each, and let the outer select keep the matches. Cost did not depend on the needle. On the 10,000-page probe graph (600k blocks) the median Ctrl+K search was 4372 ms for a needle matching nothing and 4524 ms for one matching 101 blocks. The fix derives a sound candidate needle from the branch predicate (VisibleContent, Contains or Phrase, first whitespace-free run of at least 3 characters, through And but never Or/Not/Regex/Fuzzy) and makes the trigram index over search_substring_fts the DRIVING table of both reads, through one shared helper so a future third reader cannot miss it. Driving is the load-bearing part: the same index used as a WHERE-clause filter left SCAN b in the query plan and only skipped the rank call, which measured as no useful saving. Measured after: 1677 ms zero-hit, 1721 ms at 101 hits, and no needle slower than its baseline by more than run-to-run noise (worst +53 ms, ranges overlapping). Above BLOCK_CANDIDATE_CAP = 20000 candidates the helper returns the unbounded scan, because driving a 600k-row candidate set measured 2045 ms against 1246 ms scanned; the cap probe costs 0-4 ms at every selectivity because it counts with LIMIT cap+1, which a bare COUNT would not. The bound only narrows which rows are asked: the exact predicate still decides the answer in SQL and is re-checked per row in Rust, so a wrong bound shows up as a missing result rather than a wrong one. A needle too short to index, or any shape supplying none, stays unbounded and is answered exactly as before. One semantic delta: the driven path reaches blocks through the text index, so a block carrying no block_text row is omitted rather than surfaced through missing_text = 1. No search result changes, because a textless block cannot satisfy a Contains predicate; the consequence is that the Friendly corruption tripwire for that shape now fires only on the scan path. The page-content CTE is an inner join and never had that arm."
},
{
 "id": "REG-QUICKSWITCH-REFERENCE-NAMES-MEMO-001",
 "title": "Page-link autocomplete on a large graph answers from a remembered reference-name set instead of re-reading the whole reference index on every lookup",
 "family": "waited-path-query-cost",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "2026-09-17 quick-switcher full-scan follow-up to REG-FRIENDLY-BLOCK-CANDIDATE-BOUND-001; before/after measured with read_latency_probe_reports_per_surface_timings on the 10,000-page probe graph"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::referenced_names_are_read_once_per_cache_generation"
  ],
  "scenario": "quick-switch.reference-names-read-once-per-generation",
  "failBefore": "tine-agents/evidence/quickswitch-scan/ — memo reverted with the test kept: FAILED, 0 passed; 1 failed, on the assertion that a second call at one cache generation must not re-drain the projection; restored tree: ok, 1 passed; 0 failed. Probe pair on the 10,000-page graph: before, quick_switch 1408-1430 ms flat across 8 needles and producer:referenced 1388 ms; after, 9-12 ms per needle and producer:referenced 0 ms for the same 10,010 names."
 },
 "notes": "Blocking stateful UX on large graphs. quick_switch is NOT Ctrl+K (that is run_graph_search_latest_displayed); it backs the in-editor [[..]] page picker (limit 100, one call per debounced keystroke), the quick capture picker, the query sheet's page field and Settings. legacy_page_search_entries takes its three candidate sets BY VALUE before the needle is consulted, so the cost could not live in the match loop; a per-producer probe split the flat ~1400 ms exactly: list_pages 0 ms (10,001 entries), page_aliases_with_owners 0 ms, referenced_page_names 1388 ms. The projection answers referenced names by draining navigation_reference_names_after, whose DISTINCT spans (source_page_id, path, raw_name, normalized_name) while tine-core folds by page_key(raw_name) alone: 110,000 rows transported to yield 10,010 names, measured at 1.29 s against the real fixture. Nothing memoized it, although the sibling list_pages has had exactly this memo for exactly this reason. The fix adds referenced_names_cache keyed on cache_gen, storing the set's digest beside the names so a hit does not re-hash 10k strings, and memoizes ONLY the projection path: the parser fallback deliberately answers empty when the page cache is cold, so caching that would make 'this graph references no pages' stick for a whole generation and autocomplete would silently offer nothing. A read is memoized only if the generation still matches the one it was keyed on, so a set recorded under a stale key cannot outlive the edit that invalidated it. The memo is therefore exactly as fresh as the read it replaces, which already refuses to answer at any other generation; a save bumps cache_gen through cache_upsert_inner, so a newly linked page appears on the next lookup. Control surfaces unchanged (graph_search 1637/4516/4448/1711 ms against 1677/4601/4495/1721 baseline; search steady at 2.7-2.9 s). Two cheaper-looking alternatives were measured and REJECTED before being written: raising the cursor batch from 512 to 16384 cut statements 215 to 7 and left the time unchanged (1.293 s vs 1.352 s), because the cost is the scan and not the round trips; and a storage reader returning distinct names only measured 2.57-16.85 s, 2-13x SLOWER, because raw_name carries no index so its cursor predicate cannot seek. A covering index was investigated as the way to fix the first call too. The shape recorded here first - reference_postings(target_type, reference_kind, normalized_name, source_page_id, raw_name), credited with 1.29 s to 0.084 s - was WRONG, and the error is worth keeping: measured against the SHIPPED statement it runs 1.765 s, slower than the 1.276 s baseline, because reference_kind <= 4 is a range predicate sitting ahead of the sort keys, which costs USE TEMP B-TREE FOR ORDER BY and makes the cursor statement abandon the new index entirely. The 0.084 s had been measured on a hand-written paraphrase of the reader rather than on NAVIGATION_REFERENCE_NAMES_FIRST_SQL itself. Corrected 2026-09-17: Martin authorized the forced projection rebuild while the schema is still being tuned, so tine-storage v0.24.0 takes reference_postings(normalized_name, raw_name, reference_kind) WHERE target_type = 0 TOGETHER WITH a narrower statement that drops the pages join and source_page_id, measured 1.276 s to 0.022 s (110,000 rows to 10,010) with the returned name set proven identical by set comparison. Remaining limit, stated plainly: the first lookup after any edit still pays the full drain, because a save bumps the generation. Priming the memo at generation publish was considered and rejected, since it would put a 1.4 s background drain behind every save on a large graph."
},
{
 "id": "REG-QUICKSWITCH-REFERENCE-NAMES-INDEX-001",
 "title": "The first page-link lookup after an edit names every linked page from a covering index instead of draining one row per page-and-spelling",
 "family": "waited-path-query-cost",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "2026-09-17 follow-up to REG-QUICKSWITCH-REFERENCE-NAMES-MEMO-001, which fixed every repeat lookup but left the first one after each edit paying the full drain; Martin authorized the forced projection rebuild that makes an index affordable (\"we are still tuning the sqlite thing; it's fine to force a rebuild at an update\")"
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core-storage",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/projection_producer_census.rs::g_d_tine_storage_write_boundaries_are_pinned",
   "crates/tine-core/src/query/friendly_tests.rs::friendly_main_reader_matches_the_independent_walk_for_rank_and_identity_shapes"
  ],
  "scenario": "quick-switch.reference-names-covering-index",
  "failBefore": "The behaviour is proven in the pinned crate, not in this repo: the plan guard paged_navigation_readers_use_an_index_range FAILS against the pre-change statement, reporting plan [\"SCAN r USING INDEX reference_postings_normalized_name_idx\", \"USE TEMP B-TREE FOR DISTINCT\"] where it now requires SCAN/SEARCH ... USING COVERING INDEX reference_postings_navigation_names_idx. Measured on a faithful copy of the 10,000-page probe projection (1.2M postings, 1.55 GB): baseline as shipped 1.276 s; covering index alone, statement unchanged, 0.390 s; index plus dropping the pages join 0.152 s; index keyed to the DISTINCT with the join and page id dropped 0.022 s (110,000 rows in 215 statements to 10,010 in 20). Batch size stops mattering (0.022 s at both 512 and 4096), which is the proof the cost was always the scan. Name-set equality checked element-wise, not by count: 10,010 distinct raw_name both ways, zero elements on either side only. Gating runs archived at tine-agents/evidence/quickswitch-index/ (README names what each proves): 48 failures with the index unregistered on the ordinary creation path, then 3, then 1, then 218 passed and 0 failed with cargo fmt --check clean and api.txt byte-identical."
 },
 "notes": "Blocking stateful UX on large graphs; the residue left by REG-QUICKSWITCH-REFERENCE-NAMES-MEMO-001. The memo answers every lookup within one cache generation, but a save bumps cache_gen, so the first [[ lookup after any edit re-drained the projection for ~1.3 s. Fixed in the projection rather than in tine-core: reference_postings_navigation_names_idx on (normalized_name, raw_name, reference_kind) WHERE target_type = 0, plus a statement narrowed to the DISTINCT it actually wants -- the pages join and source_page_id are dropped because the only consumer folds by page_key(raw_name) and reads no other column. The index ALONE is just 3.3x (1.276 s to 0.390 s); the win needs index and statement together. COLUMN ORDER IS LOAD-BEARING: reference_kind first puts a range predicate ahead of the sort keys, costs USE TEMP B-TREE FOR ORDER BY, and measures 1.765 s -- slower than no index at all, which is exactly the shape an earlier note recommended on the strength of a hand-written paraphrase of the reader; the standing lesson is to measure the literal shipped SQL constant. Cost of adoption, accepted by Martin explicitly: SQLITE_SCHEMA_VERSION 28 to 29 makes validate_graph_projection_schema (the Direct reader's schema check; validate_schema_and_claim has no caller in Tine) refuse every existing projection, so each user's first launch after the update rebuilds it once. Unit cost: the index is 88.4 MB on a 1.64 GB projection (5.4%, measured with dbstat rather than by differencing a vacuumed file -- the differencing method overstated it as +218 MB by counting free pages the VACUUM reclaimed), ~1.8 s to build, and per edit the reference_postings replacement step of a save costs +0.007 ms on a 1-posting page and +0.057 ms on a 60-posting page (interleaved A/B/A/B over 4 rounds x 60 cycles; a first non-interleaved run showed the index making saves FASTER, which is impossible, and was cache/WAL ordering noise). Registering an index in tine-storage takes NINE sites, not the seven an earlier note listed: the two it missed were the execute_batch DDL string in initialize_graph_projection_schema -- the ordinary creation path, whose omission left EXPECTED_INDEXES demanding an index nothing built and failed 48 tests with 'no such index' -- and the Direct query metadata marker in SOURCE_REVISIONS_DDL, which advances 28 to 29 so an older Direct reader, which validates that table's DDL text but tolerates extra indexes, rebuilds a newer cache instead of accepting it. api.txt needed no bless: it records the public surface at name granularity, so dropping two public fields left it byte-identical; that is not evidence the change is non-breaking, since cargo semver-checks reads rustdoc and is what forces v0.24.0. Known remaining sibling, deliberately not taken here: NAVIGATION_ALIASES_FIRST_SQL still plans a pages join plus a temp B-tree for DISTINCT and another for the last ORDER BY term. It is a follow-up, not a blocker, and page_aliases_with_owners genuinely reads owner_path, so the join may not be droppable and this fix may not transfer. One real regression was caught by the rust gate and is worth recording, because the measurement that cleared the change could not see it: a page that exists only by reference can be spelled several ways ([[Ghost Page]] on one page, [[ghost page]] on another), and TWO paths answer which spelling is shown -- DirectProjection::referenced_page_names and the Friendly SQL reader's reference_choices CTE. My set-equality proof compared the SET of raw names returned by storage, which is genuinely unchanged, so it was blind to which spelling survives the per-name fold. The old navigation statement ordered by (normalized_name, source_page_id, raw_name) and the fold takes first-wins; the Friendly CTE ordered by (p.path, raw_name, ...). Those two rules agreed only by ACCIDENT on the fixture, because the opaque source_page_id blob happened to rank the same row first as the alphabetically first path. Dropping the pages join and source_page_id -- the very thing that bought 1.276 s to 0.022 s -- broke the coincidence and friendly_main_reader_matches_the_independent_walk_for_rank_and_identity_shapes failed with the two paths naming the same page differently. Fixed by making the rule explicit and path-free on both sides: the lexicographically smallest raw spelling wins, which each path can compute from the name alone (friendly.rs reference_choices now orders by r.raw_name first; the navigation read already yields that order from the covering index). The equivalence class is provably the same on both sides: the stored normalized_name IS refs::page_key(raw_name) (direct_projection.rs:3416), and the CTE partitions by normalized_name where the fold keys by page_key(raw_name). The rule is now written at the friendly.rs CTE as a cross-path contract naming the asserting test, since nothing in docs/adr or the storage contract had ever recorded why p.path led that ordering. PROVED IN THE PROJECTION CRATE, cited here as provenance rather than as coverage because this repository's catalog gate can only resolve tests it runs: tine-storage@v0.24.0 src/sqlite_graph_projection.rs carries paged_navigation_readers_use_an_index_range, navigation_reference_names_page_without_gaps_or_repeats and fresh_build_defers_secondary_indexes_and_restores_the_exact_schema. The in-repo half of the claim is the two tests listed above; the catalog has no spelling for another repository's tests, which is a real gap and not a judgement that those three do not matter."
},

{
 "id": "REG-GRAPH-ROOT-UNRESOLVABLE-VOLUME-561",
 "title": "A graph on an encrypted or other user-space volume opens and saves, even when the driver cannot canonicalize a path",
 "family": "graph-open",
 "sources": {
  "issues": [
   561
  ],
  "commits": []
 },
 "platforms": [
  "windows"
 ],
 "layer": "rust-core-storage",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/directory_identity.rs::the_fallback_absolutises_what_exists_and_refuses_what_does_not",
   "crates/tine-core/src/directory_identity.rs::a_resolvable_path_keeps_the_filesystems_own_canonical_answer",
   "crates/tine-core/src/directory_identity.rs::a_missing_path_keeps_the_filesystems_own_error"
  ],
  "scenario": null,
  "failBefore": "Structural, not same-platform. The reporter's screenshot carries the exact producer text of canonical_graph_root (src-tauri/src/state.rs): \"couldn't resolve graph path: Z:\\\\tine\\\\tine-demo\\\\tine-demo: O volume nao contem um sistema de arquivos reconhecido ... (os error 1005)\" = ERROR_UNRECOGNIZED_VOLUME from GetFinalPathNameByHandle, which is what std::fs::canonicalize calls on Windows. No Linux filesystem refuses that call, so the fallback arm is proven directly by unit test rather than by reproducing the refusal; an end-to-end Cryptomator/VeraCrypt confirmation on Windows is still outstanding."
 },
 "notes": "Creating or opening a graph inside a Cryptomator (or VeraCrypt) vault on Windows failed with 'Couldn't create the graph. (couldn't resolve graph path ... os error 1005)', after the graph folders had already been created - so it read as a half-made graph rather than an unsupported call. Dokany/WinFsp-backed volumes are allowed not to implement GetFinalPathNameByHandle. Two sites depended on it: canonical_graph_root (the command-layer root key, which produced the reported message) and open_projection_root_nofollow (crates/tine-core/src/model/projection_fs.rs), whose failure is swallowed with .ok() - so even had the first succeeded, the graph would have opened with no projection root and every Direct save would have failed. Both now call tine_core::directory_identity::canonical_existing_path, which prefers the filesystem's canonical answer and falls back to an absolute spelling of a path that exists. Canonicalization was never the safety boundary on this path: the graph tree is opened no-follow through cap_std, which is what refuses symlinks and reparse points, and that is unchanged. One accepted consequence: on a volume that cannot canonicalize, two different spellings of one root (differing case, or a symlinked ancestor) are two different root keys rather than one."
},
{
 "id": "REG-GRAPH-TEXT-HARDLINK-SAVE-571",
 "title": "A page that is also hard-linked elsewhere (git-annex, deduplication) saves instead of failing precheck.resource_alias",
 "family": "direct-save",
 "sources": {
  "issues": [
   571,
   555
  ],
  "commits": []
 },
 "platforms": [
  "linux",
  "macos",
  "windows"
 ],
 "layer": "rust-core-storage",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_tests.rs::a_hard_link_outside_graph_text_scope_does_not_block_a_save",
   "crates/tine-core/src/model_tests.rs::an_in_graph_alias_no_longer_blocks_a_resave",
   "crates/tine-core/src/model_tests.rs::existing_save_local_proofs_cover_hardlinks_and_index_uncertainty"
  ],
  "scenario": null,
  "failBefore": "a_hard_link_outside_graph_text_scope_does_not_block_a_save on the pre-fix tree: 'annex: graph text files alias one physical resource: pages/Target.md has link count 2'. The refusal fired at save_page, not at load, so reading an annexed graph always worked and only editing was blocked."
 },
 "notes": "Every Direct save refused when the target file had more than one link. The rule (validate_graph_text_single_link) compared the raw link count to 1, which cannot distinguish the case it meant to catch - two GRAPH-TEXT paths on one inode, where temp+rename publication leaves the other page holding stale bytes - from links Tine has no interest in: git-annex annex.thin links every page into .git/annex/objects/..., which graph-text scope never descends into, and users hard-link or deduplicate pages outside the graph. Martin decided (2026-09-20) to allow hard links and keep the in-graph alias refusal. Implementation note: the precise check needs the complete identity index, and an ordinary resave must never build one (GH #267 keeps saves target-local, O(1) not O(graph)), so the refusal now lives on page creation, where the index is already in hand (validate_graph_text_resource_alias), and the rename transaction's move source plus editor recovery keep the raw link count as an interim rule. Accepted consequence, asserted in an_in_graph_alias_no_longer_blocks_a_resave: if two graph pages really do share an inode, editing one leaves the other with its old bytes rather than refusing. Remaining gap closed by REG-GRAPH-TEXT-HARDLINK-RENAME-571: rename, editor recovery and the exact watcher feed."
},
{
 "id": "REG-PDF-EXPORT-MOBILE-560",
 "title": "Export to PDF is not offered on Android or iOS, and explains itself when reached by command, instead of silently doing nothing",
 "family": "android-platform-lifecycle",
 "sources": {
  "issues": [
   560
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "ios"
 ],
 "layer": "frontend",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/pdfExportPlatform.test.ts::opens the export dialog on desktop",
   "src/pdfExportPlatform.test.ts::explains itself instead of opening a dead dialog on android",
   "src/pdfExportPlatform.test.ts::explains itself instead of opening a dead dialog on ios"
  ],
  "scenario": null,
  "failBefore": "Both mobile cases fail on the pre-fix tree: openPdfExport set pdfExportPage to 'Alpha' on android and ios (expected null) and raised no toast, so the dialog opened and the export then did nothing. The desktop case passes before and after."
 },
 "notes": "A mobile WebView cannot print, so print.ts's win.print() returned normally and nothing happened: no throw, hence no error toast, afterprint never fired, and the hidden frame was reclaimed by the 60 s fallback. The cause differs per platform and was read from upstream source, not inferred from behaviour. Android: AwPrintRenderFrameHelperDelegate::IsScriptedPrintEnabled returns false, so scripted printing is disabled in the WebView renderer (Chromium issue 40342444 tracks enabling it). iOS: WebKit does not guard printing - WebChromeClient::print has no PLATFORM(IOS_FAMILY) guard and UIDelegate::UIClient::printFrame has no PLATFORM(MAC) guard - but it forwards only into the private WKUIDelegate SPI _webView:printFrame:, which wry 0.55.1 does not implement (its own print() uses macOS-only printOperationWithPrintInfo:). So iOS is unimplemented rather than impossible, and a future host-side print path would lift the gate. Martin decided (2026-09-21) to gate mobile and explain, rather than implement Android printing now. The gate lives in openPdfExport, the single funnel for the page context menu, the Export current page to PDF command and its keybinding; the context-menu item is additionally hidden so no dead control is offered. Not verified on a device: no Android or iOS hardware here, so the platform claims rest on upstream source plus the jsdom gate test."
},
{
 "id": "REG-PUBLISH-ZERO-PUBLIC-560",
 "title": "Exporting a graph with no public pages says why nothing was exported instead of reporting a bare zero",
 "family": "publication",
 "sources": {
  "issues": [
   560
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "frontend-and-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/publish_tests.rs::publish_exports_zero_pages_and_bare_templates_when_no_page_is_public"
  ],
  "scenario": null,
  "failBefore": "Not a behaviour fix: the export was already correct, and the Rust test characterises it rather than failing before. The defect was the report - Settings showed 'Exported 0 pages to <dir>' with no reason, which reads as a broken button. The message change itself is not pinned by a test; the core behaviour it describes is."
 },
 "notes": "Reported as an Android failure, but it is neither Android-specific nor a fault. Publication is the public-page capability: only pages carrying public:: true are exported unless :publishing/all-pages-public? is set, matching Logseq (publish.rs, all_pages_public defaults to false). A graph with no public page therefore exports zero pages and writes the bare index/pages templates the reporter saw. The report was diagnosed by elimination: the UI renders 'Exported N pages' only when publishHtml RESOLVES, so a successful zero excludes both unready-projection paths, which raise errors instead - verified by running them, giving ProjectionUnavailable with no projection attached and NotReady(Busy) with a projection attached but not yet built. The Guide already documents the public:: rule (templates/research-document.md, templates/files-external-edits-backups.md); the gap was in-app, so the zero case now names the rule."
},
{
 "id": "REG-PAGE-HEADER-MIDEDIT-SAVE-546",
 "title": "Editing page properties does not raise a save-failure toast while they are still being typed",
 "family": "direct-files",
 "sources": {
  "issues": [
   546
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "frontend-and-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/store.test.ts::reconciles the store with the page header the save just wrote to disk",
   "crates/tine-core/tests/gh546_page_header_mid_edit.rs::a_mid_edit_page_header_bullet_is_refused_with_the_typed_data_preservation_code"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #546. Page-header properties authored as the flagless properties-only first bullet are folded into pre_block by the frontend projection (GH #198). That fold rewrites the file but was never reconciled into the store, so from that save on the store held the header as an ordinary root with an empty preBlock while the file held it as a preamble. The next keystroke leaving the bullet transiently not properties-only then proposed pre_block:null plus a property-bearing outline block - exactly what the GH #163 data-preservation firewall refuses. That refusal is built by projection_semantic_refusal, which carries no typed DirectSaveError, so direct_save_failure_code falls through to its Unknown default; 'unknown' is absent from the frontend's non-retryable denylist, so the save was retried at 100 ms and 300 ms and the third failure pushed the reporter's red toast, verbatim, while the block was still being edited. A successful save now marks the folded root as the page header (compare-and-swap on the exact folded text), which hands the page to the header path: it folds byte-identically and defers while invalid instead of contradicting the file. Separately, autosave no longer reports an invalid header at all - it fires shortly after a typing pause, so a half-written header is the ordinary state of an unfinished edit - and the validation message moved to where the header editor closes, which is the reporter's stated expectation. Deferring is safe only because the store now knows what the file holds: on a page whose file has no preamble the same shape still saves as outline, so an unconditional defer would have made such a page never save."
},
{
 "id": "REG-RENAME-PORTABLE-LISTING-QUADRATIC-406",
 "title": "Renaming a page costs time linear in the graph, not in referrers times folder size",
 "family": "page-rename",
 "sources": {
  "issues": [
   406
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "native",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_rename_cost_tests.rs::rename_lists_each_directory_a_bounded_number_of_times_regardless_of_referrers",
   "crates/tine-core/src/model_rename_cost_tests.rs::a_case_twin_appearing_mid_rename_still_refuses_and_rolls_back",
   "crates/tine-core/src/model_rename_cost_tests.rs::a_referrer_twin_appearing_mid_rename_leaves_the_rename_all_or_nothing",
   "crates/tine-core/tests/gh406_rename_cost.rs"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #406. The reporter's timings grew faster than the graph. Cause: the portable case/NFC name check (validate_graph_text_portable_aliases_path_local) listed every entry of each ancestor directory on EVERY write, so the rename's write phase cost rewritten files x pages/ entries - measured at 0.16 ms per write on a 500-file folder, 2.2 ms at 8,000 and 4.5 ms at 16,000, about 80-85% of the write phase. The rename transaction now lists each directory once (PortableListingBatch) for its in-place referrer rewrites; a file creation (the moved page) still checks live, because a published twin cannot always be withdrawn on rollback. Fail-before: 2 vs 30 referrers listed pages/ 16 vs 72 times; after, equal. The mid-rename twin tests arm the write hook at a later mutation so the batch is provably stale; without the live creation check the destination published next to its twin and the rollback could not withdraw it (necessity run). Optimized Linux fixture, 8,000 pages: 800 referrers 2,637 ms -> 844 ms; 500/2k/8k pages 16x the pages cost 17.7x the time (was 29x). Not addressed here: every rename still reads every graph file (~5-6x the raw read floor), and the frontend reloads the whole graph afterwards (tracked with GH #535's targeted refresh). The reporter's negative follow-up on the earlier partial fix (REG-DIRECT-RENAME-LITERAL-SCANNER-406) stands; Windows latency is unmeasured."
},
{
 "id": "REG-NUMBERED-FIRST-BULLET-HEADER-540",
 "title": "An empty numbered item as a page's first bullet stays a list item and the page keeps saving",
 "family": "page-header-save",
 "sources": {
  "issues": [
   540,
   546
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "native",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/tests/gh540_numbered_first_bullet.rs::an_empty_numbered_first_bullet_stays_a_list_item_and_the_page_keeps_saving",
   "crates/tine-core/tests/gh540_numbered_first_bullet.rs::a_properties_only_first_bullet_of_page_properties_still_becomes_the_header",
   "src/store.test.ts::stays a list item, so typing its text keeps saving",
   "src/store.test.ts::keeps the block-scoped property list identical to the Rust promotion rule"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #540, DanTremonti's report (v0.6.984, Linux): a new namespaced page, first bullet made a numbered item, then its text typed; the file stayed the 32-byte `logseq.order-list-type:: number` and every later save was refused in 0-4 ms as `reason code: unknown` until restart. Cause: the numbered-list command leaves an empty first bullet whose raw is exactly that property line, and both the frontend fold (projectPageDto) and the Rust promotion (first_root_is_promotable_page_header) took a properties-only first bullet for page-header properties (the GH #198 canonicalization), writing it as the unbulleted preamble. The typed text then moved that 'header' property back into the outline, which the GH #163 firewall refuses with an untyped error. On this branch before the fix the GH #546 reconciliation made it worse: the store adopted the fold, so the typed text was never serialized at all. OG keeps such a bullet a bullet because the property sits under an empty title line and its serializer promotes only a first block whose first line holds `:: ` (file/core.cljs transform-content); Tine's raw has no empty title line, so a block-scoped key (id, heading, collapsed, background-color, logseq.order-list-type) now disqualifies promotion on both sides, kept identical by a source guard. Reproduced byte-for-byte in core before the fix; the Rust and store numbered-item tests each fail without their half of the fix (the alias test is the GH #198 neighbour and passes either way). Not addressed: the firewall refusal is still untyped (`unknown`) and retried, so any other route into it still reads as a stuck save; EllisMorrow's original multi-day report remains unexplained."
},
{
 "id": "REG-REFUSED-SAVE-NO-RETRY-535",
 "title": "A save the data-preservation firewall refuses is reported once, not retried, and offers the unsaved draft",
 "family": "page-header-save",
 "sources": {
  "issues": [
   535,
   546,
   540
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "native",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/tests/gh546_page_header_mid_edit.rs::a_mid_edit_page_header_bullet_is_refused_with_the_typed_data_preservation_code",
   "crates/tine-core/src/model_tests.rs::a_data_preservation_refusal_has_its_own_no_retry_code",
   "src/store.test.ts::is not retried, says so once, offers the draft, and clears when the page saves",
   "src/persistence.test.ts::does not retry failures a retry cannot change",
   "src-tauri/src/commands/direct_save_helpers.rs::a_data_preservation_refusal_reaches_the_app_as_one"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Every ProjectionSemanticRefusal (merge markers, a dropped preamble, a page-header property moved into the outline, an Org file that cannot round-trip) reached the frontend as `reason code: unknown`: retried at 100/300 ms, then a transient 'after 3 tries' toast, then a page that silently never saved while each further edit repeated the cycle (GH #546 screenshot; DanTremonti's GH #540 report; the remaining gap after GH #535). The refusal is a verdict on the draft's content, so direct_save_failure_code now maps that marker type to `refused.data_preservation` (no retry) in one arm covering every producer, and the frontend shows one sticky, deduplicated message per page with a Review unsaved action (the close-path recovery panel: Copy draft, Open page), dismissed when the page saves or leaves the working set. Fail-before: the core tests return `unknown`; the store test sees 6 save attempts instead of 4 without the frontend policy change. The firewall itself is unchanged. CORRECTION 2026-09-24: the v0.6.985 release claim was wrong at the app boundary. Every covering test above called direct_save_failure_code on the bare refusal, but the save command first passes the error through ensure_io, which wraps it as DirectSaveError{Unknown}; the classifier matched the outer tag, so the shipped app still reported `unknown` and retried. direct_save_failure_code now looks through that wrapper. The added src-tauri test drives the command helper on a real conflict-marker page and returned `unknown` before the fix."
},
{
 "id": "REG-SAVE-FAILURE-NAMES-PLATFORM-STEP-538",
 "title": "A save that fails on the platform says which call failed and with which OS error",
 "family": "direct-files",
 "sources": {
  "issues": [538, 590],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "native",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/commands/direct_save_helpers.rs::a_platform_failure_carries_its_call_and_os_error",
   "crates/tine-core/src/model_tests.rs::a_platform_failure_keeps_its_call_and_os_error_through_the_save_tag",
   "src/backendCallFunnel.test.ts::names the failed platform call and OS error in the save-failure message (GH #538)",
   "src/safeErrorDetail.test.ts::keeps the failed platform call and OS error of a save failure (GH #538)"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #538 (Android) and GH #590: save failures reached the user as `reason code: unknown` with only an io error kind, so the reporter's screenshot could not tell a refused RENAME_NOREPLACE on Android's MediaProvider FUSE (EINVAL) from a permission or disk error. The projection's platform errors now carry a PlatformStepError naming the step (a fixed backend string, never a path), and the save detail sends `operation` and `os_error`; the frontend shows them in the save-failure message and the safe error detail and drops an operation outside its fixed character set. Diagnosis only: this does not make saves succeed on storage that refuses the flag (a separate decision, contract §2.10b), and the cause of GH #590 is not yet known. Fail-before: the src-tauri test found no os_error in the detail before save_os_error looked through the DirectSaveError wrapper."
},
{
 "id": "REG-REFERENCED-NAMES-DIGEST-JS-SAFE-543",
 "title": "An edit that adds or removes no link does not resend the whole reference-name list",
 "family": "performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_tests.rs::the_reference_digest_survives_a_javascript_round_trip"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543. The referenced-names answer short-circuits to `unchanged` when the caller presents the current digest, but the digest was a full-width u64 and came back from JavaScript rounded, so it never matched and every save shipped the whole set (307 KB on the 10,000-page graph). The digest is now kept within Number.MAX_SAFE_INTEGER. Fail-before: the round-trip test fails on the unmasked digest."
},
{
 "id": "REG-INDEX-TURN-WRITE-COST-543",
 "title": "Renaming a page or an external change to many files does not stall on index writes",
 "family": "performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_rename_turn_commits_its_batches_together",
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_copied_wal_is_emptied_after_its_reader_finishes"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543. A worker turn committed one transaction per 32 pages; each wrote the shared index pages to the WAL again and could run an inline autocheckpoint (two fsyncs), which on Windows made a rename stall for about a minute. A turn is now one transaction (tine-storage v0.28.1 PhysicalGraphProjectionTurn), the live writer keeps temporary files in memory and disables inline autocheckpoints, and a background thread checkpoints passively once the WAL passes 4 MB. The referenced-names reader is an EXISTS over names in index order instead of a DISTINCT join, which under real statistics sorted through a temp B-tree. Measured on the 10,000-page generated graph (Linux, write-side probe): rename 603 -> 200 MB written and 3.2 -> 1.6 s index turn; external change 458 -> 257 MB; 60-block edit 736 -> 420 KB. Fail-before: without the autocheckpoint disable the checkpoint test's image grows 7 MB inline; the plan guard rejects the old DISTINCT-join form under injected statistics. In tine-storage: a_turn_commits_its_applies_together, checkpoints_run_only_where_the_caller_puts_them and paged_navigation_readers_use_an_index_range. Windows wall-clock not yet measured. Remaining cost (about 90 MB of dirty pages per rename, from delete-and-reinsert lowering) is campaign work. Windows measurement of v0.28.1 (2026-09-24, runs 36053102923/36053114979): rename 55.5 -> 6.8-8.2 s to Ctrl+K, but warm reopen 5.6-6.7 -> 15.6-28.1 s: a copied WAL kept its frames, the next open re-copied them all (13-23 s) and new read connections stalled behind it. tine-storage v0.28.2 empties a fully copied WAL when nothing holds it and the background checkpoint retries that; fail-before: tine-storage checkpoints_run_only_where_the_caller_puts_them / a_checkpoint_never_waits_to_empty_the_wal (WAL 7.2 MB / 1.9 MB left) and, in Tine, gh543_a_copied_wal_is_emptied_after_its_reader_finishes on v0.28.1 and on v0.28.2 with retries disabled (128 KB left)."
},
{
 "id": "REG-LIVE-CONFLICT-REVIEW-AFTER-REVOKE-490",
 "title": "A save conflict whose one-shot review authority was revoked early can still be captured, reviewed after a restart, and resolved",
 "family": "live-save-conflict",
 "sources": {
  "issues": [
   490
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_live_conflict_authority_tests.rs::gh490_a_conflict_whose_authority_was_revoked_is_still_captured_and_reviewable",
   "crates/tine-core/src/model_live_conflict_authority_tests.rs::gh490_a_restored_captureless_capsule_is_reviewed_against_the_disk"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "User outcome: after a save conflict, the banner 'Your draft and the current file both changed' stayed up but its comparison failed with 'live conflict authority is missing or already consumed' -- and after a restart it failed on every attempt, with no way to clear it. Mechanism (verified in core): a refused save mints a process-local one-shot token; any watcher reconcile of that path (sync_file_checked, including the echo of the write that caused the conflict) and, on Windows, any uncertain delete/rename anywhere in the graph (revoke_all_conflict_authority) revokes it. Landing between the refusal and capture_live_save_conflict, the capture failed, the capsule was stored without disk_rev, and the review routed to the dead token forever. Fix: the token is a preference, never a precondition -- a capture whose token is gone is taken from the disk as it is now (with the editor's loaded base when this process still has it), and a capsule without disk_rev whose token is gone (restart, or a Managed-era epoch -1) is reviewed durably. Apply still rechecks the displayed disk revision under the page lock, so nothing is authorized that the durable path did not already allow; the revocation rules are unchanged. Fail-before: with both fallbacks disabled, the capture test panics at the capture and the restore test at the review. Scope note: the reporter's own v0.6.982 failure is more likely the file-absent-under-a-captured-draft case fixed by a1f0c512 (v0.6.983); this row covers the remaining mechanism, found while investigating #490. Not run natively on Windows."
},
{
 "id": "REG-GRAPH-TEXT-HARDLINK-RENAME-571",
 "title": "A page that is also hard-linked elsewhere (git-annex, deduplication) can be renamed, restored after a crash, and edited externally without discarding the warm index",
 "family": "direct-save",
 "sources": {
  "issues": [
   571,
   555
  ],
  "commits": []
 },
 "platforms": [
  "linux",
  "macos",
  "windows"
 ],
 "layer": "rust-core-storage",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_alias_admission_tests.rs::a_hard_linked_page_can_be_renamed",
   "crates/tine-core/src/model_alias_admission_tests.rs::an_external_edit_to_a_hard_linked_page_keeps_the_identity_index_warm",
   "crates/tine-core/src/model_tests.rs::editor_recovery_sweep_restores_a_multi_link_artifact"
  ],
  "scenario": null,
  "failBefore": "On the pre-fix tree: a_hard_linked_page_can_be_renamed failed 'annex: graph text files alias one physical resource: pages/Old.md has link count 2'; an_external_edit_to_a_hard_linked_page_keeps_the_identity_index_warm reported invalidated: true, exact_updates: 0; the recovery test's predecessor pinned that checked open REFUSED the graph."
 },
 "notes": "After the save fix, three paths still refused on a raw link count. (1) The rename transaction's move source and editor recovery: a move keeps the inode, so no name can diverge, and the MS-REF-GRAPH-TEXT-ALIAS divergence comes only from temp+rename publication, so the check defended nothing; the referrer rewrites are ordinary publications, already allowed. (2) The checked-open recovery sweep refused the WHOLE graph over a recovery artifact with a second link, which git-annex's assistant produces under .git/annex/watchtmp. (3) The exact watcher delta required link count 1 while a complete build admits any, so every external edit of an annexed graph discarded the warm identity index and the next create or rename paid a whole-graph rebuild. All three now accept a link outside graph-text scope. Two GRAPH pages on one inode stay refused where the sibling can be named (Martin, 2026-09-20): the rename inventory refuses with both paths and changes nothing (asserted in the rename test), and an exact delta with resources.len() != 1 falls back to a rebuild. Windows/Google Drive (GH #571) is covered structurally: the Windows link count comes from GetFileInformationByHandle and no path now refuses on it; not run on Windows."
},
{
 "id": "REG-WARM-REOPEN-DRIFT-ON-PAGE-OPEN-543",
 "title": "Opening a page while the index is being checked at launch does not force a whole-graph parse",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_page_opened_during_the_warm_read_keeps_the_warm"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 warm-reopen follow-up. The launch-time warm validation reads every page to confirm the stored index is current (5.6 s for 10,000 pages on the Windows verify runner). Opening today's journal meanwhile publishes the page even when its bytes are unchanged; that moved the cache generation, the warm abandoned on drift, and every warm reopen fell back to a whole-graph parse (16 s to a working search, run 35643784742). A generation move made only of one-page publications whose session revisions equal what the warm read no longer discards it: removals and whole-cache invalidations move a separate structural counter and still count as drift. gh543_a_page_opened_during_the_warm_read_keeps_the_warm fails on the old any-move rule (unchanged case parses the whole graph) and checks that an edit or a new page written during the warm is indexed. Windows re-measure pending."
},
{
 "id": "REG-WARM-REOPEN-DERIVED-READ-PARSE-543",
 "title": "Whole-graph views opened during the launch-time index check wait for it instead of parsing every page",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::gh543_whole_graph_reads_during_the_warm_read_wait_instead_of_parsing",
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_page_list_before_the_scheduled_warm_starts_waits_for_it"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 warm-reopen follow-up (Windows verify run 35648984084: 10k warm reopen 24.9 s vs 16.0 s before). During the warm validation read the projection queue is idle and the worker not busy, so the 250 ms readiness wait returned false at once and the launch-time page list and alias reads parsed the whole graph; that installed a full snapshot which outranked the warm and was indexed after it. Page inventory, aliases, property owner rows and block ref counts now wait while the projection reports the warm in flight and no parsed cache exists. Two launch gaps fed the same parse: the app delays its warm thread 250 ms so the first paint goes first, and that paint's page list found no warm announced; and opening today's journal moved the generation under a waiting read, which then gave up. The warm is now announced when it is scheduled (held for the life of its thread, so a read cannot wait on a warm nobody runs) and a waiting read follows generation moves. Local release app on a 10k-page graph: ready at 1.4 s with no full snapshot, previously 2.7 s with one. Both tests fail on the corresponding old behaviour and check the answers with zero parses. Evidence: tine-agents/evidence/gh550-batch/gh543-derived-wait/."
},
{
 "id": "REG-COLD-PARSE-UNCHANGED-OPEN-543",
 "title": "Opening an unchanged page during a first open's page-reading pass does not discard the pass",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::gh543_cold_parse_survives_an_unchanged_page_open",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::gh543_cold_parse_reparses_a_page_edited_after_it_read_it"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit IT-04. Opening a page publishes it and moves the cache generation even when its bytes are unchanged (the publication re-lowers the page with this session's block ids, which the stored index may not carry). The cold page-reading pass installed only at its exact starting generation, so opening today's journal while it ran discarded the pass and the next reader parsed the whole graph again. Installation now uses the rule the warm check already uses: no structural move, and every page published this session has the revision and config the pass read. An edit after the pass read the page no longer discards it: since R2-02 the pass parses that page again and installs (see REG-EDIT-OR-DELETE-DURING-INDEXING-KEEPS-THE-PASS-543). The first test fails when installation requires the exact generation. Evidence: tine-agents/evidence/gh550-batch/gh543-derived-wait/cold-install-fail-before.log."
},
{
 "id": "REG-WARM-REOPEN-RENDER-READS-INDEXED-543",
 "title": "Page icons, block references, referrers, templates, calendar days and property facets answer from the index on reopen without parsing the graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::bl1_six_surfaces_answer_from_sql_after_warm_reopen",
   "crates/tine-core/src/direct_projection_tests.rs::bl1_six_surfaces_wait_during_warm_without_parsing",
   "crates/tine-core/src/direct_projection_tests.rs::bl1_six_surfaces_follow_a_page_opened_during_the_read",
   "crates/tine-core/src/direct_projection_tests.rs::bl1_loaded_runtime_id_can_miss_sql_without_a_parsed_cache",
   "crates/tine-core/src/direct_projection_tests.rs::bl1_sql_preview_runtime_ids_remain_locatable",
   "crates/tine-core/src/direct_projection_tests.rs::derived_reads_follow_generation_moves_through_one_front_door"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit IT-01. Rendering a page link (icon), a block reference or embed (resolve/preview), a block's referrers, the template picker, the calendar and property facets each called the whole-graph parser, even with a current index, so a warm reopen that needed zero parses parsed every page and enqueued a full snapshot. They now answer from SQL (candidate blocks only, reconstructed per call, nothing retained), wait for the warm check instead of parsing before ready, and follow a generation move (a page opened during the read) by re-reading the index: Graph::indexed_read is the one front door, pinned by a source-count guard. Runtime block ids the index does not hold are located through session_page_ids and hydrate one page; load_by_path now records them. Duplicate claimants resolve in path order (the parser used read_dir order). Each surface's answer equals the parser oracle on a fixture with aliases, upper-case ref spelling, nested and child-only templates, empty/property-only/renamed journals. Fail-before: every surface parsed 8-9 pages; with a page opened mid-read, all six parsed the whole graph. Evidence: tine-agents/evidence/gh550-batch/bl1/."
},
{
 "id": "REG-WARM-BESIDE-QUEUED-UPDATES-543",
 "title": "A launch-time index check is not discarded because pages opened at launch are still queued for indexing",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_warm_is_queued_beside_pending_page_updates"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit IT-03 (queued-update half). Opening a page at launch queues an index update. enqueue_warm refused a warm whenever any update was queued, and the warm itself waited at most 2 s for the queue to drain; on the realistic 10k graph the worker had not taken three launch updates when the warm read finished at 1.4 s, so the warm was refused and the whole graph was parsed (full snapshot at +4.7 s). The worker already validates a warm and applies updates after it in one turn, so the queue now takes the warm beside updates at or below its generation and the drain wait is gone. If validation finds the stored index stale, the updates taken with the warm are requeued instead of dropped, so readiness is never published without them. The test holds the worker so an update stays queued at hand-over; it parsed 9 pages before the fix. A real edit during the warm still takes the parser path (the rest of IT-03). Evidence: tine-agents/evidence/gh550-batch/it03-warm-beside-deltas-fail-before.log."
},
{
 "id": "REG-WARM-KEEPS-REAL-EDITS-AS-UPDATES-543",
 "title": "Editing or creating a page during the launch-time index check does not send the graph to a whole-graph parse",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_page_opened_during_the_warm_read_keeps_the_warm",
   "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_an_unreadable_page_does_not_keep_the_index_down"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit IT-03 (real-edit half). A page edited or created after the warm read it published an update, but the warm treated any publication at other bytes as drift, abandoned its read and the whole graph was parsed (9 parses on a 9-page fixture). The warm now keeps its read across any non-structural move; pages this session published at bytes other than it read are passed as `published`, validation leaves their stored rows alone, and their queued updates are applied after it. A new page's update taken by the worker before the warm arrived could not be placed without an inventory and latched a fresh build (10 parses); it is now parked until the warm or full snapshot seeds the page order. Neighbour fixed in the same change: a page the walk could not read was appended to the page order, shifting every later page onto a position another stored page held, so the next ordinary edit failed the worker (UNIQUE pages.position) and rebuilt the index; the warm now seeds the walk's own order. Removals and invalidations still discard the read. Evidence: tine-agents/evidence/gh550-batch/it03-real-edit-fail-before*."
},
{
 "id": "REG-STALE-REOPEN-REPAIRED-PAGE-BY-PAGE-543",
 "title": "Reopening a graph after a few pages changed elsewhere re-indexes only those pages",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::pages_changed_between_sessions_are_repaired_page_by_page",
   "crates/tine-core/src/direct_projection_tests.rs::many_pages_changed_between_sessions_build_one_complete_fresh_image",
   "crates/tine-core/src/direct_projection_tests.rs::gh550_launch_reads_of_changed_and_new_pages_settle_without_failing",
   "crates/tine-core/src/direct_projection_tests.rs::an_incomplete_stale_snapshot_keeps_the_unreadable_pages_rows_and_updates_the_rest"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit IT-03 (between-sessions half; Martin chose bounded repair 2026-09-22). One page edited, added or deleted between sessions made warm validation return fresh-build-required, and the whole graph was parsed and relowered (17 whole-graph parses on the fixture; +20.3 s to ready on the realistic 10k graph). Validation now names the changed pages when the walk is complete and at most a quarter changed; exactly those are parsed and applied in one transaction with page order reconciled, then validated again before readiness. Neighbours fixed in the same change: a launch read of a new page queued positions from the walk order before the repair, collided on pages.position and failed the worker into a fresh build; and an update for bytes the repair already wrote was lowered twice. Evidence: tine-agents/evidence/gh550-batch/it03-between-sessions-fail-before.log."
},
{
 "id": "REG-NAMED-PAGE-OPENS-DURING-LAUNCH-INDEXING-543",
 "title": "Opening a named page does not wait for launch-time indexing",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_named_page_opens_while_the_launch_check_runs"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit IT-05. load_named resolved every named page through find_entry, which needs the graph-wide page list, so a restored tab, home page, favourite or link waited for the launch warm (3.9 s on the realistic 10k graph during a cold build). A page is now opened from the file named for it when exactly one such file exists and its effective name matches; otherwise the list is used as before. find_entry now prefers the file named for a page over one claiming its name via title::, so both paths resolve the same file; both halves proven necessary. Evidence: tine-agents/evidence/gh550-batch/it05-named-open-fail-before.log, it05-named-open-10k.md."
},
{
 "id": "REG-CREATION-USES-THE-INDEX-FOR-NAME-OWNERSHIP-543",
 "title": "Creating a page after reopening or during launch indexing does not parse the whole graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::gh543_creating_a_page_during_the_launch_check_parses_nothing"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit IT-02. Name-only creation needs evidence that no other document owns the effective name. With no parsed cache (every clean reopen), direct_creation_proof ran repair_page_cache_once: a whole-graph parse and cache install on the save path, ahead of the launch warm when one ran (6 parses on the fixture; 10,000 parses and 416 ms on the realistic 10k graph). Evidence now comes from the ready index's effective page inventory, waiting for an in-flight launch check like other derived reads, and falls back to the parsed evidence only when no ready index answers or a parse failure is published. A name owned via title:: is still refused. Evidence: tine-agents/evidence/gh550-batch/it02-creation-fail-before.log, it02-creation-10k.md."
},
{
 "id": "REG-LAUNCH-WARM-ALWAYS-SIGNALS-COMPLETION-543",
 "title": "A failed launch index warm still releases aliases, ref counts and the progress indicator",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-native",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/graph.rs::a_launch_warm_that_ends_uncancelled_always_signals_completion",
   "src-tauri/src/graph.rs::a_retired_warm_cannot_finish_indexing_for_its_replacement"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit IT-04 (completion half; the cold-discard half was fixed earlier by the page-open counter change and is covered by gh543_cold_parse_survives_an_unchanged_page_open). warm_cache_async returned without warm_done or warm-cache-done whenever warm_cache_cancellable reported false, including failure, and a panic ended the thread the same way. waitForWarmCache has no other terminal channel, so aliases, page identities, block-ref counts and followIndexingProgress waited for the rest of the session. settle_launch_warm now signals on every uncancelled end, including a panic, and stays silent only when cancelled (a newer warm owns the window). Not run: the native cold-open barrier journey the audit lists as a remaining probe. Evidence: tine-agents/evidence/gh550-batch/it04-completion-fail-before.log. Round-3 audit R3-05: a same-root refresh keeps the binding generation and copies the warm generation, so the retired slot's warm matched the replacement and marked it done before the replacement's own pass began (probe: referenced names empty, then complete after its warm). Completion now requires the warmed slot to be the one installed (graph::finish_warm), and swap_refreshed moves the retired slot's warm generation so its warm stops parsing. Fail-before: tine-agents/evidence/gh550-batch/r3-05-fail-before.log."
},
{
 "id": "REG-PRESENTATION-SETTING-KEEPS-THE-INDEX-543",
 "title": "Changing a display-only setting does not restart indexing or close the unsaved-edit recovery",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-native",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/backend_command_parity.rs::settings_commands_leave_reopening_to_the_config_reach",
   "src-tauri/src/state.rs::a_settings_change_keeps_the_graph_and_its_index",
   "crates/tine-core/src/config.rs::a_change_reaches_the_graph_only_through_a_graph_field",
   "crates/tine-core/src/config.rs::a_settings_field_is_not_copied_at_open_or_into_the_index",
   "crates/tine-core/src/config.rs::a_setting_written_through_the_graph_is_the_one_it_reads",
   "src/instanceReplacement.test.ts::keeps the binding for a settings change (GH #543)"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit IT-06. set_timetracking_enabled, set_show_brackets, set_doc_mode_enter_for_new_block, set_logical_outdenting and set_guide_announced wrote config and then ran refresh_graph, which retires the projection worker, reopens the Graph and starts a new launch check; on a first launch, dismissing the Guide toast discarded the whole cold index build. The frontend also rebound (aborting print, closing unsaved recovery). tine-core reads none of these five, so they now write config and update the slot's GraphMeta in place (GraphSlot::apply_presentation_setting). preferred_format, journal title format, default home and restore_backup still refreshed. R2-P1 (round 2): the per-command choice is gone. Config::reach classifies every Config field (an exhaustive destructure, so a new field does not compile unclassified) as settings (read when used, swapped in place through Graph::config()) or graph (only a new Graph takes it in). Every settings command goes through GraphSlot::apply_config_write, Graph::write_config takes the change in, and the watcher does the same for an outside config.edn edit; only set_journal_title_format (a graph field, plus title-named journal migration) and restore_backup still reopen. default_home was the launch-time trigger: the device-local home migration calls set_default_home while the graph opens. The same change fixes exports and the CLI reading favorites and workflow as the graph opened. Evidence: tine-agents/evidence/gh550-batch/it06-presentation-settings-fail-before.log, tine-agents/evidence/gh550-batch/r2p1-fail-before.log."
},
{
 "id": "REG-FAILED-READ-DURING-REBUILD-QUEUES-NO-SECOND-BUILD-543",
 "title": "A search that fails while the index is already being rebuilt does not queue a second full rebuild",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_read_failing_during_a_fresh_build_does_not_queue_another"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit IT-10. Concurrent queries on a damaged index all fail; the first failure's repair starts a fresh build, and the query epoch moves only when that build publishes, so a sibling failure admitted before it still reached repair and queued another reset plus full payload: a second complete build after the first (at 10k pages, tens of seconds). direct_projection_repair now skips when a fresh build already owns the image (running, or a rebuild queued with its payload). Evidence: tine-agents/evidence/gh550-batch/it10-failed-read-during-fresh-build-fail-before.log."
},
{
 "id": "REG-DERIVED-READ-BEHIND-QUEUED-EDIT-PARSES-NOTHING-543",
 "title": "Launch reads that arrive while an edit is still being indexed do not parse the whole graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_derived_read_behind_a_queued_edit_waits_instead_of_parsing"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Found from Windows run 35719587395: a 100-page warm reopen showed one whole-graph parse offered as a full snapshot after a slow edit turn. wait_for_derived_read kept waiting only for a warm or a turn in progress; with an edit queued behind a turn it returned after the 250 ms delta wait, and with no parsed cache the page inventory, aliases, icons, journal days or block-ref counts parsed every page, then enqueued the parse as a full snapshot. Queued edits on a validated image now count as coming. Before validation they still do not (they wait for an inventory). Evidence: tine-agents/evidence/gh550-batch/derived-read-queued-edit-fail-before.log."
},
{
 "id": "REG-UNREADABLE-PAGE-LISTING-PARSES-ONLY-THE-FAILED-PATH-543",
 "title": "One unreadable page file does not make every page listing parse the whole graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::gh543_listing_with_an_unreadable_page_does_not_reparse_healthy_pages",
   "crates/tine-core/src/model_tests.rs::watcher_parse_failure_cannot_republish_stale_warm_page_inventory",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::a_page_that_fails_to_parse_does_not_reparse_its_healthy_siblings",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::a_failed_page_in_a_warm_sql_session_does_not_parse_the_graph",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::repeated_listings_with_a_failed_page_parse_it_at_most_once",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::the_cache_generation_moves_through_one_front_door"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Audit finding IT-07. With a known page-index failure, list_pages skipped the parsed cache and ran exact_page_inventory_from_disk, which reads and parses every page; the listing memo is dropped by every save and watcher event, so while one file stayed unreadable each listing cost a whole-graph parse. The listing now re-reads only the failed paths and, if they are all still unreadable and the generation is unchanged, answers from the parsed cache minus those paths; any failed path that became readable falls back to the exact disk inventory. Fail-before: 5 healthy-page parses per listing on the 6-page fixture, 0 after. Evidence: tine-agents/evidence/gh550-batch/it07-unreadable-listing-fail-before.log. Round-3 audit R3-04: a failed path that could be read but not parsed still sent the listing to exact_page_inventory_from_disk (every page parsed). Revalidation now re-reads and re-parses each failed path on its own and keeps the cached inventory for the rest. Fail-before at 788300f0: the probe counted a parse of every healthy page (evidence as for R3-01). Round-4 audit R4-03: in a warm SQL session (no parsed cache) one failed page still sent every listing to exact_page_inventory_from_disk. Root cause: recording or clearing a watcher failure moved the cache generation without telling the index, so it never became ready again and every indexed read fell back. Fix (shape): cache_gen moves only through Graph::move_cache_generation, where each mover states the index effect; the watcher-failure movers tell the index the new generation directly (DirectProjection::advance_generation, blocked after a stale mark). The listing has one base (the ready index, else the shared page-build flight) and revalidates only failed paths; exact_page_inventory_from_disk is deleted. A source guard pins the one front door. Fail-before at 6ef32fd6: tine-agents/evidence/gh550-batch/indexing-audit-r4/verify-by-manager.log; with advance_generation disabled the new test fails (tine-agents/evidence/gh550-batch/r4-fixes/necessity-advance-gen.log)."
},
{
 "id": "REG-REPLACED-GRAPH-READ-PARSES-NOTHING-543",
 "title": "A read still waiting when its graph is switched or refreshed does not parse the old graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_display_read_on_a_retired_graph_parses_nothing",
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_display_read_cut_short_leaves_no_memo_behind",
   "src-tauri/src/state.rs::a_display_read_on_a_retired_graph_is_answered_by_its_replacement",
   "src-tauri/src/state.rs::a_failed_refresh_leaves_the_bound_graph_serving",
   "src-tauri/src/state.rs::a_refresh_does_not_swap_over_a_window_that_moved_on",
   "src-tauri/src/state.rs::graphs_are_retired_only_where_they_are_replaced",
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_block_resolve_on_a_retired_graph_parses_nothing",
   "src-tauri/src/state.rs::a_read_takes_its_graph_through_display_read_or_is_listed"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Audit finding IT-09 (Martin chose option A, 2026-09-22). A derived read waiting on indexing when the app replaced its graph (switch, or a refresh from preferred format / default home / journal title format / restore backup) saw the projection stop, fell back to with_pages and parsed the whole retired graph: 5 of 5 fixture pages, graph-sized at 10k, competing with the replacement's cold index. Graph::retire (set on bind revocation, remove and refresh) makes display reads (commands list_pages, page_aliases, page_icons, block_ref_counts, list_templates, journal_content_days, backlink_filter_context, via state::display_read) stop waiting and skip the parse; the skipped answer is never memoized (page list, find-entry index, derived memo), and the command retries on the window's current graph. Reads that act on their answer (export, orphan assets, creation) are not display reads and keep the full answer. Fail-before: display read returned Some after retirement; memo test listed 0 of 5 pages. Evidence: tine-agents/evidence/gh550-batch/it09-*. Round-2 audit R2-03 (unreleased regression of this fix): a refresh retired the old graph before reopening the root, so a failed reopen left the window bound to a retired graph that answered no display read (probe: display=None). Refresh is now prepare (fallible, touches nothing) then commit (infallible, the only refresh retire), and the swap is compare-and-swap so a window that moved on keeps its graph. A source guard pins retire() to bind, remove and commit. Round-2 audit R2-01: only 7 of the display commands were routed through display_read, so resolve_blocks and the other reads (backlinks, queries, search, page loads, conflict lists; 27 commands) still parsed a retired graph (probe: 5 of 5 pages). They now use display_read, and a source guard lists every function that takes a window's graph directly: a new one fails until it is routed through display_read or listed as a write, export or acting read. Fail-before: the resolve test without display_read parsed 5 pages; evidence tine-agents/evidence/gh550-batch/r201/."
},
{
 "id": "REG-INDEX-READS-ANSWER-WHILE-A-JOINED-BUILD-RUNS-543",
 "title": "The indexing bar and index-backed reads answer while a whole-graph index build runs",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::gh543_progress_answers_while_a_joined_build_runs"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Audit finding R2-04. A warm that found the snapshot already captured (an orphan-asset listing got there first) joined the running SQL build and waited for it inside `if let Some(p) = self.direct_projection.lock().unwrap().as_ref().map(Arc::clone) { p.wait_until_ready_at(..) }`. The lock in that condition stays held for the whole block, in edition 2024 as in 2021, so indexing_progress and every other projection read blocked until the build finished. Fix (shape): the field is now a ProjectionSlot whose only reads return a released clone, so no caller can hold the slot lock across a wait. Guard: CI denies clippy::significant_drop_in_scrutinee on the whole workspace (the same class at 20 sites, all converted, including test hooks that ran while their own lock was held). Fail-before at aa851886: the progress read did not return while the build was paused. Evidence: tine-agents/evidence/gh550-batch/r204-fail-before.log, indexing-audit-r2/."
},
{
 "id": "REG-ORPHAN-ASSETS-UNREADABLE-GRAPH-ERRORS",
 "title": "The unused-assets list reports an error instead of listing in-use media when the graph's pages cannot be read",
 "family": "direct-files-data-safety",
 "sources": {
  "issues": [],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_tests.rs::orphan_assets_is_an_error_when_the_pages_cannot_be_read"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Found while fixing GH #543 R2-01 (Graph::with_pages answers an empty page set when graph-text admission fails). orphan_assets scanned that empty set for references, so every asset, in use or not, was offered for trashing. It now uses Graph::try_with_pages, which returns the admission error. Fail-before: a symlinked graph root (no retained no-follow capability) listed used.png as an orphan. Evidence: tine-agents/evidence/gh550-batch/r201/orphan-fail-before.log."
},
{
 "id": "REG-EDIT-OR-DELETE-DURING-INDEXING-KEEPS-THE-PASS-543",
 "title": "Saving or deleting a page while the graph is being read for indexing costs that page, not a second read of the whole graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "android",
  "linux",
  "windows",
  "macos",
  "ios"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::gh543_an_edit_during_the_cold_parse_costs_one_page",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::gh543_cold_parse_reparses_a_page_edited_after_it_read_it",
   "crates/tine-core/src/direct_projection_tests.rs::gh543_a_delete_during_the_warm_validation_parses_nothing",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::a_page_deleted_and_recreated_during_a_pass_keeps_the_pass",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::a_page_edited_after_an_earlier_open_keeps_the_pass",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::many_named_deletes_during_a_pass_keep_the_pass",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::the_structural_event_log_keeps_only_what_running_passes_need"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 2, R2-02 (cold parse) and R2-05 (warm validation). Both whole-graph passes discarded their work when any page was saved or deleted while they read, so the next reader read and parsed the whole graph again. Both now take one account of what changed since they read (Graph::drift_since over a StructuralGeneration whose counter moves only through record(Removed(paths)) or record(Unnamed)): changed pages are parsed again, removed pages are dropped, and only an unnamed change discards the pass. Fail-before on 6c4d5e2e: the cold pass was discarded for one edit, and the warm validation parsed 4 surviving pages after one delete. Evidence: tine-agents/evidence/gh550-batch/r205-fail-before.log. Round-3 audit R3-01/02/03: three ways that account still discarded or re-read work. A page deleted and recreated stayed in the removed set, so the pass was thrown away (R3-01); a page opened in the session before the pass counted as stale whenever its disk bytes differed from that older publication, even when the pass read the newer bytes (R3-02); and the change log kept 1024 events, so more named changes became unnamed drift (R3-03). Fix (shape): one event sequence orders every session publication and structural record; the latest event per path is kept unbounded; a pass notes the sequence before it reads (per path for re-reads), and an event counts only when it is newer than that path's read. Fail-before at 788300f0: all three probes failed (evidence tine-agents/evidence/gh550-batch/indexing-audit-r3/RECEIPT-indexing-audit-r3-probes.log). Round-4 audit R4-P1: that unbounded per-path log grew with every page removed or found unreadable for the life of the session. Each pass now registers its start (StructuralGeneration::begin_pass, a PassWatermark held by the warm and by the page-build flight), and the log drops events no running pass started before, pruning when it doubles. Fail-before: with pruning disabled the test keeps 300 paths with no pass running (tine-agents/evidence/gh550-batch/r4-fixes/necessity-event-log.log)."
},
{
 "id": "REG-UNREADABLE-GRAPH-EXPORT-IS-AN-ERROR-543",
 "title": "Exporting or printing a graph whose files cannot be read reports that, instead of exporting an empty graph or saying the page does not exist",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/publish_tests.rs::an_unreadable_graph_is_an_error_to_publish_and_print_not_an_empty_graph"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 2, BY6.1. Graph::list_pages is the display listing: an unreadable graph (admission failure) lists nothing. Publication source capture, PDF print, the rename/delete outside-root check and the CLI doctor acted on that listing; they now use Graph::try_list_pages, which returns the error. The display listing also memoized the empty answer; neither a failed nor a retired-graph answer is memoized now. All 13 production list_pages callers were reviewed; the remaining ones display, or read a publication snapshot whose cache is preinstalled. Fail-before on 01843ec7: publication capture returned Ok(empty) on a symlinked root. Evidence: tine-agents/evidence/gh550-batch/by61-fail-before.log."
},
{
 "id": "REG-ONE-FULL-SNAPSHOT-IS-VALIDATED-ONCE-543",
 "title": "Opening a graph validates its search index against the parsed pages once, not twice",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/direct_projection_tests.rs::the_same_full_snapshot_offered_twice_is_taken_once"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 2, BY6.2. attach_direct_projection seeds the projection with the installed parsed cache, and install_built offers the same snapshot after publishing it; when the attach landed between the two, the projection took both, re-validated the whole image a second time and dropped readiness meanwhile, so queries fell back to parsing. DirectProjection::enqueue_full now remembers the accepted snapshot (generation + identity) and takes a repeated offer once, unless a rebuild is pending, a newer generation is queued, or the worker failed. Fail-before on 01843ec7: the repeated offer dropped readiness. Evidence: tine-agents/evidence/gh550-batch/by62-fail-before.log."
},
{
 "id": "REG-CONFIG-WATCHER-SEES-WHAT-WAS-NOT-TAKEN-IN-543",
 "title": "An outside config.edn change reaches the running graph even when it was folded into Tine's own settings write or reverts to the bytes the graph opened with",
 "family": "config-live-reload",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/config.rs::the_watcher_gate_matches_disk_only_when_disk_was_taken_in"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 2, BY6.3. The watcher skipped config.edn when disk matched the bytes the graph opened with or the bytes it last wrote. A graph-reaching outside change folded into Tine's own read-modify-write matched the last write, and an outside revert matched the opening bytes, so both were never taken in. The gate is now one digest, Graph::served_config_description, of the bytes the served configuration was taken from; a change that reaches the graph never updates it. Fail-before on 01843ec7 (old gate shimmed): both cases read as unchanged. Evidence: tine-agents/evidence/gh550-batch/by63-fail-before.log."
},
{
 "id": "REG-FAVORITES-CHANGE-IS-NOT-ANSWERED-FROM-THE-OLD-MEMO-543",
 "title": "Changing the favorites page updates linked references instead of serving the answer computed under the old value",
 "family": "config-live-reload",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_tests_gh543_parse_passes.rs::a_favorites_page_change_is_not_answered_from_the_old_memo", "crates/tine-core/src/config.rs::only_an_answer_setting_is_read_where_answers_are_computed"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 3, R3-06 (regression of the unreleased in-place settings change). The favorites page excludes references from answers, but the derived memo was keyed on the parser config only, so a settings-only take-in served the old answer. Fix (shape): config_reach! now lists answer settings separately (answers: [favorites_page]); the memo key includes Config::answer_settings_digest, and a source guard fails when a setting outside that list is read where answers are computed (allowed: config, paths, publish, and preferred_format in pdf, pages_merge and graph_text_inventory, which act when they run). Fail-before at 788300f0: evidence tine-agents/evidence/gh550-batch/indexing-audit-r3/RECEIPT-indexing-audit-r3-probes.log; the guard fails when favorites_page is moved out of answers."
},
{
 "id": "REG-A-LATER-WATCHER-FAILURE-SURVIVES-AN-EARLIER-CAPTURE-543",
 "title": "A page that becomes unreadable while the graph is indexed stays known as unreadable, and the index does not report itself complete without it",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_tests_gh543_parse_passes.rs::a_capture_that_started_earlier_cannot_erase_a_later_watcher_failure", "crates/tine-core/src/direct_projection_tests.rs::a_warm_keeps_a_watcher_failure_recorded_while_it_ran"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 3, R3-07. record_watcher_identity_failure moved the cache generation without recording a structural event, so a complete capture that started earlier installed over it, erased the failure and marked the index ready. Recording and clearing a watcher failure now record StructuralChange::Reread(path) before the generation moves, so the capture re-reads that path. Fail-before at 788300f0: evidence tine-agents/evidence/gh550-batch/indexing-audit-r3/RECEIPT-indexing-audit-r3-probes.log. Round-4 audit R4-01: the warm validation never read the reread set its drift account returned, so it installed a failure list without a page the watcher found unreadable while it ran. GraphDrift is now consumed only through into_parts, which names every kind of change; the warm reads each reread page again and its failures follow that read. Fail-before at 6ef32fd6: tine-agents/evidence/gh550-batch/indexing-audit-r4/verify-by-manager.log."
},
{
 "id": "REG-A-PAGE-OPENED-DURING-INDEXING-REACHES-SEARCH-543",
 "title": "A page opened while the graph is being indexed reaches search and queries at the bytes the editor shows",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::a_page_opened_by_path_during_a_warm_reaches_the_index",
   "crates/tine-core/src/direct_projection_tests.rs::opening_a_page_the_index_holds_moves_no_generation",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::session_page_ids_have_three_writers"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 4, R4-02. load_by_path published the page's session ids with no delta and outside the cache lock; the warm then counted the page as already sent to the index, and the watcher suppressed the later delivery of the same bytes, so search and queries missed the edit for the session. Fix (shape): both load routes go through one load_exact_path, which publishes through the page-delta path unless the ready index already holds those exact bytes (then it records the ids with no delta, so opening an unchanged page moves no generation); publish_session_page_ids takes the cache write guard as proof, and a source guard pins the three session-id writers. Fail-before at 6ef32fd6: tine-agents/evidence/gh550-batch/indexing-audit-r4/verify-by-manager.log; the unchanged-open sibling fails when every open takes the delta path (tine-agents/evidence/gh550-batch/r4-fixes/necessity-held-open.log)."
},
{
 "id": "REG-A-PAGE-CONSUMER-DOES-NOT-REBUILD-THE-INDEX-DURING-ITS-CHECK-543",
 "title": "Listing assets or reading every page during the launch-time index check does not replace the check with a full index rebuild",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::a_page_consumer_does_not_offer_a_full_snapshot_during_a_warm",
   "crates/tine-core/src/direct_projection_tests.rs::a_cache_installed_inside_a_cancelled_warm_is_offered_by_the_next_warm"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 4, R4-04. orphan_assets (and any whole-graph consumer) built the parsed cache during an in-flight warm, and install_built offered it as a full snapshot, which superseded the warm's cheap validation with a complete rebuild. Fix (shape): direct_projection_enqueue_full takes a FullOffer; a Consumer offer is refused while a warm is in flight, and the warm that owns readiness offers the installed cache when it has one (Graph::offer_installed_cache, from warm_page_cache_cancellable and from the warm's cache-present branch, so a cache installed inside a cancelled warm is not stranded). Fail-before at 6ef32fd6: tine-agents/evidence/gh550-batch/indexing-audit-r4/verify-by-manager.log; the cancelled-warm sibling fails without the cache-present offer (tine-agents/evidence/gh550-batch/r4-fixes/necessity-early-offer.log)."
},
{
 "id": "REG-EVERY-WHOLE-GRAPH-PARSE-SHOWS-PROGRESS-543",
 "title": "The progress indicator shows every whole-graph page read, including one started by a page list or asset list rather than the launch warm",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::a_consumer_whole_graph_parse_shows_on_the_progress_bar"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 4, R4-P2 (plausible, reproduced). Only the paced warm parse reported itself to indexing_progress; the fast parse a consumer owns (load_all_pages_with_permit) did not, so with no index to report (none attached, or not ready and no warm announced) the bar said idle through a whole-graph parse. The fast parse now begins a Reading pass and advances it per page, across its worker threads. Fail-before: tine-agents/evidence/gh550-batch/r4-fixes/fail-before-r4-p2.log."
},
{
 "id": "REG-A-PAGE-RECOVERED-DURING-INDEXING-REACHES-SEARCH-543",
 "title": "A page that fails to read and then recovers while the search index is being checked reaches search",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::a_page_recovered_during_a_warm_reaches_the_index"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 5, R5-01 (a required neighbor of R4-04). When the last failed page recovered, cache_upsert_inner sent the recovery as a full snapshot instead of the page's delta; a warm in flight refuses a Consumer snapshot (R4-04), so the page never reached the index. Fix (shape, two layers): the recovery takes the ordinary delta, queued under the cache lock, whenever a warm owns readiness; direct_projection_enqueue_full returns a must_use FullOfferOutcome, and a recovery refused because a warm began after the lock was released sends the delta. Either layer alone passes the test; with both removed it fails. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r5/verify-by-manager.log and tine-agents/evidence/gh550-batch/r5-fixes/necessity-r5-01.log (both layers off); tine-agents/evidence/gh550-batch/r5-fixes/necessity-r5-01-layer1-only.log shows the first layer alone."
},
{
 "id": "REG-AN-EDIT-AT-THE-END-OF-THE-INDEX-CHECK-PARSES-NOTHING-543",
 "title": "An edit that lands just as the index check finishes is applied as an update instead of re-parsing the whole graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::an_edit_just_before_the_warm_offers_its_validation_parses_nothing"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 5, R5-02. An edit queued between the warm's drift check and its enqueue outranked the warm's generation; the refusal was reported as Retry and the warm fell back to a whole-graph parse. Fix (shape): enqueue_warm_with_repair returns a typed WarmRefusal (Unavailable, Superseded, Outranked) and takes the repair only when it queues. On Outranked the warm re-runs its drift check, which picks up the edit as a published update, and offers again, at most 8 times before the parse. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r5/verify-by-manager.log; with the retry bound set to 0, 5 of 5 pages are parsed (tine-agents/evidence/gh550-batch/r5-fixes/necessity-r5-02.log)."
},
{
 "id": "REG-A-READY-INDEX-DOES-NOT-HIDE-A-GRAPH-PARSE-FROM-PROGRESS-543",
 "title": "The progress indicator shows a whole-graph page read even when the search index is already ready",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::a_ready_index_does_not_hide_a_consumer_parse_from_the_progress_bar"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 5, R5-03 (a sibling of R4-P2). Graph::indexing_progress returned None for a ready index before it checked the running core pass, so an asset listing's whole-graph parse showed as idle. Fix: the running pass is consulted first. The poller in src/indexingProgress.ts has no other ready-means-idle exit. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r5/verify-by-manager.log and tine-agents/evidence/gh550-batch/r5-fixes/necessity-r5-03.log."
},
{
 "id": "REG-A-SUPERSEDED-REFERENCES-READ-CANNOT-FAIL-THE-NEW-PANEL-543",
 "title": "Moving to another page while references are still indexing never shows the new page's references as failed because of the old page's read",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "frontend",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/lib/referenceFetch.test.ts::a superseded first read cannot mark the new panel failed",
   "src/lib/referenceFetch.test.ts::a superseded first read's real failure does not mark the new panel failed either",
   "src/queryReadiness.test.ts::settles a superseded eager attempt as cancellation, whatever it returned"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 5, R5-04 (I-20). runQueryWhenCurrent's eager first attempt rethrew its own refusal after its caller had stopped being current, and createReferenceFetcher wrote that refusal to the shared panel error. Fix (shape, at the shared seam): the eager attempt settles like every retry, so a caller that is no longer current gets OperationCancelledError whether the attempt succeeded or failed; the reference fetcher also gates its error write on the current page. The sibling callers (LinkedReferences filter context, QueryBuilder, Macro) already gate on their own revision, except the query-block save rebase in Macro.tsx, which used to rebase on a stale reading and now stops with the query-changed message. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r5/verify-by-manager.log and tine-agents/evidence/gh550-batch/r5-fixes/necessity-r5-04.log (3 tests)."
},
{
 "id": "REG-SIDEBAR-REFERENCES-FOLLOW-A-REBIND-543",
 "title": "Linked and unlinked references in the right sidebar refresh after the graph is rebound and never show the previous binding's rows",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "frontend",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/components/LinkedReferences.test.tsx::refetches when the graph is rebound",
   "src/components/LinkedReferences.test.tsx::does not publish an answer that was asked of the previous binding",
   "src/lib/referenceFetch.test.ts::stops retrying, and drops the answer, when the graph is rebound"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 6, R6-06 (I-20). Both references panels keyed their resource and their ownership check on the page name alone. The main pane happens to remount them on a rebind, but the right sidebar never does, so after a rename, a backup restore or a journal-format change it kept the old binding's rows, and a late pre-rebind answer could land. Fix (shape, at the shared seam): createReferenceFetcher's read is a ReferenceRead of page name, graph binding and render epoch; both panels key their resource on it and the fetcher owns a read only while all three still match, so the panels are correct wherever they are mounted. Siblings reviewed: inline page/block previews and block embeds key on the render epoch, which every rebind also bumps; query blocks and QueryBuilder already scope by epoch and binding. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r6/RECEIPT-indexing-audit-r6-frontend-probes.log and tine-agents/evidence/gh550-batch/r6-fixes/necessity-r6-06.log."
},
{
 "id": "REG-A-FAILED-LOOKUP-NEVER-UNPINS-A-SIDEBAR-BLOCK-543",
 "title": "A block pinned in the right sidebar is removed at launch only when the open graph says it no longer exists, never because looking it up failed",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "frontend",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/pruneSidebarBlocks.test.ts::keeps a restored block when resolving it is refused for indexing",
   "src/pruneSidebarBlocks.test.ts::keeps a restored block when resolving it fails outright",
   "src/pruneSidebarBlocks.test.ts::does not act on answers from a binding that has since moved",
   "src/pruneSidebarBlocks.test.ts::still removes a block the current binding says is gone"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 6, R6-08. pruneSidebarBlocks runs at every graph load and persists what it removes, but it caught every resolve_block error as null and so deleted the pin on a transient IPC error, a stale binding or a read refused during a graph switch. Fix: an error keeps the block, and answers are acted on only if the binding has not moved while they were read. Remaining core-side sibling: a retired graph's resolve can still answer an empty result rather than an error (the with_pages f(&[]) shape); the binding check covers it here, and the indexing-owner redesign owns the core shape. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r6/RECEIPT-indexing-audit-r6-frontend-probes.log and tine-agents/evidence/gh550-batch/r6-fixes/necessity-r6-08.log (3 tests)."
},
{
 "id": "REG-PUBLISHING-DURING-INDEXING-WAITS-AND-PARSES-NOTHING-543",
 "title": "Exporting or publishing while the index is being built waits for the index and then completes, without parsing the whole graph first or showing the wait as a failure",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::a_query_export_plan_during_indexing_is_typed_not_ready_and_parses_nothing",
   "src-tauri/src/command_error.rs::ret2_query_execution_reaches_the_wire_through_one_encoder",
   "src/components/QueryExportDialog.test.tsx::waits for the index and then shows the plan",
   "src/components/QueryExportDialog.test.tsx::an export confirmed while the index catches up waits and then exports",
   "src/components/Settings.publishZero.test.tsx::waits for the index, then exports"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 6, R6-05 (I-20). Measured before the fix: a query-export plan asked for during a warm waited out the whole pass on its backend thread and then parsed every page (6 of 6 in the test), even if the dialog had been closed; a source mismatch came back as untyped prose, which the dialog showed as a refusal nothing retried. Fix, one seam per layer: capture_direct_publication_sources (shared by publish_html, publish_graph_to, publish_graph_app_to, plan_query_publication and publish_query) asks the index first and answers typed not-ready while it is working; the source-mismatch refusal is the same typed not-ready; CommandError::from(io::Error) unwraps an IO-carried QueryExecutionError into the one query wire, so every command that carries one keeps it. The dialog's plan and export and the Settings HTML export wait through runQueryWhenCurrent and say they are waiting. Fail-before: tine-agents/evidence/gh550-batch/r6-fixes/necessity-r6-05-core.log, necessity-r6-05-tauri.log, necessity-r6-05-frontend.log (3 tests)."
},
{
 "id": "REG-NO-COMMAND-FREEZES-THE-APP-DURING-INDEXING-543",
 "title": "No command freezes every window while the graph is being indexed: Quick Capture suggestions, a journal title-format change, graph-wide listings and large asset work run off the main thread",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/backend_command_parity.rs::every_sync_command_says_why_it_may_run_on_the_main_thread",
   "src-tauri/src/backend_command_parity.rs::async_commands_open_graphs_inside_spawn_blocking",
   "src-tauri/src/commands/capture_quick_switch_tests.rs::a_capture_query_on_a_retired_graph_is_answered_by_its_replacement",
   "src/refreshJournalConflicts.test.ts::keeps the newest refresh's answer when an older one finishes last"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 6, R6-02 and R6-07. A sync #[tauri::command] runs on the main thread and freezes every window until it returns. capture_quick_switch waited out the whole index pass there (wait_for_derived_read) and, on a graph a refresh had just retired, parsed that graph; set_journal_title_format waited for the storage transition lane and up to 15 s for the index worker to stop. Fourteen commands became async + spawn_blocking (capture_quick_switch, set_journal_title_format, list_journal_conflicts, list_journal_filename_migrations, graph_source_files, set_backup_keep, asset_trash_stats, empty_asset_trash, import_asset, import_native_capture, read_asset, read_local_image, list_installed_plugins, list_spellcheck_dictionaries); open_graph_window and restore_backup no longer open or refresh a graph inline on a runtime worker. Shape: state::refresh_graph is async, so no sync command can reach it; capture reads go through display_read. Guard: every remaining sync command is listed in SYNC_COMMANDS with the reason it is bounded, and the listing is exact both ways. refreshJournalConflicts keeps only the newest answer, since the listing now answers out of order. Fixed a vacuous sibling: a_display_read_on_a_retired_graph_is_answered_by_its_replacement measured its wait after joining the 200 ms binder thread. Fail-before: tine-agents/evidence/gh550-batch/r6-fixes/necessity-r6-02-tauri.log (14 unlisted sync commands, open_graph_window, capture answered by the retired graph in 2.9 ms), necessity-r6-02-frontend.log."
},
{
 "id": "REG-A-FAILED-INDEX-TURN-DOES-NOT-HANG-THE-APP-543",
 "title": "A search-index turn that fails after the launch check no longer leaves page lists, queries and search waiting forever",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_a_turn_that_fails_after_the_launch_check_does_not_hang_the_warm",
   "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_seeded_interleavings_converge"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 stage-0 interleaving harness, H1. The launch warm waited on its own announcement after a worker turn failed, so every derived read waited on a warm that could never finish. The warm now reconciles page order itself on a clean validation and a failed turn falls through to recovery. Fail-before: tine-agents/evidence/gh550-batch/stage0/necessity-warm-self-wait.log."
},
{
 "id": "REG-A-PAGE-CREATED-AFTER-REOPENING-WITH-A-DELETED-PAGE-IS-INDEXED-543",
 "title": "After reopening a graph whose stored index still lists a deleted page, a new or renamed page reaches the index instead of breaking indexing",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_a_page_created_after_reopening_a_graph_with_a_deleted_page_is_indexed"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 stage-0 interleaving harness, H2. A warm repair left a position gap for the deleted page, and the next insert failed its order check, leaving the index never ready. Warm repairs now go through reconcile_page_order, the same reconciliation as a clean warm. Fail-before: tine-agents/evidence/gh550-batch/stage0/necessity-position-gap.log."
},
{
 "id": "REG-LISTINGS-DO-NOT-PARSE-THE-GRAPH-AROUND-A-RENAME-OR-EDIT-543",
 "title": "Page listings, queries and searches during or after a rename, page merge, journal migration or concurrent edit, and right after opening a graph, read the index instead of re-parsing the whole graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::a_failed_page_after_a_rename_does_not_parse_the_graph",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::a_listing_during_a_rename_waits_for_the_rename_instead_of_parsing",
   "crates/tine-core/src/model_tests_gh543_parse_passes.rs::a_listing_overtaken_by_an_edit_reads_the_index_instead_of_parsing",
   "src-tauri/src/graph.rs::every_direct_files_graph_is_attached_through_one_function",
   "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_seeded_interleavings_converge"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 stage-0 interleaving harness, H3-H6. H3: a rename/merge/journal migration marked the index stale, and the mark outlived its own delta, so a later page failure could never make it ready. H4: a reader between the generation move and the queued delta saw nothing coming and parsed. H5: a derived read whose generation went stale while it waited parsed although the index was ready at the newer generation. H6: the launch warm was announced after the graph was published (open and configuration refresh). Shape: Graph::move_cache_generation takes IndexEffect::Sent(&IndexDeltaComing), a token the mover holds until the delta is queued and that progress_at reports as PendingEdits; only an unnamed delete and the test-only invalidate_cache use IndexEffect::Stale; wait_for_derived_read follows cache_gen when nothing is coming; begin_warm_cache returns a WarmTicket carrying the announcement, which warm_cache_async requires, and a source-order guard pins it before publication. Harness: the long run went from 33/300 to 1-2/300 failing seeds; the residuals (creation proof with page_index_failures; one session left unvalidated with nothing queued) are recorded for the single-owner design. Fail-before: tine-agents/evidence/gh550-batch/stage0/necessity-stale-after-rename.log, necessity-rename-window.log (8 parses), necessity-overtaken-listing.log (8 parses)."
},
{
 "id": "REG-A-PAGE-DELETED-DURING-INDEXING-IS-GONE-NOT-UNREADABLE-543",
 "title": "A page deleted while Tine lists or checks the graph is treated as deleted, not unreadable, so creating a page afterwards neither fails nor re-parses the whole graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_a_page_deleted_during_a_pass_is_gone_not_unreadable", "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_seeded_interleavings_converge"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Harness residual (a). The graph listing (graph_text_inventory admit_or_skip), the launch check's walk and re-read, and the parsed build each recorded a page that vanished between listing and read as a page index failure; the reparse path already dropped it. The failure left the inventory incomplete, so page creation either refused ('failure-bearing cold identity evidence cannot authorize name-only creation') or parsed the whole graph on the save thread. All four sites now treat NotFound as gone. The watcher's sync_file_checked still records a vanished path as a failure; its delete event clears it and carries the UI removal, so it is left as is. Fail-before: tine-agents/evidence/gh550-batch/stage2/necessity-vanished-page.log."
},
{
 "id": "REG-READERS-WAIT-FOR-THE-LAUNCH-INDEX-PASS-543",
 "title": "Looking up a block's page, listing page names, or finding references while the launch index check runs waits for it instead of parsing the whole graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_readers_without_a_retry_wait_for_the_launch_pass"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Design v4 stage 3. These readers had no retry path, so while the launch pass was running they fell back to parsing every page (6 of 6 in the fixture). They now go through indexed_read, which waits while index work is coming (IndexWait::WhileComing); the bounded reference path used by a caller that can retry keeps its bounded wait. Fail-before: tine-agents/evidence/gh550-batch/stage3/necessity-readers-wait.log."
},
{
 "id": "REG-A-BUSY-INDEX-LEASE-IS-WAITED-FOR-NOT-GIVEN-UP-543",
 "title": "A graph whose index database another writer still holds indexes once that writer lets go, instead of running the rest of the session without an index",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_a_writer_waiting_for_an_in_process_lease_takes_it_when_released", "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_a_lease_held_by_another_process_is_retried", "src-tauri/src/state.rs::a_graph_switch_detaches_the_displaced_graphs_index_writer", "crates/tine-core/src/model_gh543_lifecycle_tests.rs::gh543_a_reader_waits_for_a_retired_predecessor_to_release_the_index"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Design v4 stage 4 (R6-03). The worker tried the writer lease once and went terminal, so a quick switch back to a graph whose old writer had not yet exited, or a second instance, left the session with no index. It now waits: for an in-process holder it retries the moment that holder releases (a registry of writers per database path, changed under one lock with the lease), for another process on a 1 s doubling backoff capped at 30 min. While it waits nothing is coming (IndexNeed::LeaseWait), so readers take their ordinary route. Switching a window to a different graph now detaches the displaced graph's writer at once. Fail-before: tine-agents/evidence/gh550-batch/stage4/necessity-lease-retry.log. Round 8 (R8-04): an in-process holder is on its way out, so while the worker waits for it (up to 20 s) the index counts as coming and readers wait instead of parsing the whole graph; only a foreign holder, or an in-process one past the bound, is LeaseWait. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r8/necessity-r8-04-lease.log."
},
{
 "id": "REG-A-DELETE-DURING-A-FULL-REBUILD-DOES-NOT-FAIL-IT-543",
 "title": "A page deleted or renamed while the index rebuilds from scratch does not make the rebuild fail",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/direct_projection_tests.rs::gh543_a_full_turn_with_a_delete_and_a_later_update_applies", "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_long_interleaving_run"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Found by the seeded harness (seed 1288, about 1 in 100 runs under load). A deletion after a full snapshot was queued left a gap in the queue's page order; the full turn hands storage the order without gaps, so a later page's update taken with it carried a position one past its place and tine-storage refused the turn ('page order differs from complete inventory'). The owner backed off and reads parsed the graph. The full turn now reseeds the queue's order from the inventory it writes and re-places the updates it takes (reseed_order, the same step a warm repair and an incomplete full already take). Fail-before: tine-agents/evidence/gh550-batch/stage4/necessity-full-order-gap.log."
},
{
 "id": "REG-READINESS-IS-NOT-CLAIMED-WHILE-A-PASS-IS-OWED-543",
 "title": "The search index is not reported ready while it still owes a whole-graph pass, so the index owner cannot spin",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_a_stale_image_is_not_made_ready_by_a_page_update", "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_a_validation_that_needs_a_fresh_build_is_not_ready", "crates/tine-core/src/direct_projection_tests.rs::readiness_is_published_only_where_the_decider_owes_nothing"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Round-7 indexing audit R7-02/R7-02b. The worker's turn end published readiness without consulting the stale mark or a latched fresh build, so readiness stood beside a Validate/Fresh need; the owner's pass found the image ready, did nothing, counted itself settled and ran again at once (5.9M passes/s measured), and a damaged image kept answering. Both publishers (turn end, advance_generation) now publish only under image_is_current (index_need == Nothing and no queued work), the turn end withdraws readiness otherwise, and an owner pass counts as settled only if its need moved. Guard: the source scan fails on any readiness store outside image_is_current. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r7/necessity-fail-before.log."
},
{
 "id": "REG-DELETING-A-FILELESS-PAGE-DOES-NOT-WALK-THE-GRAPH-543",
 "title": "Deleting a page that has no file does not re-check the whole graph against the search index",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_deleting_a_page_with_no_file_does_not_walk_the_graph"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Round-7 indexing audit R7-01. delete_page_expected passes cache_remove the inventory's answer, but cache_remove guessed from the page-list memo and, finding nothing with a cold cache, marked the index stale: the owner walked every page (and during the launch check abandoned and repeated its walk). cache_remove now takes the inventory's answer as authoritative; a page with no file moves the generation with IndexEffect::Unchanged. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r7/necessity-fail-before.log."
},
{
 "id": "REG-A-CLOSED-WINDOW-RELEASES-ITS-INDEX-WRITER-543",
 "title": "Closing a window releases its graph's index writer, so reopening the graph indexes at once",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["src-tauri/src/state.rs::a_window_close_detaches_its_graphs_index_writer", "src-tauri/src/state.rs::graphs_are_retired_only_where_they_are_replaced"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Round-7 indexing audit R7-05. GraphRegistry::remove retired the slot without detaching its projection, unlike bind for a different root; a reopen waited in LeaseWait until every clone of the old Graph dropped. Both now go through one retire_slot, which the retirement census pins. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r7/necessity-close-detach.log."
},
{
 "id": "REG-A-PUBLISHED-GRAPH-ALWAYS-GETS-ITS-INDEX-OWNER-543",
 "title": "A graph that has been opened always starts its index owner, even if saving the recent-graphs list fails",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["src-tauri/src/graph.rs::every_direct_files_graph_is_attached_through_one_function"],
  "scenario": null,
  "failBefore": "structural"
 },
 "notes": "Round-7 indexing audit R7-03 (reasoned, not reproduced: needs a failing settings write). publish_prepared_direct_files returned remember_graph's error after the slot was published, dropping the warm ticket: no owner, no launch completion, and the open reported as failed. The owner now starts right after publication and remembering is non-fatal (a fixed diag line). The source-order guard fails if anything fallible sits between publication and warm_cache_async."
},
{
 "id": "REG-A-RETIRED-GRAPHS-INDEX-OWNER-RUNS-NO-PASS-543",
 "title": "A graph replaced by a settings refresh is not re-read by its old index owner",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_a_retired_graphs_owner_runs_no_more_passes"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Round-7 indexing audit R7-06. A refresh retires the old graph before swap_refreshed cancels its owner; in between the owner could run one pass over the retired graph. run_index_owner now treats the graph's own retirement as cancellation, for every caller. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r7/necessity-retired-owner.log."
},
{
 "id": "REG-THE-INDEX-OWNER-NEVER-WAITS-ON-ITSELF-543",
 "title": "An index turn that fails while the indexing owner finishes its pass does not hang the owner, and with it every page listing",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_lifecycle_tests.rs::gh543_a_turn_failure_while_the_owner_settles_does_not_hang_it", "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_a_turn_that_fails_after_the_launch_check_does_not_hang_the_warm"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 8, R8-01 (seed 1023). The owner prefetched derived maps at Settle through the ordinary readiness wait; a turn failure in that window made the need Fresh -- work coming only because the owner will run it -- and the owner waited for itself forever. Fix (shape): the prefetch is deleted (it memoized nothing), and the owner never reads through the readiness wait: its thread is marked (OwnerThread) and wait_for_derived_read debug-asserts it is not on it. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r8/necessity-r8-01-settle-self-wait.log (auditor probe on pre-fix 560db0e0)."
},
{
 "id": "REG-A-MISSED-EXTERNAL-EDIT-REACHES-THE-INDEX-AFTER-A-WHOLE-GRAPH-READ-543",
 "title": "An external edit the watcher first missed reaches search and queries on the rescan, even if a whole-graph read (publish, asset listing, Guide copy) parsed the page in between",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_lifecycle_tests.rs::gh543_an_acting_read_does_not_hide_a_missed_external_edit", "src-tauri/src/watcher/tests.rs::incremental_create_is_identified_as_inventory_change"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 8, R8-02 (seeds 1066/1141). A whole-graph read installs the parsed cache without sending it to the index; the watcher's unchanged checks (disk_revs, cached document equality, the shadow-journal branch) took the parsed cache for the index and skipped the page for the session. Fix: all three sites ask Graph::page_revision_current / index_has_revision -- the cache reflects the bytes AND the index has them (session publication or the ready image's stored source revision). The first cut treated a graph with no index and no parsed cache as always current; five src-tauri watcher tests caught it before commit. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r8/necessity-r8-02-acting-read.log."
},
{
 "id": "REG-A-REUSED-SNAPSHOT-ON-A-REOPENED-INDEX-TAKES-THE-QUEUE-ORDER-543",
 "title": "A page created after a full snapshot was reused on a reopened index does not fail every later index turn",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_lifecycle_tests.rs::gh543_a_reused_snapshot_on_a_reopened_image_takes_the_queue_order"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 8, R8-03 (seed 1023's first half). The seed-6 fix gave a reopened image the queue's dense order only on a clean warm validation; the worker's reuse_full branch applied deltas alone, so the image kept a deleted page's gap and the next new page took a stored page's position (UNIQUE constraint failed: pages.position). Fix (shape): every image-reuse path adopts the queue order through one helper (adopt_queue_order / PendingProjection::adopt_order), now in direct_projection/page_order.rs. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r8/necessity-r8-03-reuse-full.log."
},
{
 "id": "REG-A-RENAME-WITH-NOTHING-TO-SEND-LEAVES-THE-INDEX-CURRENT-543",
 "title": "Renaming a page whose content cannot be parsed does not leave search and queries without an index",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_lifecycle_tests.rs::gh543_a_rename_with_nothing_to_send_leaves_the_index_current"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 8, R8-07. A mover that claimed IndexEffect::Sent but queued an empty page set left the index behind the generation with nothing owed; the owner saw Nothing and derived reads had no producer. Fix: enqueue_page_set with no changes advances the index generation. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r8/necessity-r8-07-empty-send.log."
},
{
 "id": "REG-A-REFRESH-DOES-NOT-ANNOUNCE-THE-RETIRED-WARM-543",
 "title": "Refreshing a graph does not tell the window indexing finished before the replacement graph's pass has begun",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["src-tauri/src/graph.rs::a_refresh_does_not_announce_the_retired_warm"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 8, R8-05. A refresh retires the old graph in commit but moves the slot's warm generation only in swap_refreshed; the launch-settle guard and finish_warm consulted only the slot, so the retired owner announced warm-cache-done. Fix: one predicate, warm_revoked (cancelled, superseded, or retired), for both the early return and the owner's cancellation. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r8/necessity-r8-05-refresh-warm.log."
},
{
 "id": "REG-ALIASES-NEVER-BEAT-A-REAL-PAGE-AFTER-A-REPAINT-543",
 "title": "After a display change that repaints pages (typography, journal title format) and a save, a link to a real page still opens that page rather than another page that declares its name as an alias",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": ["src/navigationIndexEpoch.test.ts", "src/graph.test.tsx"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 8, R8-08. The navigation index is keyed by the render epoch; a repaint-only bump cleared it and the next save refreshed the aliases alone, so every alias beat the real page of the same name until the next create, delete or rename. Fix: no alias map is published until this epoch's page identities are loaded, and a refresh that finds the other half neither loaded nor requested fetches it (graph open still lists pages once; graph.test.tsx pins that). Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r8/necessity-r8-08-alias.log."
},
{
 "id": "REG-AN-HTML-EXPORT-RETRY-STOPS-AT-A-GRAPH-SWITCH-543",
 "title": "An HTML export waiting for the index stops when the window switches graphs instead of exporting the other graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": ["src/components/Settings.publishZero.test.tsx"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 8, R8-10. Settings' export retry checked only that the dialog was open, so a switch during the wait exported the newly bound graph. Fix: the retry is bound to graphBinding() at the click. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r8/necessity-r8-10-export.log."
},
{
 "id": "REG-PAGE-CURRENCY-ASKS-EVERY-STORE-543",
 "title": "Opening or re-reading a page is not re-indexed when Tine already holds its bytes, and an edit is never skipped because one store still holds older bytes",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_lifecycle_tests.rs::gh543_bytes_the_launch_build_carries_are_current_before_it_is_ready", "crates/tine-core/src/model_gh543_lifecycle_tests.rs::gh543_bytes_only_the_index_holds_are_current", "crates/tine-core/src/projection_producer_census.rs::page_currency_has_one_producer"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 9, R9-02 and R9-03 (R8-02 regression). Page currency had several producers, each trusting one store for another. Fix: page_revision_current asks every store with an opinion (parsed cache, served session record, index) and requires all of them to hold the revision; the index answers from what this session sent (queued and drained deltas, then the last full or warm inventory) before the stored image. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r9/necessity-C1-page-currency.txt."
},
{
 "id": "REG-A-RECOVERED-PAGE-IS-ONE-INDEX-UPDATE-543",
 "title": "A page recovered from a snapshot reaches the index as one update, with or without an index owner",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_lifecycle_tests.rs::gh543_a_recovered_page_is_one_update"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 9, R9-04. cache_upsert had a dead recovered-snapshot branch that queued nothing without an owner. Fix: the branch is gone; every upsert calls enqueue_replace under the lock. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r9/necessity-C1-page-currency.txt."
},
{
 "id": "REG-A-SNAPSHOT-REPAIRS-ONLY-WHAT-DIFFERS-543",
 "title": "One page edited while Tine was closed re-indexes that page at launch, not the whole graph, and search stays available meanwhile",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["crates/tine-core/src/model_gh543_lifecycle_tests.rs::gh543_a_snapshot_over_a_healthy_image_repairs_only_what_differs", "crates/tine-core/src/direct_projection_tests.rs::same_config_inventory_preserves_jobs_but_changed_config_cancels_them"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 9, R9-01. A full snapshot over a healthy image rebuilt from scratch whenever any page differed. Fix: full_repair_delta lowers only the pages the image differs by (an unread page keeps its rows); a fresh build only when a rebuild is owed, the image is absent or damaged, or the parse configuration changed. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r9/necessity-C2-whole-rebuild.txt."
},
{
 "id": "REG-A-SETTINGS-CHANGE-DOES-NOT-RESCAN-THE-GRAPH-543",
 "title": "Changing a setting (which rewrites config.edn) or renaming a page away does not make the file watcher rescan the whole graph or drop the page-identity index",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["src-tauri/src/watcher/tests.rs::a_config_write_reaches_only_the_configuration_queue", "crates/tine-core/src/model_watch_reach_tests.rs::a_gone_path_is_a_subtree_only_if_the_index_holds_a_file_under_it", "crates/tine-core/src/projection_producer_census.rs::watch_reach_has_one_producer"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 9, R9-05 and R9-06. Both watcher deciders read a gone path, or config.edn, as maybe a directory of pages. Fix: one answer, Graph::graph_text_watch_reach, used by the batch queue and the callback: configuration reaches only its own queue, and a gone path is a subtree only when the current identity index holds a file under it (or there is no current index). Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r9/necessity-C2-watcher.txt."
},
{
 "id": "REG-ANOTHER-GRAPHS-STATE-NEVER-SHOWS-543",
 "title": "After switching graphs, the previous graph's conflict banners, conflict listings and favourites arrangement never show on the new graph, even while the new graph's answers are still loading or fail",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["src/graphScopedState.test.tsx::does not show graph A's conflicts on graph B, pending or failed", "src/graphScopedState.test.tsx::drops graph A's late favourites arrangement once graph B has its own", "src/graphScopedState.test.tsx::drops graph A's failed favourites read once graph B is flat"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 9, R9-11 and R9-12 (I-20). Graph open emptied the store but not the conflict listings, and a favourites arrangement read from the old graph could land after the switch. Fix: loadGraphPath clears every conflict listing and revokes its in-flight refreshes (resetGraphConflicts); the favourites store carries an arrangement token that every read and write re-checks after each await. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r9/necessity-C3-frontend.txt."
},
{
 "id": "REG-A-GRAPH-REOPENED-BY-A-CONFIG-CHANGE-REFRESHES-THE-PAGE-SET-543",
 "title": "When config.edn changes outside Tine in a way that reopens the graph (for example :hidden), All Pages, page links and in-flight results follow the reopened graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["src/graphScopedState.test.tsx::revokes the old binding and repaints when the backend reopens the graph"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 9, R9-13. A command that reopens the graph is announced by the frontend when it returns (REBINDING_COMMANDS); the watcher's reopen was announced only when GraphMeta changed, which :hidden does not touch. Fix (shape): refresh_graph_for_label is private to state.rs; the watcher reopens only through refresh_graph_for_config_change, which emits graph-rebound, and the frontend revokes the binding and repaints (applyGraphReopened). Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r9/necessity-C3-frontend.txt."
},
{
 "id": "REG-THE-NAVIGATION-INDEX-IS-ASKED-ONCE-AND-COMPLETED-543",
 "title": "Page links and aliases resolve correctly after launch: the page list is read once, a failed read is asked again rather than letting aliases beat real pages, and a repaint during launch does not leave the index empty",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["src/navigationIndex.test.ts::lists the pages once at launch when a save refresh lands before the warm", "src/navigationIndex.test.ts::asks a failed page listing again instead of publishing aliases over no pages", "src/navigationIndex.test.ts::asks again at the new epoch when a repaint lands while the index is in flight"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 9, R9-08, R9-09 and R9-10. Fix: each half of the navigation index (aliases, page identities) is requested once per epoch (ensurePageIdentities), a failed half stays unloaded so the next refresh asks again, a half answered after a repaint-only epoch bump re-asks at the new epoch for the same binding, and graph open waits for the warm by binding rather than by epoch. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r9/necessity-C3-frontend.txt."
},
{
 "id": "REG-ONE-CONFIG-CHANGE-REOPENS-THE-GRAPH-ONCE-543",
 "title": "Changing the journal title format or restoring a backup reopens the graph once, so indexing restarts once rather than twice",
 "family": "direct-files-performance",
 "sources": {
  "issues": [543],
  "commits": []
 },
 "platforms": ["all"],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": ["src-tauri/src/state.rs::a_command_rewrite_runs_under_the_lane_its_reopen_holds", "src-tauri/src/backend_command_parity.rs::settings_commands_leave_reopening_to_the_config_reach", "src/instanceReplacement.test.ts::keeps the binding for a settings change (GH #543)"],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 9, R9-15b. set_journal_title_format wrote config.edn and then called refresh_graph, while the watcher also reopened the graph for the same graph-reaching change; its claimed reason (a title-named journal migration) is performed by no refresh (Concord invariant 4). It now only writes; the watcher reopens once and announces graph-rebound. restore_backup rewrote the graph before taking the transition lane, so the watcher could reopen between the restore and the command's reopen; refresh_graph now runs the command's rewrite under the lane, before the reopen, so the watcher's try-once reopen defers and then finds the configuration taken in. Fail-before: at the parent commit refresh_graph_for_label runs no rewrite, restore_backup calls restore_from_backup_source before refresh_graph, and the settings guard pinned set_journal_title_format as a reopening settings command. No Tauri app mock exists, so the duplicate itself is argued from the watcher's served-config comparison (a graph-reaching write stays untaken until a reopen, state.rs a_config_refresh_keeps_queries_answering), not counted at runtime."
},
{
 "id": "REG-A-WIDE-QUERY-ANSWERS-WITHOUT-A-REBUILD-543",
 "title": "A query with a very long or/and list answers, and a failed read on an intact index never rebuilds it",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r10_tests.rs::gh543_a_query_wider_than_sqlites_expression_depth_answers",
   "crates/tine-core/src/model_gh543_r10_tests.rs::gh543_a_read_refused_on_an_intact_image_does_not_rebuild_it",
   "crates/tine-core/src/query/sql_tests.rs::every_boolean_join_in_the_compiler_is_balanced"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 10, R10-01. The SQL compiler joined n-ary AND/OR flat, which SQLite nests one level per operand; a 985-term (or ...) (7 KB, inside the input caps) failed with 'Expression tree is too large'. The dispatcher treated every failed read as a damaged image and rebuilt the whole index, the rebuild succeeded (resetting the owner's backoff), and the query block retried NotReady forever: back-to-back whole-index rebuilds while the block was on screen. Fix: join_balanced keeps boolean depth at log2(n); a failed read requests a rebuild only when a schema check plus quick_check (once per ready generation) finds the image damaged, and otherwise answers Unavailable(ReadFailed). Fail-before: both model tests fail with either half reverted (evidence gh550-batch/indexing-audit-r10/necessity-R10-01.txt)."
},
{
 "id": "REG-A-CLOSED-GRAPH-CHANGE-TAKES-THE-CHEAPER-ROUTE-543",
 "title": "A graph changed while Tine was closed is repaired when few pages differ and built fresh when most do, on every path",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r10_tests.rs::gh543_a_config_changed_while_closed_is_built_fresh_on_every_path",
   "crates/tine-core/src/model_gh543_r10_tests.rs::gh543_a_few_pages_changed_while_closed_are_repaired_on_every_path",
   "crates/tine-core/src/model_gh543_r10_tests.rs::the_repair_share_bound_has_one_reader"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 10, R10-02. The warm validation applied a share bound (above a quarter of the pages differ -> fresh build) but the worker's full-snapshot repair path did not, so a parse configuration changed while Tine was closed was rewritten page by page in place whenever the snapshot arrived by the full path. Fix: one rule, repair::repair_is_proportionate, read by both repair paths (guard pins one reader). Fail-before: evidence gh550-batch/indexing-audit-r10/necessity-R10-02.txt."
},
{
 "id": "REG-A-CONSUMER-PARSE-STARTS-NO-INDEX-BUILD-543",
 "title": "A whole-graph read that parses the graph offers the index nothing; only the index owner starts whole-index work",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r10_tests.rs::gh543_a_consumer_parse_starts_no_index_build_during_the_backoff",
   "crates/tine-core/src/model_gh543_r10_tests.rs::only_the_index_owner_offers_a_full_snapshot"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 10, R10-03. A consumer's parsed-cache install (orphan assets, a read racing the warm) offered the index a full snapshot, refused only while work was coming, so during the owner's backoff it started a whole build. Shape: the consumer offer and its FullOffer enum are removed; the owner's pass offers the installed cache. Guard: exactly two production calls of direct_projection_enqueue_full. Fail-before: auditor probe audit543_r10_consumer_full_offer_bypasses_the_backoff (evidence gh550-batch/indexing-audit-r10/verify-by-manager-rust.log). Also closes a latent hole found by the full suite: a stale image no longer dedupes a re-offered identical snapshot."
},
{
 "id": "REG-THE-INDEXING-BAR-SHOWS-WHILE-READERS-WAIT-543",
 "title": "The indexing bar shows whenever readers are waiting for index work, including a full repair",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r10_tests.rs::gh543_the_bar_shows_while_readers_wait_for_a_full_repair"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 10, R10-04. indexing_progress decided from its own progress match, which hid the bar during a full-repair turn while readers got NotReady. It now asks the index decider (index_need) the same question readers' waits ask. Fail-before: auditor probe audit543_r10_bar_is_hidden_while_a_full_repair_runs (evidence gh550-batch/indexing-audit-r10/verify-by-manager-rust.log)."
},
{
 "id": "REG-WINDOW-EVENTS-REACH-ONLY-THEIR-WINDOW-543",
 "title": "An event addressed to one window is heard only by that window",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/windowEvents.test.ts::window-addressed events (GH #543, R10-05)"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 audit R10-05. Tauri delivers an emit_to(label) event to every JS listener registered with target Any, so graph-changed, graph-rebound, warm-cache-done and seven other window-scoped events reached every window. Shape: listenHere (src/windowEvents.ts) registers with the window's own target; guard: a bare listen names only an allowlisted broadcast, and no Rust emit_to site uses a broadcast name. Fail-before: probe R10-F2, evidence gh550-batch/indexing-audit-r10 (frontend lane notes; probes failed on 0c4c4f52)."
},
{
 "id": "REG-A-REOPENED-GRAPH-RELOADS-ITS-NAVIGATION-INDEX-543",
 "title": "After the backend reopens a graph, aliases and page identities follow the reopened graph, also during the launch warm",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/launchReadsGh543.test.ts::re-reads the navigation index after the backend reopens the graph",
   "src/launchReadsGh543.test.tsx::loads the navigation index at open and again on a reopen during the launch check"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 audit R10-06. graph-rebound bumped binding and epoch only; the navigation index was re-read only on dataRev/pageInventoryRev, and a reopen during the launch warm made loadAliases give up. Fix: onGraphRebound re-asks the navigation index on every rebind. Fail-before: probes R10-F4 and R10-F4b, evidence gh550-batch/indexing-audit-r10 (frontend lane notes; probes failed on 0c4c4f52)."
},
{
 "id": "REG-SAVES-DURING-INDEXING-QUEUE-NO-EXTRA-WHOLE-GRAPH-READS-543",
 "title": "Saves made while the index is coming leave at most one block-ref count and one alias read behind them",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/launchReadsGh543.test.ts::keeps one block_ref_counts in flight and one more for a burst of saves",
   "src/launchReadsGh543.test.ts::keeps at most one page_aliases in flight while the index is coming"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 audit R10-09. blockRefCounts re-checked only the epoch after its warm wait, so every save during the pass issued its own whole-graph block_ref_counts when the warm ended; page_aliases ran once per dataRev, in parallel. Fix: the revision is re-checked after the wait; alias reads coalesce per (binding, epoch) with one re-ask. Fail-before: probes R10-F1 and R10-F3, evidence gh550-batch/indexing-audit-r10 (frontend lane notes; probes failed on 0c4c4f52)."
},
{
 "id": "REG-ONE-PAGE-LIST-PER-GRAPH-OPEN-543",
 "title": "A graph open lists its pages once; every reader shares one page-list producer",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/launchReadsGh543.test.tsx::lists the pages once per graph open",
   "src/launchReadsGh543.test.ts::asks the backend for the page list in one place"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 audit R10-10. All Pages (pages.ts) and the navigation index (graph.ts) each called listPages, so every open and every create/delete listed the whole graph twice. Shape: listGraphPages (src/pageList.ts) memoizes one read per binding, epoch and inventory revision; guard: listPages() is called only there. Fail-before: probe R10-F5, evidence gh550-batch/indexing-audit-r10 (frontend lane notes; probes failed on 0c4c4f52)."
},
{
 "id": "REG-A-REPAINT-KEEPS-THE-INDEXING-BAR-543",
 "title": "A repaint that does not rebind the graph keeps the indexing bar's follower",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/components/IndexingProgressBar.test.tsx::keeps its follower across a repaint and replaces it on a rebind"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 audit R10-11. The bar restarted its follower (and its show-after grace) on every render-epoch bump, so a typography or journal-title change hid a showing bar for about a second. Fix: the follower is keyed on graphBinding. Fail-before: the component test fails on HEAD's IndexingProgressBar.tsx (2 followers after a repaint)."
},
{
 "id": "REG-A-GRAPH-REACHING-SETTING-TAKES-EFFECT-WITHOUT-A-WATCHER-543",
 "title": "A settings change that reaches the graph is taken in by the command itself, once, even without a live file watcher",
 "family": "config-live-reload",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/state.rs::a_settings_write_decides_its_own_reopen",
   "src-tauri/src/state.rs::a_settings_write_takes_its_change_in_through_the_one_decider",
   "src-tauri/src/state.rs::a_missing_os_watcher_is_reported_and_polled_around"
  ],
  "scenario": null,
  "failBefore": "structural"
 },
 "notes": "GH #543 indexing audit round 10, R10-07 (class: a command's completion delegated to a best-effort observer). After R9-15b the journal-title-format change relied on the watcher, and a failed recommended_watcher() was swallowed. Now state::apply_config_write (the only command path; GraphSlot::apply_config_write is private) reopens through take_in_config_change, the one decider the watcher also uses, re-decided under the transition lane so one change reopens once; a failed OS watcher emits graph-watch-error and the cycle polls. Substitute evidence: the app-bound path needs an AppHandle (no tauri test runtime), so the decider is tested app-free and the wiring by source guards; the pre-fix code lacks both wires."
},
{
 "id": "REG-AN-OUTSIDE-PAGE-DELETE-RESCANS-ONE-PATH-543",
 "title": "An external delete or rename-away of a page file is one exact path, not a whole-graph rescan",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_watch_reach_tests.rs::a_gone_path_is_answered_by_the_parsed_cache_or_the_index_inventory",
   "src-tauri/src/watcher/tests.rs::ambiguous_windows_event_kinds_take_the_exact_path_arm_unless_pages_went"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 10, R10-08 (class C2b: one watch reach). A gone path's reach needed the guarded identity index, which is usually absent, so every external delete ran a whole-graph stat walk and revoked conflict authority. graph_text_watch_reach now answers from the identity index, else the index's page inventory, else a complete parsed cache, else lexically by page extension. Fail-before: both core branches and the watcher boundary (evidence gh550-batch/indexing-audit-r10/necessity-R10-08.txt)."
},
{
 "id": "REG-A-REFUSED-GRAPH-SLOT-IS-RETIRED-543",
 "title": "A graph the window registry refuses to bind is retired, and a restore whose window moved on reports success",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/state.rs::a_refresh_does_not_swap_over_a_window_that_moved_on",
   "src-tauri/src/state.rs::a_slot_the_registry_refuses_is_retired"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 10, R10-12 (class: a slot handed to the registry is bound or retired, never dropped). swap_refreshed's refusal dropped a reopened graph with its projection attached and failed a restore that had succeeded on disk; bind's overlap refusal dropped the same way. Both now retire the refused slot. Fail-before: evidence gh550-batch/indexing-audit-r10/necessity-R10-12.txt."
},
{
 "id": "REG-NO-RECONCILE-INTO-A-GRAPH-MID-TRANSITION-543",
 "title": "The watcher does not reconcile files into a graph while its load or reopen holds the transition lane",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/watcher/tests.rs::the_watcher_reconciles_a_graph_only_under_its_transition_lane"
  ],
  "scenario": null,
  "failBefore": "structural"
 },
 "notes": "GH #543 indexing audit round 10, R10-13 (PLAUSIBLE, fixed by rule). During a restore's rewrite the watcher lowered the restored files into the graph about to be retired. The reconcile loop now try-locks the root's transition lane, skips the graph when it is held and carries a full diff to the next cycle. Substitute evidence: the runtime loop needs an AppHandle, so a source guard pins the order and the never-wait rule; not reproduced on a 10k graph."
},
{
 "id": "REG-A-REINDEX-OVER-AN-UNREADABLE-PAGE-IS-STOPPABLE-543",
 "title": "Re-indexing the graph while one page cannot be read builds fresh, keeps that page searchable, and stops promptly on close",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r11_tests.rs::gh543_a_config_change_over_an_unreadable_page_rebuilds_and_keeps_it",
   "crates/tine-core/src/model_gh543_r11_tests.rs::gh543_a_close_stops_an_in_place_repair_between_batches",
   "crates/tine-core/src/model_gh543_r11_tests.rs::gh543_a_carried_page_lowers_as_its_file_parses",
   "crates/tine-core/src/projection_producer_census.rs::page_lowering_has_one_loop"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 11, R11-01 (decision DK4). An incomplete snapshot (one unreadable page) was never built fresh: the owner re-lowered the whole graph in place as one repair turn, ~4x slower per page than a staged build (10k pages: 74 s) and unstoppable (close waited 71 s). Fix: one lowering loop, lower_in_batches, for the fresh build, its carried pages, the in-place repair and queued deltas; it commits in batches, checks the stop flag between them and reports progress. A fresh build carries the unreadable pages by re-lowering them from the rows the old image stored (raw block text + tree + preamble), with their stored source revision, so they are re-read once readable. 10k scale: turn 74.0 s -> 16.8 s, close 71.3 s -> 6.6 ms. Fail-before: both model tests fail on 08dbc570 (evidence gh550-batch/indexing-audit-r11/necessity-R11-01.txt)."
},
{
 "id": "REG-A-SETTING-CHANGED-DURING-A-REOPEN-IS-KEPT-543",
 "title": "A setting changed while the graph is being reopened (a restore, a config reopen) is served by the reopened graph and not lost",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/state.rs::a_settings_write_during_a_reopen_reaches_the_replacement",
   "src-tauri/src/state.rs::every_graph_handover_serves_the_config_on_disk"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 11, R11-03. A reopen opens its replacement graph from config.edn, then swaps it in; a settings command (or the watcher) that arrived in between took the change into the slot being replaced, so the replacement served the older value and nothing asked again. After a restore the frontend's forceRefresh load answered AlreadyCurrent with that stale meta and re-seeded favorites without the new one; favorites are written as a whole list, so the next toggle removed it from disk. Fix: state::serve_disk_config at every hand-over (reopen swap, open bind, AlreadyCurrent) takes a settings change in and emits graph-config-changed, or spawns the one decider for a graph-reaching change. Fail-before: the hand-over guard fails without the calls; backend state per the auditor probe (evidence gh550-batch/indexing-audit-r11/necessity-R11-03.txt)."
},
{
 "id": "REG-AN-UNWATCHED-GRAPH-ROOT-IS-POLLED-543",
 "title": "Changes to a graph the system refuses to watch (inotify limit) are still seen, and caught up once watching succeeds",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "linux",
  "macos",
  "windows"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/watcher/tests.rs::an_unwatched_root_is_polled_and_diffed_when_watched"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 11, R11-04. A per-root watch() failure (typically fs.inotify.max_user_watches with a second large graph) emitted graph-watch-error and left the root out of `watched`, but the cycle still ran in event mode for it: no full diff, no configuration check, only a watch retry every 3 s; and the cycle the watch finally succeeded did not diff what changed meanwhile. Fix: a root no watched directory holds is polled like poll mode (full diff, poll observation, config recheck), assets under it too, and a newly watched root is diffed once. Proof is structural (the loop needs an AppHandle); evidence gh550-batch/indexing-audit-r11/necessity-R11-04-05.txt."
},
{
 "id": "REG-A-RESTORE-DOES-NOT-RESYNC-EVERY-RESTORED-FILE-543",
 "title": "After a restore or config reopen, the watcher does not re-sync every file the reopened graph already read",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/watcher/tests.rs::a_reopened_graph_is_diffed_against_its_own_baseline",
   "src-tauri/src/watcher/tests.rs::the_watcher_rebaselines_a_replaced_graph"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 11, R11-05 (R10-13 residual). The same-root watcher entry kept the old graph's stamp baseline across a reopen, so the carried full diff reported every file a restore rewrote (200/200 for byte-identical copies) into the new graph via sync_file_checked, overlapping the new graph's own owner pass. Fix: when the slot hands the watcher a different Graph for the root, the watcher takes that graph's baseline. Evidence gh550-batch/indexing-audit-r11/necessity-R11-04-05.txt."
},
{
 "id": "REG-A-FAILED-INDEX-TURN-IS-VALIDATED-NOT-REBUILT-543",
 "title": "One failed index write on an intact index re-checks the index instead of rebuilding it whole",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r11_tests.rs::gh543_a_failed_turn_on_an_intact_image_is_validated_not_rebuilt"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 11, R11-07 (class K1). A worker turn that failed (a disk error, a busy database) latched a whole fresh build unconditionally: ~17 s of re-indexing on 10k pages with search withdrawn, for a transient blip. The turn rolled back; on an intact image a validation re-derives what it carried. Only a turn that was building a new image, or one on a damaged image, owes a new one. Evidence gh550-batch/indexing-audit-r11/necessity-R11-06-07.txt."
},
{
 "id": "REG-CONTRADICTORY-INDEX-ROWS-ARE-REBUILT-ONCE-543",
 "title": "An index whose rows contradict each other is rebuilt, so task and reference queries recover in the same session",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "integration",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r11_tests.rs::gh543_contradictory_rows_rebuild_the_index_once"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 11, R11-06 (class K1). A read that found contradictory rows answered InvalidSnapshot, but the repair asked only schema + quick_check, which cannot see logical damage, so queries failed for the rest of the session. The reader's own evidence now owes one rebuild per projection; a contradiction on the rebuilt image is a lowering defect and is not rebuilt again. Reachable only through a Tine defect writing such rows (precedent: the stage-0 seed-6 pages.position bug). Evidence gh550-batch/indexing-audit-r11/necessity-R11-06-07.txt."
},
{
 "id": "REG-SAVES-DURING-INDEXING-KEEP-ONE-LOOKUP-WAITING-543",
 "title": "Saving while the graph indexes keeps one lookup waiting per surface instead of one per save",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/gh543R11Reads.test.tsx::block references keep one read in flight across saves",
   "src/gh543R11Reads.test.tsx::a visible block ref re-resolved after each save sends one batch at a time",
   "src/gh543R11Reads.test.tsx::a batch superseded while it queued answers its waiters at the newest revision",
   "src/gh543R11Reads.test.tsx::a read for a new graph epoch does not wait behind one parked on the old graph",
   "src/dataRevReads.guard.test.ts::every use of dataRev has a named owner"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 11, R11-09 (class: who reads the index again on every save?). Block references, block-ref resolution batches, reference counts, the calendar, embeds and the peek preview re-fetched on every dataRev bump; during an index pass each fetch waits in the backend, so a burst of saves parked one read per save. They now read through readLane (src/readLane.ts): one read in flight per source, a read superseded while queued is skipped, and a lane is keyed to graph binding + epoch so a read parked on the old graph never holds up the new one. The guard pins every dataRev use with its owner. Evidence gh550-batch/indexing-audit-r11/necessity-R11-09-12.txt."
},
{
 "id": "REG-RESTORED-SIDEBAR-PINS-CHECKED-IN-ONE-REQUEST-543",
 "title": "Restored right-sidebar block pins are checked in one request, and a failed check keeps them",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/gh543R11Reads.test.tsx::resolves every restored pin in one command and keeps pins a failed read cannot judge"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 11, R11-10. pruneSidebarBlocks issued one resolve_block per pin at launch, each waiting on the index pass; it now issues one resolve_blocks and keeps every pin when that read fails. Evidence gh550-batch/indexing-audit-r11/necessity-R11-09-12.txt."
},
{
 "id": "REG-INDEXING-BAR-FOLLOWER-SURVIVES-FAILED-POLLS-543",
 "title": "The indexing progress bar keeps following after its status reads fail, so a later pass is still shown",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/indexingProgress.test.ts::keeps a failing follower at the watch cadence until its binding ends (GH #543, R11-12)",
   "src/indexingProgressBarFollower.test.tsx::starts again after its follower ended, within one binding"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 11, R11-12. Ten failed polls ended the follower, and nothing restarted it while the binding stood, so a later index pass showed no progress for the session; the bar's singleton also kept a reference to the ended follower. The follower now slows to the watch cadence and ends only with its binding, and the bar forgets a follower that ended. The old test 'gives up when every poll fails' pinned the removed behaviour and was rewritten to the new contract. Evidence gh550-batch/indexing-audit-r11/necessity-R11-09-12.txt."
},
{
 "id": "REG-CONTRADICTION-DURING-BULK-UPDATE-REBUILT-543",
 "title": "An index found inconsistent while a long update turn or a fresh build's finish runs is rebuilt exactly once",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r12_tests.rs::a_contradiction_during_a_bulk_update_turn_is_rebuilt",
   "crates/tine-core/src/model_gh543_r12_tests.rs::a_contradiction_during_a_small_update_turn_is_rebuilt",
   "crates/tine-core/src/model_gh543_r12_tests.rs::a_rebuild_asked_for_while_a_fresh_build_finishes_is_that_build",
   "crates/tine-core/src/model_gh543_r12_tests.rs::index_progress_is_presentation_only"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 12, R12-01. The decider asked 'is a fresh build replacing the image?' through the presentation progress counter, which also counts any lowering over one batch and stops before a fresh build's finish: a contradiction met during a bulk update spent its once-per-projection rebuild on nothing (queries failed for the session), and a rebuild asked for during the finish queued a second whole-graph build. The answer is now the worker-owned fresh_build_running flag, set when a turn decides a fresh build and cleared when its outcome is recorded; the guard keeps the counter presentation-only. Evidence gh550-batch/indexing-audit-r12/necessity-r12-core.txt."
},
{
 "id": "REG-MOVE-WHILE-VALIDATION-OWED-KEPT-543",
 "title": "A page change while an index re-check is owed is not lost, so listing pages afterwards reads no page",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r12_tests.rs::a_move_while_a_validation_is_owed_is_not_lost"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 12, R12-02. advance_generation returned early while a validation was owed, dropping the move; readiness never reached the moved generation and list_pages parsed the graph. The move is always recorded; readiness still waits for the validation. Evidence gh550-batch/indexing-audit-r12/necessity-r12-core.txt."
},
{
 "id": "REG-CORRUPT-DERIVED-ROW-REBUILDS-INDEX-543",
 "title": "An index row a read cannot decode asks for a new index image instead of parsing the graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r12_tests.rs::a_corrupt_derived_row_asks_for_a_new_image",
   "crates/tine-core/src/model_gh543_r12_tests.rs::index_readers_report_damage"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 12, R12-05. Index readers turned SQL/decode errors into None with .ok(), so detected damage never reached failure_owes_new_image and every session parsed the graph instead. Readers now call .reported(self), which maps the error to an IndexFailure and asks the one decider; indexed_read waits for the owed image. Guard: no raw .ok() on an index answer. Evidence gh550-batch/indexing-audit-r12/necessity-r12-core.txt."
},
{
 "id": "REG-WATCHER-REPLACED-GRAPH-ONE-REBIND-543",
 "title": "The file watcher takes a replaced graph on in one place, and scans a newly watched graph once",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/watcher/tests.rs::the_watcher_rebaselines_a_replaced_graph",
   "src-tauri/src/watcher/tests.rs::a_replaced_graph_is_taken_on_in_one_place",
   "src-tauri/src/watcher/tests.rs::an_unwatched_root_is_polled_and_diffed_when_watched"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 12, R12-03 and R12-07. The R11-05 rebaseline guard sliced the wrong arm and could not fail; the two same-root arms reset different fields. Both now call WatchedGraph::rebind (a full reset through WatchedGraph::new, keeping only a drained frontier the new graph owns), pinned behaviourally. A first cycle no longer re-walks a polled or newly watched root it just walked for its baseline. Mutation checks: gh550-batch/indexing-audit-r12/necessity-r12-tauri.txt."
},
{
 "id": "REG-QUERY-PARSE-RETRY-ENDS-WITH-BLOCK-AND-GRAPH-543",
 "title": "A query block's wait for the index ends when the block is removed or the graph is switched",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/queryParseRetryOwner.test.tsx::stops polling query_parse once the block is gone",
   "src/queryParseRetryOwner.test.tsx::stops polling query_parse once the app switches to another graph",
   "src/queryReadinessOwners.guard.test.ts::gives every component retry a lifetime that ends with the component"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 12, R12-06. The query_parse readiness retry was gated only on its own request, which never changes after unmount, so a removed block polled for as long as the index stayed not ready, and into the next graph. runQueryWhenCurrent now owns the binding half for every caller; each caller's gate names its disposal (owner table). Evidence gh550-batch/indexing-audit-r12/necessity-r12-frontend.txt."
},
{
 "id": "REG-QUERY-WAIT-SURVIVES-SAME-GRAPH-REOPEN-543",
 "title": "A query block, export dialog or publish waiting for the index answers after the same graph reopens",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src/queryReadiness.test.ts::reads the reopened graph when the same graph is reopened",
   "src/queryReadiness.test.ts::ends when the app switches to another graph",
   "src/queryParseRetryOwner.test.tsx::reads its query once the same graph is reopened",
   "src/components/QueryExportDialog.test.tsx::shows the plan after the same graph is reopened while it waits",
   "src/components/Settings.publishZero.test.tsx::exports the same graph after it is reopened while it waited",
   "src/queryReadinessOwners.guard.test.ts::gives every component retry a lifetime that ends with the component"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 13, R13-01 (regression from 39bf74dc) and R13-08 (the r12 owners guard did not fail when its fix was removed). A config.edn reopen bumped the graph binding, which cancelled a readiness retry on the same graph, and nothing asked again. The retry's graph identity is now the captured root; its end is a Lifetime made only by componentLifetime (ends in onCleanup) or manualLifetime. Evidence gh550-batch/indexing-audit-r13/RECEIPT-indexing-audit-r13.md; fix d5d3c894."
},
{
 "id": "REG-WATCHER-SAME-ROOT-REBIND-KEEPS-ASSET-CHANGE-543",
 "title": "An asset change queued as the same graph reopens is still delivered",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "src-tauri/src/watcher/tests.rs::a_replaced_graph_keeps_a_queued_asset_change",
   "src-tauri/src/watcher/tests.rs::a_moved_asset_folder_is_watched_afresh"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 13, R13-02 (regression from 254690f2). A same-root rebind replaced the watched graph and its asset state together, dropping an asset change queued for that cycle. WatchedGraph::take_on is the one hand-over: asset state is kept when the asset root is unchanged. Evidence gh550-batch/indexing-audit-r13/RECEIPT-indexing-audit-r13.md; fix 1d9b1661."
},
{
 "id": "REG-LAUNCH-WALK-SURVIVES-NAMED-CHANGES-543",
 "title": "A launch check is not thrown away, and the graph not parsed, by a failed turn, a journal migration or a file rescue",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r13_tests.rs::a_failed_launch_validation_turn_on_an_intact_image_is_validated_again",
   "crates/tine-core/src/model_gh543_r13_tests.rs::a_change_during_the_launch_walk_keeps_the_walk",
   "crates/tine-core/src/model_gh543_r13_tests.rs::only_tests_record_an_unnamed_change"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 13, R13-03 and R13-04. The owner's private escalate flag turned a failed validation turn on an intact image into a whole-graph parse (K1 now decides alone); migration and rescue recorded an unnamed structural change though they knew their paths, so a walk in flight was discarded. discard_parsed_cache now takes the touched paths (StructuralChange::Reread); Unnamed is test-only. Necessity gh550-batch/indexing-audit-r13/necessity-r13-core.txt."
},
{
 "id": "REG-INDEX-READS-DECODED-ROWS-543",
 "title": "A damaged page kind is rebuilt, and a title::-dated journal keeps its day, from the index",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r13_tests.rs::a_corrupt_kind_in_a_derived_page_asks_for_a_new_image",
   "crates/tine-core/src/model_gh543_r13_tests.rs::a_journal_titled_by_a_year_less_format_is_listed_from_the_index",
   "crates/tine-core/src/model_gh543_r13_tests.rs::the_model_reads_decoded_index_rows",
   "crates/tine-core/src/direct_projection_tests.rs::extractor_version_participates_in_disposable_source_revision"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 13, R13-05 and R13-06. The model decoded stored kinds itself and dropped damage as None; the index derived a journal's day from the file stem, which disagreed with the page's own date_key for a title::-named journal and failed the inventory under a year-less title format. Kinds are decoded once (derived_reads::page_kind, reported Corrupt); pages.journal_day is PageEntry::date_key (lowering, carried pages keep their row's day); facts version 3 relowers old images. Necessity gh550-batch/indexing-audit-r13/necessity-r13-core.txt."
},
{
 "id": "REG-WAITING-INDEX-READ-SLEEPS-543",
 "title": "A lookup waiting for announced index work sleeps instead of spinning a core",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r13_tests.rs::a_read_waiting_for_announced_work_sleeps"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 13, R13-07. indexed_read looped on coming() while the readiness wait returned at once, spinning a core; it now waits on the projection's change condvar (wait_while_coming, 50 ms bound). Necessity gh550-batch/indexing-audit-r13/necessity-r13-core.txt."
},
{
 "id": "REG-INDEX-RECONCILER-PANICKING-PAGE-543",
 "title": "A page whose parse panics no longer keeps the index from ever becoming ready at launch",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r14_tests.rs::gh543_r14_panicking_page_changed_between_sessions_becomes_ready",
   "crates/tine-core/src/model_gh543_r14_tests.rs::gh543_r14_panicking_page_from_an_earlier_session_becomes_ready"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 14, R14-01 (regression from r13 5a7e66b7). The launch walk could not settle a panicking page and nothing decided a rebuild, so the owner re-walked forever. Fixed by replacing the walk/repair protocol with a reconciler (per-page generation marks, one survey from bytes): the survey records the parse failure and the page keeps its rows. Fail-before tine-agents/evidence/gh550-batch/reconciler/r14-fail-before.log."
},
{
 "id": "REG-INDEX-RECONCILER-MIDSESSION-PATHS-543",
 "title": "A migrated journal or a rescued page stays in the index when a check runs mid-session",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r14_tests.rs::gh543_r14_mid_session_walk_keeps_a_migrated_journal",
   "crates/tine-core/src/model_gh543_r14_tests.rs::gh543_r14_mid_session_walk_keeps_a_rescued_page",
   "crates/tine-core/src/model_gh543_r14_tests.rs::gh543_r14_control_reread_without_a_walk"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 14, R14-02 (regression from r13). A mid-session walk kept only pages from its own listing, so a path named by a re-read was deleted from a ready index. The reconciler records every named path as a mark at its generation; the survey never overrides a newer mark. Fail-before tine-agents/evidence/gh550-batch/reconciler/r14-fail-before.log."
},
{
 "id": "REG-INDEX-RECONCILER-SAVE-DURING-CHECK-543",
 "title": "An editor save during an index check no longer removes the saved page from the index",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r14_tests.rs::gh543_r14_a_page_saved_during_a_validation_walk_stays_indexed",
   "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_seeded_interleavings_converge"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 14, R14-03 (interleaving harness seed 1085). A save briefly leaves no file at the page's name; the walk read that as a deletion and its carried repair was applied after the save's own update. Marks older than what the image holds for a page are dropped, and a stored page is deleted only after a read under the graph-text identity gate finds it gone. Fail-before tine-agents/evidence/gh550-batch/reconciler/r14-fail-before.log."
},
{
 "id": "REG-INDEX-CONSTRAINT-TURN-REBUILDS-543",
 "title": "An index write that meets contradictory rows rebuilds the index once instead of retrying forever",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r12_tests.rs::an_update_turn_that_meets_contradictory_rows_rebuilds_the_index_once",
   "crates/tine-core/src/direct_projection_tests.rs::a_constraint_violation_reads_as_contradictory_rows"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543, found while building the reconciler. An update turn whose writes violated a UNIQUE/NOT NULL/CHECK constraint was classified as a transient failure, so the owner backed off forever and the index never became ready; quick_check does not see such rows. IndexFailure::of_turn classifies a constraint violation as ContradictoryRows, which spends the once-per-projection token and rebuilds. Fail-before tine-agents/evidence/gh550-batch/reconciler/constraint-turn-fail-before.log."
},
{
 "id": "REG-CREATION-DURING-SURVEY-NO-PARSE-543",
 "title": "Creating a page at launch beside an unparseable page that could be it is refused without parsing the whole graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r14_tests.rs::gh543_a_creation_waiting_for_the_survey_refuses_without_parsing",
   "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_seeded_interleavings_converge"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543, found by the extended interleaving harness (seed 4, panicking-page op). The launch survey published readiness before its parse failures, so a creation waiting for it read no failure, then found it and parsed the whole graph only to refuse. Failures are now published before readiness, and a creation whose index answer was withheld by a failure re-checks the evidence and refuses without the parse. Fail-before tine-agents/evidence/gh550-batch/reconciler/creation-during-survey-fail-before.log (3/3)."
},
{
 "id": "REG-SURVEY-FINDINGS-MOVE-GENERATION-543",
 "title": "A page deleted while Tine was closed leaves the page list when the launch survey confirms it",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r14_tests.rs::gh543_a_page_deleted_while_closed_leaves_the_cached_page_list",
   "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_seeded_interleavings_converge"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543, interleaving seed 2503. With a writer holding the identity gate at launch, the survey confirmed a deletion only after publishing readiness and without moving the cache generation, so a page list memoized at that generation kept listing the page for the session. Every survey finding (marks, confirmed deletions, an owed fresh build) now moves the generation as any other change does. Fail-before tine-agents/evidence/gh550-batch/reconciler/deferred-absence-page-list-fail-before.log (3/3)."
},
{
 "id": "REG-ONE-UNREADABLE-PAGE-BLOCKS-CREATION-543",
 "title": "One page Tine cannot read no longer blocks creating unrelated pages or makes the watcher retry forever",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_tests_unreadable_pages.rs::one_unreadable_page_blocks_neither_unrelated_creation_nor_the_watcher",
   "crates/tine-core/src/model_tests_unreadable_pages.rs::an_unreadable_page_is_announced_once_per_breakage",
   "crates/tine-core/src/model_tests_unreadable_pages.rs::cold_malformed_owner_refuses_without_census_or_mutation",
   "src/lib/unreadablePages.test.ts"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Found while reconciling the GH #543 harness. A page whose bytes were not UTF-8 or that the parser rejected refused every name-only page creation for the session, and the watcher treated the deterministic rejection as a failed cycle: it retried forever, never acknowledged the observation, and toasted that it would keep retrying. Creation now refuses only a name the failed file could be (its file name, or a name its folded bytes contain), naming the file; the watcher records the rejection as reconciled state and the app announces the page once (graph-unreadable-pages). Refusal scenario DIRECT-REF-CREATE-UNREADABLE-OWNER. Fail-before tine-agents/evidence/gh550-batch/reconciler/one-unreadable-page-fail-before.log; the old refusal was pinned by the previous forms of the cold/warm creation tests."
},
{
 "id": "REG-PROGRESS-PUBLISH-RACE-543",
 "title": "A query asking while the index becomes ready is not told the index is unreadable",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::readiness_published_while_asking_is_not_reported_stale"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543, seen as a full-suite-only failure of a_listing_overtaken_by_an_edit_reads_the_index_instead_of_parsing. progress_at read readiness before taking the queue lock under which the worker publishes it, so a turn ending in between read as 'not ready, nothing queued' = Stale, which a query reports as ReadFailed. Readiness is now read under the lock. Fail-before tine-agents/evidence/gh550-batch/reconciler/progress-publish-race-fail-before.log (3/3)."
},
{
 "id": "REG-CACHE-DECLINE-DISCARD-543",
 "title": "A page renamed right after a publish does not make the page list, aliases or search read the whole graph",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r14_tests.rs::gh543_a_cache_discarded_after_a_read_chose_it_costs_no_parse",
   "crates/tine-core/src/model_gh543_r14_tests.rs::index_fallbacks_go_through_indexed_or_fallback"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543, long interleaving run seed 3012 (1 in ~4,500 seed runs). A derived read declined in favour of an installed parsed cache; a rename discarded the cache before the reader read it, and the reader parsed the whole graph although the discard had queued its delta. Every index fallback now asks through Graph::indexed_or_fallback, which reads the named cache at once and sends a read whose cache vanished back to the index; a derived source guard pins every fallback to it. Fail-before tine-agents/evidence/gh550-batch/reconciler/cache-decline-discard-fail-before.log (3/3, 4 consumer parses)."
},
{
 "id": "REG-UNREADABLE-RECORD-DISK-TRUTH-543",
 "title": "A page Tine cannot read keeps refusing a new page of its name until the file is fixed, restored or deleted, and only then",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_survey_keeps_its_failures_across_a_mid_survey_edit",
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_survey_keeps_its_failures_control",
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_journal_migration_keeps_the_unreadable_owner_refusal",
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_rename_keeps_the_unreadable_owner_refusal",
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_rename_after_an_edit_keeps_the_unreadable_owner_refusal",
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_control_no_rename_keeps_refusing",
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_deleting_the_unreadable_page_clears_its_refusal_watcher_delete",
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_deleting_the_unreadable_page_clears_its_refusal_sync_file",
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_a_vanished_file_is_not_recorded_as_unreadable",
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_a_listing_skip_does_not_refuse_every_creation",
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_an_unreadable_date_titled_page_refuses_its_journal",
   "crates/tine-core/src/model/unreadable_pages.rs::tests::retire_removes_a_file_and_its_listing_skip_but_not_its_directory",
   "crates/tine-core/src/model/unreadable_pages.rs::tests::merge_keeps_the_newer_observation_per_path",
   "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_seeded_interleavings_converge"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 15, R15-01..R15-06 and H1 (DIRECT-REF-CREATE-UNREADABLE-OWNER). page_index_failures was a copy of disk truth tied to the parsed-cache lifecycle: the survey dropped it when the generation moved, every cache discard (rename, merge, rescue, journal migration) cleared it, the watcher recorded vanished files, deletes never retired it, a FIFO listing skip refused every name, and a date title:: missed its journal. It is now one owned type (UnreadablePages) written only per path under the cache lock, merged by drift for the survey. The interleaving oracle asserts the record at settle both ways. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r15/RECEIPT-indexing-audit-r15.md (10 probes at c79a2546) and 9/60 long-run seeds with the record oracle. The rename probes passed at c79a2546 (a rename then restored the record from its memo, now removed) and stay as neighbors; the sync_file delete variant now follows the watcher's sync_deleted_file."
},
{
 "id": "REG-INDEX-ANSWER-WAITED-GENERATION-543",
 "title": "Page-name suggestions and block-reference candidates answer from the index like every other read, including names only an unreadable page references",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_cache_fallback_answers_like_the_index_for_an_unreadable_page",
   "crates/tine-core/src/model_gh543_r15_tests.rs::index_answers_are_asked_at_a_waited_generation"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 15, R15-07. direct_projection_referenced_page_names and _block_referrer_candidate_pages asked the index at a generation read straight from cache_gen, so mid-turn they declined and answered from a parsed cache lacking an unreadable page's retained rows. Both now go through indexed_read; a derived source guard pins every generation-keyed DirectProjection answer to a waited generation (necessity: reverting the names read fails it). Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r15/RECEIPT-indexing-audit-r15.md."
},
{
 "id": "REG-FRESH-BUILD-CONTRADICTION-LATCH-543",
 "title": "An index build that fails with contradictory rows twice gives up instead of rebuilding forever",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_a_deterministically_failing_fresh_build_is_not_repeated",
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_fresh_pass_over_a_cache_with_one_failure_does_not_reparse_the_graph"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 15, R15-08 (and R15-11's dead-branch residue). A fresh build that failed deterministically was rebuilt on backoff forever, bypassing the contradiction_rebuilt latch. A second ContradictoryRows from a fresh build now marks the worker failed and unavailable, so readers take their ordinary route. Superseded 2026-09-24 by REG-INDEX-LIVENESS-594: every failure class (not only contradictions) now ends in Failed(code) after INDEX_ATTEMPTS = 3; the test asserts builds <= INDEX_ATTEMPTS and progress Failed(constraint). Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r15/RECEIPT-indexing-audit-r15.md."
},
{
 "id": "REG-RENAME-UNREADABLE-REFERRER-NAMED-543",
 "title": "A rename blocked by a page Tine cannot read says which file blocked it",
 "family": "direct-files-performance",
 "sources": {
  "issues": [
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh543_r15_tests.rs::gh543_r15_a_rename_an_unreadable_page_fails_names_that_page"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "GH #543 indexing audit round 15, R15-09 (partial). The referrer scan's read error named no file; it now carries the file path. Skipping the unreadable referrer and reporting it, so the rename proceeds, needs a DTO field and a frontend notice and is carded separately. Fail-before: tine-agents/evidence/gh550-batch/indexing-audit-r15/RECEIPT-indexing-audit-r15.md (probe audit543_r15_one_non_utf8_page_does_not_block_every_rename)."
},
{
 "id": "REG-REFERENCES-EDITED-PAGE-REOPEN-594",
 "title": "Linked and Unlinked References keep the blocks of a page edited before a relaunch",
 "family": "query-readiness",
 "sources": {
  "issues": [
   594
  ],
  "commits": [],
  "internal": ""
 },
 "platforms": [
  "all"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh594_liveness_tests.rs::gh594_a_page_edited_before_a_reopen_keeps_its_references",
   "crates/tine-core/src/model_gh594_liveness_tests.rs::gh594_a_block_is_found_by_its_id_in_the_session_and_after_a_reopen",
   "crates/tine-core/src/direct_projection_tests.rs::live_ids_decode_only_with_the_stored_revisions_exceptions",
   "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_seeded_interleavings_converge"
  ],
  "scenario": null,
  "failBefore": "tine-agents/evidence/batch-gh594/necessity-liveness.txt (reopen test: backlinks and unlinked references both empty before the fix; found by the L6 reference oracle on seed 9. Reverse lookup: the page hint of a block kept across an insertion is None after a reopen before R3)"
 },
 "notes": "Blocking core operation. An editor save stores the session's live block ids in the index; after a relaunch a fresh parse names the same blocks by structural ids (docs/contracts/direct-query-identities.md, R3). The reference panels' candidate filter compared stored ids to parsed ids directly, bypassing ResultIdentity, so every block of a page edited in an earlier session dropped out of other pages' Linked and Unlinked References until that page was edited again. The filter now resolves stored ids through ResultIdentity::public_id, the same function result construction uses. Candidate cause of the reporter's missing backlinks in GH #594 (inference: the report does not say whether the pages were edited before relaunching). The reverse direction (a block's id back to its stored row) failed after a relaunch too; R3 fixes the class at its source: the index stores structural ids only, and the session maps live ids through exceptions keyed by stored source revision (ResultIdentity::stored_id for lookups). FACTS_VERSION 4 re-lowers every page once."
},
{
 "id": "REG-INDEX-LIVENESS-594",
 "title": "A search index that cannot be built ends in a visible failure with Retry, instead of references and queries staying silently unavailable for the whole session",
 "family": "query-readiness",
 "sources": {
  "issues": [
   594,
   543
  ],
  "commits": [],
  "internal": ""
 },
 "platforms": [
  "all"
 ],
 "layer": "frontend-and-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_gh594_liveness_tests.rs::gh594_a_build_that_always_fails_ends_failed_and_panels_say_so",
   "crates/tine-core/src/model_gh594_liveness_tests.rs::gh594_a_derived_read_waits_at_most_its_patience",
   "crates/tine-core/src/model_gh594_liveness_tests.rs::gh594_a_reference_panel_asked_while_indexing_is_told_at_once",
   "crates/tine-core/src/model_gh594_liveness_tests.rs::index_readiness_contract_matches_the_code",
   "crates/tine-core/src/query/execution_error.rs::index_failure_classes_read_real_errors",
   "src-tauri/src/debug.rs::an_index_failure_is_recorded_as_fixed_codes",
   "src/components/LinkedReferences.test.tsx::shows a failed index with its code, Retry and the diagnostic report",
   "src/components/LinkedReferences.test.tsx::says it is indexing while the index is not ready, then shows the references",
   "src/components/UnlinkedReferences.test.tsx::shows a failed index with its code, Retry and the diagnostic report",
   "src/components/QueryMacro.currentPage.test.tsx::shows the failure code, Retry and the diagnostic report",
   "crates/tine-core/src/direct_projection_tests.rs::an_owed_registry_waiting_out_a_backoff_is_work_coming",
   "crates/tine-core/src/model_gh543_interleaving_tests.rs::gh543_seeded_interleavings_converge",
   "crates/tine-core/src/model_gh594_liveness_tests.rs::gh594_an_unhydratable_candidate_does_not_parse_the_graph"
  ],
  "scenario": null,
  "failBefore": "tine-agents/evidence/batch-gh594/necessity-liveness.txt (derived-read and reference-panel tests hang at c2afaaa0; the stuck-state diagnosis showed Working(Recovering) for 70 s across 8 failed builds; the four render tests fail against the pre-change LinkedReferences/UnlinkedReferences/Macro components)"
 },
 "notes": "Blocking core operation. GH #594: on a v0.6.985 first launch the owed index rebuild failed on every attempt; the owner retried under backoff forever while progress_at answered Working(Recovering) and index_work_coming answered nothing-coming, so queries and reference panels failed not-ready 4,673 times with no visible state and the failure reached only stderr (absent on the Windows release binary). Contract docs/contracts/index-readiness.md (L1-L6): L1 a failing build or turn gets INDEX_ATTEMPTS = 3 per session, then Failed(code); a worker that cannot set up is Failed at once; retry only on the user's Retry (retry_index = graph reopen) or next launch. L2 one index_state function feeds progress and coming. L3 derived reads wait at most DERIVED_READ_PATIENCE (60 s, thread-local deadline) then fall back to parsed pages; reference panels ask reference_readiness first (250 ms) and get NotReady or IndexFailed at once; rename/delete warm the page list before taking the identity lock. L4 Linked/Unlinked References and query blocks show IndexFailedNotice (code, Retry, Create diagnostic report); Linked References shows indexing… instead of hiding. L5 IndexFailureClass (13 fixed codes, platform-specific OS error tables) recorded as flight-recorder event index.failure. The reporter's root trigger is still unknown (candidates: 32-bit address space on the x86 build, a persistent Windows sharing violation); L5 exists to identify it. Windows is unverified; L6: the interleaving seeds inject 1-4 failed index turns, require every settle to end Ready or Failed(code), require a failed index to answer query and reference reads with IndexFailed within 5 s and recover on relaunch, and compare Linked and Unlinked References per page against a parse-only graph. The oracle found a registry capture waiting out a failure backoff reported as Idle, so readers parsed instead of waiting (seed 5; fixed in index_state; necessity in the evidence file). A long run (800 seeds, 60 steps) then found a Ready-state whole-graph parse: a reference candidate page that could not be parsed, or was deleted outside Tine without a watcher event, made candidate hydration decline as a whole and the panel fell through to walking every page (33/800 seeds). Candidates are a superset hint, so such a page is now skipped; revision-checked sources still decline. Necessity: 12 of 12 pages parsed before the fix. L6 remainder (13k scale lane, unclean relaunch, schema change) is open."
},
{
 "id": "REG-CTRLK-SHORT-SEARCH-SCAN-BUDGET-543",
 "title": "A one- or two-character Ctrl+K search answers promptly in a large graph and says when older blocks may match",
 "family": "search",
 "sources": {
  "issues": [543],
  "commits": [],
  "internal": ""
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/friendly_tests.rs::an_unindexed_interactive_scan_stops_at_its_budget_and_says_so",
   "crates/tine-core/src/model_gh594_liveness_tests.rs::gh543_ctrl_k_records_which_path_answered",
   "crates/tine-core/src/query/friendly_tests.rs::a_short_cjk_search_finds_matches_past_the_scan_budget",
   "crates/tine-core/src/model_gh594_liveness_tests.rs::gh543_pre_ready_ctrl_k_builds_its_page_inventory_once_per_generation",
   "src/queryReadiness.test.ts::asks again within a quarter second once the index may be ready (GH #543)"
  ],
  "scenario": null,
  "failBefore": "tine-agents/evidence/batch-gh594/necessity-ctrlk.txt (without the budget the scan visited 20052 rows for a pair only the oldest block holds)"
 },
 "notes": "Blocking core operation (latency), Martin's Ctrl-K follow-up to GH #543. A needle under three characters has no trigram index, so the interactive read scanned blocks newest first until 300 verified matches; a rare pair visited every block (~1.5 s at 616k blocks, subagent note 2026-09-24-gh543-ctrlk-latency.md). The scan now stops at INTERACTIVE_SCAN_BUDGET (20,000 rows, ~50 ms at that scale) and sets has_more, which the switcher shows as More matches exist. Each Friendly search also logs on the --debug channel whether the index or a page scan answered it, with its elapsed time, and the interactive cursor logs its visits. A needle with Han, kana or Hangul is exempt from the budget (interactive_scan_budget): one or two characters are an ordinary word there, and the budgeted scan returned 0 of 26 matches for one measured word at 10k pages (subagent note 2026-09-24-ctrlk-0.6.982-parity.md; necessity tine-agents/evidence/batch-ctrlk-cjk/necessity-cjk-budget.txt). Such a needle scanned every block, as in v0.6.985, until the short-word index (REG-CJK-SHORT-WORD-INDEX-543, ADR 0069) replaced that scan. The pre-ready page fallback ranks on the cached fold (visible_lower, asserted equal to canonical_fold by a source test) and builds its page inventory once per cache generation (necessity-inventory-memo.txt: 3 builds for 3 keystrokes before); the frontend readiness retry is capped at 250 ms instead of 800 (necessity-retry-cap.txt)."
},
{
 "id": "REG-CJK-SHORT-WORD-INDEX-543",
 "title": "A one- or two-character Chinese, Japanese or Korean search reads only the matching blocks",
 "family": "search",
 "sources": {
  "issues": [543],
  "commits": [],
  "internal": "ADR 0069"
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/query/friendly_tests.rs::a_short_cjk_search_finds_matches_past_the_scan_budget",
   "crates/tine-core/src/direct_projection_tests.rs::a_short_cjk_page_name_reads_its_unlinked_candidates_from_the_short_word_index",
   "crates/tine-core/src/query/sql_tests.rs::a_short_cjk_term_is_bounded_by_the_short_word_index",
   "crates/tine-core/src/query/candidate.rs::every_short_cjk_substring_is_a_token_of_its_text",
   "crates/tine-core/src/query/candidate.rs::a_short_cjk_needle_is_index_driven_and_mixed_ones_are_not"
  ],
  "scenario": null,
  "failBefore": "tine-agents/evidence/batch-cjk-short-word/necessity.txt (without the short-word bound Ctrl-K visited 20053 blocks for a word only the oldest block holds; the query and unlinked-reference tests fail)"
 },
 "notes": "Blocking core operation (latency). ADR 0069 (accepted by Martin 2026-09-24): a one- or two-character needle written entirely in Han, kana or Hangul was exempt from the scan budget and scanned every block (1.5 s per keystroke for a rare word at 616k blocks). tine-storage 0.28.0 adds short_word_fts (schema 31), fed from candidate::short_word_tokens, the unigrams and bigrams of each CJK run of the same folded text, by the router's own script predicate; the planner bounds such needles by it for Ctrl-K, content-match queries and unlinked references. Completeness: every 1-2 scalar CJK substring of indexed text is one of its tokens (property test); candidates are still verified exactly. Mixed short needles and OR arms bounded by different tables keep the unbudgeted scan. Unit cost measured through Tine's writer: +12.4 KB WAL per 1-block CJK edit, +25-33 KB per 60-block CJK edit, 0 without CJK. Shares the FACTS_VERSION 5 / schema 31 rebuild with the search-fold change. Not measured on the 10k CJK graph in Tine."
},
{
 "id": "REG-OWNER-READINESS-WAIT-ON-OWNER-WORK-001",
 "title": "An index owner waiting for readiness does not wait for work only an owner pass can do",
 "family": "index-readiness",
 "sources": {
  "issues": [],
  "commits": [],
  "internal": "Full CI run 36073511057 (v0.6.986 candidate 45d34787): d1_damage_found_by_the_background_check_rebuilds_the_index hung 600 s"
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::a_readiness_wait_returns_when_the_next_step_is_owner_work",
   "crates/tine-core/src/direct_projection_tests.rs::d1_damage_found_by_the_background_check_rebuilds_the_index"
  ],
  "scenario": null,
  "failBefore": "a_readiness_wait_returns_when_the_next_step_is_owner_work timed out at 5 s with marks=1 rebuild=true need=Fresh before the fix. A loaded loop of the d1 test (3 workers beside the full tine-core suite) hung 7 times in about 20 minutes, every time inside warm_cache with rebuild=true need=Fresh."
 },
 "notes": "Exact availability (no hang). wait_until_ready_at, the owner's unbounded readiness wait (warm_cache: CLI, headless runs, tests), kept waiting while has_work() was true; a queued edit or registry snapshot counts as work, but the worker takes neither while a rebuild is owed, and a rebuild, validation or failed index's retry is owner work. The waiting owner waited on itself. It now returns when the index need is Fresh, Validate or Failed. The integrity check's rebuild request arriving beside queued work exposed it. At 45d34787 the wait's only production caller was the headless CLI's warm_cache (src-tauri/src/cli.rs)."
},
{
 "id": "REG-SEARCH-FOLD-KEEPS-LETTER-MARKS-001",
 "title": "Search folds accents but keeps a mark that makes a different letter or syllable",
 "family": "search",
 "sources": {
  "issues": [],
  "commits": ["a3e7bfda9120388f5766db7d61a2d8c1638701f3"],
  "internal": "Martin decision BZ3, 2026-09-24"
 },
 "platforms": ["all"],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-search/src/fold.rs::accents_fold_and_letter_making_marks_do_not",
   "crates/tine-search/src/fold.rs::the_ignorable_marks_are_every_default_ignorable_mark",
   "crates/tine-core/src/onboarding.rs::search_guide_mark_examples_are_what_search_does",
   "src/render/searchWasm.integration.test.ts::keeps marks that make another letter and folds only accents"
  ],
  "scenario": null,
  "failBefore": "Decided behaviour change: accents_fold_and_letter_making_marks_do_not asserts か does not find が, и does not find й and क does not find कु, which the previous fold (every nonspacing mark removed) made match."
 },
 "notes": "Flexible heuristic with an exact decided rule. A nonspacing mark folds away only if its combining class is nonzero and not one of 8, 9, 84, 91, 103, 118, 129, 130, 132 (kana voicing, virama, Telugu length, Thai/Lao u, Tibetan vowels), Firefox's find-in-page rule; Cyrillic keeps its marks except ё -> е; stroked letters (ł ø đ ħ ŧ) fold to their base. Stored search tokens change: DIRECT_PROJECTION_FACTS_VERSION 4 -> 5, one re-index shared with the CJK short-word index."
},
{
 "id": "REG-CASE-VARIANT-PAGE-NAME-597",
 "title": "A page opened by a name that differs from its file name only in case opens and can be edited, and is listed once",
 "family": "filesystem-safety",
 "sources": {
  "issues": [
   597
  ],
  "commits": []
 },
 "platforms": [
  "windows",
  "macos"
 ],
 "layer": "unit",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::gh597_a_case_variant_name_opens_the_file_under_its_disk_spelling",
   "crates/tine-core/src/model_gh597_tests.rs::gh597_a_case_only_rename_outside_tine_leaves_one_page",
   "crates/tine-core/src/model_gh597_tests.rs::gh597_a_path_saved_under_another_case_spelling_opens_the_file",
   "src/components/Page.test.tsx::opens the file under its disk spelling and re-keys the tab and Recent entry"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Blocking core operation. GH #597 (Windows 10, v0.6.985; the reporter also saw it on v0.6.982): with only pages/contents.md on disk, opening \"Contents\" (home page, search, sidebar) failed with \"could not be activated for editing\". Every path built from a page name (the IT-05 and GH #550 by-name fast paths in lookup.rs, graph_text_path_for for activation by name) trusted an existence probe that a case-insensitive filesystem answers for another spelling, so the page was handed out, published and activated as pages/Contents.md, which resolve_rel refuses (path_uses_graph_text_alias) and which added a second page to search. The same probe let the launch check keep a stored row whose file was renamed by case only, and let the checked watcher entry publish an alias spelling. All four now apply the editor boundary's own alias rule: the fast paths fall back to the case-insensitive inventory, activation by name resolves the on-disk spelling, a stored alias spelling counts as gone, and the watcher entry ignores it. Tests check something only where the alias spelling resolves (Windows, default macOS); fail-before and pass-after are the focused hosted Windows runs (evidence tine-agents/evidence/batch-gh597/). macOS is unverified. A tab, Recent or sidebar entry already saved with the alias spelling opens: load_by_path follows a path that differs from the disk spelling only by case, and the page view re-keys every navigation entry to the disk spelling (necessity of the render test: tine-agents/evidence/batch-gh597/necessity-pinned-route.txt)."
},
{
 "id": "REG-LAUNCH-INTEGRITY-OFF-PATH-550",
 "title": "Reopening a graph does not wait for an integrity check of the search index, and an app kill never causes one",
 "family": "query-readiness",
 "sources": {
  "issues": [
   550
  ],
  "commits": [
   "1d8742d8"
  ]
 },
 "platforms": [
  "all"
 ],
 "layer": "rust-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/direct_projection_tests.rs::d1_a_reopen_on_the_same_boot_runs_no_integrity_check",
   "crates/tine-core/src/direct_projection_tests.rs::d1_a_reopen_after_a_reboot_checks_in_the_background",
   "crates/tine-core/src/direct_projection_tests.rs::d1_damage_found_by_the_background_check_rebuilds_the_index",
   "crates/tine-core/src/direct_projection_tests.rs::d1_a_close_interrupts_the_background_check",
   "crates/tine-core/src/direct_projection_tests.rs::d1_the_check_is_due_after_a_reboot_or_a_week_and_not_otherwise",
   "crates/tine-core/src/direct_projection_tests.rs::d1_every_tine_platform_knows_its_boot_time"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Blocking stateful UX. GH #550 / #543 family, launch design D1 (Martin 2026-09-24, BX1). The worker ran PRAGMA quick_check before every launch's first answer (1.2-1.7 s on a 10k-page graph). The projection is WAL + synchronous=NORMAL, so a killed process (Android memory/power management) cannot damage it; the check is owed only after an OS reboot or 7 days, runs in the background on its own read-only connection, and damage it finds rebuilds the index. Contract docs/contracts/index-readiness.md L7. Not measured on a phone."
},
{
 "id": "REG-LAUNCH-SERVE-STORED-550",
 "title": "During the launch index check, Ctrl+K, references, query blocks and the page list answer from the stored index and correct themselves when the check lands",
 "family": "query-readiness",
 "sources": {
  "issues": [
   550,
   543
  ],
  "commits": []
 },
 "platforms": [
  "all"
 ],
 "layer": "frontend-and-core",
 "status": "covered",
 "coverage": {
  "tests": [
   "crates/tine-core/src/model_launch_serve_tests.rs::a_clean_reopen_answers_display_reads_during_the_launch_check",
   "crates/tine-core/src/model_launch_serve_tests.rs::a_read_that_acts_on_its_answer_waits_for_the_launch_check",
   "crates/tine-core/src/model_launch_serve_tests.rs::an_edit_made_while_closed_is_corrected_when_the_check_lands",
   "crates/tine-core/src/model_launch_serve_tests.rs::an_edit_during_the_launch_check_is_read_back",
   "crates/tine-core/src/model_launch_serve_tests.rs::a_config_changed_while_closed_serves_nothing_stored",
   "src/components/LinkedReferences.test.tsx::asks again and shows the corrected answer",
   "src/components/QuickSwitcher.test.tsx::asks again when the launch index check lands",
   "src/launchReadsGh543.test.tsx::loads the navigation index at open and again on a reopen during the launch check",
   "src/launchReadsGh543.test.ts::corrects launch answers when the launch check lands"
  ],
  "scenario": null,
  "failBefore": "proven"
 },
 "notes": "Blocking stateful UX. GH #550 / #543 family, launch design D2-D4 (Martin 2026-09-24, BW4). Every display read waited for the launch survey (seconds on a 10k graph, longer on Android). Display reads now name a currency: inside Graph::display_read they are LaunchStored and are answered from the image the last session left while serving_stored holds (same facts version and parse config, no fresh build owed, no edit of this session pending); acting reads (try_list_pages, templates, name-only creation evidence) and queries outside a display read stay Current and wait. Stored answers are never memoized. The frontend asks at open and re-asks on warm-cache-done (correctLaunchAnswers). Necessity: the three serving tests fail on a3e7bfda (Ctrl-K NotReady, read-back times out); the two guard tests pass there by design; the frontend re-ask tests fail with the indexCorrectionRev reads removed; the navigation-index test fails with the old warm wait. Contract docs/contracts/index-readiness.md L8. Not measured on a phone or on Windows."
}
]
}
