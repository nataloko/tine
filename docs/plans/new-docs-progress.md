# New docs progress

## Completed prerequisites

- **Phase A — `124f6a5b`**: completed editorial calibration for `Tine Guide` and `Features/Sheets`; the index now uses the Start/Workflows/Feature reference/showcase lenses and Sheets begins with ordinary bullets.
- **Phase 0 — `0a1dca82`, `80a898fc`**: completed Guide-link hardening; accidental targets are rejected while registered pages and deliberate stubs are checked by focused regression proof.

## Phase B — candidate: Workflows/Structure repeated information

Added one canonical workflow page, registered it in the shared manifest, linked it from the Workflows lens, and added a semantic onboarding proof for its registration, copy behavior, canonical links, executable tracker/query, and observable result. It remains a manager-review candidate, not a declaration that J05 is complete.

### Behavioral claim sources

- `crates/tine-core/src/templates/sheets.md:7` — “rows are child bullets, columns are their properties, cards are child bullets or blocks a query finds.” The workflow begins with ordinary tracker children and then changes only their view.
- `crates/tine-core/src/templates/sheets.md:8` — “Show children as → Grid or Table” and “`/Query`, then run `/Table` or `/Board`.” The table/board path names current visual controls before stored syntax.
- `crates/tine-core/src/templates/sheets.md:58` — “without copying the source blocks.” The query-backed board is explicitly presented as another view of the matching tracker bullets.
- `docs/FEATURES.md:179` — “Search, list, table, and board are presentations of one result membership.” The workflow separates selecting matching blocks from choosing a result presentation.
- `docs/FEATURES.md:180` — “friendly search-text surface” and “interactive visual query builder” compile with raw DSL to “the same query plan.” Friendly search and the visual builder precede the raw query explanation.
- `docs/adr/0030-query-view-unification.md:23` — “The query block owns membership through the query DSL and builder.” The query instructions describe membership separately from view.
- `docs/adr/0030-query-view-unification.md:24` — “`tine.view::` owns presentation.” The stored-syntax explanation identifies the board/table properties as presentation only.
- `docs/adr/0042-one-query-plan-many-frontends.md:52` — “Friendly search remains the primary authoring surface; raw DSL is optional.” The raw query text appears only after the visual path.
- `src/components/QuickSwitcher.tsx:585` — “Open search tab.” The workflow names the shipped transition from the quick switcher into the persistent result workspace before using workspace-only controls.
- `src/components/QueryWorkspace.tsx:733` — “Filters / Advanced”; `src/components/QueryWorkspace.tsx:744` — Search/List/Table/Board presentations. The workflow uses the shipped workspace labels.
- `src/components/QueryWorkspace.tsx:521` — “Edit as visual query”; `src/components/QueryBuilder.tsx:599` — “➕ Add filter”; `src/components/QueryBuilder.tsx:620` — “Property.” The workflow names the current visual narrowing controls.
- `crates/tine-core/src/templates/formulas.md:19` — “a read-only column appears.” The optional formula step links the canonical Sheets and Formulas pages instead of reproducing their walkthroughs.

## Phase C — candidates: J09 files/backups map (Reference/Files, external edits, and backups)

### Behavioral claim sources

- `crates/tine-core/src/graph_text_scope.rs` — the shared policy admits eligible `.md`, `.markdown`, and `.org` files graph-wide while excluding hidden/internal trees, assets, publish output, and provider conflict copies. This is the page-discovery and snapshot boundary.
- `crates/tine-core/src/model.rs` — existing graph text keeps its exact relative path; configured pages/journals roots classify and create new files rather than limiting discovery.
- `src/components/Settings.tsx:3449-3455` — “Deleting a block never deletes its media (a safety net), so unused files can accumulate.” The assets and orphan-scan claims.
- `docs/FEATURES.md:625-628` — “saved to your graph's `logseq/config.edn` as `:ui/show-brackets?`, matching Logseq's default (on)”; `docs/FEATURES.md:516-519` reads journal formats from config; `docs/FEATURES.md:167-168` supports user `:macros` from `config.edn`. The shared-configuration claim.
- `src/persistence.ts:590` — `SAVE_DEBOUNCE_MS = 400`; `docs/FEATURES.md:511-512` — “skip byte-identical rewrites”. The automatic-save claims (“about half a second after a pause”).
- `docs/FEATURES.md:509-512,547-548` — “Saves preserve each file's exact formatting (tabs vs spaces, comments, compact EDN)”; “`atomic_write` + fsync”. The atomic, format-preserving save claims.
- `docs/FEATURES.md:513-515` — “Page rename is transactional… re-checking each file just before writing and rolling back on conflict.” The rename claim.
- `docs/FEATURES.md:539-545` — “A `.org` file is rewritten only when Tine can reproduce it **byte-for-byte** — anything it can't round-trip loads **read-only**”. The Org guard claim.
- `src/components/Settings.tsx:3232-3256` — “Watch for external edits” with “Live (inotify)… no idle CPU wakeups” vs “Poll (3s)… filesystems where inotify is unreliable… Saved per device.” The watcher claims; `src/store.ts:331` — “external change (content differs) still reloads + invalidates” backs in-place updates for unedited pages.
- `src/components/ConflictBar.tsx:45-56` — ““{name}” changed on disk… Your unsaved changes weren't written”, buttons “Use disk version” / “Keep mine (overwrite)”; header comment states conflicted pages are “skipped by every future save batch until resolved”. The banner behavior claim.
- `docs/adr/0039-filesystem-scope-boundary.md:20-21` — “The window registry rejects equal, ancestor, and descendant graph roots owned by different windows.” The two-windows claim.
- `src/components/Settings.tsx:2589-2596` — “Tine snapshots your graph's Markdown/Org files to a local folder each time it opens (outside the graph, so Syncthing never sees it). A safety net against a bad write — independent of OG Logseq's own backups.”; keep default signal 12 at `Settings.tsx:2505`; “Oldest snapshots beyond this are pruned.” The snapshot claims.
- `src/components/Settings.tsx` — restore confirms that files return to their original locations and that current state is snapshotted first. `src-tauri/src/backup.rs` schema 3 preserves graph-relative paths while schema 2 remains restorable.
- `crates/tine-core/src/model.rs` — typed trash uses `logseq/.tine-trash/pages`, `/journals`, `/assets`, and `/conflicts`, with timestamp-prefixed leaf names. `src/components/Settings.tsx` keeps non-asset recovery entries when emptying asset trash.
- `docs/FEATURES.md:520-522` — “Duplicate journal days lets you **Open** / **Merge** / **Rename** / **Trash** each”; `docs/FEATURES.md:523-531` — “`*.sync-conflict-*` (or `(conflicted copy)`)… out of your page list… **Review & merge** shows a **block-by-block diff**… per-block **keep-current / keep-copy / keep-both**… **Discard copy** trashes it… never auto-merged”. The conflict-copy claims.
- `crates/tine-core/src/templates/managed-sync.md:4,7` — “**Testing only.** Tine-managed storage is experimental. Your normal mode is **Direct files**” and “Do not enable it merely because you already synchronize your graph folders.” The single managed-sync pointer.
- `src/components/Settings.tsx:2103-2112` — Settings → Graph → “Export graph to HTML”; `docs/FEATURES.md:656` — “One-click **static HTML export** (`public:: true` pages)”; `docs/FEATURES.md:668` — “right-click a page title → **Export to PDF…**”; `docs/FEATURES.md:679` — “**Copy/export as** Markdown… with a *Rendered* mode”. The export claims.

## Phase C — candidates: J02 open-an-existing-graph path (Start/Bring an existing graph)

### Behavioral claim sources

- `src/components/Welcome.tsx:67-69` — “Open an existing graph / Point Tine at a Logseq graph folder you already have.” The Welcome-screen step.
- `src/components/Sidebar.tsx:353-356,441-464` — “Tine lists its known graphs here alongside open/create actions”, with menu items “Open graph…” and “New graph…”. `docs/FEATURES.md` now matches the shipped known-graphs menu.
- `src/components/Settings.tsx:2093-2097` — “Open another graph…” on the Graph tab; `README.md:199-200,208-209` — `TINE_GRAPH=/path/to/your/graph` and “Run one app at a time on a given graph.” The command-line and one-app-at-a-time claims.
- `docs/FEATURES.md:516-519` — “reads `:journal/file-name-format` and `:journal/page-title-format` and recognizes/creates journal files in your format”. The journal-honoring claim (`Settings.tsx:154-161` lists the shipped formats).
- `docs/FEATURES.md:511-512,532-538,543-545` — “skip byte-identical rewrites”, recursive folder scan, Org “byte-for-byte … read-only”. Cross-cited evidence shared with the Files page.
- `docs/FEATURES.md:222-227` — “config is a few harmless `tine.*` properties… Logseq renders the same file as an ordinary nested outline”. The view-property coexistence claim, linked to [[Features/Sheets]].
- `docs/FEATURES.md:506-509` — “A filesystem watcher… reconciles changes synced in from other devices”. The “changes appear automatically” claim; banner behavior cross-cited from `src/components/ConflictBar.tsx:45-56`.
- `crates/tine-core/src/templates/managed-sync.md:4` — “Your normal mode is **Direct files**… **Testing only.**” The single managed-sync pointer wording.

## Phase C — candidates: J09 recovery actions (Reference/Troubleshooting and recovery)

### Behavioral claim sources

- `src/components/ConflictBar.tsx:6-10,45-56` — “A save is refused (not clobbered) when the file changed on disk under us… skipped by every future save batch until resolved”, buttons “Use disk version” / “Keep mine (overwrite)”. The banner scenario and outcomes.
- `src/components/ContextMenu.tsx:939-947` — delete confirm: “The file moves to the graph's .tine-trash folder.”; `docs/FEATURES.md:547` — “page delete moves to a recoverable **trash**”. The trash-restore scenario; the file-manager restore path is the only restore surface (no in-app trash browser exists — verified by reading `AssetsTab` at `src/components/Settings.tsx:3330-3480`, which lists asset trash only).
- `src/components/Settings.tsx` — restore confirms “Your current state is snapshotted first, so this is reversible”; `src-tauri/src/backup.rs` captures eligible graph text at exact paths plus config and asset sidecars, and restores schema-2 snapshots through the legacy configured-root path.
- `src/components/Settings.tsx:2867-2900` — “Syncthing and Dropbox leave a `*.sync-conflict-*` copy… Review & merge shows a block-by-block diff… Discard copy trashes it (recoverable)”; fallback “The page this shadows no longer exists — discard the copy, or restore it in Logseq.” The merge scenario and the shadowless-copy case.
- `docs/FEATURES.md:520-522` — duplicate days “keeps **both**… **Open** / **Merge** / **Rename** / **Trash**”; `src/components/Settings.tsx:2799-2810` — “usually left over from changing the date format… Open reaches a file directly (it's editable and saves back to itself); Merge folds a stray into the canonical day; Rename turns it into a normal page; Trash removes the redundant one (recoverable).” The duplicate-day scenario.
- `src-tauri/src/debug.rs` — debug output defaults to `std::env::temp_dir()/tine-debug.log`, with `TINE_DEBUG_LOG` override. The Guide names the platform temporary folder rather than assuming Linux `/tmp`.
- `README.md:210-231` — the WebKitGTK GPU fallback is Linux-specific; `TINE_DEBUG=1` / `--debug` and the no-note-content diagnostic contract remain the startup recovery path.
- `docs/FEATURES.md:629-635` — “runs Tine's parser (lsdoc) against Logseq's own parser… Every divergence snippet is **anonymized**… and **re-verified to still reproduce the divergence**… nothing is uploaded.” The diagnostics/report scenario.

## Phase C — candidates: J03 day planning (Workflows/Capture and plan your day)

### Behavioral claim sources

- `src/components/Sidebar.tsx:147` — sidebar nav label “Journals”; `docs/FEATURES.md:408-409` — “Multi-day **journal feed** (one continuous editable list); today's journal created lazily on first edit”. The open-today-and-type path and “no file until you type”.
- `crates/tine-core/src/templates/quick-capture.md:13-16` — “**Leave the title empty** → the text is appended to **today's journal**… It's the real editor in there”; `src/store.ts:2013` — “Append a quick-capture”. The quick-capture pointer.
- `src/editor/marker.ts:1-5` — “cycle-marker-state: TODO -> DOING -> DONE -> (none); LATER -> NOW -> DONE -> (none); … Bound to mod+enter”; `src/keybindings.ts:385` — `{ id: "editor/cycle-todo", binding: "mod+enter" }`; `src/store.ts:3234-3247` — `cycleSelectionTasks` cycles every selected block; `docs/FEATURES.md:382-383` — “cycle with Mod+Enter (including every selected task at once)”. The marker-cycling actions.
- `src/components/Block.tsx:830-836` — marker chip `onClick` → `cycleBlockMarker`; `Block.tsx:906-915` — checkbox `toggleBlockCheckbox` (open → DONE, DONE → open marker); `src/markers.ts:44-53` — checkbox states (DONE checked, open unchecked, CANCELED/none no box); `docs/FEATURES.md:391-396` — same prose. The click paths.
- `src/editor/autocomplete.ts:422-424` — `{ label: "Priority A", action: "priority-a", key: "A" }` etc.; `src/editor/format.ts:223-224` — “Set (or replace) the `[#X]` priority … placed after any task marker”. The priority step.
- `src/components/Block.tsx:2045-2051` — slash `scheduled`/`deadline` “open the calendar popup anchored under the editor”; `Block.tsx:851-874` — date chips, title “Scheduled — click to change”, `openDatePicker`; `src/components/DatePicker.tsx:74` — “Optional clock time (`HH:mm`), like OG's ‘Add time’”. The schedule steps and outcomes (chip).
- `docs/FEATURES.md:397-405` — date picker, optional clock time `(<2026-07-07 Tue 14:30>)`, repeaters `+1w`/`.+1w`/`++1w`, “Re-picking the date keeps an existing time (and repeater)”, type-anywhere planning lines normalized on exit, fenced/inline-code planning lines stay literal. Cross-check for the stored-syntax claims.
- `src/editor/repeat.ts:42-44` — “`.+` repeats from the completion date (today); `+`/`++` from the stored date. `++` is catch-up”; `repeat.ts:68-86` — completing a repeater advances dates “and resets the marker to the workflow's open state”. The repeater summary.
- `src/components/Page.tsx:462-473` — “Agenda sits at the bottom of today's (the first) day, like OG.” `<QueryMacro body={agendaQuery()} title="Scheduled & Deadline" hideWhenEmpty>`; `src/ui.ts:469-514` — default window 7/7, window “tested against the scheduled/deadline date itself — NOT the journal day”, “Finished tasks are excluded … matches OG's `:block/marker "NIL"` default”; `Settings.tsx:1902-1928` — “Agenda window … days back · days ahead”. The agenda section.
- `src/components/Page.tsx:954-996` — carry buttons `Carry unfinished tasks → today` / `Carry from previous day` / `Carry last {N} days` under journal titles; `src/carry.ts:71` — toast `Carried ${n} items to today` / “No unfinished tasks to carry”; `ContextMenu.tsx:888` — right-click “Carry unfinished tasks → today”; `src/keybindings.ts:353-356` — command-palette presets 7/30/365/N; `src/ui.ts:432-456` — defaults (buttons shown, keep-context on, header off); `Settings.tsx:1822-1852` — the carry settings and hints. The carry-over section.
- `src/editor/queryBuilder.ts:17,187-189` — `(task TODO DOING NOW LATER)` DSL clause (list of markers). The executable-query example.
- `crates/tine-core/src/templates/showcase.md:57-73` — the Feature showcase page already ships examples using the query's four named markers — why the workflow page's outcome mentions matching tasks elsewhere without claiming that every open-marker variant is included.

### Reference page (Reference/Journals, tasks, and scheduling) sources

- `src/journal.ts:1-5` — frontend title format mirrors the backend, “defaults to Logseq's ‘MMM do, yyyy’”, fed by `GraphMeta.journal_page_title_format`; `journal.ts:90-93` — `[[journal title]]` links route “to the journal page rather than opened as an empty regular page”. The journal-routing claim.
- `src/components/Settings.tsx:1784-1794` — “How journal dates are displayed and how new `[[date]]` titles are written. Display-only — your journal *file names* are untouched and existing journals keep working. Saved to `:journal/page-title-format`”; `src/types.ts:593` — `journal_file_name_format` “`:journal/file-name-format` (default `yyyy_MM_dd`)”. Date-format scope.
- `src/components/Settings.tsx:1524-1532` — “Template inserted into a new day's journal. Saved to `:default-templates {:journals …}`… ‘(none)’ → blank days (the default). Make a template via a block's right-click menu.” New-journal template.
- `src/markers.ts:13-25` — the full marker set (TODO DOING NOW LATER WAITING WAIT STARTED IN-PROGRESS DONE CANCELED CANCELLED), “stored” as the first word (markers.ts:37-42 `MARKER_RE`).
- `src/components/Settings.tsx:1855-1874` — “Task workflow … Saved to `:preferred-workflow` in `config.edn`, so it travels with the graph”, buttons “TODO / DOING” vs “NOW / LATER”; `src/ui.ts:106-107` — `createSignal<"now" | "todo">("now")`. Workflow default.
- `src/editor/autocomplete.ts:412-426` — slash commands for every marker plus “Priority A/B/C”, “Scheduled”, “Deadline”; `src/components/Block.tsx:2045-2051` — the date commands “open the calendar popup anchored under the editor”; `Block.tsx:851-874` — date chips with title “Scheduled — click to change” / “Deadline — click to change”.
- `src/editor/planning.ts:1-10,63-64` — “type anywhere, normalize on exit… first line → SCHEDULED → DEADLINE → properties”, “Canonical order: SCHEDULED before DEADLINE”, inline-code/fenced `SCHEDULED:` never moved. Stored-syntax claims.
- `src/components/DatePicker.tsx:13` — “First day of week from the Tine display pref”; `Settings.tsx:1796-1804` — “Starting column of the calendar and the scheduled/deadline date pickers. Saved to `:start-of-week`”. The picker-startweek claim.
- `src/components/Macro.tsx:437-439` — `hideWhenEmpty && !ADVANCED_RE && total() === 0` hides the block (user-authored queries instead show “No results”, Macro.tsx:114-116). The agenda “disappears entirely” claim.
- `src/ui.ts:509-514` — `agendaQuery()` returns `query (and (or (between scheduled -7d +7d) (between deadline -7d +7d)) (not (task DONE CANCELED CANCELLED)))` at default 7/7 — the stored-query quotation on the page.
- `src/components/Settings.tsx:1877-1888` — “Time tracking … Marker transitions write OG-compatible `:LOGBOOK:` CLOCK rows. Saved to `:feature/enable-timetracking?`; seconds mode follows `:logbook/settings`”; `src/ui.ts:116-122` — both default true. Logbook claims.
- `src/sheet/fields.ts:319-334`, `src/components/SheetTable.tsx:1379` — sheet state/priority/deadline cells route through the same marker/date machinery (“The same chips and pickers appear wherever the block does”).
- `docs/SETTINGS-INVENTORY.md:30-40` — Journals-tab rows exist for every control named on the page (Journal date format, First day of week, carry-over trio, Task workflow, Time tracking, New-journal template, Agenda window).

### Discrepancies / deliberate scope

- `docs/FEATURES.md:552` (recent-graphs staleness) still pending from the J02 slice; not touched.
- No per-platform quick-capture claim beyond the desktop shortcut flow already on [[Features/Quick capture]] (its page leads with Linux DE setup); Android has no global-capture story here, so none is asserted.
- HEAD's `reference-troubleshooting-and-recovery.html` (and two sibling pages) were stale against the template corrections in `360f251d` (“Correct Guide file and recovery claims”); the J03 regen refreshed them — flagged here so the manager knows those tracked-HTML changes are build output, not new wording by this worker.
- `website/demo/search-index.js` and `website/demo/feature-showcase.html` are untracked build artifacts on this branch (untouched by the commits; noted for the manager in case tracking them is intended).
- Manager review narrowed the workflow query outcome to its literal `TODO` / `DOING` / `NOW` / `LATER` marker set and corrected the storage boundary: journal entries use the configured journal directory, while tasks on ordinary pages remain in their own Markdown or Org files.

## Phase C — candidates: J04 find and revisit (Workflows/Find and revisit + Reference/Pages, links, references, and search)

### Behavioral claim sources

- `docs/FEATURES.md:437-441` — "`Ctrl+K` quick switcher: page titles + full-text content hits (visible text only — no false hits on hidden properties/uuids)… **Shift+Enter** opens the selected page or block in the right sidebar… middle-click → background tab." Switcher scope and open directions.
- `src/keybindings.ts:270-271,273` — `go/search` = `mod+k`, `go/search-current-page` = `mod+shift+k` ("Search blocks in current page"), `go/find-in-page` = `mod+f` ("Find in page"). The three search entries and bindings.
- `docs/FEATURES.md:441-442,451-454` — "`Ctrl/Cmd+Shift+K` searches only blocks owned by the focused routed page (including collapsed descendants)"; in-page find "collapsed or lazy-rendered branches are found and opened before the active match is highlighted". Scoped-search and find-bar claims.
- `src/editor/searchQuery.ts:24-30` — the five `SEARCH_SYNTAX` rows (`foo bar`, `foo OR bar`, `foo -draft`, `"exact phrase"`, `/[A-Z]{3}/`). The switcher syntax examples, quoted on both new pages.
- `src/components/QuickSwitcher.tsx:580,585,597` — footer buttons "**Search syntax**" and "**Open search tab**". The shipped label used on both new pages (see discrepancy 1 below).
- `src/components/QueryWorkspace.tsx:722,735,746-755,523,549,765,775,779` — workspace input placeholder "Search pages and blocks", "**Filters / Advanced**", the Search/List/Table/Board presentations, "**Edit as visual query**", "**Explain query**", the title input "Name this search to save it as a page" + "**Save page**" button. All shipped workspace labels used verbatim.
- `src/components/QueryWorkspace.tsx:80-83,87-115,118-123,693` — saving writes exactly one `{{query …}}` block, "`input.presentation === "list" ? query : `${query}\ntine.view:: …"`"; name-conflict returns "A page named … already exists. Choose another title." and the save path never overwrites; on success the tab `replaceActiveRoute`s to the ordinary page. The saved-search claims on the reference page.
- `docs/FEATURES.md:195-200` — "Ctrl+K can open its complete page and block result set in a graph-scoped virtual tab… The workspace survives restart, switches between Search/List/Table/Board without changing membership, and remains outside the graph until named. Giving it a title materializes one ordinary query page". Workspace lifecycle claims.
- `docs/FEATURES.md:177-179` — "Search, list, table, and board are presentations of one result membership." Presentation-vs-membership rule (re-used from the J05 slice).
- `src/components/Settings.tsx:1719-1721` — "**Learn Ctrl+K choices**" + hint "After you deliberately open the same result more than once for a query, Ctrl+K may prefer it only among equally strong matches. History stays on this device and in this graph; saved searches and queries remain deterministic." Ranking-learning claims (see discrepancy 3).
- `docs/FEATURES.md:447-450` — "Title results are ranked first by objective exact/prefix/substring/fuzzy class." The ranking class order.
- `docs/FEATURES.md:444-446` — "Canonically equivalent composed/decomposed Unicode spellings share membership and source-accurate evidence; this does not add accent folding or transliteration." The Unicode claim.
- `docs/FEATURES.md:468-471` — "guide pages are hidden from search/page lists/backlinks". The Guide-exclusion claim.
- `docs/FEATURES.md:133-136` — "The `[[`/`#` Enter default is configurable (Settings → *Editor* → Advanced → **Link autocomplete default**)… Exact existing names always select the page." Autocomplete-default claim.
- `crates/tine-core/src/templates/tips.md:13` + `src/styles/app.css:4427-4435` — "A link whose page does not exist yet is drawn dimmed with a dotted underline. It still works — opening it creates the page"; the CSS comment records it as a "Deliberate divergence from Logseq, which renders these identically to live links". The missing-link claim and the divergence note on the reference page.
- `docs/FEATURES.md:455-458` + `crates/tine-core/src/templates/tips.md:36` — "collapsible **namespace tree** in the sidebar… an automatic **"Hierarchy"** section (breadcrumb paths of descendant pages) on any namespaced page". Namespace claims.
- `src/keybindings.ts:274-278` — "GH #274 / OG parity: `:editor/follow-link` (mod+o) and `:editor/open-link-in-sidebar` (mod+shift+o) in OG's shortcut config"; `editor/follow-link` label "Open the link at the caret". The Ctrl+O claims.
- `src/components/Page.tsx:177-180,848-850` — `alias`/`icon` excluded from the under-title property list; the "**aka**" chips render with title "Also known as — other names that link here". Alias claims.
- `docs/FEATURES.md:125-127,459` — "Values of `tags::`, `alias::`, and `aliases::` render as navigable page links… **"aka" alias chips** on pages reachable by another name." Cross-check for tags/alias rendering.
- `crates/tine-core/src/templates/showcase.md:4,15-16` — `alias:: Kitchen sink (features)`; labelled page link and autolinked bare URL/email examples. The alias and link-syntax examples quoted on the reference page.
- `docs/FEATURES.md:137-139` — "**Mod-L inserts an external link**… wraps selected plain text as a label". The Mod+L claim.
- `docs/FEATURES.md:128-129` — "The `((` popup full-text-searches blocks and inserts a **durable** reference (writes a stable `id::` first)." Block-reference authoring claim.
- `docs/FEATURES.md:149-156,160-164` — "Per-block reference count badge… grouped by page, each with its **ancestor breadcrumb**"; the right-click menu (open in sidebar / go to block / copy ref / copy embed) and `Mod+C`; "Inline block refs render as **link-styled text**… not a grey chip"; "Copying a block **strips `id::`** from the clipboard text (like OG)… though the `id::` stays in the file". Remaining block-reference claims.
- `docs/FEATURES.md:157-159` — "Block embeds remain live editing surfaces: their disclosure state is local to the visible embed…" Embed claims.
- `src/components/LinkedReferences.tsx:104-106,312,327` — "The 'Linked References' section (backlinks). Live, editable, collapsible, and filterable by co-referenced page (click a chip: include → exclude → off), mirroring OG's reference filter"; header "Linked References" plus count. Panel claims.
- `src/components/UnlinkedReferences.tsx:45,51,117` — "plain-text mentions of the page, collapsed by default"; header "▸ Unlinked References". Unlinked-panel claims.
- `docs/FEATURES.md:140-148` — "Linked & unlinked references on every page (live/editable), with co-reference filtering and hover previews — and in the **right sidebar** page view too… large panels group by source page and show bounded, highlighted excerpts… deep matches show one final ancestor breadcrumb and start folded in a view-local copy that never changes the source block's collapse state." Panel scope/grouping claims (drives the "large panels group them" wording).
- `crates/tine-core/src/query.rs:4553-4563` — "OG's `:page-ref` query rule reads `:block/path-refs`, which is the union of this block's explicit refs, every ancestor's refs, and the page a block physically belongs to." The path-refs rule taught on both pages; verified live in the regenerated demo (workflow example returns 6 results: the Roadmap outline plus the Guide bullets that link to it).
- `src/components/Macro.tsx:273-306` — an inline `{{query}}` executes via `backend().runGraphSearch` / `backend().runQuery` against the current graph. This is why the workflow example states per-surface behavior: in the read-only Guide the query runs against the user's own graph (guide pages are hidden from search per `docs/FEATURES.md:470`), while the copied Guide and the exported demo answer over guide content.

### Discrepancies / deliberate scope (J04)

- **Manager correction — stale search labels.** The worker found that `docs/FEATURES.md` and `crates/tine-core/src/templates/tips.md` still said "Open all results", "Rename it", "Query builder", and "under Search settings" while the shipped controls read **Open search tab**, **Name this search to save it as a page** / **Save page**, **Edit as visual query**, and Settings → **Editor** → **Learn Ctrl+K choices**. The manager corrected that directly affected documentation neighborhood before accepting J04; the source labels are at `src/components/QuickSwitcher.tsx:597`, `src/components/QueryWorkspace.tsx:523,735,775-779`, and `src/components/Settings.tsx:1719-1726`.
- The demo knock-on diffs are deterministic output, not new wording: `website/demo/feature-showcase.html` re-rendered because the tips embed picked up the added pointer bullet and its `{{query [[Welcome to Tine]]}}` count moved 9 → 10 (the new workflow page links Welcome); `pages.html`/`search-index.js` gained the two new pages.
- **Deliberate deferral:** the full query DSL (boolean clauses, properties, dates, sorting, aggregates) is intentionally not recreated on these pages; both pages point to the J05 walkthrough and Features/Sheets instead, keeping the DSL out of the ordinary-navigation path per the dossier's "lead with ordinary navigation/search".
- No per-platform claim beyond "Ctrl (Cmd on Mac)": the search/navigation surfaces above carry no documented Android variance in the cited sources, so none is asserted.

## Phase C — candidates: J06 research a document (Workflows/Research a document)

One canonical workflow page links the existing `Features/PDF annotation` reference rather than duplicating it; `Tine Guide` and the PDF annotation page gained one navigation bullet each. It remains a manager-review candidate, not a declaration that J06 is complete.

### Behavioral claim sources

- `crates/tine-core/src/templates/pdf.md:4,6,8` — "read it in a side pane right next to your notes", "Shift … Command … choose a color to clip a figure or equation as an image highlight", "each note jumps straight back to its spot in the document". The existing reference claims the workflow links to instead of expanding.
- `src/filedrop.ts:1-5,82-92` — "OS file drag-and-drop → insert as graph assets … insert each file as a new sibling block … the `![](../assets/…)` media form". The drop-onto-a-block step; `src/components/Block.assetPaste.test.tsx:341` — pasting a PDF yields `![notes.pdf](../assets/notes.pdf)`; `src/editor/autocomplete.ts:439` — slash command "Upload an asset". The bring-it-in path.
- `src/media.ts:175-180` — "On-disk name for a newly-inserted asset, per the user's format template (Settings → Backups → Asset names; default = the plain original filename). New inserts only — existing files are never renamed." The storage-naming note; `docs/FEATURES.md:430` — "Upload links keep the original source name while the configured asset template chooses the stored filename."
- `src/render/inline.tsx:469` — clicking the `pdf-link` asset anchor calls `openPdf(filename, label)`; `src/App.tsx:1302-1320` — the `.pdf-pane` with its resizer (320–1200 px); width persisted per device in `localStorage` at `src/ui.ts:758-771`. The open-and-resize claims.
- `src/components/PdfViewer.tsx:1791-1816,1821,1826-1848,1849-1855,1856-1870,1871-1902,1903-1922,1941-1968` — the toolbar inventory quoted on the page: page box (title "Page — type a number and press Enter to jump"), "Find in document (Ctrl+F)", zoom −/+, "Fit width"/"Fit height", "Area highlight" toggle, "Notes" (title "Open highlights & notes page"), "Outline", "⋯" ("More settings") with theme choices Light/Warm/Dark, and the find bar's `n / total` count.
- `src/components/PdfViewer.test.tsx:384-387` — `isPdfAreaModifier` is Shift on non-mac and ⌘ on mac; `docs/FEATURES.md:419-422` — "The drag must exceed 10 pixels in both dimensions, and dismissing the chooser writes nothing." The modifier split and the dismiss rule.
- `src/components/PdfViewer.tsx:1987-2041` — the highlight menu: color swatches recolor, ✕ "Remove highlight", and the desktop-only (`<Show when={menu()!.id && !isMobilePlatform}>`) "Copy ref" / "Linked references"; `PdfViewer.tsx:435-449` — "Copied highlight ref" toast; `PdfViewer.tsx:451-454` — `requestBlockReferences(id)` opens the highlight's reference panel ("Linked References" header at `src/components/BlockReferences.tsx:25-26`). The quote-and-trace claims, and the desktop-only platform boundary.
- `src/pdf.ts:45-68` — `assetKey` "MUST stay byte-for-byte in sync with the Rust `asset_key`" and `hlsPageName` = `hls__${assetKey(filename)}`; display strips the prefix at `src/components/Sidebar.tsx:243` and `src/router.ts:192`. The hls__ naming and plain-name display claims.
- `docs/FEATURES.md:422-423` — highlights "stored Logseq-compatibly (`assets/<key>.edn` + `hls__` pages, area crops as PNG assets)"; `docs/FEATURES.md:430-433` — "copied highlight references, rectangles, and sidecar metadata follow Logseq's current graph contract without discarding foreign fields". The storage and interoperability claims.
- `src/pdf.ts:78-79` — "Solid swatch colors for the highlight dot in the note bullet"; `src/render/annotationRef.test.tsx:36-77,94-125` — clicking an annotation block/pasted ref opens the owning PDF at its `hl-page` with the exact highlight id; `src/components/AnnotationBody.tsx:20`. The colored-dot and jump-back claims.
- `docs/FEATURES.md:427-429` — "writes **merge with disk** … recoloring a highlight updates its note-page badge to match." The recolor-sync sentence in "What you should see".
- `docs/FEATURES.md:424-426` — "Reader themes (**Light**, **Warm**, and **Dark**) persist app-locally"; the outline "follows named or explicit PDF destinations without entering the graph or annotation files". The device-not-graph theme claim.
- `docs/FEATURES.md:160-161` + `docs/FEATURES.md:128-129` — "Inline block refs render as **link-styled text**"; `((` "inserts a **durable** reference". The pasted-quote rendering claims.
- `crates/tine-core/src/templates/tips.md:27-28` — "**Shift-click** any bullet's dot to open it there"; "On a phone-width window (below 640 px), either sidebar opens over the page." The right-sidebar move and the phone-width boundary; `src/App.tsx:1303-1308` — `pdf-pane-mobile` fills the width; `src/components/PdfViewer.mobile.test.tsx:103,116` — on mobile the reader is the top transient layer and keeps the "Notes" action.
- `src/components/ContextMenu.tsx:851,866,878` — exact menu labels "Copy / export as…", "Copy page as Markdown", "Export to PDF…"; `docs/FEATURES.md:668-678` — prints through the OS print dialog, "always prints on a **light** background". The share-the-page claims.
- `docs/FEATURES.md:656-659` — "static HTML export (`public:: true` pages)"; `src/components/Settings.tsx:2106-2111` — Settings → Graph row "Publish" / button "Export graph to HTML". The publish step.
- `docs/adr/0017-snapshot-scope.md:20-28` — snapshots include "`assets/**/*.edn` highlight sidecars" and exclude binary asset bytes; user-facing phrasing "notes, config, and PDF highlights — not media files". The snapshot-coverage sentence.

### Discrepancies / deliberate scope (J06)

- **Deliberate deferral — no bundled sample PDF.** The Guide ships no PDF asset (`GUIDE_ASSETS` carries only `quick-capture.png`), so the page's journey is action-based with observable results rather than a live in-Guide demo; bundling a binary would enlarge every new onboarding graph and the public demo. A manager may add one later as part of the graph-wide review.
- **Desktop-only menu actions asserted as such.** "Copy ref" and "Linked references" are hidden from the highlight menu on mobile (`PdfViewer.tsx:2008`), so the page claims recolor/remove everywhere but quoting only on desktop; side-by-side reading is named a wider-window pattern (full-width reader + overlay sidebars on a phone).
- **Literal `../assets/` syntax kept out of the page on purpose.** `collect_guide_asset_refs` (onboarding.rs) treats any `../assets/…` string in a template as a bundled-asset reference, so the example asset link is described, not spelled; the reference page carries the same shapes in prose.
- **No fresh OG verification here.** "Logseq-compatibly" / "like Logseq" rest on the repo sources quoted above (`docs/FEATURES.md:422-433`, `docs/og-pdf-and-shortcuts.md`), not on an OG checkout — the manager verifies parity wording before anything public.
- **Manager verification/correction.** The manager confirmed OG's `assets/<key>.edn`, `hls__` page and highlight-property contract directly in `og/src/main/frontend/extensions/pdf/assets.cljs`, then replaced the worker's Markdown-only description with format-neutral "ordinary graph page" wording (Tine also preserves existing Org highlight pages), corrected "your pages" to "your notes", and made the clipboard result of **Copy page as Markdown** explicit.
- The regenerated demo diff is deterministic build output only: the new page, the `tine-guide` index and `features-pdf-annotation` pointer, and the `pages.html` / `search-index.js` listings; `feature-showcase.html` is unchanged (tips.md untouched in this slice).

## Phase C — candidates: J07 orientation and context layout (Start/Where things are + Workflows/Keep context visible)

Added the two pre-authorized pages: a short orientation map of the main places, and a workflow that teaches parking a source in the right sidebar, collecting/pinning tabs, splitting panes, saving the layout as a named workspace, and clearing chrome with focus mode. `Tine Guide`, `Welcome to Tine`, and Tips each gained one pointer link; the shortcut detail stays on Tips rather than being duplicated (plan §2.4). Each page has a semantic registration/copy test in onboarding.rs. It remains a manager-review candidate, not a declaration that J07 is complete.

### Behavioral claim sources

- `src/router.ts:281-283` — a fresh router "[s]tart[s] on a single journals tab"; `src/router.ts:277-280` — the saved session "round-trips through an atomic backend file rather than being tied to one WebView's localStorage". The tabs-come-back claims and the journal-feed default.
- `docs/FEATURES.md:408-409` — "Multi-day **journal feed** (one continuous editable list)". The journal-feed description on the Start page.
- `src/components/Sidebar.tsx:147,161,215,259,305` — the shipped left-sidebar labels "Journals", "FAVORITES", "RECENT", "ALL PAGES", "NAMESPACES". The sidebar inventory on the Start page.
- `src/components/Sidebar.tsx:352-354` — "Tine lists its known graphs here alongside open/create actions; Shift-click opens a peer window", with "Open graph…" / "New graph…" at `:441-470`. The graph-menu claims.
- `docs/FEATURES.md:460-461` — "Favorites and Recent fold independently and remember their state per graph." The per-graph fold claim.
- `src/components/Sidebar.tsx:313-315` — footer button "+ New page" opens the switcher; `src/components/QuickSwitcher.tsx:45` — "grouped search (Pages / Create / Commands / Blocks)". The create-from-Ctrl+K claim.
- `docs/FEATURES.md:482-486` — "Middle-click any bullet, page title, query result, or switcher row to open it in a background tab; pin (persisted), drag-reorder, `Mod+W` to close… (Logseq core has no tabs.)" The general tab claims and the OG-difference parenthesis.
- `src/keybindings.ts:297,300,329,330` — `tab/close` = "mod+w", `tab/reopen-closed` = "mod+shift+t", `ui/toggle-left-sidebar` = "t l", `ui/toggle-right-sidebar` = "t r". All four bindings named on the pages.
- `src/components/TabBar.tsx:222-230` — "click to activate, middle-click to close, double-click to pin, pointer-drag to reorder… Closing a pinned tab asks first. The whole session persists across launches." The pin/sticky/confirm claims.
- `src/router.ts:765` — closing a pinned tab asks "Close pinned tab …?" first; `src/router.ts:786-789` — Ctrl+Shift+T reopens "restoring its full back/forward history". The tab-safety claims.
- `src/components/Settings.tsx:1712-1717` — "Switch to an already-open tab when navigating" ("Plain navigation to a page, journal, or exact zoomed/file-pinned view focuses the matching tab…"); `src/navSettings.ts:20` — "Default ON." The reuse-tabs claim and its Settings path.
- `src/components/TabBar.tsx:643-654` — the ⌄ overflow button is titled/labelled "Show all tabs"; `docs/FEATURES.md:375-378` — the overview "lists every full title with active, pinned, and close controls… Drag its handle or use `Alt+Up/Down` to reorder". The overflow-overview claims.
- `src/render/inline.tsx:293-299` — Ctrl/Cmd+click a `[[link]]` → `openRouteInOtherPane`, Shift+click → `openPageInSidebar`; `src/components/Block.tsx:494` — the bullet-dot tooltip "Click to zoom; shift-click → sidebar; middle-click → new tab; drag to move". The click-modifier claims.
- `src/components/QuickSwitcher.tsx:420-422` — Shift+Enter on a page/block result opens it in the right sidebar; Alt+Enter opens it in the other pane. Both switcher modifiers.
- `src/keybindings.ts:279` — `editor/open-link-in-sidebar` = "mod+shift+o" ("Open the link at the caret in the sidebar"). The Ctrl+Shift+O claim.
- `src/components/RightSidebar.tsx:159-161` — the menu items "Collapse all" / "Expand all" / "Close all", with per-item collapse/close buttons at `:377,386`.
- `docs/FEATURES.md:461-463` — "a compact menu collapses, expands, or closes all items, committing active edits before an item is unmounted"; `src/components/RightSidebar.tsx:58` — `endEditForSurface("sidebar-collapse", key)`; `src/ui.ts:1240-1246` — shared pre-unmount preparation. The "commits the active edit first" claim.
- `src/ui.ts:1249-1254` — right-sidebar items saved via `scheduleSessionSave()` with the comment "durable right-sidebar items (localStorage isn't kept)". The "restored with your session" claim.
- `src/keybindings.ts:292-293` — `go/backward` = "alt+left", `go/forward` = "alt+right"; `docs/FEATURES.md:487-488` — "Browser-style back/forward (`Alt+Left` / `Alt+Right`, per-tab history, works mid-edit)". The history claims.
- `src/keybindings.ts:305-306` — `pane/split-right` = "mod+alt+\\", `pane/split-down` = "mod+alt+shift+\\". The split bindings.
- `docs/FEATURES.md:339-341` — "`Mod+Alt+\` splits right and duplicates the current tab"; `src/panes.ts:197-207` — the new pane starts from a snapshot of the active tab. The mirror-split claim.
- `src/App.tsx:370-393` — dragging a pane seam resizes via `setSplitRatio`. The resize claim.
- `docs/FEATURES.md:371-374` — "drag a tab within a strip to reorder it, onto another pane's strip to move it… onto a seam/pane edge to split that half… `Esc` cancels an in-progress tab drag." The drag/drop claims.
- `docs/FEATURES.md:342-346` + `src/keybindings.ts:319-326` — "`Mod+1` … `Mod+9` focuses panes in spatial reading order; `Mod+Alt+Left/Right/Up/Down` focuses the nearest pane… `Mod+Alt+Shift+…` moves the active tab"; "Moving the last page tab out closes the emptied pane; the journals feed keeps its last tab", with `src/router.ts:757` — "feed pane keeps its last tab". The pane keyboard claims.
- `docs/FEATURES.md:347-367` — the Esc pane-select ladder: "a hint pill shows at the bottom of the window… `Enter` makes a **mirror split**… typing… opens the split with the switcher pre-filled with what you typed". The staged-control paragraph.
- `docs/FEATURES.md:332-338` — "save the whole window context (split layout, every pane's tabs and back/forward history, both sidebars, and Recent) as a named workspace… Each graph keeps its own set and the active one persists across restarts. Workspaces are device-local session state — saving, switching, or deleting one never writes to your graph." The workspace claims.
- `src/components/WorkspaceSwitcher.tsx:91,116-131,142-171` — hover opens a quick-switch list; menu rows carry "Rename" and "Delete"; "+ New workspace" creates. The switcher-affordance claims.
- `src/components/Settings.tsx:1467-1469` — "Dim in focus mode" — "Auto-enable dim inactive blocks (t b) when entering focus mode (t f)."; `src/ui.ts:424-426` — the auto-dim "doesn't rewrite the persisted dim preference, so your manual `t b` choice is kept". The dim claims.
- `src/ui.ts:412-414,557-570` — focus mode "only hides the sidebars + topbar and goes fullscreen"; `crates/tine-core/src/templates/tips.md:31` — "layered **Esc** steps back out"; `src/App.tsx:1141-1145` — the hover zone reveals the topbar in focus mode. The focus-mode claims.
- `docs/FEATURES.md:607-611` — "below 640 px, the left and right sidebars are modal drawers over an unchanged page… At 640 px and wider… they remain persistent desktop-style panes"; `src/mobileDrawers.ts:20` — the classifier is `matchMedia("(max-width: 639px)")`; `src/App.tsx:1119` — drawer close button aria-label "Close navigation sidebar". The phone-layout claims.
- `src/App.tsx:1204-1207` — "The tab strip is a desktop feature; on a phone it only crowds the single-row toolbar… Hide it there"; `src/panes.ts:218,274,362,379,398` — pane mutations return early on `isMobilePlatform`; `src/nativeChrome.ts:43-44` — `isMobilePlatform` = Android/iPhone/iPad/iPod UA. The "tabs and splits are desktop-only" claims.
- `src-tauri/src/settings.rs:397-400` — the persisted UI session is "app-level window state, not graph content, so it lives next to the settings file in the app-data dir". The "session state outside your graph" claim.
- `crates/tine-core/src/templates/bring-existing-graph.md:21` — "A second Tine window on the same graph is refused"; `docs/adr/0039-filesystem-scope-boundary.md:20-21` — the window-registry rejection rule. The two-window boundary stated beside the Shift-click peer-window gesture.
- `src/App.tsx:1271` — Settings button title "Settings (t s)"; `src/components/HelpShortcuts.tsx:105-114,23-54` — the bottom-right "?" button ("Help (?)") with items Guide / Keyboard shortcuts / Release notes / Report a bug; `src/keybindings.ts:331,350` — "t s" and "shift+?" bindings. The Settings-and-Help claims on the Start page.

### Discrepancies / deliberate scope (J07)

- **Manager correction — placement and scope.** The worker found that `docs/FEATURES.md:334-335` still placed the workspace switcher "at the far left of the tab bar", while the shipped UI mounts it at the top of the left sidebar (`src/App.tsx:1114-1117`) and keeps a compact toolbar control only while the sidebar is folded (`src/App.tsx:1198-1203`). The manager corrected that inventory, limited peer-window Shift-click to desktop, removed an unnecessary unverified Logseq-tabs comparison, and replaced "the whole window exactly" with the persisted pane/tabs/histories/sidebars/Recent contract rather than implying window geometry.
- **Showcase knock-on diffs are deterministic output, not new wording.** `website/demo/feature-showcase.html` re-rendered because its whole-page embed of Tips picked up the added pointer bullet, and its `{{query [[Welcome to Tine]]}}` count moved 10 → 11 (the new Start page's closing bullet links Welcome) — the same mechanism recorded for the J04 slice.
- **Platform claims are gated, and the full matrix is deferred to J10 per the dossier.** The pages assert only what the cited sources gate: sidebars become overlay drawers below 640 px window width (`mobileDrawers.ts`), while the tab strip is hidden and pane splits are disabled on the phone app (`isMobilePlatform` UA gate — Android/iPhone/iPad/iPod). No tablet-UI claims beyond FEATURES' "640 px and wider—including tablets".
- **No new reference page added.** Plan §4.3 lists a "tabs, panes, sidebar, focus and multi-window behavior" reference family; this dossier authorized at most the two new pages, so that family stays covered by `Features/Tips & shortcuts` plus the new workflow until a manager grants a third page.
- **OG parity wording not re-verified in this lane.** "(Logseq core has no tabs.)" and the "Shift-click opens a peer window" contrast rest on the repo sources quoted above (`docs/FEATURES.md:482-486`, `src/components/Sidebar.tsx`), not on an OG checkout — the manager verifies parity wording before anything public.

## Phase C — candidates: J08 extend Tine (Workflows/Extend Tine)

Added the one pre-authorized page: a hands-on install → review → enable → settings → disable/uninstall path for plugins, plus a compact themes section and a platform/distribution boundary section. `Tine Guide` gained one Workflows-lens bullet and `Features/Plugins` one pointer bullet; `Welcome to Tine` was deliberately left untouched (see deferrals). One semantic registration/copy/link test in onboarding.rs. It remains a manager-review candidate, not a declaration that J08 is complete.

### Behavioral claim sources

- `crates/tine-core/src/templates/plugins.md:4-5` — "capability-limited WebAssembly guests… **not Logseq or Obsidian plugins**"; "cannot directly access the DOM, Tauri, files, processes, the network, or arbitrary graph paths." The boundary bullets on the new page.
- `src/plugins/manifest.ts:19-27` — the closed `PLUGIN_CAPABILITIES` list (`commands.register`, `slash-commands.register`, `block-decorations.register`, `graph.read.visible`, `graph.write.block`, `settings.read`, `settings.write`); `docs/FEATURES.md:573-575` — "command-palette actions, slash commands, bounded block decorations, declarative settings, and narrowly preconditioned focused-block edits only through declared capabilities." The user-readable capability summary.
- `docs/plugins/README.md:65-66` — "a small host-owned visual vocabulary (`thread-lines`, `badge`). A plugin cannot inject HTML or CSS." The decoration and no-HTML/CSS claims.
- `docs/plugins/README.md:68-72` — "A write effect requires `graph.write.block`, may target only the block Tine included in the triggering event, records normal undo, and reaches disk only through Tine's existing conflict-checked persistence engine." The `graph.write.block` limits sentence.
- `src/components/Settings.tsx:800-805` — safety verdicts "Human-reviewed before publication" / "Low-risk automated pass" / "Automated review passed"; `Settings.tsx:841` — the "Safety report" toggle; `Settings.tsx:900` — "Automated review is evidence, not a guarantee." The review claims in step 1.
- `src/components/Settings.tsx:866-868` — "Tine holds every graph-writing plugin for human review, even when the automated checks otherwise pass." The parenthetical explaining the human-review badge.
- `src/components/Settings.tsx:609-611,660-662` — local install toast "installed disabled. Review it, then enable it here." and community toast "installed disabled. Enable it after reviewing its capabilities.", both followed by `setView("installed")` + opening the detail page. The installed-disabled and lands-on-Installed claims (also `plugins.md:10`).
- `src/components/Settings.tsx:688-690,953` — detail/row hints list author, license, platforms, and "Capabilities: …". The review-identity step.
- `src/components/Settings.tsx:819-821` — unsupported platforms render `Unavailable on ${currentPlatform()}` in place of Install. The "Unavailable on …" claim.
- `src/components/Settings.tsx:734-735,747` — section "Install a local package", hint "Select its `manifest.json` and `.wasm` file together", button "Choose package…". The sideloading step (also `plugins.md:9`).
- `src/components/Settings.tsx:701-702` + `PluginSettingsForm` at `:471-582` — the **Settings** section; host-drawn Toggle/select/number/text controls at `:517-564`; per-key **Reset** at `:566-568`; "**Reset all settings**" at `:577-579`; hint at `:580` — "Stored on this device only. Plugin settings are never written into your graph."; `docs/adr/0046-declarative-plugin-settings-and-themes.md:22-28` — the host-renders-controls, device-local, reset/uninstall contract. The declarative-settings section.
- `src/components/Settings.tsx:635` — uninstall confirm "This removes the plugin from this device. It does not change your graph or notes."; `plugins.md:13` — "Disable a plugin at any time without removing it. **Uninstall** removes only that app-local package version and never changes your graph or notes." The disable/uninstall claims.
- `docs/plugins/README.md:8-11` — "Disabling a plugin must leave every graph readable; plugins may not invent a required graph storage format." The readable-pages claim.
- `docs/FEATURES.md:567` — "cached signed revocations apply before startup activation"; `src/plugins/startup.ts:19-28` — verified cached revocations seed `pluginManager.initialize` before persisted plugins load; `plugins.md:14` — "A revoked version stays disabled even when the live catalogue is temporarily unavailable." The revocation sentence.
- `src/components/Settings.tsx:1240-1249` + `src/styles/themes/index.ts:17-36` — the **Themes** gallery: Default (badge "Stock") plus Nord, Solarized, Gruvbox; `docs/adr/0046-declarative-plugin-settings-and-themes.md:30-31` — themes are "one-selected presentation data under Appearance, not Wasm"; `docs/FEATURES.md:639-640` — "stores only the selected theme id in its backend settings… never writes to your graph" (device-local claim); `Settings.tsx:1268-1294` — the community **Theme packages** list with per-theme **Install**; `Settings.tsx:1305-1317` — "Install a token theme… only whitelisted color tokens and metadata—no scripts, selectors, imports, or remote assets", button "Choose theme.json…". The install-a-theme and theme section claims.
- `src/components/Settings.tsx:1263-1265` — "Themes recolor Tine using Logseq's `--ls-*` variables. If you keep your own `logseq/custom.css`, it still takes priority."; `docs/FEATURES.md:641-645` — the cascade "loads last" claim; `Settings.tsx:1154` — theme uninstall confirm "does not change your graph or custom.css." The custom.css-outranks claim.
- `docs/adr/0047-android-plugin-platform-at-launch.md:16-17` — "Tine 0.6 exposes plugin and theme browse, install, enable/select, settings, disable, and uninstall flows on Android as well as desktop" (version number deliberately left off the user page); `:28-29` — "Tine shows it as unavailable rather than trying to run it or silently hiding the incompatibility." The Android parity claims.
- `docs/FEATURES.md:568-572` — "**F-Droid distribution boundary** — F-Droid builds omit the network community plugin/theme catalogue to comply with the store's runtime-code-download policy. The bounded plugin host, installed and locally selected plugin/theme packages, and built-in themes remain available"; `vite.config.ts:86` + `src/plugins/registry.ts:25` — the `TINE_COMMUNITY_REGISTRY` build flag; `src/components/Settings.tsx:752,1267` — both catalogue sections gate on it; `src/components/Settings.communityRegistry.fdroid.test.tsx:28-36` — "hides the network catalogue but keeps local plugin sideloading". The F-Droid claims.
- `docs/FEATURES.md:613-615` — "Distribution — sideloaded, release-signed APK attached to each GitHub release… Play Store / F-Droid are planned; iOS is being scoped." The no-iOS-build-today claim.

### Discrepancies / deliberate scope (J08)

- **`Welcome to Tine` deliberately untouched.** Its "Keep exploring" list already links `[[Features/Plugins]]` (welcome.md:24); adding a second plugin-adjacent pointer would crowd the landing path, so the new page is reached from the Guide index and the Features page, matching the J04/J06 link pattern. Flagged so the manager can add it during the graph-wide Phase D review if wanted.
- **No iOS vocabulary on the user page.** Per the dossier the page asserts only what shipped builds prove: "There is no iOS build of Tine today" (FEATURES.md:615 basis). The `ios` manifest value stays developer vocabulary (docs/plugins/README.md:83-84, ADR 0052) and is not surfaced.
- **Release version numbers left off the page.** ADR 0047 and docs/plugins/README.md say "Tine 0.6"; the Guide templates carry no version pins, so the page states the flows without numbering.
- **F-Droid claims stop at the in-repo flag boundary.** The off-switch wiring, hidden-catalogue UI, and retained local-sideload behavior are proven in-repo (`vite.config.ts:86`, `registry.ts:25`, the fdroid render test); the external F-Droid recipe that actually sets the variable lives outside this repo, so nothing about that recipe itself is asserted beyond FEATURES.md/README's existing public claims.
- **Full platform matrix deferred to J10 per the dossier.** Only Android parity, the unavailable-platform treatment, F-Droid, and the absent iOS build are claimed here.
- **No fresh OG verification in this lane.** The `--ls-*` variable/token-theme cascade claims rest on repo sources (`Settings.tsx:1263-1265`, `docs/FEATURES.md:641-645`), not on an OG checkout; OG theme-variable compatibility wording is manager-verified before anything public.
- **Deterministic generated diff only.** `website/demo/workflows-extend-tine.html` is new; `tine-guide.html` and `features-plugins.html` gained exactly their one new bullet; `pages.html`/`search-index.js` list the new page. `feature-showcase.html` is unchanged (tips.md untouched and the new page does not link `[[Welcome to Tine]]`, so its query count does not move).
- **No CHANGELOG or regression-catalog entry.** This slice changes no production behavior, matching the J06/J07 documentation-slice pattern.
- **Manager correction - experimental and release scope.** The worker's page did not tell users that plugin API 0.x is experimental, despite `docs/plugins/README.md:7-11` promising that an incompatible plugin may be disabled while its package, settings, and graph remain intact. The manager added that user-relevant boundary. The worker's broader "There is no iOS build ... today" wording was narrowed to the evidenced public-release and plugin-host status, with the full matrix delegated to J10.

## Phase C — candidates: J10 platforms (Reference/Platforms and mobile)

Added the one pre-authorized page splitting "narrow window" (640 px width classifier, any device) from "native mobile platform" (UA-detected app behavior), then mapping Android, desktop, updates/distribution, and current iOS status. `Tine Guide` gained one Feature-reference bullet; `Start/Where things are` (its "On a phone" section) and `Workflows/Extend Tine` (its existing forward-reference sentence) each gained the pointer they were designed to carry. One semantic registration/copy/link test in onboarding.rs. It remains a manager-review candidate, not a declaration that J10 is complete.

### Behavioral claim sources

- `docs/FEATURES.md:608-612` — "below 640 px, the left and right sidebars are modal drawers over an unchanged page: tap the shaded outside edge, use their close control, press Escape, or press Android Back to dismiss them safely. At 640 px and wider—including tablets—they remain persistent desktop-style panes, can be open together, and retain their resize controls." The width-vs-device section, verbatim.
- `src/mobileDrawers.ts:7-8,10,20` — `isMobileDrawerViewport(width) { return width < 640; }` + the sole classifier is `matchMedia("(max-width: 639px)")`. Proof the drawer boundary is width-only and applies to any narrow window, not to a device class.
- `src/App.tsx:1204-1207` — "The tab strip is a desktop feature; on a phone it only crowds the single-row toolbar … Hide it there" (`<Show when={!isMobilePlatform …>`); `src/panes.ts:218` — `if (isMobilePlatform) return null` on split (+274/362/379/398 siblings); `src/session.ts:228,241` — saved pane layout only restored when `!isMobilePlatform`. The width-independent mobile-gated surfaces in "Question 2".
- `src/nativeChrome.ts:43-44` — `isMobilePlatform = /Android|iPhone|iPad|iPod/i.test(navigator.userAgent)`. The UA detection named on the page.
- `docs/FEATURES.md:589-594` — "On first run you grant 'All files access' and pick your graph folder (e.g. a Syncthing folder), then Tine reads and writes it directly, so it coexists with the Logseq mobile app on one graph. The file watcher runs in poll mode so external edits appear live." The Android graph-access + watcher claims; `src/graph.ts:469` — toast 'Grant "All files access" for Tine, then tap Open again.'; `src-tauri/src/watcher.rs:1291` — '"poll" (default on Android)'.
- `docs/FEATURES.md:595-598` — "Above-keyboard editing toolbar … outdent / indent, move block up / down, soft line break, TODO, camera, voice memo, date, `[[ ]]` / `(( ))`, the slash menu, and a pinned hide-keyboard button."; `src/components/MobileKeyboardToolbar.tsx:29-42` — the exact shipped labels (Outdent/Indent/Move block up/…/Slash menu + Undo/Redo), gate at `:175` `isMobilePlatform && visible()`. The toolbar bullet.
- `docs/FEATURES.md:599-602` — "the camera button takes a photo (or picks an existing image) straight into the graph's assets/ and inserts it; the mic button records a voice memo (.m4a) into assets/ … (permission-prompted on first use)". The camera/voice bullet.
- `docs/FEATURES.md:604-605` — "a hardware Back button that navigates within Tine (exiting only at the root)"; `src/App.tsx:643-655` — `installAndroidBackHandler({ … dismissTransient … dismissDrawer … historyBack … closeRoot })`. The Back bullet.
- `docs/FEATURES.md:603-607` — "a real app icon, an edge-to-edge layout whose native status/navigation icons follow the selected theme … Interface size scales the complete Android document and persists locally"; `src/components/Settings.tsx:1391-1392` — label "Interface size", hint "Zoom the whole interface … Saved on this device." The interface-size/theme bullet.
- `docs/adr/0047-android-plugin-platform-at-launch.md:16-17` — "Tine 0.6 exposes plugin and theme browse, install, enable/select, settings, disable, and uninstall flows on Android as well as desktop." The Android plugin-parity bullet (version number left off the user page, matching the J08 pattern).
- `src-tauri/src/lib.rs:317` — `Err("quick capture is only available on desktop")` in `#[cfg(not(desktop))] quick_capture_activate`; quick-capture page template is desktop-shortcut based. The no-quick-capture line.
- `docs/adr/0038-multi-window-multi-graph.md:82` — "Scope is desktop only. Android/iOS are single-activity; multi-window is a non-goal there". The no-multi-window line.
- `src/contextMenuPolicy.ts:9-29` — "Ordinary block rows keep desktop right-click, but Android long-press may open Tine's block menu only from the explicit bullet affordance" + "Android WebView uses `contextmenu` to begin text selection, so leave the event wholly native there". The context-menu bullet.
- `src/components/PdfViewer.tsx:2008` — highlight-menu copy actions gated `<Show when={menu()!.id && !isMobilePlatform}>` (labels "Copy ref" / "Linked references", J06 slice). The PDF-desktop-only bullet.
- `src/update.ts:44-56` — `updateMode()`: non-desktop → `"unavailable"` ("Mobile builds update through their distribution channel; a platform-detection failure must therefore fail closed"), macOS UA → `"manual"` (releases page), else `"self"`. Plus header comment :9-13 "Android/iOS update through their distribution channel, so both the notifier and installer are disabled there."; `docs/adr/0011-in-app-self-update.md` — ship updater "for Windows and Linux (AppImage)" and keep "macOS on the manual path" until notarization; `src/components/AboutTab.tsx:88` — button label "Check for updates". The update bullets for both platforms.
- `docs/FEATURES.md:613-615` — "Distribution — sideloaded, release-signed APK attached to each GitHub release (built and signed in CI). Play Store / F-Droid are planned; iOS is being scoped."; `.github/workflows/release.yml:431-446` — the `android:` release job and the hard "Require Android signing" step. The distribution line.
- `docs/FEATURES.md:568-572` — "F-Droid builds omit the network community plugin/theme catalogue to comply with the store's runtime-code-download policy. The bounded plugin host, installed and locally selected plugin/theme packages, and built-in themes remain available." The F-Droid clause in the distribution bullet.
- `README.md:60-68` — desktop app inventory: AppImage runs on any distro / open the `.dmg` / run the `.exe` installer. The desktop heading.
- `README.md:295` — "iOS is being scoped." The iOS-status bullet; `docs/adr/0047:25-26` — "ios remains an API vocabulary value for portable guests, not a promise that Tine ships an iOS host." The developer-vocabulary distinction.
- `src/plugins/manifest.ts:5` — `PLUGIN_PLATFORMS = ["desktop", "android", "ios"]`; `src/graph.ts:443-472` — iOS folder-picker branches exist in source; `src-tauri/gen/` contains only `android` and `schemas` (no iOS app scaffold). The "checked-in scaffolding, not a shipped app" claim — the page teaches exactly this limit.

### Discrepancies / deliberate scope (J10)

- **No contradictions found between sources.** The one tension worth flagging to the manager: `docs/FEATURES.md:614` says "Play Store / F-Droid are planned" while `:568-572` already documents an F-Droid build boundary; the page phrases both together ("planned … will omit the network plugin/theme catalogue"), matching the J08 page's stance that the F-Droid recipe lives outside this repo.
- **Tips template untouched** (authorized but unnecessary): `templates/tips.md:29` already carries the exact 640 px drawer sentence, and plan §2.4 says reference detail must not be duplicated. `Workflows/Keep context visible` likewise keeps its one-line phone summary and stays unedited — the new page replaces its role as the platform map, so a second pointer there would duplicate the J07 page's closing links.
- **`Workflows/Extend Tine`'s final sentence was a designed forward-reference** ("The platform reference in this Guide carries the full map."); it now links `[[Reference/Platforms and mobile]]` as the J08 record anticipated.
- **No feature/user-visible version numbers on the page** (ADR 0047's "Tine 0.6" stays developer-facing), and no per-padding refresh of `docs/FEATURES.md` — it is manager-owned; FEATURES remains the richer catalogue, this page the scannable map.
- **Spell-check, dev tools, window-frame toggle** exist but are intentionally not catalogued (minor chrome, not platform-availability claims); the page stays a scannable comparison per the dossier rather than an exhaustive feature list.
- **No OG parity claims on this page** — everything is Tine's own shipped behavior, so no OG checkout verification is owed.
- **Deterministic generated diff only:** `website/demo/reference-platforms-and-mobile.html` is new; `tine-guide.html`, `start-where-things-are.html`, `workflows-extend-tine.html` gained exactly their one new line each; `pages.html` / `search-index.js` list the new page; `feature-showcase.html` unchanged (no new `[[Welcome to Tine]]` links, so its query counts do not move).
- **No CHANGELOG or regression-catalog entry** — documentation-only slice, consistent with the J06–J08 slices.

## Phase D — whole-graph editorial review (Guide as one journey)

All 24 registered templates were read in rendered journey order after the Phase C pages landed. Material claims were re-checked against current code/tests and the Phase A–C ledgers; corrections were limited to nine templates plus regeneration. No new pages, titles, URLs, or manifest entries. Answers to the plan §6 questions: a novice reaches a useful journal/task result through Welcome → Where things are → Capture and plan your day without reference material; exact scope/default/compatibility rows sit on the J03/J04/J09/J10 reference pages; every workflow links its reference page instead of duplicating it (one intra-page duplication removed, item 3 below); experimental and platform-limited behavior is labeled where encountered (managed-storage **Testing only** at all five mention surfaces, plugin API experimental, quick capture desktop, PDF desktop menu entries, F-Droid/iOS status).

### Corrected claims and source evidence

1. `extend-tine.md:27` — "The F-Droid build ships without the network community catalogue" presented an F-Droid build as distributed today, contradicting `platforms-and-mobile.md:25` ("F-Droid [is] planned") and `docs/FEATURES.md:613-615`: "Play Store / F-Droid are planned; iOS is being scoped." Corrected to planned status; the build-boundary sentence stays grounded in `docs/FEATURES.md:568-572` and `src/components/Settings.communityRegistry.fdroid.test.tsx:28-36`.
2. `sheets.md:47` and the `formulas.md` walkthrough — the label "**Add/Edit formula**" does not exist in the shipped UI. The shipped menu labels are "**Add formula…**" (`src/components/SheetTable.tsx:661,698`; `src/components/ContextMenu.tsx:556`) and "**Edit formula…**" (`SheetTable.tsx:642`); corrected on both pages.
3. `formulas.md` — carried two overlapping walkthroughs ("Add one yourself" and "Create one yourself — formula"). Merged into the established "Create one yourself" convention, keeping its unique details: the ⋮-menu alternative, the `plan` naming hint, and the later "**Edit formula…**" maintenance step.
4. `capture-plan-day.md:41` — "the command palette (Ctrl+K)" conflated the quick switcher with the app's command palette, which is `mod+shift+p` (`src/keybindings.ts:280`: id `command-palette/toggle`, label "Command palette"). Corrected to **Ctrl+Shift+P**, matching `tips.md:6`; the carry presets themselves are `src/keybindings.ts:353-356`.
5. `welcome.md:4` — "the same Markdown files as Logseq" dropped Org support that `bring-existing-graph.md:4` ("same Markdown/Org files"), `files-external-edits-backups.md:6` ("one Markdown or Org file per page"), and `docs/FEATURES.md:539-545` back. Corrected to "same Markdown/Org files as Logseq".
6. `welcome.md:20` and `guide.md:21` — quick capture appeared with no platform qualifier although it is desktop-gated (`src-tauri/src/lib.rs:317`: "quick capture is only available on desktop"; `platforms-and-mobile.md:21`). Added "(desktop)" hints at both mention points.
7. `roadmap.md:6` — "Tine round-trips your files byte-for-byte" overstated the guarantee. The verified claim is format-preserving saves (`docs/FEATURES.md:509-512`; `files-external-edits-backups.md:12`); byte-for-byte applies only to Org rewrites, with a read-only guard otherwise (`docs/FEATURES.md:539-545`). Corrected to "Tine's saves are format-preserving".
8. Stranded links — `welcome.md:26` narrated graph opening inline without linking the Start page; now links `[[Start/Bring an existing graph]]`. `quick-capture.md` had zero outgoing links; gained "Where next" → `[[Workflows/Capture and plan your day]]` + `[[Reference/Platforms and mobile]]`. `bring-existing-graph.md` gained "Where next" → `[[Start/Where things are]]` + `[[Workflows/Capture and plan your day]]`, closing the Start lens into the first workflow. All new targets are registered pages; `guide_link_set_is_closed_over_demo_pages` stays green.

### Mechanical and rendered checks

- `npm run docs:build` republished all 24 pages (32 files); every generated diff is attributable to items 1–8 or their query/embed knock-ons (`feature-showcase.html` and `index.html` re-render the Welcome outline and the embedded Roadmap bullet; `workflows-find-and-revisit.html` re-renders its live `{{query [[Project/Roadmap]]}}` with the corrected Roadmap sentence, count unchanged at 6).
- Local link/asset sweep of every generated page: all `href`/`src` targets resolve; the only absent targets are the eight deliberate click-to-create stubs allowlisted in `docs/guide-deliberate-stubs.json` (all pre-existing).
- Structural width probe (headless Chromium, 1280 px and 400 px) over all 11 changed pages: no new horizontal overflow. Two pre-existing conditions reproduce *identically* on HEAD's demo and are recorded, not repaired: `feature-showcase` has one overflowing SVG path at both widths, and `features-sheets` overflows a 400 px viewport because the positional-grid demo pins `tine.col-widths:: 0=140;1=130;2=220` (≈490 px).
- Gates: `cargo fmt --all -- --check` clean; `cargo test -p tine-core onboarding` 22/22 pass; `npm run docs:check` ("Guide/demo OK: 32 generated files match"); `git diff --check` clean.
- Visual acceptance remains manager work (plan §8): this lane's model cannot display images, so the structural probe above substitutes; the manager should do the rendered-page pass at desktop and phone widths before acceptance.

### Deferrals and observations

- **Pre-existing demo-site narrow-width issues** (above): the demo's `style.css` comes from `scripts/build-guide-demo.mjs`, which is manager-owned (plan §3.1), so the 400 px Sheets overflow and the showcase SVG overflow are recorded for the manager, not repaired here.
- The Guide index "Workflows" section keeps its Phase-A-accepted mix of six `Workflows/*` pages plus the four task-forward `Features/*` pages (Sheets, Formulas, Quick capture, PDF annotation); not a contradiction, left as-is.
- The exact 640 px boundary remains stated on three pages (`where-things-are.md:22`, `tips.md:29`, `platforms-and-mobile.md:6`) — consistent and correct; the platforms page is canonical, the others keep it glanceable.
- "Ctrl+F" (workflow pages) vs "Mod+F" (`pages-links-references-search.md:29`) name the same binding on their own pages; per-page consistent, left alone.
- Sample-data owner names (`Martin`, `Codex`) inside `sheets.md`/`showcase.md` example tables predate this review (Phase A accepted); cosmetic, untouched.
- Welcome's "no import, no export" idiom kept: it names the Tine⇆Logseq workflow (no conversion step), matching `bring-existing-graph.md:4`; the app's export features live on their own pages.
- Managed-storage/sync copy untouched (frozen, plan §3.1); **Testing only** wording verified at the five mention surfaces (welcome:25, guide:24, files:32, bring:4, managed-sync:4).
- `Reference/Settings and scope` and `Reference/Logseq compatibility` (plan §4.4) remain uncreated — new pages are not authorized in Phase D; settings defaults/scopes ship inside the existing J03/J04/J09/J10 reference pages.
- Journey dispositions (plan §5 — worker reports, manager accepts): J01–J10 all remain **candidates** for manager acceptance; this review regressed none, and J01/J02 gained the Welcome→`Start/Bring an existing graph` link and the Start-lens "Where next" closures.
- No CHANGELOG or regression-catalog entry — documentation-only slice, matching the J06–J10 pattern.
