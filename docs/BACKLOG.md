# Tine — product backlog and decision archive

The live, inspectable work board is the public
[Tine GitHub Project](https://github.com/users/martinkoutecky/projects/1); release
milestones answer when work is targeted. This document is not a second status
board. It preserves durable product context, researched candidates, and explicit
WONTFIX rationale that would be too large or too architectural for a Project
card. Current issue status and Now/Next/Later views must be read from GitHub.

Detailed engineering/data-safety debt remains private under `tine-agents/specs/`;
**lsdoc**, the parser, has its own repository and issue tracker.

---

## Active product context (non-canonical snapshot)

These notes explain active threads, but GitHub—not this ordering—is authoritative.

_(**Shipped & merged to master**, removed from Now: **Sheets** — the grid/table/board
layout engine (spec [docs/breadth-grid-spec.md](breadth-grid-spec.md)) — and **Split view**
with per-pane tabs/history and the TreeSheets grid nav model (spec
[docs/split-view-spec.md](split-view-spec.md), ADR 0032); both merged `8bc66a9` and shipped
in **v0.5.0**. **Multi-graph + multi-window workflow** (GH #55/#70/#56; ADR 0038)
merged to master as `ad9bf67`.)_

- **Experimental managed sync stabilization** (ADR 0049) — The operation-backed
  compatible mode is now on `master` and exposed only as a **Testing only** opt-in
  under Settings → Backups & recovery → Storage & sync. It keeps Markdown/Org as
  an editable projection, records managed history in `.tine-sync/`, supports
  conservative external-file import and two-device shared-provider setup, and
  leaves Direct files as the default for every graph. The current managed scope is
  page and journal text; assets, PDF sidecars, and configuration remain ordinary
  provider files. Remaining before it can leave experimental status: full
  desktop/mobile CI and deployment, real two-device Syncthing/Dropbox soak tests,
  adversarial long-history performance measurements, canonical private-mode
  projection policy, and provider-blind encryption.

- **Inline-code property lookalikes parse as properties** (Martin, Jul 8 2026:
  fix in Now, "should be treated as code"). A bullet whose text STARTS with an
  inline-code span containing `::` — e.g. a line beginning with
  backtick-`tine.view:: grid`-backtick — is recognized as a property line and
  hidden, because property recognition runs on raw lines before inline parsing.
  Expected: a line-leading inline-code span suppresses property recognition (it
  is code, not metadata). Touches the ONE property-line recognizer (and check
  what OG/mldoc does for the parity record before changing behavior — if OG has
  the same bug we deviate deliberately). Found while authoring the tine-test
  [[Sheets guide]] page.
- **Experimental Tine-native plugin platform** (ADR 0041) — a capability-limited
  WebAssembly guest runtime, host-owned contribution API, developer SDK and
  conformance tools, public community catalogue, and an automated local-machine
  audit/quarantine pipeline. Desktop installation ships first while the runtime
  stays mobile-capable. This deliberately does not implement `@logseq/libs` or
  Obsidian compatibility; their plugins are ported to the smaller Tine API.
---

## Near-term candidate context

These are researched candidates retained for their details. Their order here is
not priority; the GitHub Project and milestones are authoritative.

_(**Shipped & released**, removed from the queue: **#23 paste-URL-over-selection** & **#24 copy-code/inline/links** (both in **v0.4.2**, Martin-tested, **issues CLOSED**), **Android camera/photo capture**, **Android voice-memo recording**, and the whole **Queries batch** — result aggregation/group-by (1a), the "⚙ advanced" switch (1b), and Datalog coverage expansion (1c) per [docs/plans/queries-batch.md](plans/queries-batch.md) — all in **v0.4.1**. **Excalidraw + drawio assets** — render-as-image + "Edit in your own editor" via a generic **external-media-editor registry** (GH #38) — shipped `76c42e5` (spec [docs/excalidraw-assets-spec.md](excalidraw-assets-spec.md)). **Flathub** moved to **WONTFIX** — Flathub bans AI-built apps.)_

1. **Publish on F-Droid — reviewer round 1 addressed, CI green, awaiting re-review (Jul 7 2026)** — [fdroiddata MR !42240](https://gitlab.com/fdroid/fdroiddata/-/merge_requests/42240). Reviewer **linsui** left 3 requests; all fixed + all 9 CI jobs green on the fix commit (`fdroid build` + `check apk` incl.): **(1)** removed `metadata/dev.tine.app/en-US/{summary,description}.txt` from fdroiddata — store text now lives in the **tine repo's `fastlane/metadata/android/en-US/`** (`short_description`/`full_description`/`changelogs/4003.txt`), which forced cutting **v0.4.3** (F-Droid reads fastlane from the source *at the built tag*, and v0.4.2 predated the files); **(2)** filled the **App Inclusion MR template** + checkboxes; **(3)** switched Rust install to **`apt-get install rustup` + `rustup default stable`** (was `curl | sh`). Build now targets tag `v0.4.3` / versionCode 4003. **`UpdateCheckMode: None`** kept deliberately (Tauri derives the integer versionCode at build time → not in a tracked file the tag-checker can read; offered linsui a `Tags`+`VercodeOperation` or in-repo-pinned-versionCode switch if they want auto-updates). **Recipe formatting gotcha:** match the fork branch's *existing* byte formatting (folded `output:` w/ trailing space) — the fdroiddata CI's fdroidserver version folds it where local 2.4.5 doesn't. **Remaining: (a)** linsui re-review (their clock); monitor + push fixups via the stored gitlab.com PAT (`/aux/koutecky/logseq/fdroid-work/.glab-token`); **(b)** post-merge: revoke the PAT; **(c) post-merge (Martin's directive Jul 7): kill the per-release human-merge loop** — every release under `UpdateCheckMode: None` needs a hand-authored fdroiddata MR + human merge; switch to **`UpdateCheckMode: Tags` + `AutoUpdateMode: Version`** so F-Droid's `checkupdates` bot auto-detects each git tag and auto-publishes with NO human in the loop. Blocker to solve first: pin Tauri's build-time integer versionCode (`0.y.z→y*1000+z`, git-ignored in `gen/android/`) into a **tracked** file the Tags checker can read (preferred over a brittle `VercodeOperation`). Tracked in memory `fdroid-post-inclusion-autoupdate`. Harness at `/aux/koutecky/logseq/fdroid-work/`. **→ [docs/plans/appstore-distribution.md](plans/appstore-distribution.md).**
2. **Publish on Google Play** — more friction (calendar-bound): create a Play Console account ($25 + identity verification), and because it's a new personal account, **start the required ≥12-tester / 14-day closed test EARLY**. Switch the `release.yml` android job to also emit a signed **AAB** (`tauri android build --aab`) + enroll in Play App Signing, target API 35, write a privacy-policy URL + trivial Data-safety form (local-first, no data collected), then Internal → Closed → Production. **→ [docs/plans/appstore-distribution.md](plans/appstore-distribution.md).**
3. **Block DOM windowing on very large pages** (measurement-gated) — Current state (ADR 0008): lazy render-once-**keep** (bodies mount via IntersectionObserver but never unmount, so a long scroll accumulates DOM); WebKitGTK layout/paint, not parsing, is what a big page *feels* like. **Measure first** (long-scroll DOM/frame-time probe + `npm run bench scrollBig`); **build only if it shows** — true windowing unmounts off-screen block bodies keeping height placeholders (preserve scroll geometry + anchor targets), mind the `content-visibility` clipping gotcha. Risks: caret/focus cluster (never unmount the edited block), block-ref/backlink anchor jumps, find-in-page, e2e caret harness. **Also the leading candidate fix for the 4K/1.5× fractional-scaling slowness** (fewer painted pixels/frame) — but that row stays in Later; profile on Martin's box.
4. **iOS build** (scoping session, not a build task — do LAST) — Tauri v2 supports iOS from the same project, but the graph-access model is the hard part and differs from Android: iOS has **no `MANAGE_EXTERNAL_STORAGE`** — apps are sandboxed, so opening the user's real Logseq/Syncthing folder means `UIDocumentPicker` + **security-scoped bookmarks** (persisted, re-resolved each launch) and possibly iCloud/Files-app integration, not raw `std::fs` on a shared path. So M2's real-path approach doesn't port directly; needs its own design. Also: Apple Developer Program ($99/yr) for signing/TestFlight/App Store, no sideloading. The M3 toolbar + `isMobilePlatform` gating already cover iOS on the UI side. **Martin wants to scope the approach in a dedicated session before committing** — this Next slot is a reminder to *hold that session*, not to start building.

---

## Later ideas and research (not WONTFIX)

| Item | Notes |
|---|---|
| **Pane-select: tree-aligned intermediate widening rungs** (Martin, Jul 8 2026) | The outward-widening ladder currently jumps pane side → covering seam → whole-window edge. Contiguous multi-pane edge spans ("split above two of three columns") are splittable iff a SUBTREE spans exactly those panes (the layout is a binary tree; splitting = wrapping a node) — those tree-aligned spans could become intermediate rungs (subtree-edge target kind + a wrap-node-at-path split primitive + cross-pane highlight rendering). Non-aligned spans have no well-defined split and stay unsupported (ADR 0033). Deferred until daily driving shows the need. |
| **Nav-model unification candidates** (ADR 0034) | The sheets/panes shared key protocol + contract test shipped Jul 8 2026 (`src/navProtocol.ts`, `src/navModel.contract.test.ts`). Two divergences documented as intentional but open to later unification if Martin wants: (a) shift+arrow — sheets extend a range, panes just step (would become span-widening once tree-aligned rungs exist, see the entry above); (b) remove — sheets delete a row/col *at a seam* (Backspace=before/Delete=after), panes close *a region*; sided seam-deletion for panes would close whole subtrees at once (several panes) — recoverable (views, not data) but aggressive. |
| **Guide copy: overwrite-to-update a stale `tine-guide/` copy** | Deferred. The current guide-copy action is intentionally non-destructive: it copies missing pages and assets but never overwrites an existing copied page because the user may have edited it. A future refresh flow should make stale-copy replacement explicit and reviewable. |
| **Grid edge-grow + board group-by picker — two intentional scoping limits** (Jul 9 2026) | Shipped in the sheets follow-up (grids grow via hover-`+` edge affordances; boards get a Group-by dropdown + right-click submenu). Two deliberate narrowings, both recoverable: (a) the on-screen `+` edge affordances render on **top-level grids only** — nested-in-cell subgrids (`overflow:auto` would clip an outside affordance) keep keyboard seam-insert; add an inner-inset variant if daily driving wants it. (b) the board group-by option list is built from the owner block's **literal children**, so a `{{query}}` board only offers State/Priority/Tags plus any prop fields of literal children — property axes present only in query *results* aren't auto-listed (still settable by hand or via the exotic-value passthrough in the header select). |
| **Sheet paste: "Paste special →" menu + insert-and-shift + fused drag-move** (Jul 9 2026) | Follow-ons to the mode-based nest/splat paste (ADR 0037), all deliberately unbuilt in v1: (a) a **"Paste special →"** context-menu override to force nest-a-region / splat-a-single / **insert-and-shift** (push existing cells right/down instead of the current destructive overwrite); (b) a **fused single-undo drag-move** — today "cut then paste elsewhere" composes a move as two atomic undos because there is no drag gesture for grid cells. Add if daily driving wants a non-destructive or draggable move. |
| **Contextual Guide nudges (E3)** | Lightweight "New to this? Open Guide" links in Sheets/formula/table context menus and adjacent feature surfaces. Deferred because the main Guide entry points, announcement, and copy flow now exist; add this only where daily use shows a real discoverability gap. |
| **Formula builder: nested expressions** (Martin, Jul 9 2026) | The visual FormulaEditor only builds **one level** — a single `if`, a bare comparison, or a value with at most one arithmetic op over two simple operands; anything deeper drops to the `</> raw` text box. Martin wants genuinely nested expressions buildable *in the faces*, e.g. `(a + (b * c)) / 2` — arbitrary arithmetic/logic trees with per-node editing (add/remove operands, wrap a subtree in an op, control precedence). Related gap he hit: when editing an existing formula there is **no in-face affordance to add a branch/face**, so a composite expression can only grow via raw. Scope = a recursive face renderer + node insert/wrap gestures over the existing `Ast` (`src/sheet/formula/`, `src/components/FormulaEditor.tsx`). |
| **Rendered-copy fidelity — residual math-typeset-to-text open** | The Jul 3 2026 pass (`f1c5721`) made *Copy / export → Rendered* resolve on-screen block refs + user macros; the Jul 5 2026 pass closed the actionable fidelity gaps from [the plan](plans/rendered-copy-fidelity.md): ~~math delimiters stripped~~ now inline/block math copies as `$…$` / `$$…$$` (TeX stays re-parseable), ~~off-screen refs fall back to bare uuid~~ the export modal pre-warms the async block-ref cache before copy, ~~provider macros stay literal~~ `{{embed}}`/`{{query}}`/media macros resolve to text forms, and ~~multi-line refs only copy the first line~~ the **Resolve refs fully** export toggle expands full visible ref bodies. **Still open by Martin's call:** full math-typeset-to-plain-text remains unresolved; the safe default is still TeX-with-delimiters, with only an off-by-default trivial `mathAsUnicode` approximation for cases like `E=mc²`. |
| **Quick-capture browser extension (Firefox)** | Snap a page/selection/link into today's journal or an inbox page via a capture template. Prefer a watched drop-file (no open port; rides the existing file-watch + journal-append). |
| **Git integration** ([#33](https://github.com/martinkoutecky/tine/issues/33)) — *research done, Martin deciding* | "Dumb" shell-git: call the system `git` binary (must be installed) to auto commit/push after changes + on app close, per the reporter (who links `logseq-plugin-git` as the model). **Research complete — design decisions deliberately still open (Martin thinking on it).** Findings: OG uses shell-git via **dugite** (init→add→commit) but is **commit-only, never auto-pushes** (auto-commit off by default, 60 s timer + opt-in commit-on-close, auto-inits the repo); the referenced plugin is a UX layer that adds Commit/Push/Pull buttons + an opt-in auto-push on window-hide. So #33 = OG's commit **plus** auto-push, which OG deliberately avoids. Open decisions: push-default vs commit-only; trigger model (debounced-on-change vs 60 s timer vs on-close); **system git vs bundled** (dugite bundles its own, #33 wants system git); **rejected-push / conflict / no-auth handling — the data-safety call, since it writes the real graph** (neither OG nor the plugin handles it, both just toast git stderr); commit-message format; in-place `.git` vs OG's `--separate-git-dir`. Full research + per-decision precedent: `subagent-tasks/notes/git-integration-research.md`. |
| **File the WebKit-exit double-free upstream at Mesa** (follow-up to #28 / ADR 0023) | Tine's fix (SIGKILL WebKit helper processes at quit) *prevents* the exit-time coredump, but the actual double-free lives in the GL userspace driver teardown (Mesa GBM/gallium/DRI; on hybrid boxes also the NVIDIA-EGL/libglvnd teardown ordering) — that's where it's truly fixed. Repro'd on Fedora 44 / Mesa 26.1.3 (GH #28). File at gitlab.freedesktop.org/mesa/mesa/-/issues with the exact `__run_exit_handlers` backtrace + `EGL_LOG_LEVEL=debug`; Mesa has fixed this class before (u_queue atexit, 13.0.6/17.0.1; eglTerminate/gbm double-free, LP#1414999). Cheap, correct, low-priority (our app-side fix already spares users the crash). |
| **Hybrid-GPU runtime WebProcess crash** ([#29](https://github.com/martinkoutecky/tine/issues/29)) — *awaiting reporter confirmation* | Same reporter/machine/driver-module set as #28 (Dell XPS 15, Intel Iris Xe + NVIDIA RTX 4050, Fedora 44, XWayland) but a **runtime** `WebKitWebProcess` SIGABRT via `std::__glibcxx_assert_fail` (Fedora `_GLIBCXX_ASSERTIONS` tripping *inside* Mesa gallium / NVIDIA-EGL) during a GPU repaint — triggered by expanding the ~260-row "All pages" sidebar list. **Not** a Tine content bug: the render path is clean (`PageEntry.path` is always a `String`, `pageListLabel` is pure string ops; a JS error can't SIGABRT the web process). **Candidate fix (validated against sources, ready to ship gated):** pin the WebView to the integrated GPU without disabling acceleration — `__EGL_VENDOR_LIBRARY_FILENAMES=<mesa vendor json>` (libglvnd then loads only Mesa, so `libnvidia-egl-*` never enters the process; Intel iris HW stays on) + WebKit-native `WEBKIT_WEB_RENDER_DEVICE_FILE=<intel render node>` (≥ WebKitGTK 2.42; F44 ships 2.52.1). Set on the Rust main process before WebView spawn (WebKit honors inherited env — confirmed in source). **Gate strictly** on "NVIDIA EGL vendor json present AND a Mesa-capable i915/xe/amdgpu render node present" (else a pure-NVIDIA box drops to llvmpipe software). Drop `__GLX_VENDOR_LIBRARY_NAME`/`DRI_PRIME` (noise on the EGL path). No exact-signature upstream bug (closest: WebKit 262607, WONTFIX — app-side env pinning is the expected mitigation). **Blocked on:** reporter running the posted env-var test (does the crash stop + does `/proc/<pid>/maps` still show `iris`, no `libnvidia-egl-*`/`llvmpipe`); on confirmation, implement the conditional pin. Research: `subagent-tasks/notes/webkit-hybrid-gpu-runtime-crash.md`. |
| **Query-engine indices + finer live-query refresh** (the sheets/supertags pre-req) | **Not needed today — verified Jul 4 2026**, recorded so the design is ready when it is. Current cadence: live `{{query}}` panels re-run at most ~1 Hz while typing (400 ms save debounce `persistence.ts:190` → `cache_gen` bump on save `model.rs:1488` → 700 ms `dataRev` debounce `persistence.ts:141` → panels keyed on `dataRev` re-fetch, `Macro.tsx:142`); each re-run is a native scan over `OnceLock`-cached block facets (`doc.rs:150`, O(1) field checks) ≈ ms per query at current graph size; and scoped eviction #52 (`model.rs:1504`) keeps unaffected memoized results — the keep/evict test is **two-sided** (evict if the page was in the result OR now matches the predicate, re-running the query's real `Pred::eval` against the post-edit doc, `query.rs:292`), fuzz-guarded by `tests/derived_cache_fuzz.rs`. **Becomes real when** query count × graph size × expressiveness grows: supertag/sheets dashboards with many live views, sort/group/join semantics, or much larger graphs. **Design shape:** (1) incremental facet indices (marker / priority / tag / page-ref → block ids) maintained in `cache_upsert` alongside the page cache, so query eval becomes index intersection instead of the `with_pages` full walk (`query.rs:221`); (2) per-query invalidation subscriptions replacing the global `dataRev`-keyed refetch, so an edit only re-renders the queries it actually evicted; (3) diff results before re-render — **DOM churn repainting big result lists ~1 Hz in WebKitGTK is the likely first *felt* cost, not the Rust scan**. **Alternative/complement (Martin's observation, Jul 4 2026): incremental result patching** — the eviction test already evaluates the edited page, so an evicted result could in principle be patched (`old − page's old group + page's new matches`, O(page) per edit) instead of fully recomputed (O(graph), `query.rs:221`). Deliberately NOT done today: global operators break local patching (`sample n` truncation `query.rs:270` — blocks cut by the limit are unknowable without a rescan; same for future sort/limit/group), and the current invariant "a cached result is always a full-recompute output" is what keeps the fuzz oracle trivial and keep/evict drift-free. If sheets adds sort/group/limit, prefer indices (fast recompute) over patching (correctness surface); patch only pure per-block-predicate queries if at all. Risks: index consistency with `cache_gen` + aliases + day-rollover (`today` in memo keys); extend the derived-cache differential fuzz oracle to cover the indices. **Watch signal:** Martin's sidebar TODO-query dashboard (workflow he dropped in OG, resumed Jul 2026) feeling sluggish while typing — first diagnose *which* lags: typing latency (save path; shouldn't happen) vs sidebar repaint stutter (DOM; cap result-list size, collapse coarse queries) vs stale results (harmless debounce). |
| **Query: server-side comparison predicate `(compare k > 3)`** (deferred from fork ADR mine/0001, Jul 9 2026) | A coarse-DSL clause for numeric/date ordering comparisons evaluated in Rust (`Pred::Compare` + tokenizer/parser + `eval` coercion aligned with the formula engine), plus matching builder chips. **Deliberately deferred:** it has no Logseq equivalent, so a `{{query}}` carrying it returns *nothing* in Logseq (breaks round-trip for the very blocks using it), and it duplicates coercion the Sheets formula engine already owns. The shipped `tine.query-filter::` formula refinement (fork ADR mine/0001) covers these comparisons expressively and Logseq-inertly, and the formula editor's visual builder already gives no-syntax comparison chips. Revisit only if real use shows the frontend refinement is insufficient (e.g. a whole-graph `deadline < today()` scan with an empty coarse query — the deferred server-side formula-evaluator path). |
| **Performance: remaining levers** | The July 2026 whole-codebase audit (perf × data-safety × architecture, two independent auditors per dimension) closed the found eager/superlinear paths (graph-open foreground scans, bulk icon lookup, batch block-ref resolution, advanced-query memoization). The frontend bench is now a hard same-machine A/B CI gate against both immutable v0.4.7 and the previous release, with metric-specific budgets. Remaining: block **DOM windowing** (now promoted to Next). Context: the Jul 4 2026 lsdoc third-party bench + Tine parse-cadence map — parsing is only user-visible at graph-open (background-warmed; a pre-warm search triggers a synchronous whole-graph build, ~1–2 s per 10 MB), never on keystrokes; the parser-side lever lives in **lsdoc's own backlog** ("plain-text fast path", expected ≈2–3×). |
| **UI sluggish on a 4K display at fractional scaling** (Martin, Jul 4 2026) | Fast on a native 1920×1200 laptop panel; slow on a 4K screen at **KDE Plasma 1.5× fractional scaling**. **Env workarounds tried and FAILED (Jul 4):** `GDK_SCALE=2 GDK_DPI_SCALE=0.5` → UI too small (effective 1.0×) *and still slow*; `TINE_GPU=0` (software paint) → *also too slow*. That two-way negative is diagnostic: integer 2× backing is slow too (so it's not specifically the fractional-downscale path), and software is slow too (so it's not a GPU/DMABUF-downscale pathology) → the cost is **raw device-pixel paint/composite throughput in WebKitGTK**, proportional to pixel area (1.5× on 4K ≈ 3–4× the 1920×1200 pixel count). No env flag fixes this; it's a paint-budget problem to shrink in *our* code. **Still worth an A/B (confirms + possible daily workaround):** `GDK_SCALE=1 GDK_DPI_SCALE=1.5` — draw at 1× device pixels (cheap), bump only font size to 1.5× (readable), at the cost of small-ish icons/padding; if *this* is fast, it nails the diagnosis. **Real levers (in our control), highest-value first:** (1) a **high-DPI "reduce visual effects" mode** — kill backdrop-filter/blur (modals, overlays), heavy box-shadows, and drop the per-`.query-block` `translateZ(0)` compositing layers ([[tine-rendering-perf]] pt 4; each is a GPU texture that costs ~2.25× RAM/fill at 1.5×) — auto-enable above some device-scale or expose a toggle; (2) land block **DOM windowing** (now in Next) — fewer painted nodes is the biggest win at 4K; (3) investigate rendering the webview *content* at a capped internal scale (<window scale) and letting the compositor upscale — trades crispness for fill-rate. **Need Martin's box to profile** (uni server is headless, no GPU); the first question is *what* is slow — scroll, typing, or steady-state — since it points at (2)/(1)/(1) respectively. |
| **Theming: remaining semantic-token sweep** | Built-in one-click themes shipped in `Unreleased` (Default, Nord, Solarized, Gruvbox) on top of the `--ls-*` shim, with the gallery selection persisted through backend app settings. Remaining theming work is the lower-value sweep of hardcoded colors outside the current semantic-token set, plus moving the separate light/dark preference into shared app settings so independent WebViews consume the same value. |
| **Slash-menu autocomplete UX** ([#15](https://github.com/martinkoutecky/tine/issues/15)) | Two OG-parity/discoverability gaps in the `/` autocomplete: (1) a **visible scroll affordance** — WebKitGTK hides scrollbars, so overflow items below the 280px fold look absent (affects every menu: slash, block-ref, page); (2) an **OG-style `/template` picker/submenu** — reporter expects Logseq's dedicated template chooser. Backend discovery + listing are verified working; this is the UX layer. Blocked on a repro for the "sees only one template" part. |
| **ARM binaries — validate on real hardware** | **Build side DONE + confirmed:** v0.3.5 shipped ARM artifacts (Linux `aarch64` AppImage/rpm/deb + Windows `arm64` setup/portable — verified in the GitHub release). What's left is unverifiable without hardware: nobody has *run* them on a real Snapdragon/Asahi/Pi. No action until a user reports (Martin: "we'll know when someone complains"); if a future tagged ARM build fails to link, the fix lives in `release.yml`. |
| **Graph view** | The visual node-link graph of page links. Martin's Jul 5 2026 "graphs" vote was almost certainly *this* (not multi-graph switching). **Researched** → `subagent-tasks/notes/graph-view-research.md`. Findings: the **data is nearly free** — each block's memoized `BlockProjection` (`doc.rs`) already carries `refs_page`/`refs_norm`/`block_refs`/`backlinks`, and namespaces derive from `/` in page names, so the backend is **one edge-list builder command (~1 day)**, a variant of the existing `referenced_page_names` scan (NB: `src/graph.ts` is graph-*folder* management, unrelated). OG builds `{nodes,links}` off DataScript (page-ref+tag+namespace edges, node size ∝ `cbrt(ref_count)`) and renders a live main-thread d3-force sim → WebGL (`pixi-graph-fork`). **Tine should diverge on perf** (WebKitGTK prime directive): d3-force in a **Web Worker with a convergence stop**, render **Canvas 2D first** (WebGL only if a spike proves it needed, never SVG at scale), **default to the bounded local/neighborhood graph** with a node cap on any global view. **Effort (unmeasured):** ~3–4 days for a separable **local-neighborhood MVP** (the recommended first bite — de-risks the renderer), ~1.5–2 weeks for full OG-parity global+local. **Top risk:** WebKitGTK render perf on Martin's real large graph is unmeasured → wants a rendering spike before committing a renderer. Feeds the "Interactive graph view in HTML export" row below as a near-free follow-on off the same builder (same node-cap caveat — ships to the open web). |
| **Interactive graph view in HTML export** | The static-site export has sidebar + search but no interactive graph yet. |
| **macOS notarization** | The macOS build is unsigned, so Mac users hit the "unidentified developer" wall (right-click→Open works around it). Signing needs an Apple Developer ID ($99/yr) — exploring a shared/borrowed ID or revisiting later. **Deferred, explicitly not WONTFIX.** |
| **Verso/Servo engine swap** | A longer-term answer to WebKitGTK's scroll/render gaps. Servo's web-compat for a dense editor isn't ready yet; revisit ~early 2027. |
| **Nested-grid ("breadth") views** | A TreeSheets-inspired grid over child blocks — a post-1.0 exploration. |
| **Pixel-tight keyboard-toolbar docking (native IME insets)** | The M3 above-keyboard toolbar docks via `window.visualViewport`, which can't perfectly capture every soft keyboard's geometry: on SwiftKey there's a barely-perceptible gap that flickers while scrolling (visualViewport update lag); gboard/futo are already flush. Martin: "fine for now." The pixel-tight fix is to dock natively off Android `WindowInsets` `Type.ime()` like OG's `NativeEditorToolbarPlugin` — a native rewrite of the toolbar host, deferred until the web-docked version proves insufficient. |
| **Theme marketplace scrape / Radix-token coverage** | Deferred, not part of the built-in gallery. Community-theme scraping only makes sense if the curated list grows beyond hand verification and each bundled theme has license/attribution plus screenshot proof. Radix (`--rx-*`/`--lx-*`) variable coverage is likewise outside the classic `--ls-*` shim boundary for now. |
| **Live-preview editor (CodeMirror 6)** | The only honest route to Obsidian-style in-text liveness (widget decorations, marker hiding, real bold while typing): ONE CM6 instance for the single edited block, decorations fed from lsdoc's AST + inline spans — no second parser, ~76–116 KB gz. **This is what Martin's "live facet chip" idea actually needs** (the marker shows as raw text while the caret is in/adjacent to it, and becomes a clickable chip once the caret leaves = CM6 widget-decoration + reveal-source-under-cursor). **The old blocker is cleared:** lsdoc now ships inline spans (v0.4.0). Remaining cost: multi-week, and it reopens the just-stabilized caret/focus cluster (ADR 0013, editorController, the e2e caret harness is textarea-hard-wired). A cheaper narrowing — a scoped CM6 spike on JUST the task marker — still pays most of the CM6 *integration* cost (the expensive part is adopting CM6 + caret parity, not the per-construct decoration). Decision pending Martin. The transparent-textarea overlay is NOT an alternative for chips (see WONTFIX — glyph metrics must stay byte-identical). |
| **Reference breadcrumb identity + configurable default depth** | The current Linked References/query parity pass deliberately uses the ordered breadcrumb labels already shipped in `BlockDto`, so it can bound context safely but cannot make individual ancestor fragments navigable without guessing identities. Complete OG parity needs stable ancestor page/block IDs in the native DTO/schema, then ordinary click/shift-click routing. OG also exposes `:ref/default-open-blocks-level` (default `2`); Tine currently matches the default without a setting. Add configurability only together with a coherent advanced reference-settings surface, not as another isolated toggle. |
| **PDF export — polish follow-ups** | Whole-page **Export to PDF…** shipped Jul 5 2026 (ADR 0021: reuse the HTML export + the webview's own print engine via a hidden iframe + `window.print()`). Remaining nice-to-haves, none urgent: (1) **verify the print dialog on real WebKitGTK** — the print *document* render is screenshot-verified (`scripts/shot-printdoc.mjs`), but nobody has confirmed the GTK print→"Save as PDF" flow end-to-end in the running app (the plan's 5-min smoke test; `window.print()` is documented to work on WebKitGTK, unlike the confirm/alert no-ops — [[tine-webkitgtk-confirm]]); (2) **block-subtree export** — currently exports the whole page; a "export this block ⌄" from a bullet's zoom/context menu would match the original ask; (3) **silent no-dialog export** (Route A: a Rust `export_pdf` command rendering into a hidden WebviewWindow + `webkit2gtk::PrintOperation::print()` with file-output `PrintSettings`) for a "choose filename, done" UX — Linux-easy, but Windows `PrintToPdfAsync`/mac `createPDF` are the per-platform cost; (4) **pagination fidelity** on very long KaTeX/wide-code pages is unmeasured. Research context: `subagent-tasks/notes/pdf-export-research.md`. |
| **Feature video clips for the website** | Looping clips of the motion features (PDF, audio waveform, video, tabs). Parked — headless capture is too heavy for now. |

---

## WONTFIX — not doing (with reason)

### By-design non-goals
| Item | Reason |
|---|---|
| **Whiteboards** | A separate application domain; Tine is a fast local-first outliner. |
| **Flashcards / SRS** | Needs a dedicated spaced-repetition review engine; out of scope for an outliner. |
| **Literal Logseq/Obsidian plugin API compatibility** | Reproducing `@logseq/libs`, Datascript, and either app's render model would import their architecture and authority. Tine instead has its own constrained plugin API (ADR 0041); plugins can be ported with AI assistance. |
| **Built-in git** | Delegate to your own sync tool (Syncthing, etc.). |
| **Publish on Flathub (Linux)** | **WONTFIX (Jul 10 2026).** Flathub has a policy against AI-built ("vibecoded") apps, and Tine is AI-built, so it is ineligible — the submission PR was dropped. The autoupdate-native Linux channel it would have provided stays unmet; AppImage/raw binary remain the Linux fallback, and F-Droid (Android) is unaffected. A community "Flathub for AI-built apps" effort exists and may revive this later. |

### Subsystem / feature calls
| Item | Reason |
|---|---|
| **`function` macro** | Needs a Clojure (SCI) evaluator plus query-result plumbing. Large, rarely used. |
| **Advanced Datalog queries (general)** | **Documented divergence (Jul 21 2026, Martin).** OG evaluates arbitrary Datalog over datascript; Tine has one typed Rust `QueryPlan` (ADR 0042 — "arbitrary scripting … does not enter this model"). Tine supports the simple-query DSL, a bounded advanced subset (incl. the common `[?p :block/properties ?props]` + `(get ?props :key)` page-property pair), and renders unsupported `#+BEGIN_QUERY` payloads as a visible unsupported-query notice — never a guessed result. Extending the *subset* on demand stays open; a general Datalog engine is out. |
| **`cloze` / `cards` macros** | Only meaningful inside the SRS review loop → same reason as flashcards. |
| **Zotero connector macros** | Niche; needs Zotero data-dir + item-metadata resolution Tine doesn't have. |
| **CEF / Chromium engine swap (desktop)** | Evaluated and ruled out — too heavy (a multi-process Chromium bundle per OS + a JS-compat shim). Staying on WebKitGTK; Verso is the live long-term bet (Deferred). |
| **YouTube-timestamp seeking** | OG-faithful seeking needs the YouTube player API; not worth it. (A plain clickable timestamp is the most that would be considered.) |
| **Page PNG export** | **WONTFIX (Jul 21 2026, Martin).** OG's export modal offers PNG for a normal *page* target only (og-1.0.0 `components/export.cljs:163-168` — the parity audit's block-PNG claim was an overclaim, receipt corrected). A faithful Tine version needs a per-platform webview snapshot path (WebKitGTK `get_snapshot` / WebView2 `CapturePreview` / WKWebView), hard pixel/byte caps for long pages, and real-app-only verification on every platform — large cost for a niche capability nobody has requested. Reconsider if a user asks. |
| **Sub-directory *write* semantics** ([#21](https://github.com/martinkoutecky/tine/issues/21)) | *Reading* nested pages shipped in v0.4.0 (recursive `pages/`+`journals/` scan, collision disambiguation, recursive conflict scanners — `beef12d`, `800dd3b`). The **write** side stays flat and OG-consistent by design: `rename_page` writes the new file **flat** in `pages/` (a renamed nested page does not stay nested); **no namespace-from-directory**; and **no create/rename *into* a chosen subfolder**. This avoids a directory↔namespace identity model neither Tine nor OG has. Revisit only on an explicit ask. |
| **Overlay/backdrop "live highlight" editor mirror** | The transparent-textarea-over-rendered-mirror trick can only honestly deliver metric-safe color changes — no chips, no marker hiding, no bold (glyph metrics must stay byte-identical to the textarea) — in exchange for a permanent font-metric-sync liability on WebKitGTK. The CodeMirror 6 route (Deferred) strictly dominates it. |
